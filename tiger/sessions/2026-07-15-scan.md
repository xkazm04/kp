---
type: tiger/session
date: 2026-07-15
mode: scan (diff vs [[2026-06-20-init-scan]]) — L1 static, no --live
scope: full re-inventory (14 active call sites) + diff of all 26 recorded backlog items
lenses: code (1) + value-L1 (2); model (3) NOT run live — but the repo now ships its own matrix
agents: 5 mass-parallel scouts (T0 plumbing / T1 config / T2+T4 value / T3+T5+T6 lang-eff-hardening / discovery)
prior_session: "[[2026-06-20-init-scan]]"
---

# Tiger session — 2026-07-15 — first re-scan (25 days on)

## Scope & method
Second Tiger pass, ~1 month after the init scan, over a heavily-shipped LLM layer
(the git log shows ~40 LLM-touching commits since). Cheap static diff: 5 parallel
scouts each owned a backlog cluster and returned a RESOLVED/PARTIAL/OPEN/REGRESSED
verdict per recorded finding with `file:line` evidence; a 5th did new-call-site
discovery. No `--live` (Lens 2-L2 / Lens 3) this run.

## THE HEADLINE — the economics tier flipped from broken to solid
The 2026-06-20 headline was "an unusually strong wrapper wrapped around a **broken
economics layer**, plus 4 bypasses." **That is no longer true.** Every T0 critical
finding is RESOLVED with live code and, in three cases, a regression test:
- **The `llm_usage` ledger exists again and is DEFAULT-ON** (not gated on LightTrack):
  table `db/core.ts:623`, writer `db/llm.ts:111`, emitted per-envelope `base.py:311` /
  `monitor.py:260`, sidecar set on every spawn `python-runner.ts:145`. **The platform
  can now cost and bill its own AI** — the single biggest gap from last time is closed.
- **Cost stamping** across all adapters + a price table covering every routed model +
  `test_every_routed_default_model_is_priced` guard.
- **Self-repair re-prompt** in `complete_json` (`base.py:370-409`).
- Net-new hardening only strengthens it: KP_OFFLINE fail-closed egress, self-host /
  OpenRouter adapters, an HTTP-200-error guard that stops a gateway error being billed
  as a healthy completion. code_score for [[_plumbing]] 3 → 5.

## Inventory delta
- **+1 new active call site:** [[bench-judge]] — the LLM-as-judge behind the new
  in-repo **model-quality matrix / BYOM scorecard** (commit 20b03ba). Now **14 active**.
- **0 sites removed.** All 13 prior sites still resolve; the 4 non-LLM/dead notes hold
  ([[soft-signals]] reconfirmed "Deterministic and LLM-free", `soft_signals.py:16`).
- **New non-call-site infra (no new use case):** CV/lead intake is deterministic
  (`cv-intake.ts` uses text extraction, not an LLM); self-host endpoints + KP_OFFLINE +
  backgrounded JD gen are new *providers/gating*, not new prompts.
- **Prior art discovered — Lens 3 already partly built:** the model-quality matrix
  (`bench/runner.py` + `judge.py` + `bake_quality.py` → `llm-quality-scores.ts` →
  Models tab) recommends a model per use case. Tiger reconciles against it; its coverage
  gap (multimodal cv_analysis/profile_extract, github_analysis, grounded_salary, voice)
  is where Tiger Lens 3 still adds value. See [[bench-judge]] + `docs/LLM_MODEL_MATRIX.md`.

## Backlog diff — the 26 recorded items

### T0 — critical economics (was the whole story) → **4/4 RESOLVED** ✅
1 ledger · 2 cost stamping · 3 self-repair — all RESOLVED. Tier closed.

### T1 — config/routing correctness → 3 RESOLVED · 1 PARTIAL · 2 OPEN
- ✅ **5** grounded_answer now routed through `_generate_with_retry` (`gemini.py:339`) —
  fixes retry for cv_analysis / profile_extract / grounded_salary / profile_draft.
- ✅ **8** profile-draft default path meters (`profile_draft_cli.py:241`, `gemini.py:346`).
- ✅ **9** voice emits a per-session ledger row (`api/interview/complete/route.ts:163`,
  `voice/minute-prices.ts:51`) — the billed minute meter + ledger cost share one count.
- ◐ **7** github-analysis — the two *harms* are fixed (BYOM key precedence
  `route.ts:701`; metered to ledger + LightTrack `:917/:934`) but it is **still a
  TS-direct `GoogleGenAI().models.generateContent` bypass** with a hardcoded model
  (`route.ts:829,24`). Structural bypass remains; downgraded to a hygiene item.
- ⛔ **4** campaign-pack STILL omits `env: buildLlmConfigEnv()` on the spawn
  (`api/jobs/[id]/campaign/route.ts:54`) → BYOM + UI use-case re-route dead. The static
  sonnet override survives (baked into `resolve_provider`) so only *half* the original
  claim is now true — but the one-line env bug is unfixed. **Top remaining code item.**
- ⛔ **6** interview-scorecard STILL resolves `resolve_provider("automation")` once for
  every command (`automation_cli.py:110`); the scorecard call is metered/config-tagged
  as `automation`, not `interview_scorecard`. Config row + telemetry tag still lost.

### T2 — value / trust / compliance → 4 PARTIAL · 6 OPEN (the durable gaps)
- ◐ **10** rejection draft — the *auto-reject* path was **retired** (GDPR Art. 22; now
  queued for human review, `automation-pass.ts:302`), so the "billed-then-dropped in an
  automated pass" waste is gone. But `dispatchRejection` sends a deterministic template
  (`comms-dispatch.ts:260`); the recruiter's *tailored LLM draft* still never reaches the
  candidate on the on-demand path. PARTIAL.
- ⛔ **11** devcase **self-grading** — judge still uses the same `ClaudeCliProvider` as the
  generator (`submission_eval.py:468`, `calibrate.py:360`); `devcase_judge` catalog row
  exists but **no code calls `resolve_provider("devcase_judge")`** — registered, never
  wired. **Highest-value open item.**
- ⛔ **12** devcase evaluation **still never sees the submission** — `evaluate.py:129`
  ctx is `{role,rubric,reflection,tooling}`; the seed diff/code is never threaded in.
  Grades a summary of a summary.
- ⛔ **13** group-eval seal — EU-AI-Act traceability still partial: `group-eval-run.ts:510`
  seals the *coerced deterministic summary*; `policyVersion` = tri-state source, **not a
  promptVersion**; no raw LLM output/hash. `runGroupCompare` even drops the promptVersion
  the CLI emits (`:246`). Added since: `robustness`/`cohortSource`/`cohortSize` honesty.
- ◐ **14** voice override — deploy/CI-time `--check`/diff now verifies the ElevenLabs
  override flags (`eleven-agent-diff.mjs`), closing the silent-disable-at-deploy hole. But
  **no per-session runtime confirmation** (`VoiceInterview.tsx:927`) — a runtime downgrade
  is still silent. PARTIAL.

### T3 — language threading → 3 RESOLVED · 1 PARTIAL · 1 OPEN
- ✅ group-compare (`group_compare.py:175` + `--lang` wired `group-eval-run.ts:238`),
  ✅ interview-scorecard (`automation.py:730` + Czech rubric overlay `interview-rubric.ts:161`),
  ✅ profile-draft (`profile_draft_cli.py:205` + `route.ts:47` server-locale).
- ◐ **weight-proposal** — prompt supports `language_directive` (`weight_proposal.py:146`)
  but the production caller `recruiter.py:41` / `recruiter_cli.py` never passes `lang`, so
  it defaults to English. Half-wired.
- ⛔ **jd-ingest** — no directive on `_build_extraction_prompt` (`jobs.py:420`); no `--lang`.
  (Defensible as pure extraction, but the free-form `description` is emitted un-directed.)

### T4 — grounding gaps → 1 PARTIAL-fix · rest OPEN
- ✅ **17a jd-ingest** role_family definitions now inlined (`jobs.py:414-438`).
- ⛔ **17b profile-draft** still enum-names-only (`profile_draft_cli.py:52`) → phantom band.
- ⛔ **16** campaign-pack still missing segment/brand-EVP/competitor/per-channel inputs.
- ◐ **19** match-reasoning richer CV grounding added (`match_reasoning.py:50`) but still
  **no post-check a strength cites a real CV token** + no prior-pipeline history.
- ⛔ **18** group-compare compare prompt still lacks the role must-haves (computed at
  `group-eval-run.ts:458`, never passed in). ⛔ **20** grounded-salary currency still
  CZK-locked (`market_salary_cli.py:112`), region accepted but not used → wrong-currency
  comp for every non-CZ role. ⛔ **21** soft-signals role-blind (but non-LLM — out of scope).

### T5 — efficiency → 1 RESOLVED · rest OPEN
- ✅ jd-ingest content-hash dedupe (`job-ingest.ts:40-53`). ⛔ grounded-salary + devcase
  design/seed/scenario still recompute every call. ⛔ maxTokens still 2048 on the
  sonnet-stepped sites (`base.py:33`; overrides step model but not tokens) → truncation risk.

### T6 — hardening → all OPEN
24 no protected-attribute-blindness instruction on scorecard/weight-proposal · 25 three
phantom `_USE_CASE_BY_COMMAND` rows (harmless, no test) · 26 github per-skill evidence
pointer + sim-offer "scaled by fit" copy overstates a plain midpoint (`sim/offer-draft/route.ts:30`).

## Scorecard
- **Resolved this period:** 11 items (all of T0; T1 #5/#8/#9; T3 group-compare/scorecard/
  profile-draft; T4 #17a; T5 jd-ingest cache). Plus 5 net-new hardening wins.
- **Partial:** 6 (#7, #10, #14, #17→profile-draft still open, #19, weight-proposal lang).
- **Still open:** the durable *value/compliance* gaps + 2 config bugs.

## The re-prioritized backlog (impact-ranked)
1. **[value] devcase judge ≠ generator (#11)** — wire `resolve_provider("devcase_judge")`;
   the row already exists. A self-grading hiring judge is the highest-trust risk left.
2. **[value] devcase evaluation sees the submission (#12)** — thread the seed diff into
   `assess_tooling`/`evaluate` so a probe cites the actual file/line.
3. **[compliance] group-eval seal traceability (#13)** — persist promptVersion + raw LLM
   output/hash on the auto-sealed `group_eval_lead` decision (EU-AI-Act).
4. **[code] campaign-pack env (#4)** — one line: `env: buildLlmConfigEnv()` on the spawn.
5. **[code] scorecard use_case (#6)** — resolve `interview_scorecard`, not `automation`.
6. **[value] grounded-salary currency from region (#20)** — the #1 multi-market blocker.
7. **[value] deliver-or-drop the tailored rejection draft (#10)** — don't generate a draft
   `dispatchRejection` can't send.
8. **[value] weight-proposal + jd-ingest lang (#15)** — pass `--lang` through the real callers.
9. **[code] grounded-salary + devcase input-hash cache (#22)** · maxTokens step-up (#23).
10. **[model] Lens 3** — now UNBLOCKED (cost stamping works). Reconcile with the in-repo
    matrix; benchmark its coverage gap (cv_analysis, github_analysis, grounded_salary,
    voice) + baseline the [[bench-judge]] engine itself.

## Value ledger — promised vs delivered (updated)
- **Economics: PROMISED → now DELIVERED.** The ledger exists, is default-on, costs every
  adapter, and has a regression guard. The platform can cost/bill its AI. (Was the biggest
  red mark last time.) Residual: OpenRouter/self-host slug models unpriced locally (by design).
- **Grounding:** improved on the intake seam (jd-ingest role families + cache) and
  match-reasoning; **unchanged** on campaign-pack, group-compare must-haves, grounded-salary
  currency, and profile-draft families.
- **Trust/compliance (the new frontier):** devcase still self-grades and grades blind; the
  EU-AI-Act group-eval seal still lacks the model's raw reasoning. These are now the top risks.

## Strengths to protect (regression watch — all still intact)
[[weight-proposal]] bounded weights · [[automation]] code-side fairness gate (now +GDPR
auto-reject retirement) · [[match-reasoning]] 168h cache + pinned promptVersion ·
[[devcase]] tri-state provenance · [[cv-analysis]] post-hoc skill re-verification (now
metered) · [[voice-interview]] grounded Czech brief (now +deploy-time override verify + ledger).

## Honest ceilings
- Static pass again — grounding scores are designed-output judgments. A `--live` run is
  still needed to confirm senior-bar verdicts and to run Tiger's own Lens 3.
- Lens 3 is now technically unblocked (cost works) AND partly pre-built (the in-repo
  matrix). Next run should **reconcile** rather than duplicate, and focus real generations
  on the matrix's blind spots + the [[bench-judge]] baseline.
- github-analysis is downgraded (metered + BYOM-aware) but remains a genuine TS bypass —
  it will never honor `KP_LLM_CONFIG` model re-routing until it reads the registry.
