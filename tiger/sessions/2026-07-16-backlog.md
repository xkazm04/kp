---
type: tiger/backlog
date: 2026-07-16
supersedes_priority_of: "[[2026-07-15-scan]]"
basis: 5 Lens-3 benchmarks (match-reasoning, github-analysis, cv-analysis, grounded-salary, automation) + the 2026-07-15 scan diff
---

# Tiger consolidated backlog — 2026-07-16 (post-benchmark)

The 5 Lens-3 benchmarks converged on one theme: **the model tier is almost never the lever;
prompt fixes + cheap model-independent post-checks are.** Every benchmarked site keeps its
current production model. This note folds the benchmark-surfaced fixes into the standing
backlog and ranks what to resolve.

## Benchmark-surfaced items (NEW / re-prioritized)
- **B1 [prompt] grounded-salary CZK lock (#20) — PROMOTED.** Proven live: with region=Munich,
  every tier (incl. opus) obeyed the hardcoded `CZK/month` prompt. Model-independent → must be
  fixed in the prompt: derive currency+period from `region`/`ACTIVE_MARKET`, reuse
  [[cv-analysis]]'s working inference. `market_salary_cli.py:112-127`. → [[models/grounded-salary]]
- **B2 [post-check] cv-analysis derived-fact guard.** sonnet hallucinated tenure (7y vs 5y) +
  over-leveled medior→senior. Add a check: `years_experience` consistent with earliest role
  date; don't let the narrative promote seniority above evidence. → [[models/cv-analysis]]
- **B3 [post-check] match-reasoning strength-cites-a-real-token (finding #2).** haiku's lone
  slip was a fabricated CV detail; benchmark independently confirmed. Cheap `_coerce` post-check.
  → [[models/match-reasoning]]
- **B4 [prompt] github-analysis "name unmet must-haves".** haiku wrote "4 of 4 must-haves"
  hiding the missing Kubernetes. One prompt line instructing the summary to name unmet
  must-haves explicitly. → [[models/github-analysis]]
- **B5 [post-check] automation CZ prose slashed-gender + vocative guard.** sonnet emitted the
  banned `podílel(a)`/`věnoval/a`/`prokázal/a`. Reject `\w+\(a\)`/`\w+/a` forms + validate
  vocative on any LLM-drafted candidate message; keep the deterministic template as the safe
  floor. → [[models/automation]]. Reframes finding #1 toward drop/keep-template.

## Standing backlog (from [[2026-07-15-scan]], still open) — merged & ranked
Resolve order (value × reachability × cost), cheapest-safe-first where value ties:

1. **[code] campaign-pack env bug (#4)** — one line: `env: buildLlmConfigEnv()` on the campaign
   spawn (`app/api/jobs/[id]/campaign/route.ts:54`). Resurrects BYOM + UI use-case re-route.
   Highest ROI, lowest risk. **← resolve first.**
2. **[code] scorecard use_case mis-tag (#6)** — resolve `interview_scorecard` not `automation`
   for the scorecard command (`automation_cli.py:110/145`). Restores config row + telemetry tag.
3. **B1 [prompt] grounded-salary currency-from-region (#20).** Multi-market blocker; proven live.
4. **B4 [prompt] github "name unmet must-haves".** Cheap, closes the benchmark's one real prose miss.
5. **B3/B2/B5 [post-checks]** — match-reasoning strength-token, cv-analysis derived-fact,
   automation slashed-gender. Model-independent quality guards.
6. **[value] devcase judge ≠ generator (#11)** + **eval sees the submission (#12)** — highest
   trust risk, but larger change (wire `resolve_provider("devcase_judge")`, thread the seed diff).
7. **[compliance] group-eval seal traceability (#13)** — persist promptVersion + raw LLM output.
8. **[value] rejection deliver-or-drop (#10)** — benchmark says lean DROP/keep-template.
9. **[value] weight-proposal + jd-ingest lang (#15)** — pass `--lang` through the real callers.

## Model recommendations (Lens 3 — all settled, no swaps)
match-reasoning keep haiku · automation-screen keep haiku · github Claude-portable (prefer
sonnet IF ported, but keep Gemini unless #7 routes it) · cv-analysis keep Gemini (multimodal) ·
grounded-salary keep Gemini (grounding). **The bench's value was diagnostic, not a re-route.**

## Resolution log (this session)
- ✅ **#1 campaign-pack env bug — RESOLVED.** Added `import { buildLlmConfigEnv }` +
  `env: buildLlmConfigEnv()` to the campaign spawn (`app/api/jobs/[id]/campaign/route.ts:8,57`).
  BYOM keys + KP_LLM_CONFIG use-case re-route now reach `campaign_cli`'s `resolve_provider`.
  TS typechecks clean; matches the correct pattern at `automation-run.ts:240`.
- ✅ **#6 scorecard use_case mis-tag — RESOLVED.** Added `_USE_CASE_BY_COMMAND` map in
  `automation_cli.py` (scorecard→`interview_scorecard`, others→`automation`); applied to
  `resolve_provider` + both `emit_deterministic` calls. Verified: `_use_case_for('scorecard')`
  → `interview_scorecard`, which resolves keyless to `MonitoredClaudeCli` (no dev drift).
  104 automation/scorecard/campaign tests pass.
- ✅ **B1 / #20 grounded-salary currency lock — RESOLVED.** `market_salary_cli.py:114-148` now
  branches on region: active market (default / any region naming CZ) keeps the byte-identical
  CZK prompt; any other region is told to price in that region's own ISO currency and NOT
  convert to CZK. `_coerce` already passed the model currency through, so the fix is prompt-only.
  Verified: an EUR grounded payload survives `_coerce` as EUR; CZK preserved for the default;
  31 market/metering tests pass. Honest ceiling: the deterministic FALLBACK (no grounding /
  no key) still returns the active-market CZK taxonomy band — we hold no foreign benchmark data.
- ✅ **B4 github "name unmet must-haves" — RESOLVED.** Added a prompt line
  (`app/api/github-analysis/route.ts`) instructing the summary to name unmet must-have skills
  explicitly and never imply full coverage ("N of N"). Closes haiku's one real prose miss.
- ✅ **B3 / #2 match-reasoning grounding post-check — RESOLVED.** `match_reasoning.py`: added
  `_real_cv_tokens` + `_any_strength_grounded`; `_coerce` now backfills strengths from the
  deterministic template if NONE reference a real skill/matched-skill token. Lenient (one
  grounded strength suffices). Verified: boilerplate → backfilled, grounded list untouched.
  Full pipeline suite: **1138 passed, 4 skipped, 0 failures.**

### Resolved this session: 5 items
#4 campaign env · #6 scorecard use_case · #20 grounded-salary currency · B4 github prose ·
B3/#2 match-reasoning grounding. All verified; full `pipeline/jobfit` suite green.

### Deferred (larger changes — next session)
- **B2** cv-analysis derived-fact post-check — the multimodal path is Gemini/TS; touches
  `pipeline.py` coercion. Moderate; not a one-liner.
- **B5** automation slashed-gender/vocative post-check — reframes finding #1 (drop/keep-template);
  best done together with the #1 delivery decision.
- **#11/#12** devcase judge≠generator + eval-sees-submission — highest trust risk, largest change.
- **#13** group-eval seal traceability (EU-AI-Act).
- **#10** rejection deliver-or-drop (benchmark says lean DROP/keep-template).
- **#15** weight-proposal + jd-ingest lang threading.
