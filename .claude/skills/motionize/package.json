{
  "name": "motionize-skill",
  "private": true,
  "type": "module",
  "description": "Self-contained deps for the /motionize skill tools (kept out of the app's package.json).",
  "scripts": {
    "test": "node --test \"tools/__tests__/*.test.mjs\""
  },
  "dependencies": {
    "@neplex/vectorizer": "0.1.0",
    "svgo": "3.3.5"
  }
}
