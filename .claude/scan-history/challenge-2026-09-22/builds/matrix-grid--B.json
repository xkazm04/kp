{"card":"matrix-grid/B","title":"Propose a per-role slate that resolves who goes where across the grid","status":"landed",
 "base":"8477db98f","shas":["f2ed4fc21","b695824af","719844f89","2d3078a27","0a2bfb5e6"],
 "tests_added":["app/features/insights/matrix/matrixSlate.test.ts"],
 "cases_red_before":8,"cases_green_after":8,"cases_total":8,
 "gates":{"tsc":2,"eslint":0,"i18n":0,"design":0,"docs":0,"api":null,"unit":1,"ratchet":0,"perf":1},
 "files_changed":12,"lines_changed":531,
 "deviations_from_card":"8 cases, not 7: added one for the per-cell confidence band (coordinator ask) - a pick whose confidence.low does not clear the runner-up's score carries separated=false and the panel says the lead is within the margin. A role whose only above-floor candidate was allocated elsewhere is `contested` with pick=null (naming the rival) rather than `uncovered`, since 'nobody at or above 55' would be false there. Rival copy reads 'X (88) is proposed for R1' (not 'placed on', which collides with pipeline placement). Pairs added this session (the local `added` set) count as in-flight too. perf-budget.json (shared surface) edited under the lock: page.tsx ceiling 1370/8710 -> 1371/8720. Series order: i18n -> docs -> test+allocator -> feat UI -> perf, each commit green on its own paths.",
 "notes":"tsc exit 2 is foreign only (app/_lib/apply-link-recovery.test.ts, throttle-response.test.ts, app/api/apply/[id]/reapply-capability-gate.test.ts, kpi-sim/ untracked dir); zero errors on matrix/messages paths. Full test:unit exit 1 on sibling files only: rate-limit-contract.test.ts BROKEN on tasks/[id]/retry/route.ts (sibling in-flight edit, uncommitted) and devcase-candidate-refusals.test.ts FLAKE; all 142 matrix+focus tests green. test:perf: committed HEAD had already reached 1369 modules / 8713 KB (over 8710) from sibling commits task-fanout/throttle-response/apply-link-recovery before this card; mine adds 2 modules / ~7.6 KB -> committed 1371 / 8720, ceiling raised to exactly that. The working tree reads 8724 KB because of a sibling's UNCOMMITTED TasksOutcome.tsx/task-outcome-summary.ts (+~4 KB) - that builder owes its own raise. The many app/api/**/route.ts overage lines are sibling server-graph growth (comms-dispatch/apply/rate-limit); this card touches no server module. llm-config/job-ingest lines pre-existing."
}
