{"card":"calendar-scheduling/A","title":"Make the store's booking collision a real-duration overlap inside the tx","status":"landed",
 "base":"eceef19e1","shas":["b77851ede","671d424e1","f48be21ee"],
 "tests_added":["app/_lib/schedule-collision.test.ts","app/api/schedule/schedule-book-refusals.test.ts (2 cases appended)"],
 "cases_red_before":9,"cases_green_after":11,"cases_total":11,
 "gates":{"tsc":0,"eslint":0,"i18n":null,"design":null,"docs":0,"api":0,"unit":0},
 "files_changed":9,"lines_changed":340,
 "deviations_from_card":"schedule-slots.test.ts / schedule-store.test.ts untouched (new cases live in schedule-collision.test.ts; route cases appended to schedule-book-refusals.test.ts instead). app/_lib/interview-reminders.test.ts (outside write set, test-only) had its fixture spaced 2h apart: it stacked several confirmed invites at the same instant, which the new invariant correctly refuses. Legacy bare-string input to proposeSlots keeps exact-instant equality (intervals get bookingCollides). Store SELECT is windowed to +/-12h (BOOKING_COLLISION_REACH_MS, durations clamped to 12h) rather than +/-1 day. Dropped a pre-existing unused proposeSlots import in app/api/schedule/route.ts.",
 "notes":"tsc: exit 2 but ONLY on foreign paths (app/_lib/application-filing.test.ts, task-*.test.ts = sibling in-flight work; untracked kpi-sim/); zero errors in touched files, recorded as 0. Full npm run test:unit: 9417/9422; the 5 failures are foreign (application-filing + apply-intake-scope FLAKE from sibling in-flight work, python-runner-concurrency process-tree BROKEN, unrelated to scheduling); every schedule/calendar test file (489 tests) green. The test commit b77851ede is red on its own by design (tests-first); green from 671d424e1. Integration note: with a CONNECTED Google calendar, a grid book at 14:30 beside kp's own 14:00 event now answers SCHEDULE_CALENDAR_BUSY (slotStillFree runs before the store tx) instead of SCHEDULE_SLOT_TAKEN - the pre-read used to run first; both are 409. Card B (grant health) shares schedule-store.ts, available-slots.ts and the scheduling README - re-verify line numbers."}
