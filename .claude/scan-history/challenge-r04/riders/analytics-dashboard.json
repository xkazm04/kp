[
  {
    "rider": "ops-observability",
    "files_read": 9,
    "checked": "Read in full: app/_lib/webhook-idempotency.ts + .test.ts, app/_lib/ops-telemetry.ts + .test.ts, app/_lib/sentry-client.ts + .test.ts, app/_lib/status-tone.ts (test headers read, 10 test names), app/_lib/sales-contact.ts. One hop out: app/_lib/inbound-lead.ts, app/_lib/lead-intake.ts, app/_lib/edge-drain.ts, app/_lib/pull-pass.ts, app/api/channels/inbound/[token]/route.ts, app/api/agents/report/[token]/route.ts. Hypothesis traced: the in-process claim Map (webhook-idempotency.ts:17) is what edge-drain.ts:17-19 relies on to call a crash-replay 'harmless'; the knockout branch of lead-intake.ts:118-150 has no entry-level dedupe (grep found none in recordKnockoutDecline / dispatchKnockoutDecline), so a restart replay re-records the decline and re-emails the candidate. SLOT A CARD WENT HERE ('Durable webhook claims so a post-restart replay cannot re-decline a lead', adopting the open leftover 'Persist webhook idempotency claims across process restart'). Other files, no card: sentry-client.ts scrubs message+stack before the dynamic SDK import and its source-guard test pins it - sound; ops-telemetry.ts tail reads are bounded (TAIL_BYTES 256 KB) and the two cheap leftovers (all-provider tokens e973a5950, ops-warn.log on /api/ops ee5223f30) are already built; status-tone.ts is a Record-exhaustive tone table with producer-pinning tests - nothing structural left; sales-contact.ts is two mailto helpers (the rate-limited POST /api/contact leftover stays open but is size M / risk 3, below the challenge risk floor, and /api/ops is being gated by challenge-r03 right now)."
  }
]
