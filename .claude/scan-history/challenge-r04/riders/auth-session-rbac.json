[
  {
    "rider": "invite-accept-flow",
    "files_read": 6,
    "checked": "Read app/invite/[token]/page.tsx, AcceptForm.tsx, invite-result.ts, error.tsx, loading.tsx in full, plus the door behind them app/api/invite/[token]/route.ts and acceptInvite/inviteMember in app/_lib/org-service.ts:74-173. Hypotheses traced: (1) the redeem door hand-mints its own session - route.ts:103-122 sets SESSION_COOKIE and kp_entered with its own attribute set and its own claims, and the comments at :99-102 and :116-121 record two past defects of exactly that hand-rolling (oldest-membership claims; missing kp_entered). That seam went into card A (one session issuer), which rewrites route.ts:93-126 onto the issuer. (2) an active member holding a stale pending invite: GET (route.ts:55-66) never reads user.status, so the preview renders a password form that can only end in 409 already_active (org-service.ts:157). Real, but POST /api/org/invites refuses inviting an active member (app/api/org/invites/route.ts:47-51), so it is only reachable through an invite minted before activation - too narrow for a challenge card; kept as slot B's runner-up. (3) the classifier (invite-result.ts:326-346) and the submit block (:420-432) are already pure and tested; no gap there."
  },
  {
    "rider": "login-flow",
    "files_read": 5,
    "checked": "Read app/login/page.tsx, LoginClient.tsx, login-result.ts, loading.tsx in full and the door app/api/auth/login/route.ts. Hypotheses traced: (1) open redirect through ?next= - closed, safeNextPath resolves with the WHATWG parser and requires same origin (login-result.ts:254-274). (2) 401 vs 429 vs 5xx collapse - closed, classifyLoginResult (login-result.ts:224-233). (3) the login route lands a user on listMembershipsForUser()[0] or the install's DEFAULT_WORKSPACE_ID (route.ts:123-131) and hand-mints three different session shapes (:126, :144, :172) - taken into card A. (4) the sign-in surface only exists as a page: a session that lapses mid-work (7-day TTL, session.ts:19, nothing re-mints it) is answered by proxy.ts:154-157 with a bare 401 that the workspace reads as 'not permitted - sign in as an operator' with no way to sign in short of leaving the page - taken into card B (context login-flow), which reuses classifyLoginResult for an in-place re-sign-in. The open 'Password reset door for forgotten credentials' (leftover-develop, L, architecture) was considered and not adopted: it adds a new unauthenticated account-recovery door plus a public path, which is access-widening in a mode that never builds policy-loosen."
  }
]
