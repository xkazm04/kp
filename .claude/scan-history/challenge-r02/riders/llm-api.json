[
  {
    "rider": "app-master-bench-scenarios",
    "files_read": 13,
    "checked": "Read scripts/app-master-bench/soak/night.mjs (full), night.test.mjs (imports + all 23 test names), the seven scenarios/*.json (ascent, kp-c1-night, kp-default, kp-rung0, kp-tight-budget, personas-self, systedo-case), tenures/kp-owner.json and the three wrappers (soak-night.sh, soak-night.cmd, install.cmd). Hypotheses traced: (1) does the pass-rate matrix mean what it says - passRateMatrix (night.mjs:144-175) groups by rec.scenario, but main() never sets rec.scenario and hard-codes 'kp-c1-night' at :391 and :413, so the 'scenario x window matrix' can only ever have one row and the context-map description ('runs all scenarios serially overnight') overstates the code; (2) miss taxonomy honesty - resolveVerdict (:212-226) and the three-way roster split (:480-498) hold up and are pinned by night.test.mjs; (3) health() treats a 503 as ok (:291), so a degraded kp is not rebooted - deliberate (a 503 health is still up). No card: this is operator bench tooling with no product surface, and the one real gap (single-scenario matrix) is an S doc/code alignment below the challenge floors - backlog material, not a challenge."
  },
  {
    "rider": "companion-appmaster",
    "files_read": 7,
    "checked": "Read pipeline/jobfit/companion_cli.py (docstring + _complete through main, :1-121 and :346-624), appmaster.py (schemas :110-355 and backbone_score/coerce :355-807), live_case.py (full), repo_scan_cli.py (full), repo_scan.py (bind_provider_to_repo + scan_repo :1259-1385 and the symbol index), companion_brain.py (header, recall :345-397, local index :314-340/:574-592, append_episode :637-680) and companion_blocks.py (action validation :243-330); the rest of repo_scan.py (heuristic walk) and companion_blocks.py (table/chart validators) were read by symbol index only. Hypotheses: (a) does the companion record its degradations like the other CLIs - NO: companion_cli.py:361-362 uses bare provider.available() with no provider_availability/emit_deterministic, while repo_scan_cli.py:68,88 does both; the assistant use case's fallback serves never reach llm_usage. Card B ('Routing rows show what each use case actually served') takes this fix into its write set. (b) FINDING, not carded (below effort floor, belongs to a companion card): companion_brain.recall (:345-372) runs BM25 with NO workspace predicate although _index_locally stores workspace_id and indexes (workspace_id, created_at) (:321-333), and run_turn/run_digest call recall(message|query, 6) unscoped (companion_cli.py:509,558) - on a multi-workspace install team B's companion is grounded on, and shows recallUsed excerpts of, team A's episodes; verify whether cross-workspace recall is intended before building. (c) companion_cli.py:371 puts f'{type(exc).__name__}: {exc}' (raw provider text) into fallbackReason, which app/api/companion/[id]/message/route.ts:142,166 forwards to the client and app/_lib/companion-turn.ts:224-231 re-parses as English - a code belongs there (companion API context). (d) appmaster.backbone_score's mandate_rung gate is hard-coded passed:true (:529-533) - a gate that cannot fail; harmless while the schema validator refuses rungs 3/4, noted. (e) live_case.apply_interview_case returns no reason while apply_live_case returns MintOutcome.reason - asymmetric, S-sized."
  }
]
