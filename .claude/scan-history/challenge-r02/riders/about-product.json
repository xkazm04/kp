[
  {
    "rider": "shell-companion-voice",
    "files_read": 9,
    "checked": "Read in full: CompanionVoiceMode.tsx, CompanionVoiceTicker.tsx, VoiceNav.tsx, VoiceParts.tsx, VoicePlayback.tsx, useVoiceHistory.ts, voiceHistory.ts, voiceAnnounce.test.ts, voiceHistory.test.ts. Hypotheses traced: (1) the reader model's pin/held-by-id logic loses the reader's place when a reply lands. It does not: nextVoicePosition is derived per render with no effect (useVoiceHistory.ts:164-172), it is pinned by id, and it is covered by voiceHistory.test.ts:162-181. (2) Arrow keys get stolen from inner controls. They do not: the guard is at useVoiceHistory.ts:194-195. (3) The live-region architecture. The ticker wrapper is permanent (CompanionVoiceTicker.tsx:144-148, pinned by voiceAnnounce.test.ts:30-48), but the same trap it documents recurs twice outside that wrapper: the blocked-playback hint `{playbackBlocked ? <p role=\"status\">` (CompanionVoiceTicker.tsx:168) and the reason/note spans `role=\"status\"` in VoicePlayback.tsx:554-562 both mount together with their content, so a first announcement can be silent. That is an S-size fix (move each status role to an always-mounted wrapper), below the challenge floor, so it is noted for the stabilize loop and gets no card. Also noted: the context-map description says voiceHistory 'persists the turn log across sessions', but voiceHistory.ts:1-3 is a pure in-memory projection with no persistence (map-description drift, not code). No M/L structural seam or experience leap here that is not already shipped: the V3 strip, history nav, retry and proposal errors are all in place. Both cards went to the host."
  },
  {
    "rider": "shell-palette-preview",
    "files_read": 9,
    "checked": "Read in full: PalettePreviewPane.tsx, PreviewEntities.tsx, PreviewHiring.tsx, PreviewInsightsSettings.tsx, PreviewLibraryTools.tsx, previewBits.tsx, previewCache.ts, usePalettePreview.ts, previewCache.test.ts. Hypotheses traced: (1) a cross-tenant cache leak. It is closed: keys are (workspace, query) (previewCache.ts:34-58), a tenant change clears the cache (:79-84), and previewCache.test.ts:31-94 covers it. (2) A stale preview after a mutation. It is closed by useLiveRefresh clearing the memo (usePalettePreview.ts:191-195). (3) A request storm on arrow runs. It is bounded by the 120 ms debounce plus abort (usePalettePreview.ts:169,214-234). (4) The About coupling. PreviewAbout (PreviewInsightsSettings.tsx:68-86) imports CHAPTERS and renders chapter hash links, so neither host card breaks it (slot A keeps chapters.ts untouched, and slot B does not touch anchors). Finding below the floor: several renderers print raw wire slugs as Badge labels instead of resolving them through useEnumLabel: entry stage and approvalKind (PreviewEntities.tsx:61,63), job status (:88), JD analysisStatus (:109), analysis disposition (:131), and billing status (PreviewInsightsSettings.tsx:149). The same file already fixed this for archetype and family (PreviewEntities.tsx:23-27). That is an S/M i18n sweep with no structural move behind it, so it is noted for the stabilize loop and gets no card."
  }
]
