# Journal (append-only, one line per event)

2026-07-02 BOOT started (fresh loop on kp — prior ai-bookkeeper state archived to archive-ai-bookkeeper/, that repo does not exist on this machine). Stack: Next.js 16 + React 19 + better-sqlite3 + @google/genai + ElevenLabs + next-intl + Tailwind 4 + Python jobfit pipeline (codegen'd zod schemas) + Playwright e2e + uat/ + tiger/ harnesses. Branch main (clean except untracked .claude/ship-loop).
2026-07-02 GATE(boot) launched: typecheck → lint → test:unit → test:python → build (sequential; schemas:gen shared). E2e/UAT deferred to backlog items.
2026-07-02 AUDIT: 5 lenses fanned out (functional/routes, tests, security, UX/design-system, LLM-cost&value) — results pending merge.
2026-07-02 GATE(boot): typecheck ✓ | lint ✗ 10 errors + 373 warnings (5× setState-in-effect: DevInspector, BillingTab, HistoryTab, offer/[token], onboarding/[token]; 1× refs-during-render DispositionEditor; 3× i18next literal-string: QuickApplyForm, CalibrationPanel, PipelineTab; 1× react-compiler memoization skip Workspace.tsx) | test:unit ✓ 1055/1055 | test:python ✓ 695 OK (4 skipped) | build ✓ 79 pages, 19.2s
2026-07-02 AUDIT tests lens done 🟡. Estate: 131 node test files (1055 asserts) + 72 py files (695) + 3 e2e specs (1 key-gated) + uat 15 journeys/30 chars (LLM) + eval suites (key-gated, exit-0 skip). NO CI (run_gated.py tripwire built FOR CI but nothing runs it; playwright.config references process.env.CI). Blast-radius holes: ALL 15 better-sqlite3 stores zero behavioral coverage (~5800 LOC: pipeline.ts 1500, core.ts 1322, analytics 631, devcase 640, interviews 394, billing 177…; 2 have source-regex tenancy guards only); ~125/130 API route handlers untested; voice openai.ts transcript-drop risk self-documented+unguarded; offer-finalize/onboarding-store/scheduler-store untested. Hygiene GOOD: no .only, no sleeps, no assert-nothing; 4 py skips tripwired.
2026-07-02 AUDIT security lens done 🟡. Auth EXISTS + well-built (proxy.ts fail-closed allow-list; HMAC __Host- cookie; timing-safe; prod fail-closes w/o password unless KP_ALLOW_OPEN=1) but OFF by default: .env.local sets neither KP_OPERATOR_PASSWORD nor KP_SECRET → app fully open incl. candidate PII. 🔴 LIVE keys in .env.local (Gemini, GitHub ghp_, ElevenLabs sk_) — never committed (verified) but travel if folder zipped → ROTATE. 🟡 denial-of-wallet open-mode: /api/analyze (no rate limit), /api/github-analysis (~31 REST+Gemini, 15min cache), /api/extract-text (PUBLIC even with auth ON, spawns python per request); rate-limiter exists but only on token routes+/api/demo. Voice connect route hardened (single-use, consent, terminal guards; residual: no per-token connect throttle, failed stays reconnectable). SQLi none (parameterized+allow-listed cols); XSS none (Markdown→React, no raw HTML); SSRF constrained (api.github.com pinned). workspace/import DROP/reloads whole DB — fix before KP_MULTI_WORKSPACE.
2026-07-02 AUDIT UX lens done 🟢. Debt ranked: (1) ZERO route-level error/loading/not-found/global-error files across 20 segments+root → unstyled Next defaults on crash/404; only 1 ErrorBoundary mount (Workspace.tsx:266, console-only sink); (2) NO toast layer — success silent (matches idea-f64e941d), ~6+ silent .catch(()=>{}) sites (login, SchedulePicker, WorkspaceTab, IntegrationsCard); (3) PANEL recipe in 7 files vs literal panel string 120× outside landing (maintainability only, dark still re-skins); (4) SimBar.tsx:150 row-2 no flex-wrap → mobile overflow; (5) ScheduleTab.tsx:204 hardcoded #d65a4a/#526b4f in swipe anim (dark drift). Strengths: 0 hardcoded product hex outside exempt dirs; dark ramp fully mapped incl. status shades; i18n COMPLETE (156 files, 2904 keys en+cs parity, check passes); Modal exemplary a11y; pending/disabled guards 480×/83 files.
2026-07-02 AUDIT LLM-cost lens done 🟡. TIGER CORRECTION: llm_usage ledger REBUILT since 06-20 scan (core.ts:513 table; llm.ts insert/ingest/aggregate; monitor.py NDJSON sidecar; python-runner folds per spawn) + prices/cost-stamping/self-repair/gemini-retry all fixed. Remaining: (1) HIGH — flagship cv_analysis + profile_extract + grounded_salary + profile_draft-default (gemini.py direct, 16k max tokens, multimodal) BYPASS ledger — fix = one monitor.emit_result in grounded_answer; (2) HIGH — aggregateLlmUsage has ZERO readers (no /api/llm/usage, no ModelsTab panel) — cost captured but unviewable; (3) MED cv_analysis prompt unbounded (full JD+CV+company+16k schema); (4) MED github-analysis: no retry + env-key-only (ignores BYOM/KP_LLM_CONFIG); (5) MED voice minutes debited quantity-only, provider/cost unattributed; (6) LOW deterministic fallback invisible to ledger (source:"deterministic" parseable but never emitted); (7) LOW .env.example omits GEMINI_API_KEY. Wrapper sites (7 use-cases via claude_cli/anthropic): retry+backoff+fallback+self-repair strong. Caching broad (gemini_cache 168h on analyze/match/automation; meter only on non-cached). analyses store quality per candidate but never joined to cost.
2026-07-02 AUDIT functional lens done 🟢. All 18 workspace tabs wired real (orphan scan: 0 unmounted components); 5 automation gaps verified CLOSED not stubbed (ingest→CLI parser; archetype via useAddToPipeline; comms-dispatch 15 dispatchers + GDPR footer + retry/dead-letter; offer→Hired→onboarding dispatch; scheduler heartbeat via instrumentation-node). Zero alert()/no-op buttons/fake-data-as-real. MAJOR gaps all config-gated + disclosed in-product: (1) landing BUILT NOT LAUNCHED — prod / always mounts dashboard, all CTAs dead-end at /login incl. talk-to-sales; (2) public keyless demo 302s home when gated — root cause tenancy HALF-BUILT (~28 unscoped tables); (3) comms default = outbox terminal-'queued', real delivery needs COMMS_WEBHOOK_URL. MINOR: job-board publish disabled ('coming soon'); keyless deterministic drafts look like AI (disclosed via engine dots); OpenAI voice dark; billing 503 keyless.
2026-07-02 OPS sweep (by hand): NO CI (.github/workflows absent), no Dockerfile/vercel — deploy story = local dev only; README strong (3-engine setup + keyless degradation); .env.example tracked & documented; dev servers live on :3000 (vibeman) + :3001 (kp). DISCOVERY: kp has Polar billing layer (docs/BILLING.md, billing routes 503 keyless) + multi-provider LLM key layer (docs/LLM_PROVIDER_LAYER.md, KP_SECRET AES-GCM).
2026-07-02 CP0: USER AFK → provisional: cadence=milestone; ship bar DEFERRED→CP1; UAT depth=deterministic e2e only; M1="correctness+cost visibility" items 4,7,8,9,15,21 (+12 in gate) on branch ship-loop/m1; item 6 key-rotation = user action flagged for CP1.
2026-07-02 M1 launched: 3 agents in parallel (item 4 lint; items 7+15 security guards; items 8+9 metering+usage panel); item 21 done inline.
2026-07-02 ITEM 21 ☑ .env.example gains GEMINI_API_KEY (+alias note, verified against engine-preflight.ts); base.py pricing-contract comment now cites test_llm_base.py (commit cf0d63c).
2026-07-02 ITEMS 7+15 ☑ (commit 8ef6392): per-IP limits on analyze 30/10min (verified UI sends ONE request per run, no fan-out), github-analysis 10/10min (after free cache path), extract-text 20/10min; workspace/import 503s under KP_MULTI_WORKSPACE (via canonical multiWorkspaceEnabled()); interview/connect 6/10min per TOKEN (link=credential, IP rotation can't reset; 1 start+5 reconnects). 11 new tests; suite 1066/1066; tsc+eslint clean. Auto-decisions: 503 not 409; real 429 on github-analysis despite its 200+{error} style (client handles both); no Retry-After (no existing consumer sends one).
2026-07-02 ITEMS 8+9 done by agent (commit pending — messages/*.json shared with in-flight lint agent, will commit after it): gemini.py grounded_answer now meters via monitor.emit_result (use_case threaded from callers: cv_analysis/profile_extract/grounded_salary/profile_draft; cost via shared price_usd; exception-swallowed so metering can never break analysis; spend recorded even when JSON parse fails; failures emit_error only). NEW GET /api/llm/usage (requireOperator, ?days= 1..365) + UsagePanel in ModelsTab (per-use-case calls/tokens/cost + prompt-cache stats, PANEL recipes, en+cs keys). py 703 OK (+8), unit 1069 (+3 contract), eslint/tsc clean in isolation, i18n 2919 keys parity.
2026-07-02 ITEM 4 ☑ (commit d903e8d): 10 lint errors fixed, 0 disables added. Notable: DevInspector hydration gate proven redundant and removed; Workspace selectTab deps satisfy react-compiler (memoization preserved); 3 token pages restructured to .then-callback loads with Retry-handler resets, terminal branches settle both failure flags. Lint 0 errors/373 warnings; i18n 2922 parity.
2026-07-02 ITEMS 8+9 ☑ (commit 517ff33): metering + /api/llm/usage + UsagePanel landed (details in prior entry).
2026-07-02 GATE(M1) launched: typecheck, lint, test:unit, test:python, build, deterministic e2e (modal-escape, profile-builder).
2026-07-02 GATE(M1) partial: typecheck ✓ | lint ✓ 0 errors | test:unit ✓ 1069 | test:python ✓ 703 | build ✓ | e2e ✗ 5/5 FAILED — diagnosed: NOT an M1 regression, suite rotted on main (no CI): (a) HomeGate dev-auth landing gate swallowed every workspace locator; (b) Pick wrapping-<label> defeats getByLabel (option texts pollute label text); (c) student pill label drifted to short "Student".
2026-07-02 ITEM 12 ☑ (commit 2ae3f01): e2e suite REPAIRED — e2e/dev-auth.ts seeds kp_dev_authed via addInitScript in all 3 specs; Pick selects → getByRole combobox (accname); student pill assertion updated (full phrasing still pinned on routing line + save payload). 5/5 pass in 8.1s warm. GATE(M1) now FULLY GREEN.
2026-07-02 MILESTONE 1 ☑ COMPLETE: items 4,7,8,9,12,15,21 · commits cf0d63c, 8ef6392, d903e8d, 517ff33, 2ae3f01 on ship-loop/m1 (5 commits, main untouched). NEXT: CP1.
2026-07-02 CP1: USER AFK → provisional: ship bar STILL deferred; M2="Tests & CI" (items 5,10,11); NO merge to main while AFK (M2 stacks on ship-loop/m1); UAT stays deterministic. GitHub remote confirmed (xkazm04/kp) so CI will be live on push.
2026-07-02 M2 launched: 2 agents (item 5 CI workflow; items 10+11 store+handler behavioral tests incl. loadability investigation of the "@/ alias / not unit-loadable" claim).
2026-07-02 ITEM 5 ☑ (commit ff68217): .github/workflows/ci.yml — node-quality (typecheck/lint/unit/build, python in-job for schemas:gen) + python-gate (KP_SKIP_BASELINE=4 pinned w/ per-skip derivation); SHAs live-verified; actionlint clean; every step mirrored locally exit 0. Deferred to first live run: npm ci cold, linux wheels/better-sqlite3, envless build.
2026-07-02 ITEMS 10+11 ☑ (commit 30f4c3e): "not unit-loadable" claim DISPROVED — real blockers were ESM resolution (extensionless relatives, @/ alias, JSON attrs, next/server subpath, 1 TS param property) → scripts/test-alias-loader.mjs (~100 lines, test-only) + --experimental-transform-types. unit-db.ts = per-file isolated KP_DB_PATH (import order load-bearing) + env scrub + mtime-gated sweep. 66 new behavioral tests: core migrations (double-init no-op), pipeline store (CAS, terminal ceilings, consent sweep), offer lifecycle (accept-once→Hired+onboarding; stale decline can't demote a hire), onboarding+scheduler stores, and 30 route tests (offer/onboarding/pipeline/billing/schedule: 409 stale-snapshot, 410 lapsed, 422 manual-Hired, webhook HMAC+dedupe+once-per-order, leak-free public views). Suite 1135/1135 ×3 stable. Follow-ups suggested: upgrade 2 regex tenancy guards to behavioral; fix their stale comment.
2026-07-02 GATE(M2) launched: typecheck, lint, unit ×2 (isolation), python, build, deterministic e2e.
2026-07-02 GATE(M2): typecheck ✓ | lint ✓ 0 err | test:unit ✓ 1135 ×2 (isolation confirmed) | test:python ✓ 703 | build ✓ | e2e ✓ 5/5. MILESTONE 2 ☑ COMPLETE (items 5,10,11; commits ff68217, 30f4c3e). Scorecard: Tests 🟡→🟢, Ops 🔴→🟡 (CI exists, activates on push). NEXT: CP2.
2026-07-02 CP2: USER AFK → provisional: ship bar deferred (4th ask CP3); NO merge/push (outward); M3 = items 18,19,20,14. Toast = in-house (no new dep while AFK); value-case.md = loop artifact.
2026-07-02 M3 launched: 4 agents (18 route chrome; 19 toast layer; 20 mobile/color fixes; 14 value & market lens w/ web research).
2026-07-02 ITEM 14 ☑ value-case.md written (loop artifact, ~180 lines, 12 competitors cited, repo claims verified). Headlines: (1) niche open — nobody combines Czech-first voice + LLM-era dev-case + EU-audit workflow; Maki People = closest language threat; (2) GTM wedge — Teamio (default Czech ATS, 1000+ firms, public API, NO AI) → "AI layer for Teamio shops"; (3) unit econ: full AI screen ≈ $2-3.50 COGS vs $100-200 human; $3-5/candidate undercuts all incumbents at 60-80% margin, fits Polar minute-pack scaffold; (4) EU AI Act high-risk (Annex III/4) enforceable 2026-08-02 (31 days) — approval gates+audit = head start on Art.14/26, but provider duties (conformity, CE, registration) absent; (5) sellability blockers verified in repo: single-tenant, auth-open, outbox-only comms, single-node sqlite, no deploy, sandbox billing, no SOC2/ATS connectors; (6) industry lock partially softened in code (compliance-regimes.ts 7 jurisdictions, APP_CURRENCY) but taxonomy still bank/tech → 0/20 stands; 6 CP-ready verdicts V1-V6 in artifact. Scorecard: dim 9 🔴→🟡 (lens run, verdicts pending user).
2026-07-02 M3 interruption: items 18/19/20 agents killed mid-run by the Claude subscription session limit (reset 15:20). Partial tree state: 18 mostly landed (chrome files, i18n keys missing), 20 both files edited, 19 nothing. All 3 RESUMED from transcript after reset with tree-state briefing. (Known risk — memory note "watch the session limit on big runs" confirmed again.)
2026-07-02 ITEM 20 ☑ (commit 5b11553): SimBar row-2 flex-wrap (360px verified, no overflow) + ScheduleTab swipe colors -> color-mix(var(--color-coral/moss)) (WAAPI-verified both themes). NOTE: kp dev server no longer on :3001 (Ascent took it) — agent verified on :3007 and freed it; memory note stale.
2026-07-02 ITEM 18 ☑ (commit 3c65bcb): route chrome everywhere — shared RouteError/RouteLoading (3 skeleton geometries), root 404/error/global-error (bilingual static in global-error, no provider there), 9 segments paired. Locale strategy per file documented; no lint override needed (rule is warn-level outside migrated globs + constants-as-expr). Trade-off flagged: loading.tsx converts notFound() segments to streamed 200+404-UI+noindex — drop jds/apply loading.tsx if hard 404 ever matters for crawlers. Verified: lint 0, tsc 0, i18n 2932 parity, build 79/79, manual 404 drive en+cs on :3005 (dev PID lock cleaned).
2026-07-02 ITEM 19 ☑ (commit d21320a): in-house toast layer — pure store (dedupe/cap-3/FIFO, 12 tests) + portal Toaster (top-right, --z-toast token, spring layout, reduced-motion, alert/status roles, Spark Dark sticker re-skin w/ tilt); mounted in root layout; wired: login network error, SchedulePicker ×3, WorkspaceTab ×2, IntegrationsCard load + 4 success toasts (sign-in, workspace created, webhook saved, profile saved). Visual verify both themes on :3005.
2026-07-02 GATE(M3) launched: typecheck, lint, unit, python, build, e2e.
2026-07-02 GATE(M3): typecheck ✓ | lint ✓ 0 err | unit ✓ 1147 | py ✓ 703 | build ✓ | e2e ✓ 5/5. MILESTONE 3 ☑ COMPLETE (items 14,18,19,20). Scorecard: UX consolidated 🟢, Value 🔴→🟡 (lens run, verdicts pending). Loop totals: 10 commits, +92 unit tests, 3 green gates. NEXT: CP3 (user present).
2026-07-02 CP3 ANSWERED (user present): ship bar = PUBLIC PRODUCT PATH; verdicts V1+V2+V4a+V4b adopted (sequenced: bank vertical now, unlock as growth); M4 = full /uat run.
2026-07-02 MERGED+PUSHED: ship-loop/m1 → main via --no-ff merge 3395b4c (83 files, +3902/-120); push required gh auth switch kazimi66→xkazm04 (403 fix; xkazm04 left active); origin/main b3024fe..3395b4c — first CI run triggered.
2026-07-02 M4 launched: full /uat run delegated (engine .claude/skills/uat.md + kp overlay; L1 all, L2 prioritized buyer→money-path→L1-flags; LLM spend user-approved; checkpoint-on-limit instructed). Backlog +25 (Teamio spike), +26 (AI-Act pack), +27 (deploy story) from CP3 verdicts + ship bar.
2026-07-02 CI FIRST RUN GREEN (run 28595272378): node-quality 2m06s + python-gate 20s on ubuntu — cold npm ci, linux wheels, better-sqlite3 native, envless build, skip-baseline=4 all held. Cosmetic: pinned action SHAs target deprecated Node 20 runtime (forced to 24) — bump SHAs in a future pass.
2026-07-02 M4 interruption #2: uat agent + its reconciliation subagent killed by session limit mid-L2. Checkpoint held: L1 COMPLETE 28/28 pairs, 195 findings (2 pass / 26 conditional / 0 fail); L2 buyer journeys substantially shot (evaluate-and-buy 01-07, guided-simulation 10-22) but unverdicted. Agent RESUMED after reset with disk-state briefing. L1 headline for CP4: money path simulated by default (comms outbox), buyer entry fail-closed on prod (landing dark), tenant-blind stores recur across journeys — all converge on ship-bar items 1/2/3.
2026-07-02 ITEM 13 ☑ M4 COMPLETE: full /uat run (uat/runs/2026-07-02-full — report.md, SUMMARY.md, findings.json 301 rows/238 unique, reconciliation.md). L1 28/28: 0 fail, 2 pass, 26 conditional, 195 findings. L2 6 journeys/13 character-passes: evaluate-and-buy FAIL (prod front door = password wall; anon demo read 53 named candidates), guided-simulation FAIL ×2 (NEW gsim-l2-101: deterministic crash Interview→Offer, pipeline.ts:1322-1331 — demo climax+CTA never render), offer/apply/pipeline/screening conditional. LIVE-confirmed: 3 blockers + 43 majors + 28 minors. All 3 ship blockers (landing, delivery, tenancy) confirmed live. Cross-surface rot: 3 divergent match producers (46 vs 92; calibration measures the one that never acts); 12/12 outbox queued-terminal vs 8 "sent" surfaces; 46/53 tables workspace-blind; /api/decisions/records 200 UNAUTHENTICATED w/ sealed records; fabricated match-0 auto-reject SEALED into Art.22 chain; voice /connect leaks recruiter brief to candidate; NULL locale → English letters to Czech candidates; (SIM) rows in live ROI. Strengths to protect: preview-first waves, sealed-reversal chain, candidate token pages, fairness shield, per-offer deadline, comms center honesty. PANEL VERDICT: "nobody rejected the product; everybody rejected shipping it as-is."
2026-07-02 GATE note: M4 was evidence-gathering only (no code changed) — no build gate needed; tree clean.
2026-07-02 Backlog +28..38 filed from uat L2/reconciliation (live-confirmed bugs, grouped).
2026-07-02 CP4: USER AFK → provisional: M5 = bug wave (28-32,34-38, minus 33); pillars M6+ = tenancy→delivery→landing. M5 wave 1 launched: 3 agents (K: sim crash 28+36; L: security 29+30+35; M: match-0 fabrication 31).
2026-07-02 ITEM 31 ☑ work done (commit deferred — tree shared with in-flight agents K/L): 8 `?? 0` sites judged (5 dangerous fixed, 1 dead coercion tightened, 1 sim fixed, adjacent legit left); new app/_lib/match-score.ts (isScored + null-safe comparators, unscored sorts below genuine 0); wave semantics fail-closed: unscored → explicit keep/"unscored" outcome, never enters signed reject set, sealed records carry TRUE score or none; UI dashes (ScreenWaveModal, group-eval, sim); reconsider queue flags unscored. Source-level guard test bans `matchScore ?? 0` tree-wide. 1169 unit green, i18n 2942 parity.
2026-07-02 ITEMS 29+30+35 ☑ work done (commit with wave): voice /connect no longer returns instructions/groundedPrompt — OpenAI keeps brief server-side (client_secrets), ElevenLabs candidate mode gets safe-by-construction generic agentPrompt (its signed-URL flow has no server prompt config — verified vs docs); /api/decisions/* all 7 entry points requireOperator-gated (demo sessions rejected; candidate Art.22 flows verified untouched; NOTE: gated-deploy demo sim wave calls now 401 — interacts with tenancy pillar); erasure+onboarding links absolute via ambient request origin + loud warn on empty resolution; consent TTL displayed from KP_CONSENT_TTL_DAYS (round-up months, ICU cs plurals, compliance route exposes it). 12 new tests; 1172/1172 merged tree; i18n 2942.
2026-07-02 ITEMS 28+36 ☑: sim double-advance fixed (one accept = one stage; bare Offer-accept 422s; Hired only via candidate token accept) — LIVE PROOF: keyless sim done:true in 113s, offer precedes hire, 12/12 seals auto:sim, /api/analytics byte-identical across a full demo run; (SIM) rows excluded from all 7 analytics queries + Today rail via NULL-safe predicate (board/sim unaffected).
2026-07-02 M5 WAVE 1 ☑ committed b5aa2ad (45 files, +1329/-123, 29 new tests, suite 1172/1172). Wave 2 launching: agent N (32 comms honesty + 38 polish trio), agent O (34 locale + 37 offer path).
2026-07-02 ITEMS 34+37 ☑ work done (commit with wave 2): locale — 6 NULL write-paths now infer (profile languages TS-mirror of _candidate_lang) + workspaces.default_locale column (cs, validated read) as read-time fallback in commsTranslator/formatOfferDeadline; LLM letters get --lang + cache-key fold (AUTOMATION_VERSION v2). Offer path — DecisionsTab surfaces {offerExtended, link} (toast + copyable panel); command route HOLDS Offer-stage targets (heldAtOffer in response+UI, drafted offer_review preserved byte-identical); letters carry localized deadline+start lines from the real offer row; _NEUTRAL_STYLE bans gendered phrasing in all 3 letter prompts. 9 new test files; 1201 unit + 704 py green; i18n 2974 parity.
2026-07-03 ITEMS 32+38 ☑ + M5 WAVE 2 committed 9fbd384 (57 files, +1549/-151): comms-truth.ts single-source capability + deliveryClaim vocabulary across 19 surface families (quick-apply finally shows its status link; "sent" re-engages automatically when a relay lands); scheduler-health 24h/supersede error gate; useTaskResult gives up after 5 fetches → retryable error + toast; Ano/Ne fix. MILESTONE 5 ☑ COMPLETE (items 28-32, 34-38 minus 33): 2 commits (b5aa2ad, 9fbd384), 54 new tests. GATE(M5) launched.
2026-07-03 GATE(M5): typecheck ✓ | lint ✓ 0 err | unit ✓ 1201 | py ✓ 704 | build ✓ | e2e ✓ 5/5. MILESTONE 5 ☑. Scorecard: dims 1,2,3,4,7 🟢 · 5,6,8,9 🟡 · 0 🔴. Loop day-1 totals: 5 milestones, 22 backlog items closed, 4 gates green, ~200 new tests, full uat run, value case, CI live. NEXT: CP5.
2026-07-03 CP5: USER AFK → hold push; M6 = quick wins 16,17,22,23,33 then pause. 3 agents launched (P: 16+22 ledger completeness; Q: 17+23 gemini hardening; R: 33 score single-source).
2026-07-03 ITEMS 17+23 ☑ work done (commit with M6): github-analysis gains withGeminiRetry (TS mirror of base.py policy: transient-only, 3 attempts, 500ms*2^n+jitter), BYOM→platform→env key precedence via new resolveProviderKey in llm-config.ts (undecryptable stored key fails LOUD, matching buildLlmConfigEnv rationale), + llm_usage row (useCase github_analysis, cost mirror of MTOK_PRICES). cv_analysis prompt budgets EVIDENCE-BASED: JD 30k (real corpus max 23,924 — suggested 20k would clip!), company 20k, CV 60k (fixtures 26-47× under); truncation marker; under-budget byte-identical; eval-fixture size guard test. 1226 unit + 717 py green. Follow-up flagged: engine-preflight.ts still env-only for readiness dot.
2026-07-03 ITEMS 16+22 ☑ work done (commit with M6): voice minutes → llm_usage rows (minute-prices.ts LOCAL-ESTIMATE map mirroring MTOK_PRICES conventions; openai 0.15/min, elevenlabs 0.09/min; model attribution incl. call-time env resolution; idempotent, failed calls never bill; visible via existing UsagePanel fold); monitor.py emit_deterministic (zero-cost source:"deterministic" lines) wired at 5 CLI fallback sites + devcase per-step; aggregate shows "deterministic" as own provider row with NO TS changes. 17 new tests; 1226 unit + 717 py green.
2026-07-03 ITEM 33 ☑ + M6 committed 3f03857 (50 files, +1792/-83): canonical score (analysis>snapshot>null) + ScoreProvenanceLabel on board/drawer/decisions/offer; offer pricing = own labeled concept (matchBasis, offer-v3 self-invalidating); calibration measures the ACTING score (live n 0→21, brier 0.163, source selector). MILESTONE 6 ☑ COMPLETE (items 16,17,22,23,33). GATE(M6) launched. Follow-up noted: engine-preflight env-only readiness dot.
2026-07-03 GATE(M6): typecheck ✓ | lint ✓ | unit ✓ 1237 | py ✓ 717 | build ✓ | e2e ✓ 5/5. MILESTONE 6 ☑. CP6: LOOP PAUSED — day-1 complete: 6 milestones, 27 items, 5 green gates, 15 commits (4 unpushed pending user), scorecard 6×🟢 3×🟡 0×🔴. Awaiting: push decision, key rotation (6), tenancy go/no-go.
2026-07-03 CP6 PUSH: 4 held commits (11aa76e, b5aa2ad, 9fbd384, 3f03857) pushed to origin/main (3395b4c..3f03857); local main now in sync. CI run 28645194227 triggered. Loop remains PAUSED — open user actions: rotate .env.local keys (6), tenancy-pillar go/no-go.
