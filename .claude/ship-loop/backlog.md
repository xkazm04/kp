# Backlog  (☐ todo · ◐ in progress · ☑ done · ✕ cut)  — numbering append-only, never renumber

| # | S | Dim | Size | Item |
|---|---|-----|------|------|
| 1 | ☑ | 2-Func | M | CLOSED out-of-band, code-verified at CP9: WebhookChannel POSTs kp.comm.v1 to COMMS_WEBHOOK_URL w/ bounded retry (comms.ts:54-101); fail-closed callback route updates status w/ replay guard (api/comms/callback:42-118); failure_detail persisted + surfaced; no-relay mode honestly labeled everywhere (comms-truth.ts). Residual → item 51 |
| 2 | ☑ | 2-Func | L | CLOSED out-of-band, code-verified at CP9 (subsumed by item 40/E0): assertTenancyReady fail-closed boot guard, 0 gaps by construction (tenancy.ts:241-279, core.ts:1174); ~38 scoped + 29 exempt + 24 lazy tables in lockstep w/ tenancy-coverage.test; ~20 per-store tenancy tests; per-tenant decision chains (decision-record-store.ts:134-144) |
| 3 | ☑ | 2-Func | M | LAUNCHED (M8, 284aebd+222bedf): '/' server-gates landing↔dashboard on the real session (password mode) / kp_entered marker (open mode); /api/auth/login mints in open mode (no 503 lock-out); enterWorkspace() rewires all dead sign-in CTAs; SEO (robots/sitemap/site-url); theme pre-paint decoupled to kp_entered. Verified live open-mode + full gate green. FOLLOW-UP (tenancy-blocked): public demo CTA (/api/demo) still 302s on locked prod until KP_DEMO_ENABLED — parallel session's area |
| 4 | ☑ | 1-Build | S | Fix 10 lint errors: 5× setState-in-effect (DevInspector, BillingTab, HistoryTab, offer/[token], onboarding/[token]), refs-during-render (DispositionEditor.tsx:91), 3× i18n literal (QuickApplyForm, CalibrationPanel, PipelineTab:751), react-compiler memoization skip (Workspace.tsx:103). 373 warnings out of scope |
| 5 | ☑ | 8-Ops | M | CI: GitHub Actions running typecheck + lint + test:unit + test:python:gate (+ build); run_gated.py tripwire was built FOR CI and nothing runs it; e2e stays out (keys) — ci.yml, commit ff68217; first live run GREEN |
| 6 | ☐ | 6-Sec | S | USER ACTION: rotate 3 live keys sitting in .env.local (GEMINI AIzaSy…, GITHUB ghp_…, ELEVENLABS sk_…) — never committed, but travel with any zipped/shared tree |
| 7 | ☑ | 6-Sec | M | Rate-limit + quota the open-mode money routes: /api/analyze (no limit), /api/github-analysis (~31 REST+Gemini), /api/extract-text (public even with auth ON, spawns python per request); reuse app/_lib/rate-limit.ts |
| 8 | ☑ | 5-Bill | S | Meter the flagship: monitor.emit_result in gemini.py grounded_answer → cv_analysis/profile_extract/grounded_salary/profile_draft-default finally write llm_usage rows (usage counts already returned) |
| 9 | ☑ | 5-Bill | M | Surface the ledger: GET /api/llm/usage + cost/usage panel in ModelsTab (aggregateLlmUsage + promptCacheStats have ZERO readers today) |
| 10 | ☑ | 3-Test | M | Behavioral tests for top-blast stores (in-memory sqlite): db/pipeline.ts (1500 LOC), db/core.ts migrations (1322), offer-finalize, onboarding-store, scheduler-store — all currently zero execution coverage |
| 11 | ☑ | 3-Test | M | API handler tests for money/state routes: offer, onboarding, pipeline, comms, billing, schedule (~125/130 handlers untested) |
| 12 | ☑ | 4-UAT | S | Run deterministic e2e in-loop → found suite ROTTED on main (dev-auth gate + 2 spec drifts), repaired (e2e/dev-auth.ts + role locators + pill label); 5/5 green baseline (commit 2ae3f01) |
| 13 | ☑ | 4-UAT | M | /uat FULL run done (runs/2026-07-02-full): L1 28/28 (0 fail), L2 6 journeys — 238 findings, 3 blockers + 83 majors, panel verdict "adopt internally, don't ship as-is"; spawned items 28-38 |
| 14 | ☑ | 9-Value | M | Run value & market lens → value-case.md: competitor map + per-op ROI + production-reality checklist; seed = industry-locked finding (bank/Czech/tech taxonomy, CZK comp, GDPR-only; 0/20 HR cohort adoption) |
| 15 | ☑ | 6-Sec | S | Guard workspace/import (DROP/reloads whole DB — must not survive into KP_MULTI_WORKSPACE) + per-token connect throttle on voice /api/interview/connect |
| 16 | ☑ | 5-Bill | S | Attribute voice minutes: record provider+model (OpenAI Realtime vs ElevenLabs differ materially) alongside interview_minutes quantity |
| 17 | ☑ | 2-Func | S | github-analysis: add retry (only Gemini path without one) + route key through BYOM/KP_LLM_CONFIG instead of env-only |
| 18 | ☑ | 7-UX | M | Route-level resilience chrome: root error.tsx + global-error.tsx + not-found.tsx + loading/error for the biggest segments (currently 0 across 20 segments → unstyled Next defaults) |
| 19 | ☑ | 7-UX | M | Toast/feedback layer: success + error surfacing for mutations (success is silent today; ~6 silent .catch(()=>{}) sites: login, SchedulePicker×3, WorkspaceTab×2, IntegrationsCard) |
| 20 | ☑ | 7-UX | S | SimBar.tsx:150 row-2 flex-wrap (mobile overflow) + ScheduleTab.tsx:204 hardcoded #d65a4a/#526b4f swipe-anim colors → tokens |
| 21 | ☑ | 8-Ops | S | .env.example omits GEMINI_API_KEY/GOOGLE_API_KEY (the flagship gate!) — add with comment; fix base.py:44 doc drift (cites nonexistent test_llm_pricing.py) |
| 22 | ☑ | 5-Bill | S | Emit source:"deterministic" ledger rows on keyless fallback (parseLedgerLine supports it, nothing emits) so degraded output ≠ no traffic |
| 23 | ☑ | 5-Bill | S | Bound cv_analysis prompt blocks (full JD+company+CV concatenated uncapped, 16k out; github-analysis caps everything — mirror that) |
| 24 | ☐ | 7-UX | M | Recipe adoption drive: 120 literal panel strings vs 7 PANEL imports (maintainability only — dark theme still re-skins; cut candidate) |
| 25 | ☐ | 9-Value | M | V1 Teamio-wedge spike: research Alma Career/Teamio public API, design the import/export bridge (applicants+roles in, analysis/shortlist out), scoped integration plan |
| 26 | ☑ | 9-Value | M | V2 AI-Act conformity pack DONE (M9, commit 501f95d): docs/AI_ACT_CONFORMITY.md — Annex III pt.4 classification, per-article map w/ file:line evidence (Art. 9-15/26/50/72-73/86 + GDPR 22), gap register G1-G14 → E-track, Annex IV skeleton, deployer quick-sheet. Verdict: Art.14+12 posture strong; gaps are mostly docs/process + 3 code items → spawned 52/53 |
| 27 | ☑ | 8-Ops | M | Deploy story (ship-bar requirement): Dockerfile (node+python+sqlite volume), hosting recipe, prod env checklist — DONE via E4 self-host (185a502 packaging+guide, eec5a92 slim 1.78GB→465MB, c792dc4 Helm, f7d8c97 KP_OFFLINE). Parallel session, 2026-07-04/05 |
| 28 | ☑ | 2-Func | S | L2 BLOCKER gsim-l2-101: guided sim crashed deterministically at Interview→Offer (screen-step double-advance) — FIXED + live-proven (done:true 113s, no phantom hire, auto:sim seals); commit b5aa2ad |
| 29 | ☑ | 6-Sec | S | L2 major TP-L2-VOICE-01: recruiter brief leaked to candidate browser via /connect — FIXED (OpenAI server-side, ElevenLabs safe-by-construction prompt); commit b5aa2ad |
| 30 | ☑ | 6-Sec | S | L2 major SD-L1-010: /api/decisions/* unauthenticated — FIXED (requireOperator on all 7 entry points, candidate token flows preserved); commit b5aa2ad |
| 31 | ☑ | 5-Bill | M | L2 major REC-03/SD-L1-002: matchScore ?? 0 fabrication — FIXED (match-score.ts, fail-closed unscored outcome, true scores in seals, tree-wide guard test); commit b5aa2ad |
| 32 | ☑ | 2-Func | M | L2 major REC-10: comms honesty — 8 surface families claim "sent"/"we emailed you" while ALL outbox rows are queued-terminal; until item 1 lands, every surface must say queued-not-delivered (quick-apply false claim capst-l1-002 + missing status link included) |
| 33 | ☑ | 5-Bill | M | L2 major REC-01/OO-L2-10: match score has THREE diverging producers (46 vs 92 same candidate; offer priced off a number the header never shows; calibration measures the producer that never acts) — single-source the score (overlaps Vibeman ideas 92bc1445/1425ac31) |
| 34 | ☑ | 7-UX | S | L2 major pa-l2-null-locale: 60/65 pipeline entries have NULL locale → Czech candidates get ENGLISH letters under the bank brand — infer/require locale at entry creation + fallback to workspace locale |
| 35 | ☑ | 6-Sec | S | L2 majors: dead GDPR erasure link (relative path) + hardcoded "12 měsíců" vs KP_CONSENT_TTL_DAYS — FIXED (ambient-origin links + config-derived ICU months); commit b5aa2ad |
| 36 | ☑ | 2-Func | S | L2 major REC-07/gsim-l2-105: (SIM) rows polluted live analytics — FIXED (NULL-safe exclusion in all 7 aggregate queries + Today rail; sim still sees itself); commit b5aa2ad |
| 37 | ☑ | 7-UX | M | L2 majors offer path: "Send offer" silently discards the {offerExtended, link} the server returns (DecisionsTab.tsx:176-214 — surface link + toast); letter below senior bar (no deadline in a 24h offer, no start date/benefits, masc. grammar to a woman); advance-top-N destroys drafted offers (command/route.ts:101-102) |
| 38 | ☑ | 7-UX | S | L2 minors quick wins: EB-L2-11/OO-L2-15 stale scheduler_runs error rendered as current on Pipeline; OO-L2-12 infinite "Pracuji…" no error state; capst-l2-101 hardcoded English Yes/No in cs chat |
| 39 | ☑ | 5-Bill | S | Pricing tune-down + Enterprise tier: Starter 490→240 Kč ($21→$10), Growth 1190→480 Kč ($50→$20); new contact-sales `enterprise` plan (plans.ts contactSales, isSelfServePlan gate, checkout 400, BillingTab "Custom"/Talk-to-sales card, landing band capabilities, NEXT_PUBLIC_SALES_EMAIL mailto); DONE — commit pending. Spawned the E0-E6 enterprise-readiness epics below (docs/ENTERPRISE_READINESS.md) |
| 40 | ☑ | 2-Func | L | **E0 Tenancy + identity foundation** — DONE, code-verified at CP9: identity layer live (organizations.ts, memberships.ts w/ capability overrides, invites.ts create/redeem/accept/revoke, users.ts, roles.ts); full workspace_id scoping + fail-closed assertTenancyReady (see item 2). Unblocks E1/E2/E5/E6. docs/ENTERPRISE_READINESS.md §1 |
| 41 | ☐ | 6-Sec | L | **E1 Enterprise SSO & RBAC** — SAML/OIDC + SCIM (recommend WorkOS/Stytch to compress), enforced SSO, server-side role enforcement, per-session revocation. Gated on E0. ENTERPRISE_READINESS.md §2 |
| 42 | ☐ | 6-Sec | L | **E2a Audit expansion** — per-tenant decision-chain re-architecture (org plan §6, the hard one) + broad `audit_events` (auth/role/config/PII/export) + SIEM/CSV export. Gated on E0/E1. §3 |
| 43 | ☐ | 6-Sec | M | **E2b GDPR/DPA pack** — DPA + sub-processor register, DPIA for AI candidate eval, full data-subject rights (access/portability/rectification), RoPA, EU residency; fold in AI-Act #26. §7 |
| 44 | ☑ | 7-UX | M | **E3 Brand / white-label** — per-org brand store + token injection over the dual-theme layer + custom-domain guide. DONE (dc21fdd white-label, a14db7d brand name/logo+domain). Parallel session. §4 |
| 45 | ◐ | 8-Ops | L | **E4 Self-host / licensed** — Docker/Helm (finishes #27) ☑; KP_OFFLINE no-egress ☑ (f7d8c97); self-hosted OpenAI-compat model endpoints ☑ (a3f0c85); Postgres seam audit ☑ (2a5f7a4). REMAINING: license decision (BSL), full Postgres backend, residency. Parallel session. §5 |
| 46 | ☐ | 6-Sec | L | **E5 SOC 2 (+ISO 27001)** — gap assessment (Vanta/Drata) → policies/runbooks → evidence + pen test → Type I → Type II. Calendar-bound (3–6 mo obs window + $$); start early, finishes after E1/E2 controls operate. §6 |
| 47 | ☐ | 5-Bill | M | **E6 Org billing + seats** — `org_id`-keyed billing tables, seat quantity in checkout+webhook, seat enforcement vs memberships, per-team metering, `llm_usage` attribution (org plan Ph 3). §8 |
| 48 | ☑ | 9-Value | M | Market-pulse salary recalibration: hand-authored CZK bands calibrated to ISPV rok 2025 + `market:apply` script + salary_benchmarks.manual overrides; SE-junior anchor [45000,70000]→[43000,67000]; 4 test_jobs.py expectations updated. DONE by parallel session (6e6a534) |
| 49 | ☑ | 1-Build | M | **M7 reconcile & re-green** (CP7): parallel enterprise pushes left CI red all day. Fixed: useOrgMembers set-state-in-effect (lint), /invite/[token] prerender opt-out (build), and the real root cause — npm ci ERESOLVE on the pinned next canary (.npmrc legacy-peer-deps) + the react-is peer it then dropped (promoted to direct dep). Commits 6e3ac90, 7873b39, 6fcd7d6 |
| 50 | ☑ | 8-Ops | S | CI-discipline guard DONE (M9, e9ebb52): .githooks/pre-push runs typecheck+lint+build only when a push targets refs/heads/main (feature branches free); wired via npm prepare → core.hooksPath; LF-pinned via .gitattributes; KP_SKIP_GATE=1 recorded-emergency escape. Follow-up idea: add lint to ci.yml (it's absent — why 7 lint errors accumulated silently) |
| 51 | ◐ | 8-Ops | S | Relay ops residual (from item 1 verification): .env.example documented neither COMMS_WEBHOOK_URL nor COMMS_CALLBACK_SECRET — DONE in M9 (0417b1b). Residual left open: optional in-app relay config UI (ChannelsTab has zero relay references; OutboxSection just says "set the env var") |
| 52 | ☑ | 6-Sec | M | DONE (M10, 3f49c4c): AiDisclosure on /status+/onboarding (Art. 50 coverage complete) + candidate Art. 86 decision-history: token-auth sibling endpoint (rate-limited, workspace-scoped, deny-by-default kind allowlist), closed redacted wire shape (attribution from sealed actor, score-vs-threshold facts only; no approver/rationale/hashes), consent-withheld ⇒ indistinguishable empty; 19 i18n keys ×4 locales; 7 tests incl. leakage scan |
| 53 | ☑ | 3-Test | M | DONE (M10, d531dad): name/gender-proxy neutrality PINNED — INVARIANT HOLDS (byte-identical MatchResponses across cs-m/cs-f-ová/vi/uk/ar/Roma names; sentinel asserts label is the only name carrier) + calibration_drift.py Art. 72 seed (Brier +0.05/PSI 0.25/base-rate 0.10, insufficient_data gate; unwired by design). 23 tests, no new skips |
| 54 | ☑ | 7-UX | S | DONE (M10, 7eeedd8): create CTA hoisted beside the Candidate-view switch (both projections reachable; Matrix duplicate removed); roster emptyBody copy fixed ×4 locales |
| 55 | ◐ | 7-UX | S | (a) DONE (M10, 7eeedd8): 4 unnamed Selects aria-labeled ×4 locales (skill level/provenance/evidence kind + ArchetypeManager scoring model). (b) OPEN: control-dock overlap CONFIRMED — the z-55 collapsed orb (bottom-center, SimBar always mounted) intercepts clicks on form controls scrolled through its strip; expanded bar exceeds the reserved pb-24 on narrow viewports. Cheapest fix: expanded-state bottom padding / scroll-margin on form anchors |
