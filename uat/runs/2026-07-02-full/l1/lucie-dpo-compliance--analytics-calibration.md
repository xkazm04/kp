# L1 — Lucie Procházková (DPO / Fairness & Compliance) × analytics-calibration

- **Run:** 2026-07-02-full · main @ 3395b4c · cert level: **L1 (theoretical, code-derived)**
- **Verdict:** **L1-conditional** — the decision trail is structurally the best I've audited in this codebase's lifetime (the unattended auto-reject mode is *gone*, human gate decisions and reversals now seal into the same tamper-evident chain, the export carries a Czech rationale beside the byte-stable sealed English), but three majors stand: the chain and the decision log are one global, tenant-unscoped store; no sealed record names the *model* that produced the score being acted on; and the "whole chain" dossier export silently truncates at the latest 200 records while the integrity verdict counts them all.
- **Grounding score:** **5/8** (decisive inputs ✓ · actor ✓ · timestamp ✓ · policy version ✓ · approver ✓ with the named single-operator seam · model identity ✗ · tenant boundary ✗ · full-chain export ✗)
- **Time saved (designed):** the decision-trail slice of a pre-2-Aug-2026 readiness review collapses from days of interview-and-reconstruct per system to reading a built-in verified chain + one-click dossier — est. **~10–16 h saved per audit cycle** · confidence **medium** (conditioned on AC-L1-009/010: model identity and the >200 tail I would still reconstruct by hand — partial reconstruction is exactly my non-certification threshold)

## Reachability (resolved before judging)

Internal user, dev gate on; my binding is **Decisions (records/audit), Analytics (decision logs/calibration)** plus consent/disclosure surfaces wherever AI touches a candidate. Analytics tab `app/features/tabs.ts:137` renders `DecisionRecordsPanel` + `DecisionLog` as siblings of the funnel (`AnalyticsTab.tsx:327,410`); Decisions tab `tabs.ts:103`. No per-role gating — everything below is in-set. Fixture dependency: sealed decisions to audit (a committed screen wave, human gate clicks) and ≥20 decided analyses for calibration (`uat/env.md` fixture #3). Apply-time disclosure/consent surfaces belong to the screening-decisions walk — I judged the *audit* surfaces here and did not re-score disclosure. No out-of-set findings.

## The audit surface model (what I audited, affordance → code)

**1. The two-store architecture, now with matching coverage (delta since 06-19 — my majors closed).**
- 06-19 I rated the split "mutable log = complete, tamper-evident chain = subset" a major (lucie-analytics-01), rooted in the unattended auto-mode reject that never sealed (lucie-screening-01) and contradicted the candidate disclosure (lucie-screening-02). **All three are structurally fixed:** the automation pass now declares "AUTO1 RETIRED (UAT M6 / GDPR Art. 22) … the former opt-in `auto` reject mode is gone" — every fairness-cleared reject is *queued* for a human click, and the human route both emails and seals (`app/_lib/automation-pass.ts:244-249,286-305`) → **AC-L1-S4** (resolution `fixed`, L2 must confirm live).
- Chain coverage now spans the decisions I'd actually inspect: screen-wave auto-rejects with decisive numbers + `approvedBy` (`app/_lib/screen-wave.ts:263-272`), the recruiter's direct accept/reject (`app/api/pipeline/[id]/route.ts:243-260`), the **reversal** of an auto-reject (`:174-187` — an overturned rejection is no longer an audit hole), offer terms (`:56-65`), the human scorecard verdict (`app/api/interview-prep/scorecard/route.ts:68-79`), and group-eval with governance awareness — advisory modes seal `group_eval_advisory` so the audit shows the AI *informed*, not *made*, the decision (`app/_lib/group-eval-run.ts:413-441`) → **AC-L1-S5**.
- Integrity is recomputable, not asserted: key-sorted canonical JSON, sha256 chain links, `verifyDecisionChain()` re-hashes every row and names the first divergent seq (`app/_lib/decision-hash.ts:12-40`, `app/_lib/decision-record-store.ts:173-191`); the panel headlines the verdict badge (`DecisionRecordsPanel.tsx:93-104`). The export adds `rationaleLocalized` from the structured `reasonCode`+inputs mirror while never touching the hashed English string (`:33-62`) — closes my 06-19 minor (lucie-screening-03). Attribution in the log is three-state; an unmapped kind renders UNKNOWN, never AUTO (`DecisionLog.tsx:44-53`, `app/_lib/decision-attribution.ts:92-98`) → **AC-L1-S6**.

**2. The three gaps that keep me from certifying.**
- **AC-L1-008 (major, tenancy):** `decision_records` has no workspace column (`decision-record-store.ts:56-71`) and both readers are unscoped — `/api/decisions/records` (`records/route.ts:14-19`) and the analytics decision log (`app/_lib/db/pipeline.ts:67-75,181-188`). One global chain across tenants; `/api/demo` itself documents that an anonymous demo session "can read the real tenant's PII via the ~28 unscoped tables" (`app/api/demo/route.ts:29-34` — mitigated by fail-closed demo minting in gated deploys, `:32-34`). Candidate labels and rejection rationales are personal data; a cross-tenant audit trail is a finding I must write up, not a nuance. (Correlates SD-L1-010 from my screening-decisions walk — this is the same root seen from the analytics surface, plus the *inconsistency*: calibration on the same page IS tenant-scoped, `app/api/analytics/calibration/route.ts:18`.)
- **AC-L1-009 (major, provenance):** my criterion reads "model/version, inputs, timestamp, authorizing human". The sealed record shape has no model field (`decision-record-store.ts:12-20`); `policyVersion` names the *policy* ("screen-wave/bottom10/maxMatch45", "offer", "manual", the eval source) and `inputs` carry scores/thresholds — nowhere the LLM model+version that produced the score being acted on, even though the platform *knows* it (llm_config routing `app/_lib/db/llm.ts:17-27`; the ledger even stores model per call, `core.ts:513-528`). The per-analysis provenance dossier is evidence-linked but also model-silent (`app/_lib/provenance-dossier.ts` — no model field). Under the AI Act I must be able to say *which system version* decided; today I'd reconstruct it from deploy history. (Correlates SD-L1-004: the human-ratified record also omits what the AI told the human.)
- **AC-L1-010 (major, completeness):** the route passes no limit, so `listDecisionRecords` returns the latest **200** (`records/route.ts:17`, `decision-record-store.ts:159-168`) — the panel list *and* the "Export dossier" JSON hold at most 200 records, while `chain.count` counts every row (`decision-record-store.ts:173-191`). An auditor gets a file whose own verdict says "1 400 sealed records, verified" over an array of 200, with no pagination and no truncation notice. Silent truncation in an audit export is how trails fail reviews.

**3. Smaller frictions.**
- **AC-L1-011 (minor):** the blurb instructs "na žádost o vysvětlení exportujte záznam kandidáta" (`messages/cs.json:2777`) and the API supports `?candidate=` (`records/route.ts:10,17`), but no UI passes it — the panel fetches unscoped (`DecisionRecordsPanel.tsx:26`; grep: no other caller). A DSAR response is export-everything-and-filter today.
- **AC-L1-012 (minor):** the DecisionLog CSV exports only the loaded (scrolled) filtered rows by design (`DecisionLog.tsx:120-135`); the on-screen "X of Y" makes it detectable, the file itself carries no truncation marker.
- Copy-rot: the records blurb still says only auto-rejections seal (`cs.json:2777,2780`) and a stale comment still references the retired auto mode (`automation-pass.ts:280-282` vs `:244-249`) — filed by Kateřina as AC-L1-007; for me it matters because copy that under-describes the chain makes the audit trail read thinner than it is.

**4. Calibration through a compliance lens (strength, shared).** The reliability curve is computed from real, tenant-scoped decided pairs with an honest n≥20 gate and no hardcoded confidence (`app/_lib/db/analyses.ts:100-126`, `app/_lib/calibration.ts:62-99`, `CalibrationPanel.tsx:134-142`) — Article-15-style accuracy monitoring I can point a supervisor to, with its proxy-outcome ceiling honestly written into the UI copy (`cs.json:2759`).

## Scored acceptance criteria (applied identically every run)

| Criterion | Verdict |
|---|---|
| trust/blocker — scored+rejected with NO disclosure, NO HITL, NO record | **not triggered** — auto mode retired, adverse actions queue for a human, gate decisions seal (AC-L1-S4/S5) |
| trust — AI-use disclosure + consent before processing | apply-surface (screening-decisions walk); not re-scored here |
| trust — provenance on every headline AI output (model, inputs, timestamp, human) | **fail → AC-L1-009 major** — model/version absent from every sealed record |
| completion — HITL override on the reject path, recorded | **pass** — queue + reconsider/reinstate, the reversal itself sealed (`pipeline/[id]/route.ts:174-187`) |
| senior-quality — decision record regulator-handable as-is | **conditional** — content yes; the *export* fails on truncation (AC-L1-010) and the *store* on tenancy (AC-L1-008) |
| clarity — group/fairness explains ranking | out of scope here → group-eval-fairness journey |
| missing — exportable/inspectable audit trail | **conditional pass** — verified chain + one-click dossier exist; capped at 200, no per-candidate UI scope |

## Findings (mine — full schema in `analytics-calibration.findings.json`)

AC-L1-008 (major), AC-L1-009 (major), AC-L1-010 (major), AC-L1-011 (minor), AC-L1-012 (minor). Strengths AC-L1-S4..S6 (+ shared AC-L1-S2 on calibration honesty).

## Character feedback (first person, Lucie)

„V červnu jsem napsala, že nejméně hlídaná cesta k zamítnutí má nejslabší auditní integritu. Dnes ta cesta neexistuje: automatický režim zamítnutí je odstraněn z kódu, ne z marketingu — každé nepříznivé rozhodnutí čeká na lidský klik a ten klik se pečetí do řetězu. Zvrácení rozhodnutí se pečetí také, takže stopa se zaceluje, ne přepisuje. A export vedle zapečetěné anglické věty nese českou — hash zůstává stabilní, čtenář zůstává Čech. To je řemeslo, které u dodavatelů AI nevídám, a řeknu to i generálnímu radovi.

Certifikovat ale nemohu, a důvody umím vyjmenovat regulátorovi po paměti. Za prvé: řetěz je jeden, globální, bez hranice tenantu — jména kandidátů a důvody zamítnutí jsou osobní údaje a demo sezení je smí číst; kód to o sobě sám poctivě píše, což oceňuji, ale poctivě popsaná díra je pořád díra. Za druhé: žádný zapečetěný záznam nenese model a verzi systému, který skóre spočítal — ‚kdo rozhodl' umím doložit, ‚čím' bych rekonstruovala z historie nasazení, a to je přesně ta rekonstrukce po faktu, kterou tenhle systém měl odstranit. Za třetí: dosier tvrdí ‚1 400 záznamů, ověřeno' a v poli jich nese dvě stě — tichá amputace v auditním exportu je nález, ne detail.

Všechny tři věci jsou opravitelné bez přestavby: sloupec workspace, jedno pole model_version v pečeti, limit + stránkování v exportu. Až budou, podepíšu připravenost na srpen 2026 s klidnou rukou. Dnes píšu: podmíněně, s uznáním."
