# L1 theoretical — Tomáš Dvořák (Hiring Manager) × interview-schedule-prep

- **Run:** 2026-07-02-full · main @ 3395b4c · cert_level L1 (code-only, no browser)
- **Journey leg he owns:** the prep pack + rubric (schedule mechanics are Marek's; Tomáš consumes the result)
- **Verdict:** **L1-pass** (structurally sound in his reachable set; live output quality + latency are the L2 questions)
- **Grounding score (prep surface):** **6/8** — see §3
- **Estimated time saved (if it all worked):** ~20 min per interview (30 min CV-reading/improvised questions → ~3 min: open prep, 30–130 s generate, read/copy the timed plan). Fits his <15-minute budget **iff** generation latency behaves — L2 must time it. Confidence: medium (quality is LLM-dependent).

---

## 1. Surface model (his path, import chains followed)

- **Entry:** Schedule tab → candidate card → "Interview prep" button (`app/features/sub_schedule/ScheduleTab.tsx:278-285`; label flips to "view prep" once an artifact exists via `GET /api/interview-prep?entries=` `:116-122`). The prep modal stays reachable after the round completes — it's the only surface for notes + the human scorecard (`ScheduleTab.tsx:399-408`), and a "review scorecard" handoff pulls him to Decisions scoped to the role (`:409-422`).
- **Generate:** `InterviewPrepModal` → `startTask("interview_prep", {entryId, candidateLabel, jobTitle, lang: locale})` (`app/features/sub_schedule/InterviewPrepModal.tsx:153-164`) → background task → `runInterviewPrep` (`app/_lib/interview-prep-run.ts:21-59`) → `runAutomationTask(entryId, "prep", "", signal, lang)` (`:37`) → Python `pipeline.jobfit.automation_cli prep` (`app/_lib/automation-run.ts:150-183`) → `automation.interview_prep` prompt (`pipeline/jobfit/automation.py:403-424`), deterministic fallback `:426-448`. Cancel actually aborts the Python child (`interview-prep-run.ts:34-37`).
- **Shaping:** questions + focus areas → timed 15–30-min run-of-show (`app/_lib/run-of-show.ts:108-165`, band enforced + sparse-plan padding `:139-150`); early-career entries get the six-phase student script instead of a mismatched chronology (`interview-prep-run.ts:42-44`, `app/_lib/student-interview.ts:70-100`).
- **Modal surfaces:** provenance badge "Generated by AI" vs "Template fallback" (`InterviewPrepModal.tsx:291`, `app/_components/Badge.tsx:114-137` — anything non-"llm" reads as degraded), amber fallback note + one-click "regenerate with AI" (`InterviewPrepModal.tsx:294-309`), checkable run-of-show with per-topic timebox/goal/questions/follow-up (`:321-357`), "signals to confirm" checklist (`:360-387`), coverage meter (`:313-317`), copy-whole-pack (`:170-192`), interviewer assignment (`:392-407`), durable notes (`:411-426`), autosave with unmount flush (`:100-125`).
- **Rubric (human scorecard):** `HumanScorecardPanel` (`app/features/sub_schedule/HumanScorecardPanel.tsx`) — archetype-correct rubric + role-family industry axes, localized, with BARS anchors per rating (`:44-45,126-148`), 1..5 selectors + evidence per competency, verdict chips advance/hold/reject (`:161-176`), save → validated + sealed as a decision record + **gates the entry to `scorecard_review` in Decisions** with an explicit "moved" disclosure (`app/api/interview-prep/scorecard/route.ts:37-105`; `HumanScorecardPanel.tsx:199-203`). Same axes for every candidate of a cohort — single-sourced from `interview-rubrics.json` on both TS and Python sides (`automation.py:473-496`).
- **Regeneration seam:** `humanScorecard`/`userProgress`/`interviewer` re-merged onto the rebuilt payload (`interview-prep-run.ts:52-57`) and re-seeded in the modal without echo-saves (`InterviewPrepModal.tsx:134-151`) — a Regenerate can't wipe his hand-entered scorecard. **Holds.**
- **Brief:** when the candidate books, the assigned interviewer gets an emailed brief + inline `.ics` hold — only if the free-text interviewer field carries a deliverable address; otherwise `interviewer_brief_skipped` is recorded (`app/api/schedule/[token]/route.ts:196-212`; `app/_lib/comms-dispatch.ts:289-340`).

## 2. Reachability (resolved before judging)

Tomáš = internal occasional user; his binding covers **Schedule** (prep modal + scorecard live here) — reachable via dev gate, fixture = seeded pipeline entries at the calendar stage. He never mints invites (Marek's job) and never opens `/schedule/[token]` (candidate surface) — no findings scored against him there. LLM keys absent locally ⇒ the fallback path is what L1 can see running; quality of the real path is `l2_priority`.

## 3. Grounding audit — prep pack (the journey's central AI surface)

Enumerated real-context sources vs what actually reaches the prompt (`automation.py:406-424` via `reasoning_context` `pipeline/jobfit/match_reasoning.py:50-95`):

| # | Source | In prompt? | Evidence |
|---|---|---|---|
| 1 | Candidate's full CV/profile (skills, summary, experience highlights, work links) | **yes** | serialized profile → `profile.json` (`automation-run.ts:110,147-148`); `match_reasoning.py:52-63` |
| 2 | The real job's requirements (title, seniority, must/nice-to-have) | **yes** | `--job-id` (`automation-run.ts:163`); `match_reasoning.py:80-87` |
| 3 | Match evidence (matched skills, missing must-haves, scores) | **yes** | `match_reasoning.py:88-95`; fallback probes exactly these (`automation.py:427-448`) |
| 4 | Public repo/GitHub evidence | **yes** (when present) | `automation-run.ts:126-127,177-181`; `automation.py:412` |
| 5 | Archetype-specific context (skill provenance, transferables) + student-script shaping | **yes** | `match_reasoning.py:65-77`; `interview-prep-run.ts:42-44` |
| 6 | Recruiter's locale (cs prep pack) | **yes** | `interview-prep-run.ts:29,37`; `automation.py:423` |
| 7 | Prior-round evidence: recruiter notes, voice-screen scorecard, screening rationale | **no** | notes hardcoded `""` (`interview-prep-run.ts:37`); no history in `reasoning_context` |
| 8 | Org/process context (ČS interviewing norms, comp band, brand) | **no** | prompt is candidate+job+match only (`automation.py:408-419`) |

**Score: 6/8.** The cache key honestly tracks the model's inputs (profile + github folded in, `automation-run.ts:128-140`), so a re-extracted CV invalidates stale prep — good hygiene.

## 4. Cognitive walkthrough (in-character)

1. **Will I try the right action?** Card says "Interview prep" right on the candidate I'm about to meet — one click, no tutorial. Yes.
2. **Will I notice it?** The button is on the card; after generation it flips to "view prep". Yes.
3. **Label ↔ intent:** "Run of show 25 min", topics with timeboxes, "signals to confirm" — plain enough; no HR jargon walls.
4. **Feedback:** "Generating…" state persists through the fetch (`InterviewPrepModal.tsx:129-151`); load failure is a distinct error+retry, never fake-empty (`:249-274`). Yes.
5. **Did it advance my job, at my bar?** Structurally the questions are built from *this* CV against *this* role's missing must-haves — the design targets exactly "questions I would ask". Whether the live prose clears a seasoned manager's bar is L2's question. The fallback template ("Walk me through a concrete time you used X") would **not** clear it — but it's disclosed with an amber warning + regenerate, so I'm never silently handed the generic one.
6. **Do I trust it?** Provenance badge, my scorecard survives regeneration, my verdict visibly gates the candidate to Decisions. Yes — trust by disclosure.

## 5. Scored acceptance criteria (identical every run)

| Criterion | Verdict | Evidence |
|---|---|---|
| completion — get prep in a few obvious clicks | **pass** | Schedule card → modal → generate (`ScheduleTab.tsx:278-285`, `InterviewPrepModal.tsx:275-285`) |
| effort — no recruiter work forced on him | **pass** | questions/plan handed to him; checklist + scorecard are interviewer-native, optional |
| senior-quality/trust — defensible reasoning he'd stake a decision on | **structural pass, L2-gated** | grounded prompt (§3); fallback disclosed (`Badge.tsx:114-137`) |
| trust — same dimensions for every candidate (fair compare) | **pass** | archetype+role-family rubric single-sourced TS↔Python (`HumanScorecardPanel.tsx:44`, `automation.py:484-496`) |
| senior-quality — role-specific questions, not filler | **structural pass, L2-gated** | CV+JD+match+repo evidence in prompt; deterministic fallback is filler but flagged (TOMAS-ISP-1) |
| clarity — one clear action + unambiguous confirmation (his verdict) | **pass** | verdict chips + "saved" + explicit "moved to Decisions" note (`HumanScorecardPanel.tsx:161-203`) |
| time-saved — inside ~15 min, beats reading CVs | **conditional pass** | designed path ≈ 3 min incl. one 30–130 s wait; L2 must time a cold generate |
| language/clarity — plain Czech, no jargon | **pass w/ blemish** | full cs catalog (`messages/cs.json:1624`); PREP2 localizes questions + scaffolding, but the six-phase student-script prose stays English by design (TOMAS-ISP-3) |

## 6. Findings (details in `interview-schedule-prep.findings.json`)

- **TOMAS-ISP-1 (major, keyless scope_note):** the deterministic fallback pack is generic template filler that fails his senior bar — mitigated by honest disclosure + regenerate; live LLM quality is the top L2 item.
- **TOMAS-ISP-2 (minor, quality-gap):** prep prompt never sees prior-round evidence (recruiter notes, voice-screen scorecard) — the pack can re-ask what the screen already answered.
- **TOMAS-ISP-3 (minor, language):** early-career prep mixes English script prose into a Czech pack.
- **TOMAS-ISP-4 (minor, trust):** the interviewer brief silently depends on Marek typing a deliverable email into a free-text field; a bare name means Tomáš arrives unbriefed, the skip visible only in the event log.
- **Strengths:** TOMAS-ISP-S1 (grounded prompt + honest provenance + regenerate), TOMAS-ISP-S2 (human inputs survive regeneration; verdict gates to Decisions with disclosure), TOMAS-ISP-S3 (timed run-of-show contract — a sparse pack pads to a real ≥15-min plan instead of a 7-min stub).

## 7. Character feedback (first person)

> Co po mně chcete a kdo z nich je nejlepší? Tady aspoň polovina odpovědi: otevřu kartu kandidáta, kliknu, a za minutu mám plán pohovoru s časy — 3 minuty úvod, pak témata s otázkami, u každé "jak vypadá dobrá odpověď" a doplňující otázka. To je přesně to, co si normálně vymýšlím cestou do zasedačky. A rubrika: stejné osy pro každého kandidáta, u známky vidím, co která úroveň znamená, kliknu verdikt a ono mi to samo řekne, že kandidát odešel do fronty rozhodnutí. Žádný tutoriál jsem nepotřeboval — to se u HR nástroje nestává.
>
> Dvě výhrady. Když model nejede, dostanu šablonu — "provedťe mě konkrétní situací, kdy jste použil X" čtyřikrát za sebou. To bych nepoužil; aspoň mi to poctivě ukáže žlutou výstrahu "šablona, ne AI" a tlačítko na přegenerování, takže mě to nepodvádí. Ale ostrou kvalitu chci vidět naživo, na skutečném kandidátovi — na tom stojí, jestli to budu používat. A druhá: ten balíček neví, co už proběhlo — když kandidát prošel hlasovým screeningem, chci, aby se pohovor ptal na to, co screening *neuzavřel*, ne znovu od nuly.
>
> Řekl bych o tom kolegovi? Zatím opatrně: "vypadá to, že ti to napíše otázky líp, než si je píšeš sám — jestli to naživo nebude generické." Vejde se to do mých 15 minut? Na papíře pohodlně. Změřte mi to.

## 8. L2 handoff (priorities)

1. **Generate a real (LLM) prep pack** for a seeded ČS candidate and judge: do questions name CV-specific facts + the role's missing must-haves (not filler)? Is the rubric gradeable end-to-end? (TOMAS-ISP-1 — the adoption decider)
2. **Time a cold generation** — budget 30–130 s; an early client timeout or a >15-min total path is a finding.
3. Generate in cs for an early-career entry — how does the mixed EN/CS pack actually read? (TOMAS-ISP-3)
4. Assign "Tomáš Dvořák" (no email) as interviewer, book a slot, confirm what he receives (nothing) and where the skip surfaces. (TOMAS-ISP-4)
