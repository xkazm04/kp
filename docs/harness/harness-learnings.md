# kp — harness learnings

## Structural facts
- **2026-06-25 (ambiguity-biz Wave 4 — GDPR/audit/compliance, closed 1C+2H, 0 regressions)** — FIXES-WAVE-4. **Durable facts:** (1) **erasure scrubber** `scrubPiiFromPayload` (consent.ts) used a leaf-key deny-list (`PII_KEYS`/`PII_ARRAY_KEYS`) that MISSED `evidenceTrace.{experience,skills,seniority,education,salary}` — string[] of verbatim CV quotes the provenance dossier re-exports. Added `PII_CONTAINER_KEYS` (`evidencetrace`) + `deepRedact` (blanks the whole subtree: strings→"", arrays→[]); `anonymizeEntry` runs the scrubber over linked `analyses` rows (matched by `candidate_label`, no FK). The real-DB test is `erasure-analyses-scrub.test.ts` (sets `KP_DB_PATH` to a throwaway file, statically imports `better-sqlite3` BEFORE the resolve-hook). (2) **decision audit chain:** `sealDecisionSafe({kind,actor,policyVersion,candidateRef,rationale,reasonCode,inputs})` (decision-record-store.ts, never throws) is the tamper-evident seal; AI/auto paths sealed but the HUMAN accept/reject in `api/pipeline/[id]/route.ts` did NOT — now sealed (`actor:"human:recruiter"`, kind rejected/advanced) mirroring the reinstate seal. `?candidate=<entryId>` on `/api/decisions/records` is the per-subject "right to explanation" dossier (built, but the only UI consumer omits the param = deferred dark capability). (3) **single-operator identity:** the app has ONE `KP_OPERATOR_PASSWORD`, no per-user identity (auth/require-operator.ts) — sealed-record approver fallback was the hardcoded "operator (in-app approval)". New pure `auth/operator-approver.ts` `operatorApprover()` = `KP_OPERATOR_NAME` or an honest single-operator string; keep it PURE (no next/headers) so server libs like `screen-wave.ts` can import it without dragging request-scope deps into a node --test. **Deferred Highs (dark-capability surfacing, pairs with W6):** candidate-scoped dossier UI (`?candidate=`), exportable bias-audit pack (per-role fairness matrices), published fairness-eval methodology.
- **2026-06-25 (ambiguity-biz Wave 5 — comms/candidate-experience, closed 3C+2H, 0 regressions)** — FIXES-WAVE-5 (JS unit 1032→1033). **Durable facts:** (1) **comms delivery:** `getCommsChannel()` (comms.ts) returns the local `OutboxChannel` when `COMMS_WEBHOOK_URL` is unset → every message sits `queued` forever (a terminal dev state nothing dequeues); surface `relayConfigured: Boolean(process.env.COMMS_WEBHOOK_URL)` from routes (`/api/comms` now does, like `/api/devcase/comms`) so the UI can warn. (2) **voice transcription:** the OpenAI realtime input-transcription model is now `OPENAI_REALTIME_TRANSCRIPTION_MODEL` (default streaming `gpt-4o-transcribe`; whisper-1 emits ONLY `.completed`, no `.delta`, so the final-turn fallback buffer `candBuf` was always empty under it). `parseOaiTranscriptEvent` handles both `.delta`/`.completed`. (3) **automation-event kinds live in coupled places** — adding a `recordAutomationEvent(entryId, kind, detail)` kind (e.g. new `offer_expired`) requires `DECISION_META` (decision-attribution.ts) + the **writer-coverage test** (`decision-attribution.test.ts` asserts every written kind is mapped) + `analytics.log.kinds.<kind>` en+cs labels, or the test fails / it renders UNKNOWN. (4) **candidate timeline** (`candidate-timeline.ts`) renders an offer item only on `respondedAt`; an `expired` offer has none, so read the existing `expires_at` instead (no new column). offers-store is an **isolated-connection store** but may call `recordAutomationEvent` (shared `ensureDb`, WAL-safe) like comms-dispatch does. (5) **the durable candidate touchpoint is the ack EMAIL** — `dispatchApplicationReceived(entry, {enrichLink?, statusLink?})` appends links to the body; an in-page-only token (status link) dies on tab close. `safeStatusLink`→`getOrCreateStatusLink` returns the TOKEN; the page is `/status/<token>` (build the absolute URL with `publicBaseUrl(new URL(request.url).origin)`). (6) **i18n insert gotcha (reconfirmed):** add keys by indentation-matched regex before a stable sibling (e.g. `bodyEnrich`), NOT json.dump; ICU treats a lone `'` before `{x}` as an escape (comms-dispatch.test pins this). **Deferred (genuinely Phase-2, documented in code):** scheduling per-interviewer/per-job availability + real-calendar (Google/Outlook) free-busy — W5-3 only made `TIMES` config-driven (`KP_INTERVIEW_TIMES`); collision is still global/host-blind (`bookedSlots()` selects all confirmed `schedule_invites.slot_at`, no host dimension; the assigned interviewer in interview-prep is never consulted).
- **2026-06-25 (ambiguity-biz Wave 3 — revenue/billing, closed 2C+2H+2M, 0 regressions)** — FIXES-WAVE-3 (JS unit 1028→1032). **Billing engine map:** the money-state machine is `verify → reduce (pure, billing/reduce.ts) → apply (billing/sync.ts, the ONLY billing_state writer)`; routes enforce via pure helpers in **`billing/enforce.ts`** (`meterGate`/`activeJobsGate`) and resolve entitlement via **`billing/entitlements.ts`** (`entitledPlan`). **Durable facts/fixes:** (1) Polar is NOT ordered → both directions need an out-of-order guard: `subscriptionWriteIsStale` (set) existed; added `clearSubscriptionIsStale` (revoke) — `clear_subscription` now carries `subscriptionId` and apply skips a revoke for a DIFFERENT sub than stored. (2) **`meterGate(meter, { now?, minUnits? })`** — was `(meter, now)`, now an options bag (no caller passed `now`, safe refactor); gates on `remaining >= minUnits` (default 1 = unchanged). interview **create AND simulate** both gate `interview_minutes` on the booked length (`GROUNDED_DEFAULT_MIN` / sim `durationMin`); minutes debit at `/complete` clamped `[1, bookedMin*2]` (`session.durationMin ?? 8`). (3) `entitledPlan` canceled-grace now parses `currentPeriodEnd` defensively — null/unparseable KEEPS the plan (a real lapse arrives as `revoked`→`clear_subscription`→free), never a silent immediate downgrade. (4) **Billing UI rule:** an existing paid subscriber changes plans via the **provider portal** (`/api/billing/portal`), never a fresh `/api/billing/checkout` (parallel-subscription double-charge) — `BillingTab` PlanCard gained `changeVia: "checkout"|"portal"` keyed on `data.plan.id === "free"`. (5) New **`billing_alerts`** table (idempotent CREATE in core.ts after billing_usage) + `recordBillingAlert`/`listBillingAlerts` (db/billing.ts, auto-exported via `db.ts` `export * from "./db/billing"`) = the durable "paid-but-dark" worklist for unmapped-product money events. (6) billing tests use the `registerHooks` resolve-shim + `await import()` pattern (reduce.test.ts / billing-gate.test.ts); `meterGate`/`entitledPlan` are pure and DB-state-driven (set via `upsertBillingState`). **Deferred (product decision, NOT a code bug):** BYOM `plans.ts` sets `interview_minutes: 0` while the comment says BYOM voice is unmetered — resolving sets BYOM's voice monetization (unlimited vs forced minute-pack); surfaced, not changed.
- **2026-06-25 (ambiguity-biz Wave 2 — cross-tenant keystone, closed 1C+1H, mitigated 2C, 0 regressions)** — FIXES-WAVE-2: made `KP_MULTI_WORKSPACE` **self-defending** instead of half-migrating ~50 tables. New pure **`app/_lib/tenancy.ts`** = the canonical machine-checked manifest: `TENANCY_SCOPED_TABLES` (only `analyses`+`profiles` — tables with a verified `*-tenancy.test.ts`; **`pipeline_entries` has the `workspace_id` column+index+backfill but a BLIND `listPipeline` read, so it is NOT listed = an honest gap**), `TENANCY_EXEMPT_TABLES` (short global allowlist), `tenancyGaps()` (everything else = gap by default, fail-closed for new tables), `assertTenancyReady(tables, multiWorkspace)`. Wired into **`ensureDb()` (db/core.ts, right after the workspace_id backfills ~line 791)** — throws at boot when the flag is on with any unscoped per-tenant table; **no-op in the default single-tenant lock** so every existing DB test is unaffected (JS unit 1020→1028). **Key facts:** `DEFAULT_WORKSPACE="workspace"` (auth/session.ts:13); `currentWorkspace()` (auth/**current-workspace.ts**, NOT _lib root) resolves the cookie tenant, falling back to `DEFAULT_WORKSPACE` outside a request; **`pipeline_entries.workspace_id` is `NOT NULL DEFAULT 'workspace'` and analyses/profiles are backfilled to "workspace"** (core.ts:656,790-791) → there are NO NULL rows, so scoping a read by `workspace_id = currentWorkspace()` is regression-safe in single-tenant. **Open follow-ups (the concrete read-scoping that lets a table graduate into the manifest + lift the gate):** (a) scope `listPipeline()`/`createPipelineEntry` to currentWorkspace() across ~7 read + ~4 write callers (several are background paths — attention/screen-wave/sim — that can't call the request resolver, must thread workspaceId); (b) analytics aggregates (`pipelineAnalytics`/`listPipelineEvents`/`countPipelineEvents`/`listChannelSpend`) need a **`pipeline_events.workspace_id` column+backfill** first (it has none); (c) `/api/pipeline` + `/timeline` ride on (a). Each adds a `*-tenancy.test.ts` and appends to `TENANCY_SCOPED_TABLES`.
- **2026-06-25 (ambiguity-guardian + business-visionary scan — 215 findings; Wave 1 closed 5C+1H, 0 regressions)** — combined-lens scan of all 43 contexts (`docs/harness/ambiguity-biz-2026-06-25/INDEX.md` + FIXES-WAVE-1), top-5-by-value per context: **215 findings (17C/100H/92M/6L)**, lens 113🌀/102🚀. Branch `vibeman/ambiguity-biz-fixes` (off a `wip(devcase)` checkpoint of pre-existing calibration WIP). **Wave 1 (hiring-correctness) closed 6 in 6 atomic commits** (tsc 0 · JS unit 1018→1020 · Python 686→694, 0 regressions). **Durable facts:** (1) **the matching fairness gate is split TS↔Python** — TS `isFairnessProtected`/`isKnownArchetype` (archetypes.ts) fails closed on unknown archetypes, but the SCORING + seniority **KO lives in `matching.py:ko_filter`**, which only knew `_EARLY_CAREER` vs BAU; an un-profiled candidate passed as `"bau"` got the seniority floor + lost the shield. Fix: `resolveCandidate` (match-candidate.ts) now passes `"unknown"` and `ko_filter` fails closed for any archetype not in `WEIGHTS`. **`weights_for`/`dimension_labels` already fall back to bau for unknown keys**, so a sentinel archetype scores neutrally but skips the hard gate. (2) **`probeOutcomes[].handledWell` is now tri-state `boolean|null`** — observed (Live Work Surface) path emits `None` (detected-not-graded); `evaluate.deterministic()` keys judgment off `assessed=[bool handledWell]`, falling back to verif-only when none graded (was a `0.5*verif+0.5*0` halving for every in-product candidate). ProbeOutcome/OutcomeLike widened; EvalPanel renders null as "detected". (3) **`scoreAuthenticity` (devcase-authenticity.ts) waives commit penalties for `observed` sessions** → a paste scored 100; added `observedBulkPaste` (`PASTE_BULK_CHARS=600`, −65) fed from a new `paste` ProcessEvent (magnitude only). **Python `derive_signals`/`tooling_from_events` ignore unknown event kinds** (filter open/edit/decision_log) so a `paste` kind is inert there. (4) **`pipeline.analyze_cv` folds deterministic screens into `sanity_checks` via a `(manual review)` suffix** the classifier treats as a warn → flows to review_flags; `authenticity_checks` was the only one — added `credentials.credential_checks` (JD-required-missing + past-dated *regulated* licences; `credentials[].expiry` may hold an ISSUE date, so the expiry check is scoped to regulated licences to bound noise). (5) **JSON i18n edits MUST be surgical text-replace, NOT `json.load`+`json.dump(indent=2)`** — messages/{en,cs}.json mix one-line and multi-line objects; a full re-dump reformats the whole file (176-line diff for a 3-key change). em-dashes (`—`) are literal in the catalogs; match on a substring without the dash. (6) `scripts/i18n-check.mjs` validates en/cs parity (2874 keys) — a new key must land in BOTH. **Open follow-ups (from this run):** W2 cross-tenant isolation is the keystone (only 3 of ~31 tables carry `workspace_id`; `KP_MULTI_WORKSPACE` honor-system flag; `listPipeline`/analytics/`pipeline-events-public` read tenant-blind — confirmed root in `workspace-lock.ts`); 16C + 100H still open; **context-map drift**: Dev-Submissions manifest lists stale `SeedFiles.tsx` + a seed route that no longer exist (refresh that context).
- **2026-06-23 (code_refactor scan — 239 findings; Waves 1–3 closed 22 Highs, 0 regressions)** — full `code_refactor` scan of all 43 contexts (`docs/harness/code-refactor-2026-06-23/INDEX.md` + FIXES-WAVE-1..3): **239 findings (0C/41H/91M/107L)**; categories duplication 108 / dead-code 82 / cleanup 32 / structure 17. Branch `vibeman/code-refactor`. Closed the **drift-bug Highs (W1, 7)** + **dead-branch/knob Highs (W2, 7)** + **safe dead-code-deletion Highs (W3, 8)** = 22 Highs in ~22 atomic fix commits (tsc 0 throughout · JS unit 1019→**1018** [−1 = a deleted dead test] · full Python suite 675 OK / 4 pre-existing skips). **Durable structural facts (high-value for future Phase-4.1):** (1) **`.ts`-extension relative imports are the repo convention** — `allowImportingTsExtensions:true` + `moduleResolution:bundler`; source modules import siblings as `./foo.ts` (e.g. `completeness-followup.ts`→`./split-list.ts`), and these bundle in Next AND load under the bare `node --test` type-strip runner. A new shared pure `.ts` module (e.g. the new `app/_lib/github-handle.ts`) is loadable everywhere. (2) **`apply-intake.ts` must stay runtime-import-light** — its test `apply-intake.test.ts` imports it WITHOUT the `registerHooks` resolve shim, so apply-intake may only carry runtime imports bare-node can resolve (explicit-`.ts` extension OK; extensionless `./x` would break the test). It previously had ZERO runtime imports (type-only). (3) **`pipeline-stages.ts` is DB-free** = the single source for the 5 pipeline stages, safe in the client bundle; **`voice/types.ts` is browser-safe** (no server adapters) = the home for shared voice constants (added `VOICE_PROVIDER_ORDER`). (4) **`seed_jobs.py`'s `build_specs`/`spec_to_prompt`/`_SYSTEM` are LIVE default fallbacks** of the shared `generate()`/`_gen_one` (CSAS overrides them via `run_seed_main`) — NOT dead; only the `main`/`__main__` entry point (the corpus-overwrite footgun) was safely removable. (5) **Deleting an API route leaves 2 stale tsc errors** in the gitignored `.next/types/validator.ts` + `.next/dev/types/validator.ts` until Next regenerates types — clear those files (or rebuild) for an honest gate; the source is clean. (6) The built-in **Grep tool returned empty in this repo all session** (a harness quirk) — every scan subagent + I used Bash `grep -rn`/`rg`. **Gotcha:** `git rm` pre-stages a deletion, so a later `git add <other files>` + `git commit` sweeps the deletion into the wrong commit — `git reset` the deletions first, then `git add` them per-commit (or re-split via `git reset HEAD~N`). **Open follow-ups (from this run):** (a) **Theme B — single-source consolidations (~17 Highs, INDEX waves 4–6)** NOT run — user scoped to W1-3 (dead-code + drift only); these are extract-a-shared-helper duplications (TS price-line/nav-block/JdSummary/safe-url/job-resync/hireRate; Python `_generate` factory / `_scan_json_values` / CLI `_cli.py` adoption / test LLM-stub). (b) **91 Med + 107 Low untouched.** (c) **privacy `ConsentEventKind` (3 never-emitted GDPR kinds)** — deferred as a prune-vs-wire compliance decision (don't silently delete the recording side). (d) **seed_jobs generic generator full removal** — needs a core-signature refactor (make `generate`/`_gen_one` `prompt_fn`/`specs` required). (e) **`mk_candidate` suite-wide adoption** — dead helper deleted; adopting a shared candidate factory across ~9 test modules is a separate refactor. (f) the now-wired **Gemini transient-retry** (`grounded_answer`→`_generate_with_retry`) has no direct test — worth one.
- **2026-06-20 (bug-hunter + ui-perfectionist scan — 300 findings, all 10 criticals fixed)** — full scan of all 43 contexts (`docs/harness/bug-ui-scan-2026-06-20/INDEX.md` + `FIXES-CRITICALS.md`): **300 findings (10C/107H/126M/57L)**, all 10 criticals closed in 9 atomic commits across 3 waves (branch `vibeman/bug-ui-scan-2026-06-20`; tsc 0, `next build` ✓, unit 1016→1019, py green, en/cs parity). **Durable facts:** (1) auth gate is `proxy.ts` at repo root, NOT `middleware.ts` — Next 16 renamed `middleware`→`proxy`; a `middleware.ts` grep finds nothing and reads as "no auth" (the automation "unauthenticated" critical was actually a *defense-in-depth gap* — proxy fail-closed-gates it; fix adds handler `requireOperator()`). (2) `requireOperator()/isOperator()` accept ANY valid session — a DEMO-workspace session (public `/api/demo`, `signSession("demo")`) passed operator gates; now `isOperator()` rejects `currentWorkspaceId===DEMO_WORKSPACE`, and `/api/demo` minting is opt-in (`demoSessionAllowed`/`KP_DEMO_ENABLED`/multi-ws) on gated deploys. (3) tenancy half-built (`workspace-lock.ts`): only `analyses`+`profiles` carry `workspace_id`; ~28 tables workspace-blind → cross-tenant reads are real-but-architectural (mitigate via `KP_MULTI_WORKSPACE`, no full scope fix). (4) `analyses` rows hold full CV PII (`payload_json`+`github_json`) but link to a person ONLY by `candidate_label` (no FK) — `anonymizeEntry` now scrubs them by label inside the erasure txn (`scrubPiiFromPayload`). (5) `ensureDb()` now memoizes on `globalThis.__kpDb` so HMR can't re-run the CREATE/seed/backfill init. (6) archetype `weights` ({skills,career,personal}, sum 1.0) are now fail-fast-validated at `registry.py` import. (7) `Workspace.tsx` sidebar is now an off-canvas drawer below `md`. **Gotcha:** real-DB TS tests must statically `import "better-sqlite3"` BEFORE installing the `registerHooks` resolve shim, or the hook mangles its `./lib/database` CJS require. User's LLM-metering WIP committed first (`b9a3090`), untouched otherwise. Remaining: 107 High + 233 Med/Low (themes in INDEX.md — dominant: silent-failure/no-r.ok ~30, missing UI states ~30, a11y ~25).
- **2026-06-12 (biz-ui scan W4 — close the loop)** — 7 Highs closed in 6 commits; patterns 42–45 in `biz-ui-scan-2026-06-12/FIXES-WAVE-4.md`. Durable seams: pipeline entries carry recruiter `notes` (set_notes action, debounced drawer textarea, pre-fills the scorecard task). jd_build outcome links carry `?jdTask=<id>` (registered tab-scoped param) and JdBuilder rehydrates from it. `/api/interview/create` 409s on a recently-touched in_progress session unless `force:true` (`isInterviewSessionLive`/`liveInterviewByEntry`); `interviewStatusByEntries`+`latestInterviewByEntry` prefer transcript-bearing sessions over newer empty ones. `listPreparedEntries` returns `hasHumanScorecard`; human-led rounds render in Interviewed with a chip + "Prep & scorecard" reopens the modal. InterviewPrepModal hydrates carried-forward userProgress/interviewer from the regenerate task result (don't reintroduce wipe semantics). VoiceInterview has `endedAs` (completed → closing card; failed → Start again). ProfileEditor's archetype choices are registry-driven (custom ids fall back to registry label). CUMULATIVE: 32/108 findings, 26/32 Highs closed across waves 1-5.
- **2026-06-12 (biz-ui scan W5 — honest evidence)** — 6 findings closed (5H/1M) in 5 commits; patterns 38–41 in `biz-ui-scan-2026-06-12/FIXES-WAVE-5.md`. Durable seams: `observed_evidence` gates on LOW_CONFIDENCE (models.py) + `_credited_skills` is gap-aware and credits nothing on no-match (`apply_live_case` returns a reason: minted/low_confidence/below_bar/no_transferred_must_haves). seed_materializer + interview_scenario run through `generate_with_fallback`; orchestrator persists `source` in saved blobs and audits `seed_skeleton_only`/`scenario_template_only`. `dev-outcomes.recordOutcome` is an UPSERT (by ref); `latestOutcomeByRefs` joins outcomes into the postings GET; Submission.outcome is server truth. Automation screen/prep/scorecard prompts carry a "Public repo evidence" block via `--github-evidence` with a `githubEvidenceJson` cache axis (GITHUB_EVIDENCE_TASKS in automation-cache-key.ts). Entries carry `github_handle` (fill-only column, captured at conversational apply via coerceGithubHandle); drawer runs the deep-dive via the `set_github` pipeline action.
- **2026-06-12 (biz-ui scan W3 — real data)** — 6 Highs closed in 6 commits; patterns 34–37 in `biz-ui-scan-2026-06-12/FIXES-WAVE-3.md`. Durable seams: match_cli/reasoning_cli take `--jobs-json` via shared `_cli.load_jobs_arg` (seed corpus + DB overrides, DB wins) — the match route + reasoning-run write `listCorpusJobs()` to the workdir; reasoning cache key has a FIFTH axis (corpusFingerprint). `normalize_job` honors stated salary_min/max from the extraction prompt; anchor-band fallback is recorded in `defaulted_fields` and campaign facts are stated-only. Jobs carry `status` end-to-end now (listJobs/getJob SELECT it; JobStatusBadge; draft modals publish instead of linking). Public `/jds/[slug]` renders NO candidate data — the list lives in LibraryTab via `/api/jds/[slug]/analyses` (lazy). Lead entries mint `lead_token` (+`lead_passed_ko_json`, fill-only via ensureLeadEnrichToken); the enrich link + apply POST carry it; merge targets the token's entry with email fallback. WAVE PROCESS NOTE: implementation subagents work well batched on DISJOINT file sets; the messages/{en,cs}.json catalogs are the common collision point — stagger any two fixes that both add keys.
- **2026-06-12 (biz-ui scan W2 — honest automation)** — 6 findings closed (4H/2M) in 5 commits; patterns 30–33 in `biz-ui-scan-2026-06-12/FIXES-WAVE-2.md`. Durable seams: `actOnPipelineEntry` takes `opts.actor` ("system" → `auto_advanced`/`auto_rejected`, default human `advanced`/`rejected`) — NEW system callers MUST pass it; screen-wave no longer writes a separate auto_rejected event (rationale rides as the act's detail). `AutomationDecision.outcome` + `deriveDecisionOutcome()` (decision-attribution, browser-safe) is the applied/failed/skipped/fairness_blocked/queued vocabulary. `scheduler.reject_mode` (NULL = "approve") gates clock rejections behind a `rejection_review` approval on the Decisions gate (registered in APPROVAL_KINDS; payload = screening shape AiReviewCard renders; human Reject route sends the email + human attribution; evaluate_entry's pending-approval freeze stops re-deciding). `pipelineAnalytics()` now returns `koDeclined` total + per-byJob counts from the entry-less ko_declined events (registered in DECISION_META). ScreenDecision carries `commsFailed` per row. GOTCHA: kind lists live in FIVE places that must move together — DECISION_META, MOMENTUM_EVENT_KINDS, db.ts funnel IN-lists (×2), analytics.log.kinds labels (en+cs), decision-attribution.test writer list.
- **2026-06-12 (biz-ui scan W1 — comms delivery)** — 7 findings closed (4H/3M) across 6 commits; patterns 25–29 in `biz-ui-scan-2026-06-12/FIXES-WAVE-1.md`. Durable seams: `dispatchScheduleInvite` exists now (schedule link auto-ships at mint, `schedule_invite_sent` kind registered in DECISION_META + COMM_SENT_KINDS + analytics labels; the booking confirmation carries the reschedule link as a footer via `dispatchInterviewConfirmation`'s `rescheduleLink` opt). Entry-less adverse comms are a supported shape: `dispatchKnockoutDecline` sends with NO `ref` (envelope handles null context) — gate on the caller (`notifyDecline`) when a sibling surface shows the outcome live. `/api/comms` derives `recovered`/`recoveredAt` per failed row (newer same-(ref,kind) sent/queued, computed over the UNFILTERED window); CommsCenter counts only unrecovered dead-letters and subscribes to live-refresh. `promoteSubmission` now threads `contact` + lifecycle `lang` into `createPipelineEntry` — when adding a NEW bridge into the pipeline, pass contact+locale (the input type supports more than most callers pass). Public dev-case apply requires an email-shaped contact (webhook stays lenient; contact-less webhook rows get an "unreachable if promoted" badge).
- **2026-06-14 (Pipeline C waves 24–27 — Dev Case orchestration/UI, 4 small ideas, BUNDLE COMPLETE)** — the lighter dev-case leftovers; all over existing data/routes. unit 837→849. **W24 99288c0e cross-channel shortlist (commit 10dac25):** CaseDetail ranked submissions per-posting separately → one case-wide leaderboard flattening all postings' submissions sorted by transferScore + a channel chip per row (new optional `channel` prop on SubmissionRow); posting cards keep only apply-link + intake form ("Apply channels"). **W25 56a20eb2 scoped live updates (commit 883d902):** DevTab's [tasks] effect re-fetched cases+postings+lifecycles+outbox on EVERY task-array tick → a `useRef<Set<taskId>>` fires a SCOPED reload exactly once when a task hits terminal (evaluate_submission→postings; lifecycle→all four; need_analysis→lifecycles). **W26 d142462d feedback brief (commit 2bdaef5):** pure buildFeedbackBrief (devcase-feedback.ts +5; strengths celebrated, concerns+transfer.gaps reframed as growth, NO adverse wording — test asserts no reject/unsuccessful/not-selected) from the existing eval bundle; /api/devcase/feedback POST queues a `feedback` dev_outbox row via recordOutbox status="queued" (OutboxStatus enum = queued|sent|failed; human dispatches — adverse decision stays human-gated); SubmissionRow "Send feedback" on evaluated-not-promoted rows. **W27 d8a0c4cf SLA stall flag (commit 3e4df63):** pure lifecycleStall (devcase-sla.ts +7; open stage [collecting|published] + 0 submissions + age≥STALE_COLLECTING_DAYS=7, updatedAt→createdAt fallback); LifecycleSection sums submissions/case from postings → LifecycleRow shows "stalled Nd" badge + one-click Re-source (POST /api/devcase/source {caseId}) beside Close. Client read, no cron (audit-alert DB record = noted follow-up). **GOTCHAS:** (1) `git add -A` swept in a FOREIGN untracked dir (docs/harness/code-refactor-2026-06-14/* — a separate scan's output) → had to reset --soft + re-add only my files; ALWAYS `git add <explicit paths>` on this tree (it carries others' untracked WIP). (2) `react-hooks/purity` rule flags `Date.now()` in a client component RENDER body → snapshot once via `const [nowMs]=useState(()=>Date.now())`. **DEV CASE ORCHESTRATION/UI BUNDLE COMPLETE.** Only remaining dev-case: 5644f8e6 clarifying-questions graded signal (e6 — needs a new QuestionLog model + CLI step + Python assess/evaluate integration = its own engine wave), 8fc4b406 case-variant rotation (e7), + the outcome-ledger spine.
- **2026-06-14 (Pipeline C waves 21–23 — Dev Case engine-heavy differentiators, 3 ideas PHASED, BUNDLE COMPLETE)** — designed ascending effort/risk, resolved one-by-one. All achievable TS-side over signals the engine already produces (`runEvaluateSubmission` in devcase-run.ts assembles the bundle; `fetchRepoSignals` in repo-snapshot.ts pulls the GitHub trace) — no Python changes. Each = pure `app/_lib/devcase-*.ts` + colocated tests + a studio surface; persisted as new fields on the eval bundle (eval_json is free JSON, NOT zod-validated, so adding fields is safe). unit 819→837. **P1 ce28da40 process-authenticity (commit 7d49ba4):** the trace already had cadence/bursty/decisions-log + reflection readBeforeWrite/iterationPattern but only CHECKED the log existed; pure scoreAuthenticity() (devcase-authenticity.ts +6, penalty model) → {score,band,reasons} folded onto the bundle; GATES auto-promotion — promoteSubmission overrides advance→hold + top red-flag when band==="suspect" (held for the ownership-verifying followups); EvalPanel band chip. **P2 bb4f5494 probe-strength audit (commit 7281d6f):** pure auditProbeStrength() (devcase-probe-audit.ts +7) STRUCTURAL discrimination check per probe (decisionSpace ≥2 distinct options + concrete `where` + `reveals`) → strong/weak/none verdict; ProbeStrengthBanner surfaced at the approval gate (LifecycleRow ReviewPanel) + CaseDetail internal — flags dead probes before a case ships. (Synthetic strong-vs-naive submission sim = deeper follow-up.) **P3 c364a44d seed-anchored diff (commit e4f265d):** the GitHub commit detail fetchRepoSignals already pulls carries the changed-file list (previously only COUNTED) — captured `changedPaths` (union of files[].filename); pure seedDiffEvidence() (devcase-seed-diff.ts +5) diffs them vs the case's materialized seed (`devCase.seed.files[].path`, case-insensitive) → touched/untouched planted files = grounded "seam never opened" evidence beside the LLM probe read, EvalPanel strip. Seed has NO structural probe→file link (probe.where is free-text) so per-probe seam mapping + Python rubric integration = deeper follow-up; file-level engagement is the robust low-risk core. **GOTCHA (cost me a red test):** adding a sibling import to a module WITH a colocated node --test breaks it — `repo-snapshot.ts` was self-contained, importing `./devcase-seed-diff` (extensionless) made repo-snapshot.test.ts fail to load (ERR resolving the extensionless sibling under the strip-types runner). FIX: inline the helper (one loop) and keep the exported/tested copy in the sibling — a tested module must stay import-free of value siblings. **DEV CASE ENGINE-HEAVY BUNDLE COMPLETE.**
- **2026-06-14 (Pipeline C waves 18–20 — Dev Case studio insights, 3 ideas, BUNDLE COMPLETE)** — three Studio-UI features surfacing ALREADY-computed eval data in CaseDetail (the dark-capability pattern); each = a pure import-free `app/_lib/devcase-*.ts` + colocated tests + a sub_dev component, ZERO engine/API/DB changes, NO outcome-ledger dependency. Dev Studio is ENGLISH-ONLY (hardcoded strings, no next-intl) so no i18n. Key data paths: a case's submissions = `casePostings.flatMap(p=>p.submissions)`; per-submission probe outcomes = `submission.evaluation.tooling.probeOutcomes[]` ({probeId,detected,handledWell}); rubric scores = `evaluation.evaluation.dimensions[]` (ordered, preferred) ?? `dimensionScores` map; minted interview Qs = `evaluation.followups.questions[]` ({question,decision,listenFor,redFlag} — listenFor/redFlag are INTERNAL); transferScore is the per-submission ranking scalar. **W18 fec3e23a cohort probe-miss heatmap (commit 2dc05ca):** pure `probeMissHeatmap(probes, submissions)` (devcase-cohort.ts +5 tests) rolls probeOutcomes across the cohort → per-probe miss/weak rate over the EVALUATED subset (null when unscored, not 0%); CohortProbePanel in the locked internal section flags probes missed by EVERY evaluated candidate as possible miscalibration. **W19 b268f5e5 side-by-side rubric compare (commit bacd264):** pure `rubricCompare(rubricDims, submissions, maxCols=5)` (devcase-compare.ts +7 tests) → axis×candidate matrix (axes from case rubric, fallback union of scored dims; score prefers `dimensions` over dimensionScores) + leaderByAxis; CompareSubmissions matrix shown only with ≥2 evaluated. **W20 8d4f38b9 interview kit from winner (commit 505a0dd):** pure `interviewKitMarkdown()` (devcase-interview-kit.ts +4 tests) assembles the top-transferScore submission's followups into copy/export Markdown; InterviewKit component (clipboard Copy + downloadFile .md — note: export-utils has NO copyText, use navigator.clipboard directly). unit 803→819. GOTCHA: keep these pure helpers import-free (structural input types, no DevTypes import) so colocated node --test files load them. **DEV CASE STUDIO-INSIGHTS BUNDLE COMPLETE.** Remaining dev-case (own bundles): engine-heavy c364a44d seed-anchored diff / bb4f5494 adversarial probe audit / ce28da40 process-authenticity; outcome-ledger spine 43de88f2/5b5c7c09/dae06e20; orchestration d8a0c4cf SLA / d142462d auto-feedback-brief; UI 99288c0e cross-post shortlist / 56a20eb2 live updates / 5644f8e6 clarifying-Qs signal.
- **2026-06-14 (Pipeline C waves 14–17 — Analytics dashboard depth, 4 ideas, BUNDLE COMPLETE)** — all four build on the existing `pipelineAnalytics()` (db/analytics.ts) + AnalyticsTab; each new metric extracted into a PURE import-free `app/_lib/analytics-*.ts` module with colocated tests (unit 789→803). **W14 ce8e3c9e period-over-period deltas (commit 568a5a8):** `pipelineAnalytics(windowDays, { endMs })` gained an optional cohort UPPER bound (single-arg call byte-identical) so the route requests the prior equal-length window (`endMs: Date.now()-windowDays*DAY`) and diffs via pure `periodDeltas()` (analytics-deltas.ts +4 tests) — only COHORT scalars (counts, hire rate, funnel conversion matched by stage NAME, TTH); as-of-now figures (age/momentum/bottleneck) stay real-time and are NOT diffed. DeltaChip is direction-aware (lowerIsBetter for TTH → down=green). All-time view has no prior period → deltas null. **W15 82c2b8e8 configurable goals (commit 273e12e):** new `analytics_targets` key/value table (CREATE TABLE IF NOT EXISTS in core.ts — no migration; mirrors channel_spend), metric key = funnel stage name (conversion % goal) OR reserved `time_to_hire` (days). setAnalyticsTarget/listAnalyticsTargets in db/analytics.ts; `targets:{conversion,timeToHireDays}` on payload; the hardcoded `<50%` funnel-red threshold became `< (targets.conversion[stage] ?? 50)`; TTH goal pill; collapsible GoalsEditor/TargetInput (onBlur save, reuses spend-input UX). New POST /api/analytics/targets (metric ∈ FUNNEL_STAGES ∪ reserved keys, range-checked). **W16 094b5870 forecast (commit fa35675):** pure `forecastHires()` (analytics-forecast.ts +5 tests) computed CLIENT-side from the existing payload (momentum.added velocity + funnel empirical conversion + avgTimeToHireDays) — in-flight expectation (each active credited forward conversion hiredReached/reach-at-stage) + 4/8/12wk inflow projection + TTH lag; below signal floor (0 hires) says "not enough signal" not a misleading 0. ForecastPanel in right column. **W17 b39992b1 automation ROI ledger (commit 2a60fd0):** pure import-free `automationRoi(kindCounts, rate)` (automation-roi.ts +5 tests) — MINUTES_SAVED_PER_KIND map over ONLY recruiter-replacing auto kinds (failure/sentinel kinds excluded) → hours + CZK at a configurable rate; folded into the payload from the SAME kindCounts the auto/human rollup uses; rate stored under a 2nd reserved analytics_targets key `recruiter_hourly_czk` (≤1M ceiling, EXCLUDED from the conversion-goal splitter); RoiLedger in AutomationPanel reuses analytics.log.kinds labels + toCsv export. GOTCHA: when a metric ROI/forecast reads is computed in a pure module imported by a colocated test, keep it IMPORT-FREE (inline the auto-kind set / minimal input types) — node --test can't resolve the extensionless @/ db imports a non-pure sibling would drag in. PATTERN: a reserved-key escape hatch on an existing k/v table (analytics_targets) avoids a new table+route for each small setting, but every reader/splitter must explicitly exclude the reserved keys. Gates green every wave (tsc 0, build ✓, lint, en/cs parity). **ANALYTICS BUNDLE COMPLETE.**
- **2026-06-14 (Pipeline C wave 13 — standing silver-medalist alerts fdb45cd0, Jobs & sourcing 3 of 3, BUNDLE COMPLETE)** — promotes rediscovery from an on-demand per-job panel into a proactive standing feed. EXTRACTED the ranking+silver-medalist filtering out of `app/api/jobs/[id]/rediscover/route.ts` into `app/_lib/rediscover.ts` (`rediscoverForJob(job,{signal})` throws on CLI failure; `raiseRediscoveryAlertsForJob(jobId)` best-effort swallows; `sweepRediscoveryAlerts()` loops published jobs) so the panel + the alert triggers score identically (route is now a thin caller — behavior unchanged). New isolated-connection store `app/_lib/rediscovery-alert-store.ts` (`rediscovery_alerts` table, UNIQUE(job_id,candidate_id) → INSERT OR IGNORE so re-ranks never duplicate AND never un-dismiss a dismissed row; record/list-active/dismiss). Publish route raises alerts on genuine go-live only (`!already`), AFTER sourcing (rediscoverForJob excludes just-sourced = now active in role). New `/api/rediscovery/alerts` GET feed / PATCH dismiss / POST sweep; relevance (role still published, candidate not since pipelined) filtered at READ time against live candidateOutcomes()+listJobStatuses() via the PURE generic `filterRelevantAlerts` (in import-free `app/_lib/rediscovery-relevance.ts` + 5 tests; re-exported from rediscover.ts). UI: `RediscoveryFeed.tsx` at top of JobsTab (slim always-present bar w/ Refresh-sweep, list when populated, postPipelineAdd per-alert since alerts span jobs, optimistic dismiss). New STORE_ERRORS code REDISCOVERY_ALERTS_FAILED. messages en/cs jobs.rediscoveryFeed (parity 13/13). Gates green (tsc 0, build ✓, unit 784→789, lint). GOTCHA: `react-hooks/set-state-in-effect` flags a CALLED helper that sets state in an effect — INLINE the fetch as an async IIFE so the linter sees setState lands AFTER the await (the allowed useJsonFetch shape). Kept the testable filter IMPORT-FREE (generic over `{jobId,candidateId}`, no type import) so it runs under `node --test` (rediscover.ts pulls better-sqlite3 + the @/ db barrel). **JOBS & SOURCING BUNDLE COMPLETE (efb12f90 W11 + aa039d0c W12 + fdb45cd0 W13).** Deferred: automatic per-candidate-arrival re-rank (the heavier "pool change" half) — the manual feed Refresh covers it without coupling to the hot apply/analyze path.
- **2026-06-14 (Pipeline C wave 12 — pre-publish JD winnability coach aa039d0c, Jobs & sourcing 2 of 3)** — turns the existing recruiter scorers into a "will this JD fill?" advisor BEFORE sourcing. New pure tested `pipeline/jobfit/winnability.py` (`assess_winnability(candidates, job, *, fit_threshold=FIT_PROMISING_THRESHOLD=55) → {poolSize, eligible, qualified, looseGates, looseMustHaves, salary}`): re-runs the PRODUCTION `ko_filter` + `score_job` under counterfactuals — for each required language / the education floor, `job.model_copy(update=…)` then recount ELIGIBILITY (positive delta = that gate is the sole blocker for N people → "drop German +N eligible"); for each must_have, demote it to nice_to_have and recount QUALIFIED (skills aren't hard gates, they cap the score, so the lever is qualified not eligibility) + missingAmongEligible from `score_job(...).missing_skills`; salary belowMarket = jobBand top < `role_band(family,seniority)` floor (role_band imported from .taxonomy inside the fn to keep the import surface in matching/jobs core). `winnability_cli.py` mirrors recruiter_cli (same candidate-entry parse {profile}|{candidate}, row-level isolation). `app/api/jobs/[id]/winnability/route.ts` = clone of candidates/route.ts (buildCandidatePool + getJob → job.json, AbortSignal spawnPython, parsePythonJson). UI: new `CoachPanel.tsx` (useJsonFetch, verdict banner unfillable/thin/healthy, 3 stat tiles, loosen-list, salary card) added as a "Coach" tab in JobPostingModal (Gauge icon). +5 py tests; gates green (tsc 0, build ✓, unit 784 unchanged, py 591→596, lint, en/cs coach parity 19/19). GOTCHA (re-confirmed): next-intl t() rejects template-literal keys → resolve `verdict.${v}`/`stat.${k}` through explicit literal-key maps (verdictMessage const + statTiles array), never `t(\`…${}\`)`. Remaining in bundle: fdb45cd0 silver-medalist alerts (heaviest — needs persisted alert store + publish trigger + feed).
- **2026-06-14 (Pipeline C wave 11 — bulk job import efb12f90, Jobs & sourcing 1 of 3)** — multi-ad paste import. New pure tested `app/_lib/split-ads.ts` (`splitJobAds(text)` splits on a separator line `^[ \t]*[-—_=*]{3,}[ \t]*$` multiline, trims, drops <30-char chunks = the ingest floor mirrored client-side) + 5 colocated tests. `IngestAdPanel.tsx` gained a "Multiple ads" toggle, an Import-all({count}) button → `submitBulk()` that loops the SAME hardened `/api/jobs/ingest` route SEQUENTIALLY (one Claude parse in flight; content-hash dedup → "in catalog"; abortable), a per-row results list (Check/X icons), live progress. Single-ad `submit()` path untouched. messages/{en,cs}.json jobs.ingest bulk keys. Gates green (tsc 0, build ✓, unit 784, lint). PATTERN: to bulk-ify an existing single-item ingest, loop the proven route client-side rather than building a new batch endpoint — reuses its dedup/validation/parse for free.
- **2026-06-14 (Pipeline C wave 10 — JD authoring quality, 3 ideas, BUNDLE COMPLETE)** — (1) inclusive-language 7469c05f: extended pure jd-lint.ts (refactored findVaguePhrases→collectPhrases; added findExclusionaryPhrases + EXCLUSIONARY_PATTERNS gendered/coded/masc-coded/ageist EN+CS + a MUST_HAVE_RE count >8) → new finding kinds `exclusionary`/`manyMustHaves`; JdBuilderResult renders them (the old `f.kind==="vague" ? : f.what` chain needed all 4 kinds or tsc errors on `.what`). +3 tests (16 total). (2) duplicate 7159056b: JdRow list items lack the body (only slug/title/preview), so DuplicateJdButton fetches GET /api/jds/[slug] then POSTs /api/jds/save {title:"…(copy)", body} (reuses the save path → also ingests the jd-<slug> Job) → load(). (3) version-history 6a18e0fc: jds PATCH overwrote in place (typo = unrecoverable). New `jd_revisions` table (added to core.ts schema CREATE — idempotent, no migration needed); updateJd now snapshots the PRE-edit row into jd_revisions in ONE transaction before the UPDATE; revertJd snapshots current then restores; GET/POST /api/jds/[slug]/revisions; History panel in JdActions.tsx (English-only surface — no i18n). No db CRUD test (repo convention); the pure jd-lint logic IS tested. Gates green (tsc 0, build ✓, unit 776→779, py 591 unchanged, lint). PATTERN: add a table to core.ts's schema CREATE block (CREATE TABLE IF NOT EXISTS) for a no-migration add; snapshot-before-overwrite in a tx gives free undo history.
- **2026-06-14 (Pipeline C wave 9 — CV authenticity cae71d45 — Candidate analysis bundle COMPLETE)** — deterministic authenticity screen reusing the EXISTING trust-ledger plumbing (no schema/codegen change): new pure tested `pipeline/jobfit/authenticity.py` (`authenticity_checks(raw_text, skills_count, years_experience) → list[str]` of `Authenticity: …` sentences: buzzword density ≥4, long-CV-with-<8-digits, ≥25 skills + <1500 chars, years>45; warn-shaped ones carry "(manual review)" so the sanity classifier counts them; a clean CV returns one non-warn line). Wired into pipeline.analyze_cv: `sanity_checks += authenticity_checks(raw_text, skills_count=len(profile.skills), years_experience=int(profile.years_experience) or None)` right after the sanity_checks build (line ~232) — so it flows into review_flags + QualityStrip with ZERO schema change. UI: `authenticityBand(checks)` in sanity-checks.ts (0 warns→high, 1→medium, 2+→low; null if no Authenticity line = older payload) + a TrustBand chip in QualityStrip (both clean & warn headers). +6 py + 1 TS test (py 585→591, unit 775→776). It's a SCREEN (every finding "verify", human-confirmed); LLM cross-exam over text+GitHub is the deferred deeper layer. PATTERN: to add a per-analysis signal WITHOUT touching the Pydantic→zod codegen, fold it into sanityChecks (free-text ledger) with a recognizable prefix and derive UI state from the prefix. **CANDIDATE-ANALYSIS BUNDLE COMPLETE (0832ec48 W7 + b8d711c4 W8 + cae71d45 W9).** Gates green (tsc 0, build ✓, lint).
- **2026-06-14 (Pipeline C wave 8 — blind-screening b8d711c4, Candidate analysis 2 of 3)** — CRITICAL engine fact: `analyze_profile_with_gemini(path)` (gemini.py) uploads the ORIGINAL FILE BYTES to Gemini (`types.Part.from_bytes(path.read_bytes())`) — so the model sees name+photo; text redaction alone can't blind it. Blind mode therefore sends REDACTED TEXT as the only part (`parts=[]` + the text appended to the prompt) instead of the file. New pure tested `pipeline/jobfit/redact.py` (`redact_pii(text)→RedactResult{text,categories,detected_name}`: masks a guessed header name + its tokens, email/phone/profile-links, EN+CS gendered pronouns/titles, explicit age/birth-year). Threaded `blind`: cli.py `--blind` → service.analyze → pipeline.analyze_cv (redact pypdf_text after _extract_pre_pass, pass `blind_text=` to gemini, RE-ATTACH `profile.name = redaction.detected_name` after _profile_from_payload since the blind LLM returns null name, append a non-warn sanity note listing categories) → gemini.analyze_profile_with_gemini(`blind_text=`). TS: analyze-run.ts cliArgs `--blind` + AnalyzeParams.blind; cache-key.ts folds `blind` in ONLY when true (`if(input.blind) field("blind")`) so existing non-blind cache stays valid + a blind run is distinct; route reads form `blind`; AnalyzeApi→runAnalysis(AnalysisInputs.blind)→useAnalyzeForm(blind state, exposed in inputs+setters)→AnalyzeForm checkbox. NOTE: `python-runner.ts buildCliArgs` is DEAD (no callers; lacks --lang) — analyze-run.ts cliArgs is the live spawner. +4 py tests; gates green (tsc 0, build ✓, unit 775, py 581→585, lint). Remaining: cae71d45 CV-authenticity (next).
- **2026-06-13 (Pipeline C wave 7 — Candidate analysis, 1 of 3: evidence-linked verdict 0832ec48)** — the analysis (AnalysisResult, schemas.generated) already carries the full auditable trail: score{total,experience,skills,roleSeniority,education,traits}, evidenceTrace{experience,skills,seniority,education,salary} (per-dimension CV quote lists), jobFit{recruiterRiskFlags,mustProveEvidence,...}, softSignals{strengths,antipatterns each w/ evidence+confidence}, sanityChecks[] (English sentences, classified by sanity-checks.ts splitSanityChecks). So 0832ec48 was a pure UI/logic wave: new tested `app/_lib/provenance-dossier.ts` (buildProvenanceDossier → Markdown bundling every score component beside its evidence + risk flags + soft signals + warn/pass ledger; degrades on sparse payload) + a "Provenance dossier" download in ReportActions (history page passes parsed.data). Full char-offset span highlighting in raw CV is deferred (engine emits per-dimension quotes, not offsets). **DEFERRED the 2 engine-heavy differentiators — their own waves:** b8d711c4 blind-screening (a Python PII-redaction pre-pass before analyze_profile_with_gemini + re-attach + --blind flag through analyze-run/pipeline.py) and cae71d45 CV-authenticity (deterministic timeline/contradiction heuristics + LLM cross-exam pass → new sanity checks/review_flags + trust band). Gates green (tsc 0, build ✓, unit 770→775, py 581 OK unchanged, lint). GOTCHA: a pure lib importing a VALUE sibling (./sanity-checks) can't run under bare node --test → use the registerHooks resolve shim + `await import()` in the test (like schedule-store.test.ts), OR keep the lib import-free.
- **2026-06-13 (Pipeline C wave 6 — reasoning popover, deec915c — Matching bundle COMPLETE)** — the "reasoning popover needs reasoning-input assembly" blocker dissolved: `writeMatchInput` (match-input.ts) accepts `{ profileId }` and loads the CandidateProfileV2 from the DB server-side, and the matrix candidate id IS a profileId. So `POST /api/match/reasoning {profileId, jobId, lang}` works with zero new plumbing. MatrixTab cell click (normal mode; select mode unchanged) now opens a viewport-fixed popover (anchored under the cell via getBoundingClientRect, full-screen catcher + Esc to close) that lazily fetches + client-caches the Reasoning ({verdict,strengths,gaps,interviewProbes} from MatchTypes) — the server prompt-cache makes repeats instant; blocked cells show the KO reason; a "View full match" link still deep-links. UI-only over the existing reasoning route; no new tests. Gates green (tsc 0, build ✓, unit 770, py 581 OK, lint). All 3 Matching/decisions ideas now shipped (e1e4e0ea W5, e43fa801 W4, deec915c W6). LESSON (again): the blocker was imagined — check writeMatchInput/the existing route's accepted body shape before assuming you need to assemble a payload.
- **2026-06-13 (Pipeline C wave 5 — Fair Rank, e1e4e0ea)** — the "fairness_matrix needs a Python entrypoint" assumption from wave 4 was WRONG: `recruiter_cli.py` already exposes `fairness_check` (per-candidate bounded weight proposals + the cross-scheme matrix) and `/api/jobs/[id]/candidates` already returns it as `fairness` (the route passes the whole recruiter_cli payload through) — a DARK CAPABILITY with no UI caller (the feature-scout "dark capabilities" theme again). So Fair Rank was a pure UI surfacing in RecruiterCandidates.tsx (sub_jobs, NOT the Matrix tab — that's candidate×role; fairness is candidate-pool-for-one-job, which RecruiterCandidates already is): a "Fair Rank" toggle re-orders columns by `fairness.mean` (robust) vs own-weight `result.total`; a per-card robust badge with Δ=mean−own (positive=robustly strong, negative=flattered by own weights); a collapsible Fairness audit panel (own/robust/Δ sorted by robustness) + CSV export of the full per-scheme matrix via export-utils toCsv/downloadFile. `fairness.candidateIds` aligns to CandRow.candidateId (recruiter_cli uses the same entry id for both row.candidateId and matrix candidateIds). UI-only over the already-tested Python engine (test_recruiter.py/test_recruiter_cli.py); no new tests. Gates green (tsc 0, build ✓, unit 770, py 581 OK, lint). LESSON: before assuming a Python function needs an entrypoint, grep for an existing *_cli.py + whether an API route already spawns it and passes the field through — kp has several built+returned-but-unsurfaced engine outputs.
- **2026-06-13 (Pipeline C wave 4 — Matching & decisions, 1 of 3 shipped)** — user picked the matching/decisions bundle (e1e4e0ea fairness Fair-Rank, deec915c reasoning popover, e43fa801 re-review queue); SCOPED HONESTLY to the one self-contained idea — the other two need genuine new plumbing (fairness_matrix() in matching.py exists but has NO entrypoint AND ranks a pool-for-one-job, a different shape than the candidate×role Matrix tab → needs a Python CLI + a new surface; the Matrix Cell type is `{score,blocked,koKeys}` with no refs/reasoning and /api/match/reasoning needs the full MatchInputBody payload the matrix doesn't hold → reasoning popover needs reasoning-input assembly). Both left accepted for their own waves. **SHIPPED e43fa801 human re-review queue for auto-rejects** (commit 114117b): `reinstatePipelineEntry(id)` (db/pipeline.ts) reverses an auto-rejection → status active + stage Screened + clear approval + `reinstated` audit event, guarded to a still-`rejected` row (res.changes===0 = lost race → null); `listReconsiderQueue()` = rejected entries JOINed to an `auto_rejected` event, GROUP BY e.id + MAX(created_at) newest-first (dropped a bare-column `ev.detail` reliance — the UI doesn't render the reason); new GET /api/decisions/reconsider + a `reinstate` action on /api/pipeline/[id]; DecisionsTab gains a collapsible Reconsider section (loads alongside the pending queue, same live-refresh). Candidate re-notify on reinstate is a deliberate recruiter follow-up (NOT auto-sent — new comm template deferred). No new unit tests — the repo doesn't unit-test db/pipeline CRUD directly (pure logic lives in pipeline-status/screening/fairness helpers, which ARE tested); gates green (tsc 0, build ✓, unit 770, py 581 OK, lint clean). NOTE: screen-wave records `auto_rejected` (audit) + `rejection_comms_failed` events; actOnPipelineEntry "reject" → status 'rejected' (terminal); recordEvent kind is a free `string`.
- **2026-06-13 (Pipeline C wave 3 — pipeline board UX, 2 ideas)** — both pure-UI over already-tested API contracts (no new unit tests; the set_stage/accept/reject CAS is tested elsewhere). (1) **Drag-and-drop** (cea12908): PipelineBoard had none despite the name. Added optional drag props to the shared CandidateRow (`draggable`/`onDragStart`/`onDragEnd`, off in select mode), made each StageCell a drop target (onDragOver preventDefault + dropEffect, dragLeave-ignores-child-nodes guard, highlight ring), lifted a `dragging:Entry|null` to PipelineBoard so any cell's drop resolves the source row, and PipelineTab's `moveEntry` does optimistic set_stage with the prior stage as expectedStage + rollback + load()-reconcile. Pointer-only; keyboard path stays select+bulk-move/drawer (full keyboard DnD deferred). (2) **Bulk approve/reject** (bdc7fc01): extended the existing bulk bar (which only did set_stage) with `bulkDecide(accept|reject)` looping accept/reject on /api/pipeline/[id] with each card's expectedStage; acts ONLY on the awaiting subset of the selection (needsHumanDecision && active), non-awaiting stay selected untouched, successes deselect, 409s stay-for-retry; per-approval-kind breakdown via enumLabel("approvalKind",k) (graceful labelize fallback — no enum catalog needed); bulk reject is confirm-gated (emails N). bulkResult gained a `verb` discriminant so the status line reads move/accept/reject correctly. All gates green (tsc 0, build ✓, unit 770, py 581 OK, lint clean). PATTERN: HTML5 DnD drop zones MUST preventDefault on dragOver; track the dragged item in React state (not just dataTransfer) so the drop handler can read the full object.
- **2026-06-13 (Pipeline C wave 2 — candidate-apply loop, 3 ideas)** — from the same 25-context scan backlog. (1) **Save-and-resume** (939d96e9): ConversationalApply held all answers in volatile state → persist a draft {idx,answers,msgs,answeredIds} to localStorage `kp:apply-draft:<jobId>`, restored once on mount behind a `hydratedRef` gate (SSR-safe, eslint-disable react-hooks/set-state-in-effect), "welcome back" banner + Start fresh; cleared on done/restart. (2) **Status page** (e76a6fb2): the candidate had NO handle after applying (the code deliberately never exposes pipeline-entry id — IDOR). New isolated `application-status-store.ts` (token→entry_id, UNIQUE entry_id so a re-apply reuses the link) + pure tested `application-status.ts` ((status,stage)→candidate enum: rejected/rematched→not_selected, declined→withdrawn, else stage map) + `/api/status/[token]` (candidate-safe projection via getPipelineEntry+getJob) + `/status/[token]` page (5-step timeline). Apply route mints token via best-effort `safeStatusLink`, returns `statusToken`, success card links. NOTE: added STATUS_LOOKUP_FAILED to api-response.ts STORE_ERRORS union (safeJsonError codes are a closed union). (3) **CV-first autofill** (cddec0bf): the `cv` file step was LAST in buildApplyScript (after name/email) so prefill was moot — MOVED it to the front; on upload, pure tested `cv-autofill.ts` (email regex + Title-cased 2-4-word name heuristic rejecting CV headers + a TITLE_WORDS stoplist — wrong guess worse than none) seeds the name/email inputs as editable defaults (seed effect on idx, not on `input`, so it doesn't re-seed after the candidate clears it). Full year/skill parse via profile_cli is a documented follow-up. All gates green (tsc 0, build ✓, unit 762→770, py 581 OK, lint clean). GOTCHAS: next-intl `t()` rejects template-literal keys → build literal-key lookup maps in render, don't cast (the `as Parameters<typeof t>[0]` cast breaks the .tsx parser); a pure module that imports a sibling (even a type) can't run under bare `node --test` — keep tested pure libs import-free (inline the stage names) like interview-reminder-policy.ts/offer-policy.ts; colocated `*.test.ts` import siblings WITH the `.ts` extension.
- **2026-06-13 (Pipeline C wave — candidate scheduling table-stakes, 3 ideas)** — feature+moonshot scan over all 25 contexts (≤5/context), then implemented the Scheduling & Offers table-stakes bundle. (1) **Timezone-aware slots** (b51106df): the ALREADY-EXISTED catch — `use-slot-label.ts` (SCH4) already renders slot ISO in the candidate's browser-local zone; the real gap was no zone INDICATOR, so rescoped to a pure tested `app/_lib/timezone.ts` (`timeZoneShortLabel`/`isValidTimeZone`) + a "times in your timezone (GMT+2)" note + a new `schedule_invites.candidate_tz` (validated in route, COALESCE-preserved, shown on InviteLifecyclePanel). The .ics already carries the absolute instant so calendars localize — no change. (2) **Offer expiry** (29361408): pure `app/_lib/offer-policy.ts` (OFFER_TTL_MS 7d, `isOfferExpired` fail-open on NULL/legacy), `offers.expires_at` stamped at mint, lazy-lapse on read (`expireOfferIfDue` in offers-store, used by offerView+respondToOffer) AND a heartbeat sweep `lapseExpiredOffers()` wired in instrumentation.ts; respond returns 410 on expired. (3) **RSVP** (87af39c5): `attendance_status`/`attendance_at` on schedule_invites; `confirmAttendance`/`cancelAttendance` (cancel frees slot → status back to 'pending', resets reminder cycle, leaves the pipeline entry stage alone); route gains an `rsvp` action handled BEFORE the idempotent re-confirm echo; (re)confirm clears stale RSVP. All gates green (tsc 0, build ✓, unit 752→762, py 581 OK, lint clean). KEY: instrumentation.ts is where periodic sweeps wire in (reminder sweep + now offer-lapse); schedule-store/offers-store are isolated-connection stores with ALTER-TABLE migration arrays — add a column to BOTH the CREATE and the migration loop.
- **2026-06-10 (feature-scout#2 W4 — recruiter i18n)** — 8 findings; the `--lang` template generalized across every remaining recruiter LLM path + the report frame. CV3: Analyze form "Report language" select → reportLang in submit FormData → /api/analyze prefers it over getServerLocale (cache already lang-keyed). MAT1: reasoning_cli --lang → match_reasoning.generate(lang); runReasoning threads it + adds a 4th cache-key axis (cached cs verdict must never serve en — +test); MatchCard passes active locale in task params (detached task can't read cookie). SCOR6: market_salary _fallback summary localized (bilingual map; TS normalizer passes through). DEC4: ScreenDecision gains reasonCode+reasonParams beside the UNCHANGED byte-identical English rationale (audit/test pin intact); ScreenWaveModal renders decisions.wave.reasons.* — localize-display-keep-the-audit-string pattern. SHELL5: next/font subsets += "latin-ext" (Czech diacritics were fallback-font); static metadata → generateMetadata() reading new `meta` catalog + og:locale en_US/cs_CZ. PREP2: automation_cli global --lang (only prep reads it) → interview_prep + language_directive; runAutomationTask threads lang + prep-only cache axis; run-of-show.ts/student-interview.ts deterministic scaffolding localized via SELF-CONTAINED in-module bilingual tables (pure tests unchanged, lang defaults en); STUDENT_SCRIPT phase prose stays English (voice agent delivers it). PREP3: canonical English competency stays the storage/scoring KEY; cs strings in a TS-side RUBRIC_CS overlay keyed BY canonical (interview-rubrics.json + its Python drift test UNTOUCHED); localizedRubric/rubricLabel swap display, POST/match canonical; +4 drift guards. RES2: new `report` namespace — FRAME migrated (ResultPanel tabs, ReportActions, AddToPipelineButton, DispositionEditor, shared.tsx widgets, history page); DEFERRED (documented FIXES-WAVE-4): ~50 deep per-tab BODY labels (extraction/compare/job-fit/salary/interview section headings) — the data they wrap is already localized narrative. PATTERN: localize narrative, keep the KEY (audit string / scoring key / join key stays byte-identical English; a structured code or overlay drives display, pinned by a drift test). Patterns in FIXES-WAVE-4.md.


- **2026-06-10 (feature-scout#2 W3 — candidate i18n)** — 6 findings: SIM3 persists `pipeline_entries.locale` (idempotent ALTER, captured via getServerLocale at apply) + the 8 comm templates moved to a `comms.*` namespace rendered by comms-dispatch through a per-locale `createTranslator` (the CORE next-intl API — NOT getTranslations, which resolves the REQUEST locale; load the catalog via the same relative-path dynamic import the request config uses, cache per locale). LLM bodies (outreach/offer) stay as written; only deterministic chrome localizes. Locale reaches the partial-entry dispatchers via a dueReminders `p.locale` join + an entry lookup in interview-create. GOTCHA: ICU treats a lone `'` before `{x}` as an escape that can swallow the placeholder — comms-dispatch.test.ts pins every key in both locales asserting no `{…}` survives. SCH4: `use-slot-label.ts` formats slot ISO via Intl.DateTimeFormat(activeLocale) for DISPLAY; the server-minted English label stays the stored/emailed canonical value (same split as enum labels); offer salary toLocaleString(locale). JOB3+JDL5: copy-out artifacts (jobToMarkdown, JD builder) keep a small SELF-CONTAINED bilingual heading table IN the module (not the catalog) so a posting/JD-language toggle can render a non-active locale without bundling all messages client-side. DEVP5+JDL5: the Python `language_directive` (i18n.py) localizes NARRATIVE while skill/role-family/seniority CODE values stay verbatim (matching/rubric/cache unbroken); `--lang` threads through devcase CLI → design_role/design_case/materialize_seed/scenario_from_case (deterministic fallbacks stay English); dev-case lang persisted on new `dev_lifecycle.lang`, captured at need intake, threaded by the orchestrator; JD lang threaded through runJdBuild → runDesignArtifacts(--lang)+runMarketSalary(--lang already existed). APP4: copyApplyLink appends `?lang=` (the proxy honours it); apply page mounts LanguageSwitcher. Patterns in FIXES-WAVE-3.md.


- **2026-06-10 (feature-scout#2 SWEEP)** — Med/Low close-out, 7 items: report pipelineRef carries the detected archetype (was null with real downstream cost — rubric selection + unknown-archetype audit); history filters by disposition; analytics bySource (origin = entry's EARLIEST event kind via MIN(id): applied/matched/added — derived, never stored); decision-log ?kind/?attribution filters (allow-listed via decision-attribution; keyed-remount restarts useInfiniteScroll) + CSV exports (toCsv/downloadFile; new chrome print:hidden); PIPE3 two-way board URL sync (filter edits write the SAME ?q/?quick/?stage the mount hydration reads — debounce typing, immediate clicks) + Copy-link on view pills from a CLEAN query string; live-refresh mirrors onto BroadcastChannel("kp:data-changed") — one shared channel per document, a channel never receives its own posts so no origin-dedup needed; SHELL4 g+key chords DERIVED from NAV_GROUPS (first untaken letter, in order) + "?" overlay, suppressed in editables and under modals via Modal's new isAnyModalOpen(). LESSON: a sweep's best targets are seams the campaign itself opened — 5 of 7 here were two-correct-findings-in-different-waves gaps.


- **2026-06-10 (feature-scout#2 W10)** — Ops verbs: `POST /api/tasks/[id]/retry` replays any failed/interrupted/canceled row from persisted params (server-side; verify per-kind that params are self-contained/DB-keyed before promising replay — kp's all are); Done rows expand via fetchTask into per-kind summaries + deep links (analyze→/history/<slug> from result.persistence, entry kinds→board ?q=); TasksIndicator unseen-failure badge vs `kp.tasksFailedSeenAt` watermark. Task history filters: ?kind/?status threaded into listTaskHistory as presence-chosen fixed clauses; the accumulating useInfiniteScroll RESTARTS via keyed remount on the filter combo (don't add reset plumbing). `engine-preflight.ts` (Gemini env + PATHEXT-aware claude PATH probe, once-per-process) rides /api/health informationally — NOT a degradedReason (CLI-less = designed fallback mode); hints on AnalyzeForm + SchedulerControl. `/api/ops` + SystemCard: bounded JSONL tail readers (`ops-telemetry.ts`, last 256KB, torn lines skipped) over analyze/pipeline/comms logs + promptCacheStats; always-200 dashboard read. `db-portability.ts` = db-dump/load script cores in TS (scripts keep bare-node copies; FORMAT/VERSION + $blob cell encoding are the contract); export/import routes keep refuse-to-clobber + all-or-nothing + dry-run-first, coerceDumpPayload identifier-checks names, BOTH routes carry the ccb4d851 auth flag. AUTO6: reminders sweep is now scheduler job `reminders` — `ensureSchedule(name, defaults)` + `ensureReminderJob()` (created ON @1min so registration can't silently stop candidate comms); heartbeat gates on claimDueRun, records runs only on sends/errors (zero-send rows = noise at 1-min cadence); schedule GET/POST carry reminders+reminderRuns, POST takes remindersEnabled.


- **2026-06-10 (feature-scout#2 W9)** — Shell + analytics actionability: the board filter bar is URL-hydratable (`?q=`/`?quick=`/`?stage=` read ONCE at mount via lazy initializers — tabs unmount per switch; the params are in TAB_SCOPED_PARAM_KEYS + its pinned test, so add deep-link params THERE). Everything composes off that substrate: analytics funnel/bottleneck/byJob link into it; `/api/search` + CommandPalette (Ctrl/Cmd+K, Modal chrome, input focus via rAF AFTER the trap's first-focus); `kp.recents` localStorage (capped 8, re-front on re-open, recorded at drawer/profile/job opens + palette picks + detail pages via render-nothing RecordRecent island). `/api/attention` + declarative `badgeKey` on tabs.ts nav defs → count pills in BOTH sidebars (Workspace polls visibility-gated 60s + live-refresh; server WorkspaceNav calls attentionCounts() directly). `pipelineAnalytics(windowDays?)` = cohort-by-entry windowing + momentum (rolling 7-day buckets, NON-OVERLAPPING kinds: added+intake_degraded=1 per creation; advanced-except-Hired; advanced-to-Hired=hired) + automation rollup folded through the SHARED decision-attribution module — extraction audit found 15 writer kinds UNMAPPED (incl. auto_rejected!) rendering UNKNOWN badges; writers⊆map is now a test. GOTCHA: every kind added to DECISION_META needs an `analytics.log.kinds.<kind>` en+cs label or the log renders raw keys. PIPE1 bulk moves = the single-item set_stage verb in a loop, each item carrying its OWN expectedStage; 409 failures STAY SELECTED (MatrixTab W11 grammar). New STORE_ERRORS: SEARCH_FAILED, ATTENTION_FAILED.


- **2026-06-10 (feature-scout#2 W8)** — Lifecycle CRUD: jobs gained a terminal status — `isJobOpenForApplications` (job-ingest.ts) is the ONE open-for-applications gate, imported by the apply page, the apply POST (410 — never gate page-level only) and the public JD CTA; `/api/jobs/[id]/close` mirrors /publish. JDs are no longer append-only: PATCH `{title,body}` or `{archived}` (`archived_at` column; body edit re-runs `ingestJobAd` — safe ONLY because the upsert preserves lifecycle status: check what an update path preserves before wiring it to a richer writer), library rows decorate `jobStatus` via one `listJobStatuses()` call + "Ingest as job" on unlinked rows. The candidate ranking decorates persisted sourcing state at the API boundary (`entryIdsWithEvent` chunked-IN helper + `listEntriesForJob` → `{inPipeline, outreachSent}`; client unions optimistic session state on top). APP1: a dedup check is a MERGE opportunity — re-apply folds fresh signals onto the original entry (`mergeReapplication`: SQL fill-only contact backfill; `buildApplicantProfile(intoProfileId)` rebuilds a healthy profile IN PLACE via updateProfile so the pool never grows a stale duplicate, falls through to save+re-point for degraded stubs); `findApplicationByApplicant`'s email-miss falls back to name match restricted to CONTACTLESS rows only (the different-address doctrine survives); a newly-backfilled contact re-sends the dead-lettered ack.


- **2026-06-10 (feature-scout#2 W6)** — Comms + links have operators now: `POST /api/comms/[id]/resend` (append-only — NEW outbox row + `comm_resent` event; never mutate the failed row), `GET /api/comms` (?entry/?status; refs resolved server-side) → Channels CommsCenter + drawer Messages; `listScheduleInvites()` + `GET /api/schedule` (SCHEDULE_LOOKUP_FAILED in catalogue) → ScheduleTab InviteLifecyclePanel (needs_more_slots/needs_reconcile finally read). Interview links: `isInterviewLinkExpired` is the ONE expiry authority (7d TTL, created-only) shared by /connect + portal; `revokeInterviewSession(s)` WHERE-guarded (completed never revocable); /create REVOKES prior open sessions (one live link per entry, response carries `revoked`); reject auto-revokes; `POST /api/interview/revoke`. GOTCHA: react-hooks/purity flags `Date.now()` during render EVEN in async server components — capture at fetch time or hoist into a lib helper.


- **2026-06-10 (feature-scout#2 W7)** — Automation trust loop closed: `executeAutomationPass` runs a PRE-POLICY scoring sweep (recruiter_cli per job; FILL-ONLY `setEntryMatchScore` WHERE match_score IS NULL; new `scored` event kind in EVENT_KINDS — register catalog row + en/cs verb when adding kinds); `runAutomationPass({dryRun})` is read-only and BYPASSES the single-flight (joining would return applied results as a "preview") while still consulting `assertAutoRejectFair`; per-pass `decisions[]` persist in `scheduler_runs.decisions_json` from clock AND manual routes (the run route records trigger "manual" unless `isPassInFlight()` said it joined); the human scorecard route now sets `scorecard_review` (same JSON.stringify(Scorecard) detail the AI paths set, gate-open = approval null|calendar at Interview stage) and `/api/interview/compare` unions human-scorecard-only entries via `listEntriesForJob` (scoringModel from `isEarlyCareer(archetype)`).


- **2026-06-10 (feature-scout#2 W5)** — Dev-case loop is closed end-to-end: public `/devcase/apply/[token]` page (probe-safe `caseToMarkdown`, seed download, form → inbound webhook; ApplyTokenPill copies the PAGE url now); `recordPipelineOutcome` (dev-outcomes, ref+outcome idempotent, ds- prefix inlined to stay self-contained) auto-fires at BOTH terminal transitions (reject in `actOnPipelineEntry` AFTER the IMMEDIATE tx; Hired CAS winner in offer-finalize) + one-click outcome buttons on promoted SubmissionRows; `POST /api/devcase/lifecycle/[id]/close` is the FIRST writer of stage "closed" (wrap-up comms deduped by recipient, postings closed, inbound 410s); the approve gate has a review panel with bounded edits ({title,brief,tasks,timeboxHours} merged server-side, audit names edited fields) and `POST .../redesign {feedback}` → `devcase_cli --feedback` → `design_case(feedback=)` re-runs ONLY the design step. Probes/rubric are deliberately NOT hand-editable — regenerate covers them. Late-arrival re-eval (inbound resumes from `collecting` only) still open.


- **2026-06-10 (feature-scout#2 W2)** — GitHub deep-dive is now persistent + integrated: `analyses.github_json` (attached via PATCH `/api/analyses/[slug]` with `githubAnalysis` body — validates against githubAnalysisSchema, 413 >256KB) and `pipeline_entries.github_json` (compact summary; `app/_lib/github-summary.ts` is the SINGLE shape authority — `build()` at add-to-pipeline, `coerce()` at both the POST and DB-read boundaries). `analysisSchema` now declares `persistence {slug createdAt}` (CV1 enabler — zod was STRIPPING the server-attached receipt; check the client parse schema before the producer when a wire field "doesn't arrive"). `/api/github-analysis` has a 15-min in-process TTL cache keyed sha1(lc(user)+JD); errors never cached. GitHub-only analyze runs exist (no CV); SubmissionRow can assess the submitter's profile (owner from parseRepoRef) against the role spec.


- **2026-06-10 (feature-scout#2 W1)** — Soft-signal panel wired end-to-end: `SoftSignal`/`SoftSignalPanel` (+ source/kind constants) now live in `models.py` and are RE-EXPORTED from `soft_signals.py` (import cycle: models→soft_signals→profile→models — pure model classes move, detector functions stay). `AnalysisResult.soft_signals` is a typed field → codegen emits zod. `_v2_profile_and_routing` returns `(dump, checks, v2_obj)` 3-tuple; the panel builds under its OWN `_softly`. UI: `SoftSignalsSection` in InterviewTab (renders without an interview kit).
- **2026-06-10 (feature-scout#2 W1)** — `analyses.review_flags` column (idempotent ALTER): warn-shaped sanity-check count stamped at save by `countSanityWarns` (`app/_lib/sanity-checks.ts` — classifier markers are deliberately matched against the engine's UNLOCALIZED English check strings; the vocabulary is pinned by `sanity-checks.test.ts`, update BOTH when adding a warn shape in pipeline.py). NULL on pre-column rows = no pill.
- **2026-06-10 (feature-scout#2 W1)** — `match()`/`rank_candidates_for_job` now return `learningSignals`/`transferableSkills`/`domainDistance` beside `potentialScore`; shared expandable `app/_components/PotentialBadge.tsx` renders them on RecruiterCandidates / Match header / GroupEvalModal (group-eval-run passes them through; evals persisted before the fields render the plain pill). Matrix blocked cells carry `koKeys` (stable `KoReason.key`), localized via `matrix.ko.*` messages — localize by KEY, never engine prose.


- **2026-06-07 (bug-hunt W8)** — UI state-desync fixes: `useAnalyzeJdLibrary` type-guards `jd.body`/`jd.slug` (a non-string white-screened the tab from `?jd=`); `AnalyzeForm` sequences saved-JD picks (`jdPickSeqRef`) to drop stale body responses. `PipelineBoard` lane membership uses the SAME 3-way key as the position map (`jobId ?? jobTitle ?? "?"`) and folds unknown-stage entries into the first column (no counted-but-invisible candidate). `CandidateDrawer` is keyed on `entry.id` (remount resets per-entry state). `SchedulerControl` has an `inFlightRef` single-flight + skips the poll mirror while the interval field is focused. `offer-finalize` re-reads the offer for the authoritative status on a lost CAS (no "declined" default for an accepter). **Deferred** (with rationale in FIXES-WAVE-8): Scheduling#3 (recruiter/candidate slot-vocabulary mismatch), Scheduling#4 (business-TZ slot rework + its WIP-modified tests), CV#7 (decorative stage strip — no per-stage progress exists to drive it).
- **2026-06-07 (bug-hunt W7)** — `runAutomationTask`'s single-task `screen` now CASes on `entry.stage` (`expectedStage`) like the policy pass — the read→Python-hop→apply gap existed on BOTH paths. The schedule-confirm POST + `approve_event` (db.ts) now reject a terminal (rejected/declined) linked entry (`isTerminalEntryStatus`). Tasks: a partial UNIQUE index `uq_tasks_active_dedupe ON tasks(dedupe_key) WHERE status IN ('queued','running')` (guarded creation) + `createTask` catches the collision → returns the existing active row, making dedup atomic across connections; `markTaskRunning`/`setTaskProgress` guard `AND status IN ('queued','running')` (terminal is final). `prunePromptCache(limit?)` bounds the on-write prune (500/pass via rowid subselect; boot prune unbounded). `codegen --check` treats an unreadable generated file as stale, not a crash.
- **2026-06-07 (bug-hunt W5)** — Dev-case engine: `evaluate_submission` deterministic filters non-dict `probeOutcomes` and rests judgment on verification alone when there are no probes (no free 0.5 midpoint); `ClaudeCliProvider.map._one` wraps ANY exception (not just `ClaudeCliError`) so an empty-prompt `ValueError` can't abort a `run_judge` sweep; `interview_scenario._SCRIPT` loads via guarded `_load_script()`; `analyze_need` flags `loc<=0` snapshots. **Deferred**: DevCase#2 (per-dimension LLM provenance in `coerce`) overlaps the WIP canonical-score contract (`MISSING_DIMENSION_SCORE`/`_mirror_dimension_scores`, models.py) — coordinate there. DevCase#1 DONE: both harnesses collect `FALLBACK_REASON_KEY` (set only when the LLM raised, ≠ `--no-llm`) into `Row.fallback_reasons`; `signals()` exposes `error_fallbacks`; the report shows a WARNING and `submission_eval --strict` fails on any error-fallback — an all-error-fallback run no longer reads as a 100%-reliable green.
- **2026-06-07 (bug-hunt W6)** — `analyze-run.ts` now uses `parsePythonJson(stdout, stderr)` (not raw `JSON.parse`) — ALL LLM-invoking task CLIs must, since the interpreter prints shutdown chatter after the JSON line. `executeAutomationPass` isolates each decision's apply+dispatch in a try/catch (a reject's DB commit precedes its comm, so a comm throw can't abort the batch); `AutomationSummary` gained an `errors` count. The schedule-confirm route returns `confirmationSent` (false → flagged via `markScheduleInviteNeedsReconcile` + `logScheduleReconcile`); `SchedulePicker` softens the success copy when false (don't promise a confirmation that didn't send — matters most for short-notice bookings with no separate reminder).
- **2026-06-07 (bug-hunt W4)** — `VoiceInterview.tsx` end-of-call now preserves the transcript: ElevenLabs `end()` defers finalize to `onDisconnect` (+ `EL_DISCONNECT_GRACE_MS` fallback) so the lagging final `onMessage` is captured (mirrors the OpenAI VAD grace); `persistTranscript` (sessionStorage stash + 3× backoff retry + `res.ok` + `keepalive`, banner on total failure) replaces the fire-and-forget POST; unmount `sendBeacon`s a partial as `"failed"`. The 30s connect timeout latches `finalizedRef`, and `onConnect` + the OpenAI go-live step now bail when it's set (no zombie-live); `getUserMedia` stops its tracks if torn down mid-acquisition; `start()` resets `sessionIdRef`/`sessionTokenRef`.
- **2026-06-07 (bug-hunt W3)** — The analyze run is now abortable end-to-end: `watchAnalysis`/`executeAnalysis`/`resumeAnalysis` (sub_analyze) take an `AbortSignal`; `useAnalyzeForm` holds `abortRef` + a monotonic `analysisRunIdRef` (callbacks from a superseded run are ignored) and aborts on unmount; `reset()`/`cancel()` share `stopActiveRun()` (abort + DELETE the task + reset flags). The poll bounds failures (404 = terminal, 10 consecutive non-OK = bail). A "Cancel scan" button lives in `AnalysisProgress` (`onCancel` prop).
- **2026-06-07 (bug-hunt W3)** — `spawnPython` has always wired `opts.signal → SIGKILL`, but task handlers called it with NO signal, so cancel never killed the child. The **`analyze`** handler now forwards `ctx.signal` (`runAnalyze(p, onProgress, signal)` → `spawnPython(args, { signal })`, dispatched from `tasks.ts`). **Data#1 now FULLY closed**: `ctx.signal` is forwarded to ALL Python-spawning task handlers (analyze/reasoning/group_eval/jd_build/automation + devcase need_analysis/design_artifacts/commit_reflection/evaluate_submission). Each `run*` takes an optional trailing `AbortSignal` → `spawnPython(args, {signal})`. `runLifecycle` checks `signal.aborted` between stages and forwards to its analyze/design steps (its best-effort publish-stage sub-steps — interview-scenario/seed/source — remain un-threaded). `job-ingest.ts:156` already did it.
- **2026-06-07 (bug-hunt W2)** — The grounded Gemini path (`use_grounding=True`) sets NO `response_mime_type`, so `_parse_json` decodes with a stock `JSONDecoder` that admits `Infinity`/`NaN`. `int(float('inf'))` raises `OverflowError` (NOT ValueError) and `inf` passes `>= 0` guards. `pipeline.py` `_optional_int`/`_optional_float` now reject non-finite (`math.isfinite`) + catch `OverflowError`; route ALL numeric LLM fields through them (or `salary_band`'s inf/nan-safe `normalize_band`/`round_salary`). `market_salary_cli._coerce` likewise catches `OverflowError`.
- **2026-06-07 (bug-hunt W2)** — `gemini.grounded_answer` now retries transient failures via `_generate_with_retry`/`_is_transient_error` (429/5xx/timeout by code or message → retry; auth/4xx → fail fast; 3 attempts, 90s each, backoff+jitter). Callers no longer need to pass a `fallback` just to survive a rate-limit blip.
- **2026-06-07 (bug-hunt W2)** — Salary midpoint is clamped to `[min,max]` in `_salary_from_payload` (a model midpoint is trusted only when in-band, else re-derived). `apply_company_salary_context` bails on a 0/0 band (`salary.maximum <= 0`).
- **2026-06-07 (bug-hunt W1)** — Two event-dedup helpers in `db.ts`: `hasEvent(entryId, kind)` (unbounded "ever logged" — gates real-world side effects across multi-day cache windows) and `hasEventToday(entryId, kind)` (now buckets on the **business timezone**, `BUSINESS_TZ` env, default `Europe/Prague`, DST-correct via `Intl`, compares the most-recent event's local-date — NOT UTC midnight anymore).
- **2026-06-07 (bug-hunt W1)** — `outreach` delivery is idempotent: `runAutomationTask` skips `dispatchOutreach` when an `outreach_sent` event already exists (prompt cache makes the draft cheap, but the SEND is gated on the durable marker). Any new candidate-facing send added to automation must follow the same gate-on-event pattern, not rely on the cache.
- **2026-06-07 (bug-hunt W1)** — Scheduler clock: `claimDueRun` is the clock-path writer of `next_due_at`; the `force`/manual path in `tickScheduler` now calls `advanceAfterForcedRun` (scheduler-store.ts) to advance the window itself, so a manual "Run now" no longer leaves the window due for the next heartbeat to re-fire.
- **2026-06-07 (bug-hunt W1)** — `dueReminders` (schedule-store.ts) is now entry-lifecycle-aware: LEFT JOINs `pipeline_entries` and requires `status='active' AND stage!='Hired'` (a Hired candidate keeps status `active`). The isolated schedule-store connection can JOIN db.ts tables — same kp.sqlite file.
- **2026-06-07 (run #3)** — The `expectedStage` CAS on `actOnPipelineEntry` is now passed by ALL THREE destructive batch/snapshot callers: the automation pass (run #2), `runScreenWave`, and the Decisions UI (via `POST /api/pipeline/[id]` body `expectedStage` → 409 + fresh entry on mismatch). Any NEW caller that decides from a snapshot must pass it too — grep `actOnPipelineEntry(` when adding decision surfaces.
- **2026-06-07 (run #3)** — Python stdout MUST be parsed with `parsePythonJson(stdout, stderr)` (python-runner), never raw `JSON.parse`: the CLIs print asyncio/ResourceWarning chatter after the JSON line. All spawn sites now comply (match, reasoning-run, matrix, group-eval, automation).
- **2026-06-07 (run #3)** — Matrix perf model: ONE batched Python spawn (good by design), now content-address-cached in `app/api/matrix/route.ts` (sha1 of the exact profiles/jobs JSON — profiles/jobs tables have NO updated_at, so content hashing is the only edit-safe key). JD tokenization memoized per description (`matching._description_words`, lru_cache). Group-eval: reasoning spawns parallelized (cap 6 = concurrency bound), candidate rows resolved once and shared.
- **2026-06-07 (run #3)** — `getJobsByIds` (db.ts) is the batched IN-query twin of `getJob`; prefer it for any multi-id job lookup.
- **2026-06-07 (run #3)** — TOOLING GOTCHA: writing a raw NUL character (U+0000) into a source file makes git classify it as binary (and bash refuses NUL in command args). Use the `backslash-u0000` escape sequence in string literals; commit messages via `git commit -F <file>`.
- **2026-06-07 (run #2)** — Pipeline write-concurrency model: `actOnPipelineEntry` is the single stage-transition authority and now runs read→write in an IMMEDIATE transaction with an optional `expectedStage` CAS; the automation pass passes its snapshot stage so stale Python-hop decisions no-op. Pass-level overlap is contained by in-process single-flight in `automation-pass.ts` (`claimDueRun` only guards the clock path). `app/_lib/db.ts`, `app/_lib/automation-pass.ts`.
- **2026-06-07 (run #2)** — Offer lifecycle invariants: at most one open offer per entry (partial unique index `uq_offers_open_entry` + transactional `getOrCreateOpenOffer`); offer-response side effects (onboarding dispatch, Hired transition) belong exclusively to the `markOfferResponded` CAS winner (`{offer, claimed}` contract). `app/_lib/offers-store.ts`, `offer-finalize.ts`.
- **2026-06-07 (run #2)** — Activity feed contract: `/api/pipeline/events` serves a PUBLIC projection (initials, no entryId/archetype — `pipeline-events-public.ts`) and a loss-free cursor (`?since=<id>`, AUTOINCREMENT pk as cursor, oldest-first bounded). PipelineTab keeps the cursor in a ref.
- **2026-06-07 (run #2)** — Slot trust boundary: proposal AND validation live in pure `schedule-slots.ts`; `offeredSlotFor` validates structurally and re-mints the label server-side — the client's slot label never reaches storage/emails. `schedule-store.ts` keeps only persistence (collision authority = `confirmScheduleInvite`'s transaction).
- **2026-06-07 (run #2)** — Public POST throttling: in-process fixed-window limiter `app/_lib/rate-limit.ts` guards `/api/offer/[token]`, `/api/schedule/[token]` (10/min per IP+token) and `/api/schedule/invite` (30/min per IP). In-process by design — single Next server process.
- **2026-06-07** — Voice-interview architecture: `/connect` mints short-lived provider credentials server-side (OpenAI ephemeral client secret, ElevenLabs signed URL) so no API key reaches the browser; transcripts POST back to `/complete`, which runs `runInterviewScorecard` → `runAutomationTask(entryId, "scorecard")` → `setApproval(entryId, "scorecard_review")`, the Interview→Offer gate. `app/_lib/interview-run.ts`.
- **2026-06-07** — The session **token** (`tk` + 24 random bytes, `randomToken`) is the only credential on the candidate link, and is now also the required completion capability at `/complete` (`app/api/interview/complete/route.ts`). `randomId("iv")` session ids are NOT capabilities.
- **2026-06-07** — kp has **no authentication layer at all** (no middleware.ts, no session checks). The recruiter-facing read APIs `/api/interview/by-entry`, `/api/interview/compare`, `/api/interview-prep` remain unauthenticated — idea ccb4d851 (server-side authz) was rejected as out-of-scope for a scan run; it needs an app-wide auth decision. Same applies to most other recruiter API surfaces.
- **2026-06-07** — `app/api/interview/simulate/route.ts` exists but is NOT in the "Interviews" context group's file list (context drift). It creates demo sessions (mode "candidate", no entryId) and returns the token; the sim tab passes it to `VoiceInterviewClient`.
- **2026-06-07** — Error-hygiene pattern: `safeJsonError` + `STORE_ERRORS` catalogue in `app/_lib/api-response.ts`, locked by source-level guard tests (`app/api/jds/error-message-hygiene.test.ts`, `app/api/interview/error-message-hygiene.test.ts`). `jsonError` forwards raw `err.message` — only safe for routes whose messages are client-safe by construction.
- **2026-06-07** — OpenAI Realtime transcription is configured with `whisper-1` (`app/_lib/voice/openai.ts`), which emits only final `.completed` events — no streamed input deltas. The candidate-side delta buffer in `VoiceInterview.tsx` is a forward-compat fallback for gpt-4o-transcribe-style configs; the load-bearing last-answer fix is the VAD-pending grace window in `finalize()`.

- **2026-06-08 (feature-scout W1)** — Dark-capability wiring (built backends that lacked a UI caller, now reachable): (1) `setPipelineEntryStage(id, toStage, opts?)` in `db.ts` — the recruiter manual-override twin of `actOnPipelineEntry` (same IMMEDIATE-tx + `expectedStage` CAS, clears approval, stamps `stage_changed_at`); exposed as the `set_stage` action in `app/api/pipeline/[id]/route.ts` and a stage `<select>` in `CandidateDrawer`. (2) New `moved` pipeline event kind (`EVENT_KINDS`/`EVENT_CATALOG` in `PipelineShared.tsx`) — a manual move is NOT `advanced` (can be backward). (3) `dispatchInterviewInvite(entry, link, {durationMin})` in `comms-dispatch.ts` — delivers the voice-screen link to the candidate; `/api/interview/create` now sends it (gated on `voiceAvailability()[provider]`, best-effort, returns `delivered`). (4) `useJobsList` gained `reload()`; `IngestAdPanel` is the UI caller for the previously-orphaned `/api/jobs/ingest`. (5) `ResultPanel` takes an optional `pipelineRef` → `AddToPipelineButton` (reuses the now-committed `useAddToPipeline.postPipelineAdd`); wired on the history page. (6) `Results.tsx` (Match) addToPipeline now returns `boolean` for a bulk-shortlist runner.
- **2026-06-08** — `useAddToPipeline.ts` (+ `postPipelineAdd` pure network fn, `pipelineAddBody`) is the canonical optimistic add; new add surfaces must reuse `postPipelineAdd`, never hand-roll the POST. (Was pre-existing-but-uncommitted WIP; committed in feature-scout W1.)
- **2026-06-08 (feature-scout W2)** — Candidate-loop keystone: `pipeline_entries.contact` column (nullable, migrated via the idempotent ALTER block) captured at inbound apply (`email` step in `buildApplyScript`, validated in `api/apply/[id]/route.ts`, threaded through `createPipelineEntry` + `PipelineEntry`/`PipelineRow`/`rowToEntry`). `candidateRecipient` now resolves **contact → candidateLabel → candidateId → "candidate"** — so the comms stack is deliverable for applicants (was dead-lettering to the literal "candidate"). `dispatchApplicationReceived(entry)` (kind `acknowledgement`) fires best-effort on a fresh apply acceptance. Dedup is still NAME-based (`applyDedupeKey` untouched — dedup-by-email deferred). **APP1**: a new `file` ApplyStep type — the apply chat extracts an uploaded CV via `/api/extract-text` (client-side, stored as the `cv` answer) and `buildIntakeProfile` folds it as high-weight `kind:"cv"` evidence; route head-samples to `MAX_CV_TEXT_LENGTH` and the apply body cap rose to 256 KB.

## Conventions enforced
- Unit tests are colocated `*.test.ts` run by Node's built-in runner with type stripping (`npm run test:unit` → `node --test "app/**/*.test.ts"`); imports inside tests use relative `./x.ts` paths (the `@/` alias does not resolve under node --test) — route contracts are locked via source-level regex guards instead.
- Validation is hand-rolled coercers at the trust boundary (`coerceProviderId`, `coerceLanguage` in `app/_lib/voice/types.ts`, `parseEntriesParam`), NOT zod — zod is reserved for the Python-generated LLM schemas (`schemas.generated.ts`).
- Browser-safe pure-decision helpers live beside the voice adapters (`finalize-status.ts`, `preflight.ts`): pure function over a snapshot, unit-tested; only a thin collector touches browser globals.
- Trust-critical truncation policy is named, documented and head+tail-sampling (never front-slice): `interview-transcript.ts` (`MAX_TURN_TEXT_CHARS`, `MAX_TRANSCRIPT_TURNS`, `MAX_SCORECARD_NOTES_CHARS`).
- Concurrency guards go in the SQL `WHERE` clause (`completeInterviewSession`, `markInterviewStarted` return `applied`/boolean), not read-then-write in routes.
- `npm run build` = `schemas:gen` (python codegen) + `next build`; `npm run typecheck` also regenerates schemas first.

## Anti-patterns to avoid
- Casting `request.json()` straight to a typed shape — the cast IS the vulnerability; every interview route now validates field-by-field (idea-c7df6b55).
- Page-level guards standing in for API guards: the portal page blocked *rendering* a completed session while `/connect` happily reopened it (idea-836e08d8).
- Scoring before persisting: any side-effecting step (approvals, skill minting) ordered before the durable artifact write produces phantom state on partial failure (idea-55fd89f9).
- An `else` catch-all on a parsed-event union: adding a new event kind silently misroutes through the old `else` (VoiceInterview's `handleOaiEvent` had to switch to explicit kinds when the parser grew).

## Open follow-ups (from Feature Scout 2026-06-08 — opportunity scan + Wave 1)
- **Scan**: 60 opportunities across 10 candidate→hire contexts (30 High / 20 Med / 10 Low) in `docs/harness/feature-scout-2026-06-08/` (`INDEX.md` + 10 reports + `FIXES-WAVE-1..10.md`). 7-wave plan in the INDEX (Waves 8–10 = post-plan heavyweights: DEC1+DEC2 guardrail (Wave-1 deferral), DEC3, PREP1 human scorecard). **Wave 1** (A "dark capabilities") 6/7; **Wave 2** (B "candidate loop") COMPLETE 5/5; **Wave 3** (C "export/share") COMPLETE 5/5; **Wave 4** (D "search/filter") both High (PIPE2+RES3; PIPE5/JOB5/MAT6 deferred); **Wave 5** (E "decision record") 4/5 (PIPE3+DEC4+PREP2+RES5; only PREP1 deferred); **Wave 6** (F "recruiter config") headline MAT1 shipped (PIPE4/SCH4/DEC5/VOX5/PREP4 deferred); **Wave 7** (G "AI-assist") VOX3+MAT2 shipped (VOX2 deferred); **Wave 8** DEC1+DEC2 (screening-wave run + dry-run preview — the Wave-1 deferral); **Wave 9** DEC3 (decide inline from the group eval — completes the Decisions group's 3 Highs + DEC4); **Wave 10** PREP1 (human interviewer scorecard — completes the Interview-Prep group's 3 Highs).
- **2026-06-08 (feature-scout W2, JOB3)** — Sourcing "Reach out": `POST /api/jobs/[id]/candidates/outreach` does `createPipelineEntry` (idempotent) + `runAutomationTask("outreach")` (drafts via automation_cli, dispatches via Outbox, gated on the per-entry `outreach_sent` marker) — so file-and-contact is at-most-once across clicks. `useReachOut(jobId)` hook mirrors `useAddToPipeline`'s per-candidate state; wired into RecruiterCandidates + RediscoverPanel (a reached candidate collapses to "✓ Reached out", since reaching also pipelines). `OUTREACH_FAILED` added to the `STORE_ERRORS` safeJsonError catalogue. To outreach a candidate from a new surface, reuse this route — don't re-implement the draft/dispatch.
- **2026-06-08 (feature-scout W2, SCH2)** — Candidate self-reschedule: `rescheduleScheduleInvite(token, slot, slotAt)` in schedule-store mirrors `confirmScheduleInvite`'s synchronous-transaction collision authority (slot_at identity), bounded by `MAX_RESCHEDULES`, frees the old slot, re-anchors `confirmed_at`, RESETS the reminder cycle (reminder_sent_at/attempts cleared) so the reminder fires for the new time. New migrated `reschedule_count` column. The `/api/schedule/[token]` POST gained a reschedule branch sharing the entry-record + dispatch logic with first-confirm via ONE local helper (so they can't drift); GET offers slots to a confirmed-under-cap invite (+ `canReschedule`). Pinned by `schedule-store.test.ts` (first store-level test — KP_DB_PATH temp DB + registerHooks extensionless-import shim, same pattern as offers-store.test.ts).
- **2026-06-08 (feature-scout W16, Med/Low sweep batch 2)** — **PREP5**: interviewer assignment — an "Interviewer" field on the prep modal rides PREP2's SAME debounced PUT (`saveInterviewPrepProgress` now takes `{checked, notes, interviewer}`, one write path → no race; cleared on regenerate); `listPreparedEntries` returns `{createdAt, interviewer}` so the schedule card shows "👤 name". **PIPE4**: per-stage aging SLAs — `STAGE_SLA_DEFAULTS` + `slaForStage(stage, overrides)` in PipelineTypes (Accepted 14/Screened 7/Interview 5/Offer 3; Hired 0=never), driving isStale + the aging filter; localStorage override editor (`kp.pipelineStageSla`, mirrors PIPE5); server `sla_breach` event deferred. **DEC5 reordered later** — on full read it's per-role config keying + a new auto-advance automation branch in runScreenWave = DEC1/DEC2-sized, not a sweep item.
- **2026-06-08 (feature-scout W15, Med/Low sweep batch 1)** — **dedup-by-email**: `applyDedupeKey(name, email?)` + `findApplicationByApplicant(jobId, name, email?)` key on email when present (email's non-alphanumerics → hyphens so a.b@x vs ab@x survive the createPipelineEntry slug strip distinctly), name fallback; `normalizeContact` helper in apply-intake. **PIPE5**: saved board views — named {query, quick} presets in `localStorage` (key `kp.pipelineViews`) as `SavedView`, rendered as pills, no schema. **MAT5**: `JobCompare` (sub_match) transposed roles×attributes table reusing the match-results selection (2–4) — no fetch, all on MatchResult.
- **2026-06-08 (feature-scout W14, PREP1 cross-surface follow-up)** — human scorecards now surface in the interview-REVIEW surfaces, not just the prep modal + drawer: `/api/interview/compare` enriches each candidate with `getHumanScorecard(entryId)`; `CompareInterviews` shows a "human: <verdict>" header badge + human ratings/evidence in the evidence card; `InterviewTranscriptModal` gains a `HumanScorecardSection` (via a `/api/interview-prep` fetch) that renders even with NO voice session. AI-session-gated (interviewedForJob = voice-interviewed only), so a human-ONLY candidate shows in drawer + transcript modal, not the compare grid. NOT done: wiring a human scorecard to the Decisions `scorecard_review` APPROVAL gate (that's an AI-pipeline approval mechanism, not a display — separate change).
- **2026-06-08 (feature-scout W13, MAT4 matrix half — Fit Matrix COMPLETE)** — `MatrixTab` "Export CSV" downloads the grid AS SHOWN (visible `cols` × filtered+sorted `rows`, blocked→"–") via the shared `toCsv`/`downloadFile` (export-utils, W3). Closes MAT4 (Match half W3) and ALL Fit-Matrix scan opportunities (MAT2 W7/W12, MAT3 W1/W11, MAT4 W3/W13, MAT6 W12). Matrix-tab MAT1 (weighting, W6) is separate; MAT5 (compare-jobs-for-one-candidate, Med) still open.
- **2026-06-08 (feature-scout W12, MAT6 + MAT2 row counterpart)** — `MatrixTab` (client-only): a "Min fit" floor (Off/≥55/≥70 state `minFit`) filtering `rows` by best-VISIBLE score; clickable column headers set `sortCol` (original position index) to rank rows by that role's score (cleared by the fit/A–Z toggle; ignored if a family filter hides that column). MAT2-row = an "N★" pill in each name cell counting visible roles where score >= STRONG_THRESHOLD (reused from matrix-stats). Matrix now substantially complete; lone follow-up = MAT4-matrix (grid CSV — toCsv/downloadFile already exist in export-utils).
- **2026-06-08 (feature-scout W11, MAT3 matrix half — committed on `main` post-merge)** — Fit Matrix "Shortlist" mode (`MatrixTab`): a `selectMode` toggle flips cell clicks from navigate → select (cell key `candId|posId`); an action bar batch-files each selected (candidate→position) via the canonical `postPipelineAdd` (sequential, failures stay selected for retry, successes ring locally — no placement refetch). Only scored/not-already-in-pipeline cells selectable. Completes MAT3 across both surfaces (Match-results half = W1 6995bba). `postPipelineAdd` now powers all 4 add surfaces (recruiter-candidates, rediscovery, match results, matrix). NOTE: this was the first wave committed directly on `main` (the feature branch was merged first).
- **2026-06-08 (feature-scout W10, PREP1 — human interviewer scorecard)** — `Scorecard` gained optional `source` ("ai" default / "human"). `saveHumanScorecard(entryId, sc)` + `getHumanScorecard(entryId)` (interview-prep.ts) persist a source:"human" Scorecard under a reserved `humanScorecard` key in the prep artifact payload — same seam as PREP2's `userProgress`, NO schema change. `POST /api/interview-prep/scorecard?entry=` validates field-by-field (bounded competency, rating clamped [1,RATING_MAX], capped evidence/summary, coerced recommendation). `HumanScorecardPanel` (sub_schedule) renders `rubricForArchetype(entry.archetype)` with anchors + 1..RATING_MAX selectors + evidence + verdict/summary, hydrates from the saved artifact, collapsed by default. CandidateDrawer reads it (best-effort via the existing /api/interview-prep GET) and shows it beside the AI "Interview outcome". Completes Interview-Prep Highs (PREP1 W10 + PREP2 W5 + PREP3 W3). Deferred: merge human scorecards into Decisions queue + CompareInterviews grid (they read interview_sessions — a 2nd-source merge); PREP4/PREP5.
- **2026-06-08 (feature-scout W9, DEC3 — decide from the group eval)** — GroupEvalModal's PerCandidateTabs/CandidateDetail gained inline Advance/Reject (gated on an optional `onDecide(label, action)`; read-only when absent, e.g. the simulation). DecisionsTab resolves the eval candidate BY LABEL back to the live `evalGroup.entries` Entry and calls the existing `act(e, action)` (same expectedStage CAS + comms as the queue). Local `decided` map flips buttons → "Advanced"/"Rejected" pill + tab badge (cached eval snapshot doesn't refetch; queue updates underneath). This completes the Decisions group's 3 Highs (DEC1/DEC2 W8, DEC3 W9) + DEC4 (W5). Deferred: "advance lead, reject rest" batch shortcut (multi-reject wants a DEC1/DEC2-style preview), DEC5 (per-role rules), DEC6 (calibration).
- **2026-06-08 (feature-scout W8, DEC1+DEC2 screening-wave guardrail — the Wave-1 deferral)** — `runScreenWave(jobId, override?, { dryRun })` (screen-wave.ts): dryRun runs the full ranking/fairness/tie-break math + returns `decisions[]` ("Would auto-reject…") but commits NOTHING (no actOnPipelineEntry CAS, no recordAutomationEvent, no dispatchRejection; also skips the unknown-archetype audit marker). Route forwards `dryRun` (default false). `ScreenWaveModal` (sub_decisions) — per-role "Screening wave" button in RoleDecisionRow (only when `g.jobId`); dry-runs on open + on each slider change (debounced), commits on explicit "Reject N & notify", then `load()` refreshes the queue. Server stays sole fairness+clamp enforcer on preview AND commit. **screen-wave is NOT node --test-able** — its chain (archetypes.ts) uses `@/` imports the strip-only runner can't resolve (repo convention: regex guards / pure-module tests, not integration tests); the gated math is tested in decision-config-schema.test.ts. (comms.ts line 49 `constructor(private readonly url: string)` is a parameter property — the one TS form strip-only node --test rejects — so the comms chain isn't node-testable either; left as-is.)
- **2026-06-08 (feature-scout W7, Theme G AI-assist — 2 pure-client items)** — **VOX3** = scorecard evidence quotes anchored to transcript turns in `InterviewTranscriptModal` (`findEvidenceTurn`: containment then distinctive-word-overlap ≥0.5, else -1/plain-text so a paraphrase isn't mis-anchored); clicking scrolls+ring-highlights, cited turns badged; pure client. **MAT2** = Fit Matrix per-column distribution strip (5-bar histogram + best/median/strong≥72) computed client-side over `data.cells`; pure stats in testable `app/features/sub_matrix/matrix-stats.ts` (`columnStats`, +5 tests), rendered by `MatrixShared.ColumnStats`. Both add NO storage — re-present data already on screen. Deferred: **VOX2** live co-pilot (NOT a read — needs the candidate side to stream in-flight partial turns before /complete + a recruiter monitor; real voice-runtime change). Smaller: VOX4 digest, VOX3-for-CompareInterviews, MAT2 row-counterpart.
- **2026-06-08 (feature-scout W6, Theme F recruiter config — headline MAT1)** — `match(candidate, jobs, *, weights=None)` (matching.py) now takes a recruiter weight override: resolved ONCE via the existing `resolve_weights(archetype, proposed)` (clamps to `weight_bounds` + renormalizes to sum 1) and applied to every `score_job`; the resolved vector + bounds ride back on `MatchResponse.candidate` (a free `dict[str,Any]` — no model change). `match_cli --weights` (JSON, non-dict → baseline); `/api/match` forwards a validated numbers-only object; `WeightsPanel` (sub_match) = archetype-labelled bounded sliders re-running via `runMatchFor(ref, weights)`. Server is the single fairness enforcement point — client can't push out-of-bounds/non-summing. 4 tests in test_matching.py. Deferred Med/Low: PIPE4 (per-stage SLA vs flat STALE_DAYS), SCH4 (availability windows), DEC5 (per-role rules), VOX5 (per-role language), PREP4 (question bank).
- **2026-06-08 (feature-scout W5, Theme E decision record — 4/5)** — **PIPE3** = `listPipelineEventsForEntry(entryId)` (db.ts) + `GET /api/pipeline/events?entry=<id>` (full, oldest-first, recruiter-keyed — NOT the anonymized public projection) + a drawer "History" section (EventDot/eventVerb); no schema, the events were already there. **DEC4** = the accept/reject branches of `actOnPipelineEntry` now record the optional `detail` on the advanced/rejected event (route already forwarded body.detail + DecisionLog already rendered d.detail — only the `recordEvent` arg + a capture UI were missing); AnalysisSummaryModal got a "Decision note" field, threaded via `act`/`decide`. **PREP2** = interviewer ticks + notes persist under a reserved `userProgress` key in the prep artifact's `payload_json` (`saveInterviewPrepProgress` preserves `created_at`) via `PUT /api/interview-prep?entry=`; modal hydrates once + debounce-autosaves. **RES5** = migrated `analyses.disposition`/`decision_note` (`ANALYSIS_DISPOSITIONS` = advance|hold|pass) + `setAnalysisDisposition` + `PATCH /api/analyses/[slug]`; `DispositionEditor` on history detail + a decision pill on history rows. Deferred: **PREP1** human scorecard (a NEW scoring surface — human-scorecard store keyed on entry.id source:"human", POST /api/interview-prep/scorecard, rubricForArchetype rating rows, read paths in Decisions/CompareInterviews; PREP2 laid the persistence groundwork).
- **2026-06-08 (feature-scout W4, Theme D search/filter — both High items)** — Client-side search/filter over already-loaded lists: **PIPE2** = `PipelineTab` filter bar (free-text + quick-filter chips Interview/Aging/Awaiting/Needs-intake); position grouping extracted to module-level `groupPositions(entries)` so the full board (count) and filtered board (lanes) key lanes identically (must match PipelineBoard's lane key `jobId ?? jobTitle ?? "?"`). **RES3** = `HistoryTab` filter bar (search + role-family/seniority selects from the loaded set). Both filter the fetched set in-component — NO schema/server change. Deferred Med/Low: PIPE5 saved views, JOB5 saved searches, MAT6 matrix filter, + RES3 tagging/server-query (only when history outgrows the 200-row `listAnalyses` cap).
- **2026-06-08 (feature-scout W3, Theme C export/share COMPLETE)** — Shared export toolkit `app/_lib/export-utils.ts`: `toCsv` (RFC-4180 quoting), `buildIcs` (single-event VCALENDAR, UTC stamps, RFC-5545 escaping — takes `stamp` explicitly so it's pure/testable), `downloadFile` (Blob+object URL, browser-only), `copyText` (clipboard). Pure serializers pinned by `export-utils.test.ts`. Reused by: MAT4 (Results.tsx "Export CSV"), SCH1 (SchedulePicker "Add to calendar" — `schedule/[token]` GET now exposes `slotAt`), RES1 (`ReportActions` on history page — copy `/history/<slug>` link + print), PREP3 (InterviewPrepModal "Copy prep"), RES6 (`ListBlock` header "Copy"). New export surfaces should reuse this toolkit, not re-roll CSV/ICS/clipboard. Action chrome carries `print:hidden`.
- **Candidate-loop (Theme B) COMPLETE.** Lone refinement left: **dedup-by-email** — apply dedup is still name-based (`applyDedupeKey` untouched); now that APP2 stores a contact, fold email into the dedup key so two same-named applicants are distinguishable.
- **DEC1+DEC2 SHIPPED (Wave 8, c66d847)** — the screening auto-reject wave now runs from the Decisions tab (per-role "Screening wave" button) BEHIND a dry-run preview, exactly as required (they shipped together, never DEC1 alone). The demo (`SimDecisionWave`) is no longer the only caller.
- **CV1 (Analyze-tab) deferred**: ResultPanel's "Add to pipeline" only wires on the history page; the live Analyze tab lacks the saved analysis slug client-side (not threaded through the analyze task result → crosses the `schemas.generated.ts` boundary). Thread `saved_slug` from the task result to surface it there.
- **MAT3 (Matrix) deferred**: bulk-shortlist shipped on Match results; the Fit Matrix cross-tab multi-cell select is still per-cell-navigate only (`MatrixTab.tsx`).
- **Remaining Themes B–G (54 opportunities)**: candidate-loop comms (APP1-3, SCH2), export/share (RES1 PDF, SCH1 .ics, PREP3), search/filter/saved-views (RES3, PIPE2/5, JOB5), decision record (PREP1/2, PIPE3, DEC4), recruiter config (MAT1, PIPE4, SCH4, DEC5), AI-assist + guardrails (VOX2/3, MAT2, DEC2, SCH3). See INDEX.
- **WIP-tree caveat**: this scan ran with kp's working tree heavily mid-WIP (a separate idea batch). Wave 1 lives on branch `feat/feature-scout-wave1-dark-capabilities`; two commits carry adjacent idea-batch code (noted in `FIXES-WAVE-1.md`).

## Open follow-ups (from bug-hunt 2026-06-07 — Bug Hunter scan)
- **Scan COMPLETE**: 51 findings across 8 high-risk contexts (3 critical / 17 high / 21 medium / 10 low) in `docs/harness/bug-hunt-2026-06-07/` (`INDEX.md` + 8 reports + `FIXES-WAVE-1..8.md`). **All 8 waves done + Data#1 fully closed → 47/51 closed.** **All 3 criticals + 16/17 highs closed.** No regressions throughout (tsc 0, next build ✓, unit 585, py 474).
- **4 findings deferred, each with rationale in its wave summary**: DevCase#2 (High — coordinate with the canonical-score-contract WIP); Scheduling#3 (Med — recruiter/candidate slot-vocabulary reconciliation); Scheduling#4 (Med — business-TZ slot-machinery rework + its WIP-modified tests); CV#7 (Low — decorative stage strip needs server per-stage progress). Pattern catalogue: 24 items across the FIXES-WAVE docs.
- TOOLING GOTCHA: `npx vitest run` reports "no test suite found (69 failed)" — a FALSE baseline. kp's unit tests target Node's built-in runner; use `npm run test:unit` (`node --test`) + `npm run test:python`. Don't trust vitest here.
- W5 (dev-case) and W8 (scheduling-format) overlap the pre-Wave-1 WIP snapshot `7597c20` (`evaluate.py`/`models.py`; `schedule-slots.ts`/`schedule/[token]/route.ts`/`SchedulePicker.tsx`) — re-read those before applying; some findings may already be addressed by that WIP.

## Open follow-ups (from Pipeline C run #3, 2026-06-07)
- The decisions/match/matrix routes still return raw `error.message` on their catch paths (`/api/decisions/config|group-eval|screen-wave`, `/api/match`, `/api/match/reasoning`, `/api/matrix`) — the safeJsonError sweep covered interview/pipeline/schedule/offer only. Same low-risk conversion + hygiene-test pattern when next touched.
- The matrix cache is single-entry and in-process (same caveat as the rate limiter): fine for one server process, swap the store if kp scales out.

## Open follow-ups (from Pipeline C run #2, 2026-06-07)
- The Activity feed shows initials-only until an auth layer lands (idea-4c41d103 tradeoff): full candidate names on the feed are blocked on the same app-wide auth decision as the rejected ccb4d851. When auth ships, add an authenticated full-detail mode to `/api/pipeline/events`.
- The rate limiter is in-process; if kp ever runs multiple server processes, swap the Map store behind the same `rateLimit()` shape.
- bug_hunter run #2 explicitly CLEARED as already-hardened: reminder dispatch (atomic CAS claim + bounded retry/backoff), slot double-booking (synchronous transaction + 409 + picker refresh), PipelineTab stale-fetch aborts, schedule-confirm-vs-advance drift (needs_reconcile). Don't re-flag these.

## Open follow-ups (from Pipeline C run #1, 2026-06-07)
- **Recruiter API authz (rejected idea ccb4d851)**: `/api/interview/by-entry`, `/compare`, `/interview-prep` (and sibling recruiter surfaces) expose transcripts/scorecards/PII unauthenticated. Needs an app-wide auth layer decision — right-sized as its own Pipeline A goal.
- `app/api/interview/simulate/route.ts` was out of the scanned group: it still uses `jsonError` (raw err.message) and unvalidated `language`. Apply the same safeJsonError + coerceLanguage treatment when next touching it; also consider adding it to the Interviews context group.
- The Interviews context group's `file_paths` omit `simulate/route.ts`, `InterviewSidebar.tsx`, `finalize-status.ts`, `interview-consent.ts` — group definition has drifted from the real surface.
- ElevenLabs path has no last-answer protection equivalent (idea-b70b8bd7 was OpenAI-scoped); the SDK delivers complete messages, but endSession() racing a final onMessage is unverified.

## Structural facts (from code-refactor scan 2026-06-14)
- **New shared helpers introduced (prefer these over re-implementing):** `runDevcaseCli<T>()` in `devcase-run.ts` (the devcase-CLI spawn/parse/cleanup envelope — was 9 copies; forwards `signal` uniformly); `rankPoolForJob()` in `app/_lib/recruiter-run.ts` (the `recruiter_cli` ranking spawn — 4 call sites); `openStore()` in `db-path.ts` (isolated-connection bootstrap = `new Database`+WAL+`busy_timeout=5000`+memoize — 12 stores); `entryLaneKey(e)` in `PipelineTypes.ts` (the `jobId ?? jobTitle ?? "?"` lane key — count AND membership must share it); `navLabel(t,key,fallback)`/`kindLabel`/`labelOr` (i18n has-fallback idiom); `jdJobId(slug)` in `jd-limits.ts` (the `jd-<slug>` identity); `pickDefaultProvider(requested,avail)` in `voice/index.ts`; `splitSpend` in billing `entitlements.ts` (included-then-credits, read+write); `applyFairnessVerdict` in automation (dry-run==commit fairness math); `MATRIX_BANDS` in `matrix-stats.ts`; `parseStoredGithubAnalysis` for stored `github_json`; `configure_stdio(errors=)` in `pipeline/jobfit/_cli.py` (now has an `errors` param — ~10 CLIs route through it).
- **3 stores intentionally do NOT use `openStore()`** — `decision-config-store`, `templates-store`, `job-ingest` set WAL **only** (no `busy_timeout`); folding them in would change locking semantics. Leave them.
- **Python adapter base seam**: provider `available()`/key-resolution now lives in `TextProvider` (`_env_keys` + `_import_sdk`); `_load_env` must dispatch through the concrete adapter's module or the per-adapter `load_local_env` test patches stop biting. Azure keeps its endpoint+key override.

## Conventions reinforced (code-refactor scan 2026-06-14)
- `app/_lib/*.ts` pure helpers stay import-free for bare `node --test`; a tested helper that consolidates a constant should HOST it (consumers import from it), never the reverse. `matrix-stats.ts` hosts `MATRIX_BANDS`; `format.ts` hosts `SCORE_COMPONENT_KEYS`.
- A duplicated wire/contract type is a drift BUG, not just debt (`WaveDecision`/`ScreenDecision` had already lost DEC4 localization fields). Single-source the producer's type, `import type` on the consumer.
- A preview/commit pair (dry-run vs commit; UI-remaining vs debit) MUST share one function — drift there is a correctness bug on fairness/money paths.
- `node --test` needs the explicit `.ts` extension on relative imports; after any import rewrite, re-run the unit suite (a missing suffix silently unloads a colocated suite).

## Open follow-ups (from code-refactor scan 2026-06-14 — full-stack, all 25 contexts, 104 findings)
- **Campaign COMPLETE: 89/104 findings fixed across 10 fix waves**; 15 deliberately not changed (all documented). Scan + per-wave docs in `docs/harness/code-refactor-2026-06-14/` (`INDEX.md` + 25 reports + `FIXES-WAVE-1..10.md`). Gates green every wave: tsc 0, unit 849, python 596 OK. Commits LOCAL on `main`, UNPUSHED.
- **Pre-existing test flakiness (NOT introduced)**: ~1-in-5 full-suite `npm run test:unit` runs reports 6 transient failures (clean on re-run; ~8/10 runs are 849/0). A small set (`billing-gate.test.ts` flagged) appears to share DB state / race under parallel `node --test`. Worth a dedicated test-isolation fix (serialize the offending files or give each its own temp DB).
- **Deferred-with-reason**: `data-layer #4` — split the 1098-line `core.ts` god-module (schema DDL + 4 seeders + safeRowParse); deferred for boot-ordering coupling risk. `WaveDecision` localization wiring — `ScreenDecision` fields now reach `SimDecisionWave.tsx`, but it's hardcoded-English (no `useTranslations`); localizing is a UI change, not a type dedup.
- **Skipped as false-positive/intentional on inspection** (~the expected ~9% FP rate): `analysis #4` (console.error IS the route convention), `automation #3` (`--strengths-json` is a documented CLI surface), `matching #3` (reasoning renderers genuinely diverge), `voice #1` (only 2 of ~5 "verbatim" compliance lines were truly identical — extracted only those), plus `github #4`/`llm-provider #2,#4`/`scoring #3,#4`/`dev-case-py #4` (intentional anti-drift / doc-only / test-only).
- **Out of scope, noted**: ~14 other hand-rolled `configure_stdio` copies exist outside the two scanned Python contexts — a future repo-wide pass.

## Moonshot scan + first conversion (2026-06-14)
- **Moonshot-architect Pipeline B scan** (scan-only): `docs/harness/moonshot-2026-06-14/` (INDEX + 6 cluster reports), 31 bets → 8 themes. Two foundational primitives gate most Tier-1 bets: **P1 a persisted outcome ledger** (the accepted-but-unbuilt outcome-feedback spine) and **P2 multi-tenancy** (kp is single-workspace today — `db/billing.ts`). Conversion sequence in INDEX.
- **First moonshot converted → Calibration Engine MVP (shipped, Pipeline A, all gates green: tsc 0 / next build ✓ / unit 857 / py 596 / lint clean).** New structural facts:
  - `app/_lib/calibration.ts` — PURE, import-free `computeCalibration(pairs, minOutcomes?)` → reliability curve (10 bins) + Brier + `MIN_CALIBRATION_OUTCOMES` gate. Tested (`calibration.test.ts`). Reusable wherever a (prediction, outcome) calibration is needed.
  - `calibrationPairs()` in `db/analyses.ts` — the first (prediction, outcome) dataset: `score` → `disposition` (advance=1/pass=0; hold/null excluded). This is the **outcome-label source** for now; the analyses table is where calibration data lives. `GET /api/analytics/calibration` (+`?roleFamily`) serves it; `CalibrationPanel` in sub_analytics renders it with an honest below-gate state.
  - **Deliberate MVP scope**: prediction = the existing `score` (no engine probability-head). Follow-ups (the moonshot's later steps): emit a probability head from `score_job`; Python isotonic/Platt fit per role-family in `pipeline/jobfit/eval/calibration.py`; weekly recompute via the tasks queue; gate the SCORE DISPLAY (Match/Matrix/History) behind "calibrated since N"; calibration-by-subgroup for adverse-impact. A richer outcome label (pipeline "Hired") needs analysis→entry linking — deferred.
- **Second moonshot converted → Decision System of Record MVP (shipped, Pipeline A, all gates green: tsc 0 / next build ✓ / unit 857→865 / py 596 / lint clean).** New structural facts:
  - `app/_lib/decision-hash.ts` — PURE (imports node:crypto, still node-test'able): `canonicalize()` (key-sorted JSON) + `decisionContentHash(prevHash, payload)` = sha256(prevHash + "\n" + canonical). +8 tests. The tamper-evidence primitive — reuse for any hash chain.
  - `app/_lib/decision-record-store.ts` — isolated connection (decision-config-store pattern, WAL-only, one of the non-`openStore` stores), table `decision_records` (seq/prev_hash/content_hash/kind/actor/policy_version/candidate_ref/rationale/reason_code/payload_json/created_at). `sealDecisionRecord()` appends a link in ONE transaction (reads latest hash + inserts atomically → concurrent seals can't fork). `listDecisionRecords({candidateRef?,limit})`, `verifyDecisionChain()` re-hashes the whole chain (`{ok,count,brokenAtSeq}`). verify re-canonicalizes the stored `payload_json`, so the stored JSON needn't be canonical.
  - **Seal site**: `screen-wave.ts:201` (the `auto_rejected` COMMIT path, after the dryRun `continue`) — additive, best-effort try/catch so a seal failure never aborts the wave. `GET /api/decisions/records` (?candidate + whole-chain verdict); `DecisionRecordsPanel` in sub_analytics (verify badge + JSON dossier export).
  - **MVP scope = auto-reject writer only.** Follow-ups (moonshot later steps): backfill seal writers at group-eval lead (`group-eval-run.ts`), human+AI scorecard (`interview-prep/scorecard/route.ts`, `attachInterviewScorecard`), offer terms (`offer-finalize.ts`); stamp AI actor provenance (model + promptVersion, e.g. `scorecard-v3`); candidate-facing token `/why` endpoint (mirror `offer/[token]`); content-addressed full inputs snapshot; per-candidate PDF dossier. Kept it a hash chain in SQLite, NOT a blockchain (moonshot risk note).
- **Third moonshot converted → Live Work Surface foundation slice (shipped, Pipeline A, gates green: tsc 0 / next build ✓ / unit 865 / py 596→604 / lint clean). Sandbox EXPLICITLY deferred (escalated; user chose the no-sandbox slice).** New structural facts:
  - `pipeline/jobfit/devcase/process_events.py` — PURE `tooling_from_events(events, cover_probes)` + `derive_signals(events)`: deterministic tooling dict from an OBSERVED event stream (open/edit/decision_log/submit), read-before-write from event order, never infers over-reliance. +8 py tests (`test_process_events.py`). TS mirror type `ProcessEvent` in `DevTypes.ts`. Free-form JSON — NO Pydantic/codegen.
  - `reflect.assess_tooling` gained an optional `events` param: present → `tooling_from_events`, source `"observed"`, conf 0.8; absent → existing commit-metadata inference (backward compatible). `devcase_cli --events-json` plumbs it. `SourceKind` does NOT include `"observed"` (it's an exhaustive union feeding `describeSource`) — the engine emits `"observed"` as a free per-step source string; compare it via `String(...)` at the UI, don't add it to SourceKind.
  - `dev_sessions` + `dev_session_events` tables (core.ts no-migration schema) + store fns in `db/devcase.ts` (shared `ensureDb`, NOT isolated): `startDevSession`/`saveDevSessionFiles`/`appendDevSessionEvents` (tx-atomic append-only seq)/`getDevSession(+Events)`/`submitDevSession`. **A session submits by creating a `dev_submissions` row with `repo_ref="session:<id>"`** — `runEvaluateSubmission` detects that prefix, loads the observed events, passes `--events-json`, and skips `fetchRepoSignals` (commits=[], no git log by design). This reuses the whole eval machinery.
  - Surface: editable `LiveWorkSurface.tsx` on `devcase/apply/[token]` (lazy session on first interaction, debounced edit events, decision_log on DECISIONS.md, 8s flush + on-submit keepalive). Replaced the read-only SeedFiles; `DevApplyForm` repo-link path kept as fallback. EvalPanel shows an observed/inferred badge from `perStepSources.tooling`.
  - **MVP boundary (documented):** session TOOLING is observed end-to-end, but session work-CONTENT scoring is thin — the candidate's edited files are NOT yet fed to the Python `evaluate_submission` (the CLI has no files input; commits=[] for sessions → deterministic reflection). FOLLOW-UPS: sandboxed run (test-went-red observed) · feed session files into evaluate_submission for real transfer scoring · `probe_touch` events (probe seams are internal) · Monaco-grade editor · reflect_commits also consuming events.
  - GOTCHA (cost me a fix-forward): `npx tsc … | grep -c "error TS"` exits 0 when there ARE matches → a chained `&& git commit` runs even with errors. CHECK the tsc number BEFORE committing; don't chain commit after `grep -c`.
- **Fourth moonshot converted → Durable Skill Profile credential MVP (shipped, Pipeline A, gates green: tsc 0 / next build ✓ / unit 865→872 / py 604 / lint clean). The flagship (theme A) — needed NO new infra (signing/canonicalize/token-page all existed).** New structural facts:
  - `app/_lib/skill-profile.ts` — PURE: `buildDurableSkillProfile()` freezes dev-case `evaluation.dimensionScores` (durable axes) + `transfer.transferScore` + propagated `evaluation.confidence` into a versioned `DurableSkillProfile`; `signProfile`/`verifyProfile` = HMAC-SHA256(`KP_SECRET`) over `canonicalize()` (REUSES `decision-hash.canonicalize`). Constant-time verify, rejects tamper + rotated secret. +7 tests. **GOTCHA repeat: its relative import of `./decision-hash` MUST carry the `.ts` extension or bare `node --test` throws ERR_MODULE_NOT_FOUND and the WHOLE test file fails to load (counts as 1 fail, your tests silently don't register). The app build tolerates `.ts` import extensions (tsconfig allowImportingTsExtensions; voice/index.ts uses them), so add `.ts` on relative imports in any `_lib` file a test loads.**
  - HMAC signing model = SYMMETRIC → verification is kp-hosted ("FICO lookup"); offline/asymmetric is a follow-up. `KP_SECRET` (the existing master secret from `llm-secret.ts`) is required to issue/verify; tests set `process.env.KP_SECRET` like `llm-secret.test.ts`.
  - `skill_profiles` table (core.ts no-migration) + `app/_lib/db/skill-profiles.ts` (shared ensureDb): `issueSkillProfile(submissionId)` mints ONLY from a `status="evaluated"` submission (earned-not-given), idempotent per submission; `getSkillProfileByToken`/`verifySkillProfileToken` (sig recomputes AND not revoked)/`revokeSkillProfile`. Barrel re-exports it.
  - Surfaces: recruiter mint button in `SubmissionRow.tsx` (dev studio = English-only, no i18n) → `POST /api/devcase/skill-profile`; public candidate-owned **server** page `app/skill/[token]/page.tsx` (token-gated like `/offer/[token]`, calls `verifySkillProfileToken` directly, verified/revoked/tampered badge, en/cs); public machine-verify `GET /api/skill-profile/[token]/verify` (the badge/demand-side lookup).
  - FOLLOW-UPS (the standard/network vision, NOT built): carry-it-in skip-re-grade for inbound applicants · published methodology/calibration report · SDK/LinkedIn badge embed · asymmetric offline-verifiable signatures · auto-issue on eval. Cold-start adoption is a MARKET risk, not a code one.
- **Decision SoR deepened → seal-writer backfill (shipped, gates green: tsc 0 / next build ✓ / unit 872 / py 604 / lint).** Added `sealDecisionSafe(input)` to `decision-record-store.ts` (best-effort wrapper: try/catch+console.warn → null; an audit seal must NEVER abort the decision it records) and refactored the screen-wave auto-reject seal to use it. Backfilled seals at FOUR more decision points, each at ROUTE/ORCHESTRATION level (after the state write — excludes sim/demo, no store→store coupling) with precise actor provenance:
  - `ai_scorecard` — `api/interview/complete` after `attachInterviewScorecard`, actor `auto:${AUTOMATION_VERSION.scorecard}` (=`auto:scorecard-v3`).
  - `human_scorecard` — `api/interview-prep/scorecard` after `saveHumanScorecard`, actor `human:recruiter`.
  - `offer_terms` — `api/pipeline/[id]` offer-extend, sealed ONLY on `getOrCreateOpenOffer` `created` (not an idempotent re-extend), actor `human:recruiter`, inputs salary/currency/jobTitle.
  - `group_eval_lead` — `runGroupEval` top recommendation, actor `auto:group-eval`, policyVersion = eval source. SAFE because the backend `runGroupEval` is only invoked via the task runner (`tasks.ts` kind `group_eval`) — the sim has its OWN client-side `runGroupEval` in SimulationProvider, so this is the real path only.
  - The existing `DecisionRecordsPanel` + dossier export surface all new kinds automatically (de-snaked). candidateRef = pipeline entry id throughout. Remaining follow-ups: candidate-facing `/why` token endpoint; per-candidate dossier filter; manual advance/reject seal.

## Auth & multi-tenancy (P2) — started 2026-06-14
- **P2 (multi-tenancy) is a MULTI-GOAL PROGRAM, escalated.** kp = **28 single-tenant tables** (no `workspace_id` anywhere), **NO auth layer at all** (no next-auth/session/middleware — the open `ccb4d851`), explicit single-`workspace` model (`billing_state` has one row id='workspace', `const WORKSPACE = "workspace"`). Multi-tenancy can't even start on the data side until identity exists → **auth first**. Roadmap: auth (DONE) → tenant column + data migration on 28 tables → scope every query → tenant-aware billing/UI → then Talent Graph / Benchmark Network / marketplace / API moonshots.
- **Auth & identity foundation SHIPPED (Pipeline A, gates green: tsc 0 / next build ✓ / unit 872→884 / py 604 / lint; closes ccb4d851 when enabled).** Structural facts:
  - `app/_lib/auth/session.ts` — `signSession`/`verifySession` = HMAC-SHA256(`KP_SECRET`) over base64url `{workspace,iat,exp}` (node:crypto, 7d TTL, constant-time). `currentWorkspaceId(session)` = THE tenancy seam (returns the single default `"workspace"` today; real multi-tenancy fills it). +7 tests.
  - `app/_lib/auth/edge-verify.ts` — Edge-safe `verifySessionEdge` via **Web Crypto** (`crypto.subtle`), SAME format/secret → node-SIGN ↔ edge-VERIFY interop (tested). **The cookie-name const `SESSION_COOKIE` lives HERE** (edge-safe) and is re-exported from session.ts, so `proxy.ts` can import it WITHOUT pulling node:crypto into the Edge bundle.
  - **GATE LIVES IN `proxy.ts`, NOT `middleware.ts`** — kp is on Next 16 which renamed middleware→`proxy.ts`; having both fails the build (cost a fix-forward). The existing proxy.ts (locale `?lang=` override) now runs the auth gate FIRST, then locale. Its matcher was changed to **also cover `/api`** (the original excluded `api` — recruiter APIs would've stayed ungated). proxy() is now `async`.
  - **Fail-closed allow-list** (verified 28 public / 32 recruiter / 0 misclassified): public = candidate token pages (`/apply /offer /schedule /interview/ /status /skill /devcase/apply`), their APIs, webhooks (`/api/billing/webhook`, `/api/devcase/inbound`, `/api/channels/`), `/api/interview/connect`+`complete` (candidate voice, EXACT — create/by-entry/compare/revoke are recruiter), `/api/skill-profile/` (verify) vs gated `/api/devcase/skill-profile` (mint), `/api/schedule/<token>` but NOT `/api/schedule/invite`, `/api/auth /api/health /api/extract-text`, `/login /about /landing`. Everything else gated.
  - Login: operator password (`KP_OPERATOR_PASSWORD`, constant-time sha256 compare) → `__Host-kp_session` cookie (httpOnly/secure/sameSite=lax; Secure works on http://localhost — trustworthy origin). `/login` page reads `next` from `window.location` (avoids useSearchParams Suspense), open-redirect-guarded.
  - **OPT-IN: auth OFF when `KP_OPERATOR_PASSWORD` unset** (no regression for existing local deploys), ON+enforcing when set. **PROD MUST SET `KP_OPERATOR_PASSWORD`** (and `KP_SECRET`) to actually close ccb4d851.
  - FOLLOW-UPS: full names on Activity feed behind auth; multi-user accounts; the tenant-column + query-scoping data layer (P2 next goal); then the network/marketplace moonshots.
- **Tenant data layer — `analyses` vertical slice SHIPPED (Pipeline A, gates green: tsc 0 / next build ✓ / unit 884→886 / py 604 / lint; behavior-preserving — one workspace today).** The careful narrow-deep first slice of multi-tenancy (NOT all 28 tables at once). Structural facts:
  - `workspaces` table (tenant root) + `db/workspaces.ts` (list/get + `DEFAULT_WORKSPACE_ID = "workspace"`, single-sourced). Default row seeded `INSERT OR IGNORE` in `ensureDb`.
  - `currentWorkspace()` (`app/_lib/auth/current-workspace.ts`) — async, reads the session cookie in REQUEST scope; outside a request (`cookies()` throws → catch) returns the default. Background work carries its workspace explicitly (NOT this).
  - `analyses.workspace_id` — ALTER-added (matches the `disposition` migration convention), `idx_analyses_workspace`, post-seeder backfill of any NULL → default (order-independent).
  - **THE SCOPING PATTERN (the replication recipe for the other 27 tables):** every store fn gets a **trailing optional `workspaceId: string = DEFAULT_WORKSPACE_ID`** → behavior-preserving + zero forced caller churn; INSERT stamps it, SELECT/UPDATE add `WHERE workspace_id = ?`. Thread the REAL workspace at: (a) **request reads** — caller does `await currentWorkspace()` and passes it; (b) **background-task writes** — carry the workspace on the TASK params (resolved at enqueue from the authed request, exactly like `lang`; the task can't read the cookie) → task handler passes it down. Verify with a **source-regex guard test** (`analyses-tenancy.test.ts`: asserts every `FROM/INTO/UPDATE <table>` SQL block carries `workspace_id`) — no live DB needed.
  - **`analyses` proves BOTH halves** (request reads via `currentWorkspace()`; background write via the analyze task carrying `workspace` → `runAnalyze` → `persistAnalysis` → `saveAnalysis`). Deep task-context READERS (`group-eval-run`/`match-candidate`/`candidate-pool`/`candidate-timeline`) intentionally keep the default for now (replication follow-up — they need their tasks/callers to carry + pass the workspace).
  - **Real isolation is NOT active yet** (one workspace; `currentWorkspace()` always resolves to it). It activates with a later goal: workspace CRUD + session→workspace mapping. Then replicate the scoping recipe across the remaining 27 tables (one domain per goal), and the cross-tenant Talent Graph / Benchmark Network / marketplace / API moonshots unlock.
- **Tenancy flipped LIVE — workspace CRUD + session→workspace mapping SHIPPED (Pipeline A, gates green: tsc 0 / next build ✓ / unit 886 / py 604 / lint).** `analyses` isolation is now real per session.
  - `createWorkspace(name)` (db/workspaces.ts) + `GET/POST /api/workspaces` (list+current / create) — recruiter-gated.
  - `POST /api/auth/switch-workspace` re-mints the signed session cookie with the chosen workspace (`signSession(workspaceId)`); SELF-GUARDS (it's under the proxy-allow-listed `/api/auth/`, so the handler requires a valid session + validates the workspace exists). After switch, the client `window.location.reload()`s so server components re-read the new session → `currentWorkspace()` returns it → the scoped `analyses` store filters by it. **No change to `currentWorkspace()` was needed — it already read `session.workspace`; the mapping is just "switch re-mints the session."**
  - `Workspace` Settings sub-tab (`sub_workspace/WorkspaceTab.tsx`): list/create(create-then-switch)/switch + active indicator, wired via `tabs.ts` (`WORKSPACE_TAB_IDS` + settings group `items`) + `Workspace.tsx` (dynamic import + render case). en/cs. Adding a tab = those two edits + the panel; no exhaustiveness breakage.
  - **HONEST PARTIAL SCOPE surfaced in-UI** (amber banner): only `analyses` isolates today, so switching to a new workspace shows empty Analyses but shared everything-else — expected, not a bug. Caveat lifts as the scoping recipe replicates to the other 27 tables.
  - Model: single operator password owns all workspaces + switches between them (per-user accounts deferred). Switch requires auth → when `KP_OPERATOR_PASSWORD` is unset (auth off) the switch 401s and the UI says so. No new unit tests (route/store/UI glue needs DB+cookies; `signSession(workspace)` is already covered by session.test). FOLLOW-UPS: replicate scoping (lifts the caveat); per-user accounts; rename/archive/delete; shell-header workspace indicator.
- **Scoping replication progress (P2). Tables scoped so far: `analyses` (slice goal), `profiles` (2026-06-14, gates green tsc0/build✓/unit887/py604/lint).** The recipe is now confirmed MECHANICAL — `profiles` followed it exactly (ALTER+backfill+index in core.ts beside analyses; trailing optional `workspaceId=DEFAULT_WORKSPACE_ID` on the 7 profiles-table fns; `profiles-tenancy.test.ts` source-guard; threaded real ws only at the RECRUITER request routes `/api/profile` (GET/POST/PUT/DELETE) + `/api/matrix`). REPLICATION NUANCES learned: (1) a store module can hold fns that query OTHER tables (`listOpenPositions`/`pipelinePlacements` in profiles.ts query jobs/pipeline) — scope only the module's OWN-table queries; the guard's table-matcher naturally excludes the rest. (2) The CANDIDATE-facing write (apply route, unauthed) keeps the default — its correct workspace is the applied-to JOB's, which needs the jobs/pipeline domains scoped first (cross-domain dependency). (3) One task-context write exists (`devcase-run` updateProfile crediting skills) — keeps default until the dev domain carries a workspace. REMAINING DOMAINS (by size): jobs (12 fns/22 callers + job-ingest isolated-conn write), interviews (12/6), pipeline (28 fns/42 callers — the funnel heart, its own multi-goal effort), dev_* (8 tables), billing_*, channels/campaign/analytics_targets. gemini_cache = shared content-addressed cache, intentionally NOT tenant-scoped. Each domain = one goal; per-domain guard test.
