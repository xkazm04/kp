# English source defects — found by the localization sweep

Raised by the 2026-08-12 `/i18n-translate` full sweep. These are defects in
**`messages/en.json`**, the source of truth. They cap quality for *every*
locale, and the skill's standing rule is to report them rather than fix them
silently: changing source copy is a product decision, not a translation one.

None of these were changed by the sweep itself. The one deliberate exception in
that sweep was the no-em-dash recast, which was explicitly authorized (see
[`contract.md`](./contract.md) §5).

**291 keys** flagged across 5 classes.

## 2026-08-12 fix pass (14 keys resolved)

A follow-up pass fixed a first batch, prioritized per the operator's
instructions: hardcoded currency/amounts first (real call-site changes, not
just catalog edits), then missing ICU plurals, then the "several claims in
one value" class, then a clean concatenation split, then mechanical wording
fixes. Rows fixed are marked **RESOLVED 2026-08-12** inline below, with what
changed and (where relevant) which component. Highlights:

- **Currency/duration (4 keys, call-site changes):** `agentsWorkforce.spendNote`,
  `analytics.compute.unpriced` ("$0" → a `{zero}` placeholder formatted via
  `fmtUsd()`/`useFormatter()`); `devcase.integrity.backdatedTitle`,
  `simulation.error.groupEvalTimeout` (glued "s" unit → `Intl.NumberFormat`
  duration formatting at the call site).
- **Missing ICU plural (8 keys, catalog-only):** `devcase.outbox.count`,
  `devcase.outbox.deadLetters`, `nav.attentionBadge`, `nav.attentionBadgeGo`,
  `profile.matrix.groupCount`, `profile.roster.count`, `analyze.charsCount`.
  Call sites already passed a raw numeric count, so no code changes were
  needed. `devcase.outbox.countFiltered` and `devcase.cohort.evaluated`
  (grouped with these in the original list) are left open — see their row.
- **Several distinct claims (1 of 3 keys):** `channels.email.notWiredHowTo`
  split into `notWiredHowTo` + `notWiredHowToSetup`. `data.eraseExplainer` and
  `jobs.tab.intro` left open — both are single GDPR/product-intro paragraphs
  where splitting changes the key shape and reads as a bigger call than this
  pass should make unilaterally; see their rows.
- **Other (2 keys, mechanical only):** `about.archLink` ("&" → "and"),
  `integrations.ats.tokenPlaceholderKeep` (double-space typo).

Not started in this pass: the remaining ~76 missing-plural keys, the ~38
concatenation keys, the ~136 "Other" keys, and `data.eraseExplainer` /
`jobs.tab.intro` from the "several claims" class. All of the "Hardcoded
currency" class not listed above as resolved (mostly `analytics.roi.*` and
the `landing.pricing.*` tiers) is also still open — see "Left open" below.

## Other (wording, jargon, or leaked internal detail) (138)

| key | problem |
|-----|---------|
| `about.archLink` | **RESOLVED 2026-08-12** — en now reads "and"; de fixed to match. "(v1 & v2)" uses an ampersand in prose. cs/fr both correctly expanded it to "a"/"et"; de kept "&". A plain "and" in the source would remove the ambiguity. |
| `about.intro` | The rich tags wrap the English colour word as their own content (<moss>moss</moss>, <coral>coral</coral>). cs/fr translate the visible word, de leaves it as capitalised English "Moss"/"Coral" — the string cannot be consistent across locales because the tag name and the visible label are the same token. The visible label should be a separate key from the token name. |
| `about.students.colConstruct` | "Construct" (psychometrics sense) is a bare noun with no context in a table header; it collides with the everyday verb sense and forced fr to substitute "Critère". A source like "Construct measured" or a description key would disambiguate. |
| `about.students.scriptFootnote` | Same shape: one ~50-word sentence chaining three unrelated rules with commas plus "and". I had to split it to remove the dash; it should have been separate keys or a list from the start. |
| `aboutPage.meta.title` | A meta <title> built as "About the app — KandiDate" relies on the dash as a site-name separator, which is a typographic convention rather than sentence syntax. It should be composed from a page title and a site name in code, not carried in one catalog value. |
| `agentFit.verdict.complete / .temporary / .unassessed` | Three bare adjectives with no head noun in the value. English adjectives do not inflect so the source reads fine, but every target language must guess what they agree with (here: the *verdict*, or the *fit* named only in the sibling heading key). This is what let cs ship two feminine forms and one neuter. A source value carrying the noun, or a doc comment naming the referent, would prevent the whole class. |
| `analytics.automation.holds` | "Holds raised → resolved" is a headless compression of two counts; languages with adjectival agreement have to invent a construction. "Holds: raised / resolved" would translate cleanly. |
| `analytics.calibration.samples / driftBrierTitle / momentumWeekAria / sourceLine` | Same flat-count shape: "{n} decided candidates", "{added} new, {advanced} advanced, …". All break at n=1 in en and in every one/other locale. Not fixed here because the counts are threshold-gated or aria-only, but they are the same source-level omission. |
| `analytics.channels.variantsTitle` | "Creative variants" uses the adtech noun "creative" with zero context; two of three locales read it as an adjective. "Ad variants" or "Ad creatives" would be unambiguous for translators. |
| `analytics.compute.computePerHire` | "LLM compute" is an English mass-noun with no equivalent in cs/fr; a note that it means the metered inference cost (not "a computation") would prevent the ambiguous renderings now in cs and fr. |
| `analytics.compute.manualWindowed` | "all-time view only" refers to a filter option whose own label is "all-time" but does not name it as a UI string, which is what pushed fr into capitalising the reference mid-sentence. A quoted or placeholder-fed option name would remove the ambiguity. |
| `analytics.empty.metricCostPerHireBody` | "Channel spend divided by the hires it produced" — the antecedent of "it" (spend) is far from the pronoun and inverts the causality readers expect, which is exactly what the Czech relative clause got wrong. |
| `analytics.forecast.eta` | "Average time to hire: {days} days." is flat; likewise forecast.horizon "Next {weeks} weeks" and goalDays / deltaDays. |
| `analytics.forecast.inFlight` | "Expected from in-flight" ends on a bare adjective with no head noun. English tolerates it as a dashboard label; Czech and German cannot without supplying a noun. |
| `analytics.log.kinds.intake_degraded` | "Intake degraded" is jargon whose meaning (automatic profiling failed, manual capture needed) is only recoverable from pipeline.legend.intakeDegraded. Translators without that cross-reference guess. |
| `analytics.log.kinds.interviewer_brief_sent` | The event id is interviewer_brief_sent but the label is "Interviewer briefed", while its siblings interviewer_brief_skipped and kindsQueued.interviewer_brief_sent both say "brief". The inconsistent head noun forces every locale to choose between the person-verb and the artefact-noun (cs: "Tazatel informován" vs "Podklad pro tazatele"). |
| `analytics.stageDwellRow` | "avg {days}d · {count} waiting" is flat on both counts and also glues the unit to the number ("{days}d"), which forces every locale to re-separate it. |
| `apply.script.koAuth` | "…to work in {location}" hardcodes the English preposition for a free-text job.location that may be a city or a country. English tolerates "in" for both; French does not (dans/à/en), so the string is unfixable in fr without restructuring. Same shape in apply.script.koModeLocation. |
| `channels.comms.deadLetters` | "dead-letter" is mail-server jargon surfaced to a recruiter in an operator chip. It has no natural equivalent in cs or fr, so both locales must paraphrase and the label grows. Worth renaming in en (e.g. "undelivered") rather than translating around. |
| `channels.email.gmail2 / gmail4 / outlook3 / ads.linkedin2 / ads.meta2` | The empty rich tag `<endpoint></endpoint>` sits mid-sentence with no visible content in the source string, so a translator cannot tell whether the injected value is a URL, an email address or a code chip, and cannot judge the preposition that must precede it. A short placeholder gloss in the key name or a `{endpoint}` argument would be safer. |
| `channels.eyebrow` | "Inbound studio" is a coinage with no companion elsewhere in the catalog (the domain term is *intake*). Every locale has to invent a head noun for "studio"; naming it "Intake studio" would let the glossary carry the decision. |
| `channels.sim.filed` | "Filed at Accepted on {role}" hardcodes the English stage label "Accepted" inside prose. Every locale has to guess whether to translate it or match the board's own stage string; cs/de/fr currently all render it differently (Přijato / „Angenommen" / « Accepté »). The stage name should arrive as a placeholder resolved from the stage catalog. |
| `channels.stats.received / channels.receivers.received / channels.guide.live` | "Received" is used both as a stat-tile count label and as a table column header, and channels.guide.live labels the live state "Receiving" while channels.relay.statusOn labels its live state "Delivering". The three-way overlap makes a one-word target rendering ambiguous without reading the call site. |
| `comms.there` | One English token "there" serves three semantically different slots: a salutation name ("Hi there,"), a recipient name for the interviewer brief, and the {candidate} label inside "You're set to interview {candidate}". English gets away with it; every inflected language needs at least two different words (vocative vs nominative), and German needs a singular greeting rather than a group one. Splitting this into `comms.greetingFallback` and `comms.candidateFallback` would unblock cs/de/fr. |
| `comms.theRole` | "the role" / "a role" are English-shaped fallbacks that only work because the English templates put the placeholder in a bare slot. Any locale that needs a preposition + case around the job title (cs "na pozici {role}", fr "pour le poste de {role}", de "an {role}") either double-prints the noun or lands in the wrong case when the fallback fires. The robust source fix is to branch on the missing title (an ICU `select` or a separate no-role template variant) instead of substituting a noun phrase. |
| `decisions.aiReview.deadlineLabel + decisions.aiReview.deadlineDays` | "Respond within" and "days" are two keys straddling a number input. The unit cannot agree with the number in cs (dní/dny/den) and the phrase order is fixed to English; a single ICU message with the day count as an argument would be translatable. |
| `decisions.compliance.aiCheckPlaceholder` | A textarea placeholder that doubles as a format spec: the header row ("Group, selected, total") is also the literal column contract the parser expects, but the localized values ("Skupina, vybráno, celkem") suggest a translated CSV header is accepted. decisions.compliance.parseIgnored repeats the same triple. Clarify whether the parser is positional-only. |
| `decisions.compliance.statusOk / statusNa` | "ok" and "n/a" are English-shaped abbreviations used as table cell values; "n/a" has no established Czech equivalent, which is why cs simply keeps it. A spelled-out short status would localize. |
| `decisions.groupEval.dimLeader` | The source uses "lead" for two different concepts in one namespace: decisions.groupEval.lead = the overall recommended top candidate (a chip), decisions.groupEval.dimLeader = the best candidate on a single dimension (a cell marker). Translators must guess which; cs correctly split them (favorit / nejlepší) but only by inference. Rename the second to "best on this dimension". |
| `decisions.groupEval.eligibilityList` | "Eligibility list (ordinal, by fit)" leaks internal jargon: "ordinal" is opaque to a recruiter and adds nothing beyond "by fit", but every locale is obliged to carry it. |
| `decisions.summary.field` | "The field" is a sports metaphor for the candidate pool, and *field* is a homonym of *form field* in en, cs (pole) and de (Feld) alike. It has already produced one shipped defect (cs "Pole kandidátů"). A concrete source label such as "Candidates on this role · {count}" would translate cleanly everywhere. |
| `decisions.summary.languagesLabel` | The colon is baked into the value ("Languages:", also educationLabel, fairnessLabel). French needs U+202F before it, so the punctuation must be re-typeset per locale inside the string rather than being emitted by the component. |
| `devcase.checks.baselineSimilarity` | "the one-shot AI baseline" uses a different phrase than the sibling devcase.processTrace.singlePass ("single pass") for what is the same idea, which is what pulled de and fr into two renderings each. |
| `devcase.checks.briefPasteAimInterview` | "~{pct}" bakes the approximation sign and the percent formatting into the source string. cs needs a space before % when it is a noun and "~" is not a Czech/German device; the approximation should be part of the localizable prose ("roughly"), not a glyph. |
| `devcase.checks.canary.unverifiable` | Key name says "unverifiable" but the value says "not gradable", and devcase.integrity.chainUnverifiable uses "unverifiable" for a different concept. The mismatch invites terminology drift across locales. |
| `devcase.emptyLedger.cta` | "Open the first case" is ambiguous between open-an-existing and create-a-new. The handler is `onDefine` and the state is 0 cases, so the intent is create/start. The ambiguity is what split the locales (cs/fr "open", de "anlegen"). "Define the first case" would match the button's own verb and the sibling honestDarkness line ("Define the need and the engine designs the assignment"). |
| `devcase.lifecycle.closeTitle / devcase.lifecycle.closeConfirm` | "non-promoted submitters" is a coined agent noun with no HR equivalent in cs/de/fr; each locale had to paraphrase to "candidates" (cs/fr) or the stilted "Einreichende" (de). "candidates who were not promoted" in the source would translate cleanly. |
| `devcase.lifecycle.intro` | One 35-word sentence carrying a participial list, a parenthetical, and an arrow chain (evaluating → ranking → promoting). It survives as a colon list but is at the edge of what a UI intro should hold; splitting the arrow chain into its own sentence would help every locale. |
| `devcase.outbox.relayHint` | The rich-tag contents <sent>sent</sent> / <failed>failed</failed> are the literal English enum words in en and de but translated in cs. Either they are DNT status codes (then cs is wrong) or they are copy (then de is wrong) — the source does not say which, and the string also mixes a code identifier, a shell env var and prose in one 40-word sentence. |
| `devcase.review.flagged` | "Why this was flagged" has a bare "this" with no head noun. cs and de rendered it literally ("Proč bylo označeno", "Warum dies markiert wurde"), which reads as a dangling demonstrative (constructions-cs CS-DEM); fr supplied the noun ("Pourquoi ce cas a été signalé"). Naming the referent in en ("Why this case was flagged") would remove the ambiguity for all three. |
| `diagrams.status.gap` | "To build" is a bare verb phrase used as a status chip. Every target language has to invent a nominal or gerund form (cs "K vytvoření", de "Zu bauen", fr "À construire"), and the results are uneven beside the nominal siblings "Automated" / "Human gate". A nominal source ("Planned", "Not built yet") would localize cleanly. |
| `enums.provenance.professional` | en value is "prod" for the slug `professional`. In a provenance badge row (observed / prod / intern / self / OSS / cert / academic) "prod" reads as "production", not "professional" — almost certainly a typo for "pro". Every locale had to guess: cs "praxe", de "beruflich", fr "pro". |
| `enums.stage.Accepted` | The English label for the pipeline's front-door stage is "Accepted", which in a strip ending in "Hired" reads as "offer accepted" rather than "application received" (the code comment in app/api/sim/inbound/route.ts calls it "the new pipeline front for inbound apps"). The ambiguity is what forces the cs Přijato/Přijat/a collision; renaming the source stage label to "Received" or "Intake" would dissolve it in all four locales. |
| `errors.INTAKE_ATTACHMENT_FAILED` | en says "Could not update the attachments" but all three locales say "save". Likely the source word drifted from the actual behavior (the call site saves); worth confirming which is true before a locale is 'corrected'. |
| `errors.SESSION_TOKEN_REQUIRED` | "This work session belongs to a different apply link" is internal jargon on a candidate-facing surface — the candidate has no model of a "work session". Every locale had to guess at a rendering. |
| `feedback.done` | Bare "Done" is ambiguous between a status readout and a dismiss button. It is a button here (app/features/shell/nav/FeedbackDialog.tsx:110) but the same leaf reads as a status elsewhere (InterviewAttachToCandidate.tsx:67 renders a t("done") as a <p>). A translator with no call site will guess wrong; "Close" would be unambiguous for the button. |
| `hiringPlan.gateAria` | "Approval gating" as an aria-label for a two-option group (Human approves / Auto) does not say it is a mode selector; every locale has drifted to naming one option or a metaphor (cs "Schvalovací brána", de "Freigabe-Schranke", fr named only the human option). "Approval mode" would be translatable. |
| `history.colJd` | The column header is the bare abbreviation "JD", which is opaque outside English-speaking recruiting and gives translators no head noun to work from. "Job description" as the source value would let each locale pick its own short form; as written, de had simply been left as "JD". |
| `integrations.ats.tokenPlaceholderKeep` | **RESOLVED 2026-08-12** — double space fixed in all four locales. Contains a double space before the parenthesis ("••••••••  (leave blank to keep)"). It is faithfully mirrored in cs/de/fr, so the typo is now in four catalogs. |
| `integrations.calendar.connectedAtLabel` | The bare label "Connected" is reused for two different things in one panel: here it labels a TIMESTAMP field (fr correctly had to write "Connecté le"), while integrations.personas.statusConnected uses "Connected" as a STATUS chip. Locales that need a case or a preposition cannot tell them apart from the string alone. Consider "Connected on" for the timestamp label. |
| `integrations.saved` | "Webhook settings saved." sits in the integrations namespace whose visible panels are Google Calendar, ATS connections and the Personas bridge — none of which is a webhook. Either the key is dead (no webhook form under app/features/settings/integrations) or the message names the wrong object; worth a `grep -rn '"saved"' app` before any locale spends effort on it. |
| `interview.voice.keysNotConfigured` | "{provider} keys not configured" places a brand placeholder in an English attributive-noun slot. German has to hyphenate it ("{provider}-Schlüssel") and cs/fr must invert to "Klíče {provider}" / "Clés {provider}"; a phrasing like "Keys for {provider} are not configured" would be locale-safe. |
| `interviewSim.modes.student-case.blurb` | "the mechanism, counterfactual and hint work a shared designed case" uses "work" as a transitive verb with no object preposition. All three locales had to invent a relation (cs "pracují se", de "arbeiten an", fr "s’appuient sur"). Rephrase, e.g. "are all drawn from one shared designed case". |
| `invite.unavailableBody` | "Ask an admin to send a new one." — the pronoun "one" has no clear antecedent in the sentence (the subject is "This link", but the thing being resent is the invitation). All three locales had to disambiguate independently (cs "o nový", de/fr "a new link"). Naming the noun in en would remove the guesswork. |
| `jobMarket.families.frontline_service` | "Frontline Service" is Title Case (inconsistent with sentence-case UI elsewhere) and semantically ambiguous between customer-facing service staff and field/on-site service. It is the one family label in the 16-family catalog that translators misread — fr had landed on field service. |
| `jobMarket.hero.updated` | Two unrelated statistics are welded into one string with a "·" ("{n} live vacancies as of {date} · {pct}% posted in the last 90 days"). Translators cannot re-order or re-scope the halves, and the `one` branch produces the odd "1 live vacancy … · 40% posted in the last 90 days". Two keys would localize better. |
| `jobs.campaign.intro` | Uses `·` as an inline list separator inside a parenthetical (hook · offer · proof · CTA) while the surrounding prose uses commas. Harmless but it means every locale must preserve a decorative glyph as content; a plain comma list would translate cleanly. |
| `jobs.campaign.scriptTitle / beat*` | Unit style is inconsistent inside one cluster: the title writes "15s" closed up while the beat labels write "(0–1.5s)", "(1.5–6s)". Neither uses a space before the unit, so no locale can copy the pattern and each invents its own. |
| `jobs.candidates.addingShort` | The value is the single character "…" in all four locales — a UI spinner state expressed as a message key. It is untranslatable by construction and costs four catalog rows plus a parity obligation; it belongs in the component as a non-localized token, not in messages/. |
| `jobs.compare.confidence.wide` | "wide confidence" (and "tight confidence") is elliptical — what is wide is the confidence *interval*, not the confidence. English gets away with the ellipsis because the label sits under a numeric range; German and French do not, which is exactly why both locales produced a calqued collocation ("breite Konfidenz", "confiance large"). "wide confidence band" or a level scale ("low / moderate / high confidence") would translate cleanly into all three targets. Caps quality for de and fr regardless of the translation. |
| `jobs.drafts.* vs jobs.posting.withdrewCount` | English capitalizes the funnel inconsistently: "Sourced # candidate into the Pipeline" / "Source into Pipeline" (capital P) against "Withdrew # candidate from the pipeline" (lowercase) in the same feature. Per glossary the capitalized form names the product area and the lowercase one the technical funnel, but these two are the same object. The inconsistency has propagated verbatim into cs ("do Pipeline" vs "z pipeline"). Pick one casing in en. |
| `jobs.ingest.bulkHint` | 'separate each ad with a line of ---' embeds a literal delimiter that is also the string's own punctuation; jobs.ingest.bulkPlaceholder spells the same thing out differently ('a line of dashes (---)'). One phrasing should be canonical. |
| `jobs.ingest.minChars` | `~{min} characters` hardcodes the approximation tilde as prose. It is not a locale-neutral marker (cs/de/fr would normally write "přibližně"/"etwa"/"environ"), and it reads as a typo in a UI hint. |
| `jobs.posting.tabAgentFit` | "Agent fit" is uninterpretable without the component: it can read as fit-for-an-AI-agent, fit-of-an-agency, or an agent's fit score. Every locale had to guess (cs 'Vhodnost agenta', de 'Agenten-Eignung', fr 'Adéquation agent'). Needs a disambiguating source label or a translator comment. |
| `jobs.rediscover.whyNow.rejected` | `{score}%` hardcodes the percent sign in the string rather than formatting the number. This forces every locale to hand-place spacing (cs wants `{score} %` for the noun reading, fr wants a narrow no-break space, de wants none) and it will be wrong the moment the value is ever formatted. Same defect in whyNow.closed and whyNow.elsewhere. |
| `jobs.shared.reqPrereq` | "prereq" is an English clipping with no equivalent clipping in cs/de/fr, so every locale must spell the word out into a chip sized for 6 characters. Either widen the chip or ship a full word in en too. |
| `jobs.tab.launchpad.route2Title` | Route 2 has a Title and a Cta but no Body, while Route 1 has all three. The asymmetry forces the Cta to carry the explanation ('Use the import panel above'), which reads as instructions rather than a button in every locale. |
| `jobs.tab.launchpad.unlockChannels / unlockPipeline / unlockDecisions` | The three chips are subject+verb clauses ("Channels open", "Candidates flow", "Decisions queue") whose verbs are all readable as nouns. "Decisions queue" in particular is ambiguous out of context and every locale nominalized at least one of the three. A parallel, unambiguous set (e.g. "Channels start listening" / "Candidates arrive" / "Decisions pile up") would translate consistently. |
| `landing.pricing.enterprise.source` | Hardcodes a UI navigation path inside prose ("Analytics → ROI"). The path duplicates shell/analytics labels that are themselves localized, so the reference drifts silently whenever those labels change. It happens to be in sync now (cs "Analytika → Návratnost" matches analytics.roi.title "Návratnost:…"), but nothing enforces it. |
| `landing.voice.cardTitle vs landing.previews.voice.screenTitle` | The identical English string "First-round screen" is duplicated across two namespaces. Duplicated source with no shared key is exactly what let the German and French renderings drift apart; the same applies to "live" in landing.voice.cardMeta vs landing.previews.voice.meta and "human gate" in landing.trust.art.human.stations.gate vs landing.previews.gates.humanGate. |
| `legal.privacy.meta.description` | "candidate and workspace data" pairs a person-noun with an internal product noun. All three locales independently rendered "workspace" as "customer" (zákazníků / Kunden- / clients), which is what the body text actually says (legal.privacy.roles.account: "the customer's own workspace account and billing data"). The source would be clearer as "candidate and customer data". |
| `legal.privacy.subprocessors.trustLink` | Refers to "the trust page", but no page or section in the product is named Trust in any locale — landing.nav.trust is "Responsible AI". The reference is unresolvable for a translator and for a reader, and all four locales calqued it. |
| `library.builder.codebaseLabel` | Field label carries three qualifiers inside one parenthesis (public GitHub URL / optional / for dev roles). I compressed it to "(optional public GitHub URL for dev roles)"; a real fix is a separate hint line under the field. |
| `library.builder.optDescriptionHint` | Written as an imperative clause ("Draft the role's responsibilities and requirements from your need") while its two siblings optMarketHint and optCaseHint are noun phrases ("A web-grounded salary band…", "A work-sample exercise…"). The checklist reads as three parallel options, so the hint grammar should be parallel too; the locales have already split (cs/de third-person, fr infinitive). |
| `library.form.saveJd` | Near-duplicate of library.form.saveAjd ("Save a JD" vs "Save JD") with no hint of which is the heading and which the button. Translators cannot pick a length budget or an aspect (cs perfective CTA vs nominal heading) without reading the component. Rename or add a description. |
| `library.result.openPipeline` | The button says "Open Pipeline" but the destination's own nav label is nav.tabs.pipeline = "Overview". The source points the user at a tab name that no longer exists in the UI, and every locale inherits the mismatch. |
| `library.result.savingDraft` | library.builder.savingDraft is "Saving…" while library.result.savingDraft is "Saving draft…" for what is presented as the same action (both paired with "Save as draft"). One of the two should be aligned in en so the locales are not forced to encode a distinction that is not real. |
| `library.tab.analysisStale` | "JD edited {date}" gives translators no way to know whether {date} arrives as an absolute date or a relative phrase. fr commits to "Fiche modifiée le {date}", which breaks if the value is ever relative ("il y a 2 jours"); cs/de dodge it by omitting the preposition. Same ambiguity in library.tab.savedAt ("Saved {date}"). |
| `library.tab.candidatesEmpty` | "Generated roles also source candidates into the Pipeline." — "to source X into Y" is not standard English; "source" means to find candidates, not to move them somewhere. Every locale had to guess the intent (feed / bring into the pipeline). Suggest "Generated roles also feed candidates into the Pipeline." |
| `library.tab.intake.attachments.noteTitlePlaceholder` | The English source uses ASCII straight quotes: "Title (e.g. 'Note from the team lead')". Every target locale then has to invent its own quote convention. The source should use typographic quotes (“…”) so the localized forms are a straight glyph swap. |
| `library.tab.intake.columns.badge.turns` | Counts the same conversation turns as the sibling library.tab.intake.turns but words it differently: "# messages" here, "# turn" there. One concept, two source nouns, which forces each locale to guess whether the split is meaningful. |
| `library.tab.intake.draft.workingNote` | The note hardcodes a reference to another button's label ("generated at Create JD"), so every locale must keep two strings manually in sync and the reference silently rots if library.tab.intake.promote is reworded. Same coupling exists in the sibling draft.workingNoteNoMarket. A placeholder carrying the button label would remove the trap. |
| `library.tab.intake.promoted` | "find it under Saved JDs" names a UI label ("Saved JDs") that has no corresponding catalog key, so no locale can reference the real rendered label; cs/de/fr all paraphrase ("mezi uloženými"). Either add the label key or drop the cross-reference. |
| `library.tab.intake.shape.powerUnit` | The en label "Backfill" is narrower than what the code actually classifies. pipeline/jobfit/intake.py:256 triggers power_unit on "backfill\|replacement\|same as\|another one\|one more\|clone\|the old jd\|existing jd" — i.e. also net-new headcount that simply repeats a known shape. Every locale is forced to pick either the narrow reading (de Nachbesetzung, fr Remplacement) or the broad one (cs originally "Posila týmu"), and they will keep diverging. A source label like "Known shape" / "Repeat hire" would translate consistently. |
| `library.tab.intake.side.brief` | library.tab.intake.side.{brief,draft,materials} appear to be dead keys: no `useTranslations("library.tab.intake.side")` exists in app/, and JdsIntakeLayoutTriptych.tsx reads library.tab.intake.columns.* instead. Three keys × 4 locales are being maintained for nothing (contract.md §9: report a dead key rather than translating it). |
| `library.tab.intake.side.brief / library.tab.intake.side.draft / library.tab.intake.side.materials` | Dead keys. No call site anywhere in app/ (`grep -rn '"side' app` and a scan of app/features/library/jds/intake/*.tsx return nothing); the triptych layout renders col.brief / col.materials instead (JdsIntakeLayoutTriptych.tsx:152). Per contract.md §9 ("Dead keys … report a dead key rather than translating it") these are reported, not fixed — which is why the de "Anzeigenentwurf" / cs "Návrh inzerátu" JD-term drift in side.draft was deliberately left alone. Delete them or wire them up. |
| `library.tab.jobStatusChip` | "role: {status}" uses "role" where the rest of the namespace has migrated to job/position vocabulary (library.tab.colRole is also "Role" but renders as Position/pozice/poste in every target). Harmless but it forces each locale to silently re-map; naming it "job: {status}" in en would remove the mismatch. |
| `match.shared.allKnockedOut` | The one branch reads "All # role was knocked out" — "All 1 role" is not English; the branch needs its own wording ("The only role was knocked out"). |
| `matrix.emptyGrid` | 'Positions are the jobs that appear in the pipeline.' defines a term with a near-synonym. Every locale that follows the glossary's single word for role/position/job (cs pozice, fr poste) renders it as a tautology. Rephrase, e.g. 'Positions are the roles you are hiring for; they appear on the pipeline board.' |
| `models.benchOps.interview_prep` | "Interview prep pack" and models.benchOps.campaign_pack "Campaign pack" use "pack" as a bench-op noun while billing.pack.* uses "pack" for a purchasable credit bundle. Two unrelated senses of the same English word in one product; cs and de both silently dropped "pack" in interview_prep rather than pick a sense. |
| `models.quality.colWins` | "Best in" is a column header ending in a dangling preposition above cells that render a bare ratio ("5/12"). Languages with case government (cs) cannot render a governing preposition with no governed noun. A noun header ("Wins", "Top ranks") would translate cleanly everywhere. |
| `models.quality.na` | The source value is a lone em dash used as a data glyph meaning "no measurement". A punctuation character carrying semantics cannot survive the §5 rule and cannot be translated; this should be a real localizable abbreviation (as now written) or, better, rendered by the component rather than living in the catalog. |
| `models.useCaseDesc.agent_fit` | The English source used U+2013 as a word-joining hyphen in "agent–candidate bridge". Normalised to a plain hyphen; an en dash between words is neither a range nor allowed punctuation under §5. |
| `onboarding.progress` | "{done}/{total} done" is flat; fr must pick "terminé" or "terminés" for a ratio where the numerator can be 1 or 0. A count-invariant source wording (e.g. "{done} of {total}") would translate cleanly everywhere. |
| `onboarding.task.compliance-training` | "HIPAA / patient-safety compliance training" hardcodes a US regulator in a template shipped to cs/de/fr markets, forcing each locale to invent a substitute (all three generalised to data protection). Same issue with onboarding.task.safety-orientation ("OSHA / BOZP", where BOZP is already Czech) and onboarding.task.work-auth ("I-9"). |
| `pipeline.actions.scorecardNote / pipeline.actions.offerNote` | Both encode the sentence's main relation as a bare "→" ("From your notes → a structured scorecard"). Verbless arrow syntax forces every locale to invent a connective, and cs/de/fr each solved it differently. |
| `pipeline.drawer.notConfigured` | `{vars}` is a pre-joined English list of env-var names built in code. A translator cannot control the conjunction or the separator, and cs had to invert the whole sentence ('Není nastaveno: {vars}') to keep it grammatical. |
| `pipeline.scheduler.lastAutoRun / neverRun / ranNoChanges / ranWith` | The "· " separator is baked into the translated value rather than rendered by the component, so every locale carries a layout decision it cannot change and the string cannot be reused outside that one row. |
| `pipeline.scheduler.runScope` | "{mine} of {total} yours" is a telegraphic ellipsis with no verb. English gets away with the trailing "yours"; cs/de/fr all need either a verb (which then has to agree with {mine}) or a recast. It also reads ambiguously in English itself (is it "3 of your 12" or "3 of the 12 are yours"?). A framed source like "Yours: {mine} of {total}" would localize cleanly everywhere. |
| `pipeline.tab.batchScreenAll` | Dead key. grep over the repo finds it only in the four messages/*.json catalogs (plus a casesim fixture); no component references it. Same for pipeline.tab.batchScreening, pipeline.tab.batchTitle and pipeline.tab.passSummaryLead. contract.md §9 says report dead keys rather than spending translation effort on them — these four are carrying four locales each for nothing. |
| `pipeline.tab.batchScreenAll / batchScreening / batchTitle` | DEAD KEYS. `grep -rn 'batchScreen' app` returns nothing — no call site in the repo. They are translated in all four locales (the fr value is a 60-char button string). Per contract.md §9 these should be reported rather than maintained; I left all three untouched. |
| `pipeline.tab.previewNote` | en joins two independent clauses with a semicolon. FR-SEMICOLON (Microsoft, house authority) bans semicolons outright, so the French can never mirror the source's rhythm. Two sentences in en would let all four locales agree. Same in pipeline.command.doneHeldAtOffer (I split that one because the dash rewrite reached it). |
| `pipeline.tab.slaEditorNote` | "Blank = default." uses an equals sign as prose. It survives translation only by accident; a real sentence ("Leave blank to use the default.") localizes cleanly. |
| `pipeline.today.* (inbound, scorecards, offerReviews, awaitingSlot, offersOut, hired)` | These use the ICU exact-value branch `=1` rather than the `one` category. It compiles everywhere, but it silently bypasses CLDR selection, so a locale whose `one` category covers more than the literal 1 (e.g. fr, where 0 is `one`) cannot express it without restructuring the source-shaped message. |
| `profile.archetypes.fairnessProtectedAria` | "Fairness-protected" is a compressed English compound with no verb. As an aria-label it must stand alone, and cs/fr both had to invent a relation ("protected BY fairness") that inverts the meaning. Spelling it out ("Protected attribute, excluded from scoring" or similar) would fix all three locales at once. |
| `profile.archetypes.idLabel` | "Id (immutable)" — the source itself is inconsistently cased against the rest of the namespace, which says "Id must be lowercase…" in `validation.id_invalid` while the API and DB call it `id`. "ID" is the conventional English form; the odd casing propagated into fr. |
| `profile.evidence.evidenceTitle` | "Evidence" is the head term of a whole UI cluster and is a false friend in Czech (cs *evidence* = record-keeping/registry, not proof). Not a defect in English, but worth noting in the source glossary so the term is never machine-carried; it is the root cause of the cs split logged in nativeReview. |
| `profile.matrix.openProfileTitle` | English possessive on a placeholder ("Edit {name}'s profile"). Czech and German need a case-inflected name, which is impossible; every locale has to restructure into a colon or a de-genitive form. Same defect in profile.matrix.openDetailTitle and profile.roster.staleTitle ("this candidate's CV"). |
| `profile.tab.projectionLabel` | "Candidate view" is ambiguous between *view of candidates* (what the List/Matrix toggle actually switches) and *the candidate's own view*. All three locales guessed the former; a clearer source ("Candidate list view" or "View candidates as") would remove the guess. |
| `report.compare.narrativeDriver` | The select branch values are inflected English verbs ('loses'/'wins') that must agree with the {component} subject. Two of three locales shipped them untranslated, which is the predictable failure mode. A `dir` select whose branches are full clauses (or a separate key per direction) would be safer. |
| `report.importToPrepNoPrep` | One sentence split across a message and a JSX <Link> (importToPrepGoTo, 'generate one in Schedule'). The dash was carrying the join. Word order in de/fr wants the link elsewhere; a rich-tag message (<link>...</link>) would let the translator move it. |
| `report.panel.extractionQuality` | Title Case is applied inconsistently across sibling headings in the same namespace: 'Extraction Quality', 'Score Breakdown', 'Evidence Trace', 'Keyword Coverage', 'Salary Estimate' vs sentence-case 'Grounding sources', 'Market notes', 'Parser details', 'Education level'. Translators into sentence-case languages must decide per string whether the casing carries meaning. |
| `report.panel.filter` | Bare "Filter" is noun/verb ambiguous with nothing in the key to disambiguate, and the call site is a passive label, not a control. The three locales already split on it (cs/de noun, fr infinitive). "Filter by" or a key comment would settle it once. |
| `report.panel.filterAll` | The value is a bare "All" but the call site composes it as `${t("panel.filterAll")} (${questions.length})` (InterviewTab.tsx:138). The string carries no hint of its antecedent (interview questions), which is exactly what makes the French gender call unresolvable from the catalog alone. A key comment or a fuller source string ("All questions") would settle it. |
| `report.panel.questionNumber` | "Q{n}" is an English-only abbreviation with no equally short equivalent in cs/de/fr; every locale must expand to a full word ("Otázka 1", "Frage 1", "Question 1"), roughly tripling the width of a label that sits inline next to a bucket chip in a card header. Worth a width check, or an abbreviation the locales can also abbreviate. |
| `results.github.languageMix` | The results.github.* headings are Title Case ("Language Mix", "Top Repositories", "Contribution Signals", "Job Skill Matches", "Potential Gaps", "Complexity Assessment", "Evidenced Skills", "Unverified Claims", "Hidden Strengths") while the rest of the catalog's headings are sentence case. Inconsistent source casing invites Title-Case calques in every locale (a locale-convention error under all three style guides). |
| `results.quality.trustTitle` | "AI-generated / embellished résumés" uses a slash as a coordinator inside a noun phrase. It has no clean equivalent in cs (where the two adjectives take different positions) and forces an awkward literal; "or" would translate cleanly. |
| `rubric.competency.craft_portfolio_depth.description` | The competency has a `.label` but no `.description`, unlike every other rubric.competency.* entry in this batch (technical_depth, problem_solving, communication, experience_fit, motivation, operational_execution, service_orientation_reliability, scientific_rigor, safety_hands_on_competence, clinical_judgment_patient_safety all carry one). Likely a missing source string rather than an intentional omission. |
| `scheduleTab.lifecycle.attendanceConfirmed` | en "attending" is a bare participle used as a status chip while the sibling closed-state chips are past participles ("cancelled", "declined", "no-show"). The tense/aspect mismatch is what pushed cs toward a full clause ("Účast potvrzena") and de/fr toward a finite verb vs. a noun phrase. "confirmed" or "RSVP: yes" would be a more translatable chip. |
| `scheduleTab.lifecycle.queuedAgo` | "queued in Outbox {time}" names the Outbox as a proper-noun UI area, but nothing in the catalog pins whether Outbox is translatable. cs kept "Outbox", de rendered "Postausgang", fr rendered "boîte d’envoi" — three different calls for one product surface. Needs a glossary row. |
| `scheduleTab.prep.copyHeading` | Two adjacent placeholders "{name}{job}" with the separator baked into the {job} value upstream. The target locale cannot re-punctuate or reorder the pair, and cannot know whether {job} arrives empty. |
| `scheduleTab.relay.label` | "Where the baton is" is a relay-race metaphor. It survives in de (Staffelstab) and fr (témoin) but has no compact Czech equivalent (the baton is `štafetový kolík`, too long for the META_LABEL slot), so cs has to weaken it. Fine as a deliberate voice choice; noted so the metaphor is not extended to more keys without checking cs. |
| `scheduleTab.transcript.readbackHeading` | "read back" / "read-back" is aviation-and-clinical jargon with no established equivalent in cs, de or fr. It forces each locale to coin a term (cs zopakování, de Zurücklesen, fr relecture), and two of those coinages are calques. A plainer source ("Technologies confirmed at the close" / "Closing recap") would translate cleanly everywhere and remove three separate term decisions. |
| `scheduleTab.transcript.telemetryDuration` | "Spoken duration" is ambiguous: it can mean total time spent speaking or the span of the spoken session. The field it renders (app/_lib/interview-telemetry.ts:107, `durationSec`) is max(timestamp) − min(timestamp), i.e. the session span, not accumulated speech. The three locales have already split on the reading (cs "Délka hovoru" = call length; de "Sprechdauer" and fr "Durée de parole" = speaking time). Renaming the source to "Session length" (or "Interview length") would settle all three. |
| `scheduleTab.transcript.telemetryTalkShareValue` | Not a defect in the value itself, but the source "{pct}%" is unspaced and every locale correctly spaces it, which is why the catalog is now split on WHICH space. Worth pinning the intended glyph (U+00A0 for cs/de, U+202F for fr) in contract.md §2 so it stops being decided per string. |
| `simulation.jd.period` | "mo" is an unglossed abbreviation with no context in the value; a translator cannot tell whether the call site has room for an abbreviation or a full word, and there is no sibling ("yr"/"hr") to calibrate against. |
| `simulation.log.noScore` | A punctuation character used as a data value. The absence of a score should be a state the caller renders, not a translatable string whose only content is a dash. |
| `simulation.step.match.caption` | One ~190-character sentence carrying a colon list, a parenthetical status name, and a semicolon-joined second clause. Every locale had to break it into two sentences to stay readable; splitting it at source would make the three translations converge instead of each inventing its own break. |
| `skillProfile.staleAge` | "issued {issued}" leaves it ambiguous whether {issued} is an absolute date or a relative phrase ("3 months ago"). Translators must guess a preposition: de committed to "ausgestellt am {issued}" and fr to "émis le {issued}", both of which break if the value is relative. Either rename the placeholder or fix the format at the call site. |
| `skillProfile.staleMethodology` | Same ambiguity as staleAge — "(issued {issued})" forced de/fr into "am"/"le", which only works for an absolute date. |
| `skillProfile.tampered` | A status badge asked to carry two clauses ("Signature invalid — do not trust"). Every locale now needs a comma-joined two-clause badge; German runs to 40 characters. The warning half probably belongs in the body text below the badge, not in the badge label. |
| `status.now.hired` | Candidate-facing, flat, and gender-bearing: "You're hired". Czech has to ship "Jste přijat/a" and French "Vous êtes recruté(e)" because the source carries no gender signal. Not fixable in the catalog, but it is why two locales show slash/paren forms on the warmest string in the app. |
| `tasks.kind.jdBuildUntitled` | en reads "Build JD · role" where every sibling untitled-variant reads "· untitled" (tasks.kind.needAnalysisUntitled, tasks.kind.designArtifactsUntitled). All three locales independently rendered it as "untitled/ohne Titel/sans titre", i.e. they all silently corrected the source. Either the en is a typo or the key is misnamed. |
| `tasks.system.avgAnalysis` | "Avg analysis" omits the measured unit (duration). Every locale inherits the ambiguity — cs "Průměrná analýza" reads as "the average analysis", not "average analysis time". |
| `workspaceAdmin.lockedNote` | Same class as scopeNote, and additionally leaks the term "tenant" (untranslatable domain jargon; cs had to borrow "tenanta") into an end-user string. |
| `workspaceAdmin.members.removed` | "Removed {name}" is a bare participle plus a person placeholder. Every gendered locale has to guess agreement (fr fell back to "retiré(e)"); a full sentence or an explicitly neuter frame would remove the guess. |
| `workspaceAdmin.permissions.footnote` | Introduces a role name — "a Writer" — that exists nowhere else in the UI. The role vocabulary the user just saw is Owner / Admin / Recruiter / Hiring manager / Viewer (workspaceAdmin.members.role.*). "Writer" is an internal capability-tier name leaking into user copy; every locale has had to carry it verbatim (cs "rolí Writer", de "zum Writer", fr "un Writer"), which reads as untranslated English in all three. Same leak in workspaceAdmin.permissions.caps.manageMembers.desc ("a “Writer”"). Either name it with a real role or drop the parenthetical. |
| `workspaceAdmin.scopeNote` | Ships an internal implementation-status caveat ("only Analyses is workspace-isolated so far") as translated product copy in four locales. It will be wrong the moment per-table scoping lands, and the cost of the drift is multiplied by four. |

## Missing ICU plural (a flat string that needs one) (84)

| key | problem |
|-----|---------|
| `analytics.activeSub` | "{count} active" and its siblings rejectedSub / declinedSub are flat counts; Czech had to add plural blocks the source does not signal, and there is no way to know from the source whether the participle refers to the candidate (gendered) or the record (neuter). |
| `analytics.activeSub / analytics.rejectedSub / analytics.declinedSub` | Flat count strings ("{count} active") with no ICU plural. English itself renders "1 active" fine because the adjective is invariant, but every inflecting locale has to invent the plural structure. cs and (now) fr expand it; making these ICU plurals in en.json would make the requirement explicit rather than discovered per locale. |
| `analytics.automation.split` | "{auto} actions by automation · {human} by humans" carries two live counts with no plural branches; at 1 it renders "1 actions by automation · 1 by humans". de and fr mirror the break. |
| `analytics.bottleneck` | Both counts are flat: "the <m>{count}</m> active candidates … have waited <m>{days} days</m>". No ICU plural, so de and fr render "1 candidats actifs" / "1 Tage". cs already carries locally-added plurals; de/fr were left matching the source per their style guides ("keep en's plural branches"). |
| `analytics.calibration.samples` | "{n} decided candidates" is flat; the same flat shape recurs in driftBrierTitle, driftCohortN and uncalibratedBody. Every locale with count agreement breaks at n=1. |
| `analytics.compute.basis` | "From the usage ledger · {calls} metered calls." is a flat string with a live count and no ICU plural, so it renders "1 metered calls". Every locale inherits the defect; cs was fixed here by expanding branches, de ("{calls} gemessene Aufrufe") and fr ("{calls} appels mesurés") still break at 1. |
| `analytics.compute.perHireHires` | "over {hired} hired" has no plural branch, so n=1 renders "over 1 hired". de "auf {hired} Einstellungen" and fr "sur {hired} recrutés" inherit the same break at 1. |
| `analytics.offers.notEnough` | "{n} of {min} needed" is a flat string; the verb does not agree at n=1, and Czech has to invert the sentence to dodge it. |
| `analytics.orgBenchmark.footnote` | "across {teams} teams" is a flat string that needs a plural ("1 teams"). |
| `analytics.orgBenchmark.locked` | {teams} is a bare count with no ICU plural, so English renders "1 contributing so far" and the German copies the bug. I made the English count-safe by moving the number to the end after a colon ("Contributing so far: {teams}."), but the real fix is a plural on the source. Czech already works around it with its own plural block. |
| `analyze.charsCount` | **RESOLVED 2026-08-12** — en now `{count, plural, one {# character} other {# characters}}`. "{count} chars" is a flat interpolated count with no ICU plural, and abbreviates the head noun. Every inflecting locale has to invent the plural block (cs already had one, fr did not and rendered "1 caractères"). Should ship as {count, plural, one {# character} other {# characters}}. |
| `analyze.stages.readingSub` | "the uploaded file(s)" uses the parenthetical-plural hack instead of an ICU plural. The count is known at that point; "(s)" is untranslatable into cs/de/fr and forces each locale to guess a number. |
| `channels.receivers.confirmLive` | A long shared tail sits outside the plural block and the plural block is only the first sentence, so translators cannot move count-agreeing words into the branches. Compounded by "The forwarding rule / ad flow" using a slash as an inline disjunction, which several languages cannot render as a slash. |
| `channels.waiting` | "{count} waiting in the pipeline" is a flat string with an interpolated count and no ICU plural, so every target language has to invent the plural structure itself (cs already does). Wrapping the count in a plural in en would make the intent explicit and the parity check meaningful. |
| `comms.scheduleInvite.length / comms.interviewConfirmation.length / comms.interviewReminder.length / comms.interviewInvite.length` | All four English values hardcode the plural noun ("about {minutes} minutes") instead of using an ICU plural, so {minutes}=1 renders "about 1 minutes" in en, and de/fr faithfully mirror the defect ("etwa 1 Minuten", "environ 1 minutes"). Only cs escapes it, because Czech case agreement forced the translator to expand the branches. The en values need {minutes, plural, one {# minute} other {# minutes}}; de and fr can then be expanded in the same change. |
| `decisions.batch.failed` | Flat string with a bare {count} in front of a verb phrase, no ICU plural. Czech needs one/few/other on the verb ("1 se nepodařilo rozhodnout" vs "3 se nepodařilo rozhodnout" vs the agreeing forms) and French needs singular/plural agreement on "sélectionné"; both are currently frozen to one form. Same shape as the sibling decisions.batch.accepted/rejected, which DO use a plural. |
| `decisions.batch.selectedCount / accepted / rejected` | Flat "{count} selected" with no ICU plural. English tolerates it; every target language that agrees a participle (fr) or wants a neuter verb order (cs) has to invent the plural structure, which means the four locales cannot be verified against one shape. These should carry an explicit plural in en. |
| `decisions.groupEval.advanced` | "Advanced" / "Rejected" are bare participles used as a per-candidate outcome pill, with no gender or number context in the string. Every gendered target (cs, de, fr) has to invent an unknown-gender form; fr shipped a plural. An impersonal source ("Decision: advance" / "Decision: reject") or a documented note that this labels ONE candidate would remove the guess. |
| `decisions.rules.ruleFamilyAppend` | Same concatenation chain: the value starts with " · " and carries a plural whose branches differ only in singular/plural of the appended noun. It cannot be re-ordered by a translator relative to ruleSentence/ruleLog, which is exactly what German verb-final and French adjective placement would want. |
| `decisions.wave.rejectedHeading` | The ICU plural has two identical branches: `{count, plural, one {Rejected (#)} other {Rejected (#)}}`. The plural carries no information in English, but every locale must still carry (and a reviewer must still check) dead branches — and it invites translators to expand cs to one/few/other for nothing. Should be a flat `Rejected ({count})`. |
| `devcase.checks.canaryUngradable` | Both ICU branches are identical in en ("one {# not gradable} other {# not gradable}"), so the plural block does no work in the source. Harmless, but it signals to translators that no agreement is needed — fr correctly expanded it (évaluable/évaluables), which is exactly the divergence a reviewer would otherwise flag as a format drift. |
| `devcase.outbox.count` | **RESOLVED 2026-08-12** — en/cs expanded to ICU plurals (de/fr already were). "{count} messages" likewise has no plural branches; renders "1 messages". cs escapes via the count-invariant "Zpráv: {count}", but de/fr are pinned to the defect. |
| `devcase.outbox.count / devcase.outbox.countFiltered / devcase.outbox.deadLetters / devcase.cohort.evaluated` | **PARTIALLY RESOLVED 2026-08-12** — `count` and `deadLetters` fixed (see their own rows). `countFiltered` ("{shown} of {total} messages") and `cohort.evaluated` left open: both need a product decision on whether `messages`/`evaluated` should agree with `shown`/`total` or with the live count, which is a judgment call past what this pass covers. Flat interpolated counts with a plural noun or verb and no ICU plural ("{count} messages", "{count} need attention"). English itself renders "1 messages" / "1 need attention"; every locale has to invent the plural. These should be ICU plurals in en.json. |
| `devcase.outbox.deadLetters` | **RESOLVED 2026-08-12** — en now an ICU plural. "{count} need attention" has no ICU plural, so it renders "1 need attention". Every locale inherits the break: de "{count} brauchen Aufmerksamkeit" is ungrammatical at 1 (cs and fr dodged it with count-invariant recasts). Needs {count, plural, one {…} other {…}} in en before the locales can be fixed cleanly. |
| `devcase.probeAudit.strong` | Flat count: "{loadBearing} of {total} probes are load-bearing" has no ICU plural, so Czech cannot agree the noun (1 sonda / 3 sondy / 5 sond) and has to be written around with a colon construction ("Nosných sond: X z Y"). Same defect in devcase.probeAudit.weak. |
| `interviewSim.regularDesc` | "under {min} minutes" hardcodes the unit word next to a bare number placeholder, so no locale can use ICU plural agreement on the unit. Fine for de/fr/en, but a Czech-style unit-agreeing language cannot render it correctly for all values. |
| `jobs.candidates.notEligible` | "{count} not eligible (KO-filtered)" duplicates jobs.candidates.notEligibleWhy ("Not eligible (#): see why") almost exactly, but only the latter is written as an ICU plural. The flat form is what let fr freeze "non éligibles" and break at count=1. Both should be plural-shaped in en so every locale is forced to expand branches. |
| `jobs.candidates.notEligibleWhy` | The English plural block has byte-identical `one` and `other` branches ("Not eligible (#): see why" twice), so the ICU plural buys nothing in en and reads as an oversight. It is load-bearing for cs (one/few/other differ: Nevhodný/Nevhodní), so do NOT delete it — but en should either differentiate the branches or the key should document why it is flat. |
| `jobs.coach.gateEducation` | Same shape, milder: `<b>{n}</b> {n, plural, one {candidate} other {candidates}}` puts the number outside the plural block and the noun inside. The fr value had drifted to `one {candidate} other {candidate}` — an untranslated, unpluralized English noun in both branches (fixed here). Wrapping the number inside the block would make the agreement bug impossible. |
| `jobs.coach.notAssessed` | The English splits a bare copula into its own plural block: `and {n, plural, one {is} other {are}} not counted above`. A copula-only branch is untranslatable structure — the German catalog had literally shipped the English words `{is}`/`{are}` inside it (fixed to wird/werden in this batch), and French had to invent a whole negated verb phrase. The sentence should be recast so the plural block carries a full predicate. |
| `jobs.posting.lifecycle.decisions` | Inconsistent plural skeletons inside one lifecycle cluster: lifecycle.channels and lifecycle.funnel carry an =0 branch, but decisions/schedule/offers/hired jump straight from =1 to other, so a zero renders as '0 decisions pending' instead of an empty-state phrase. |
| `landing.previews.rediscover.title` | "New role opened · {n} people you already know" interpolates a count into a flat noun phrase with no ICU plural. It happens to be safe today only because the call site passes HITS.length === 2 (app/landing/spark/previews/RediscoverPreview.tsx:22); any change to that array silently breaks Czech agreement (2 lidé → 5 lidí) and English (1 people). Should be an ICU plural in the source. |
| `legal.privacy.consent.retention` | "up to {months} months" is a flat interpolated count with no ICU plural. Every locale must guess the agreement; cs expanded it correctly, de did not ("1 Monate"). The en source should carry {months, plural, one {# month} other {# months}}. |
| `library.result.lintManyMustHaves` | Flat string with a bare {count} standing before a noun ("{count} “must-have” requirements"). Czech needs one/few/other agreement (1 požadavek / 3 požadavky / 5 požadavků) and German needs one/other; all four locales are currently frozen in one form. Should be an ICU plural in en. Not restructured in this pass per instructions. |
| `library.tab.intake.columns.materials.reveal` | en's `one` and `other` branches are byte-identical ("Materials: # attached"), so the plural block does no work in en. It is load-bearing for cs, so keep it, but the en source reads as an accidental duplicate. |
| `library.tab.intake.thinkingSlow` | Assistant speaks in the first person singular in cs/de/fr ("Stále přemýšlím", "Denke noch nach", "Je réfléchis"), which style-cs.md bans for progress states. en is person-neutral, giving translators no signal. Normalised to 1st person plural in all three; confirm this is the intended assistant voice for the intake chat. |
| `match.card.moreCount` | "+{count} more" is flat for the same reason; cs had to expand it into a plural unilaterally. |
| `match.results.selectedCount` | "{count} selected" is flat where the count is live; locales with agreement have to invent a plural or a count-invariant recast (fr expanded it, cs could not). Should be an ICU plural in en. |
| `match.shared.koReasonsNote` | The {reason} placeholder is a sentence fragment concatenated after "because they …", so every target locale is forced into English's subject-verb frame. de could not agree its verb without a second plural block; fr had a singular/plural agreement bug ("1 poste n'ont pas passé") that only surfaced because I had to touch the string. Concatenated-fragment source defect. |
| `matrix.addedPartial` | Two flat counts ({ok}, {failed}) with agreeing verbs outside any plural block ('{failed} failed', cs 'selhalo'). Ungrammatical in cs for 2-4 and 5+. Also carries a semicolon, which the French style guide bans outright. |
| `matrix.countLine` | '{cands} candidates × {positions} positions' has no ICU plural, so en itself renders '1 candidates × 1 positions'. Every locale inherits the bug; Czech breaks hardest (three categories), which is why cs was fixed here. de/fr were left alone rather than diverging from a defective source — fix en and all three can follow in one sweep. |
| `matrix.emptyAxisReadyCandidates / matrix.emptyAxisReadyRoles` | Both keys carry byte-identical en values ('{count, plural, one {# ready to plot.} other {# ready to plot.}}'). Keeping them separate is correct (cs/de/fr need different gender agreement), but the identical source invites a future dedupe that would break Czech. Worth distinct en wording or a comment. |
| `matrix.strongFitTitle` | The message splits '{total} shown' from the plural block '{total, plural, one {role} other {roles}}', leaving a bare noun alone in a branch with no article or verb. That shape is what let the German branches survive untranslated, and it forces every locale to either duplicate {total} or restructure the sentence. |
| `matrix.unassessedHidden` | Flat '{count} not scored' with no ICU plural. Czech needs one/few/other agreement on both the participle and any noun; the value is currently a frozen neuter. |
| `models.quality.method` | "{limit} run(s)/cell" is a flat string with a parenthesised plural. It needs an ICU plural on {limit}; the "(s)" form is also explicitly banned by the French guide (FR-PLURAL), so fr currently ships "exécution(s)" as the least-bad option. Same for "{limit} Lauf/Läufe" in de, which is a hand-rolled slash plural. |
| `models.usage.cache` | Flat count nouns with no ICU plural: "{rows} cached responses · {expired} expired awaiting cleanup" breaks at rows=1 / expired=1 in every locale. cs has already been expanded to one/few/other locally; de and fr are stuck echoing the flat English shape. The en source should carry {rows, plural, …} and {expired, plural, …} so all locales can branch from it. |
| `models.usage.fallbackCalls` | "{count} fallback" is a bare adjective fragment with no head noun and no plural, so every target locale has to guess the elided noun (calls) and invent its own agreement. cs guessed "volání", de left "Fallback", fr left "de repli" — three different shapes from one ambiguous source. |
| `nav.attentionBadge` | **RESOLVED 2026-08-12** — en now an ICU plural (`one {# needs attention} other {# need attention}`). "{count} need attention" is flat, with a plural verb and no ICU plural block. Ungrammatical in English itself at count=1 ("1 need attention"), and it forces every locale to invent the plural structure. Rendered as an aria-label, so a screen reader reads the defect aloud. Should be {count, plural, one {# needs attention} other {# need attention}}. |
| `nav.attentionBadgeGo` | **RESOLVED 2026-08-12** — en now an ICU plural, same shape as attentionBadge. The English is a flat string with an unagreed verb: "{count} need attention" renders "1 need attention". It should be an ICU plural (one {# needs attention} other {# need attention}). de is flat for the same reason ("{count} benötigen Aufmerksamkeit" is wrong at 1). I expanded cs to one/few/other as the Czech CLDR categories require, but did not restructure en/de plurals in this dash pass. Its sibling nav.attentionBadge (NavSectionRail.tsx:85) very likely has the same defect and should be checked in the same fix. |
| `offer.deadlineHours` | "{hours, plural, one {# hour} other {# hours}} left." puts the count-carrying plural first and the predicate outside it. Inflected languages need the verb inside the branches (cs already does: "zbývá # hodina" / "zbývají # hodiny"), so the English shape silently invites the agreement bug in any locale that copies it. Wrapping the whole clause in the plural in the source would remove the trap. |
| `onboarding.ledgerSignaturesCount` | "{count} requested" is flat. Fine in English (participle invariant) but it gives every gendered/number-agreeing locale no branch to hang agreement on; fr resorted to "demandé(s)". |
| `onboarding.preboardQuestions` | The English put the agreeing verb OUTSIDE the plural block: "{count, plural, one {# pre-boarding question} other {# ...questions}} go to the new hire — {list}", so the `one` branch rendered "1 pre-boarding question GO to the new hire". The dash recast forced me into this string, so I moved the verb inside the branches (goes/go) in all four locales. Flagging it because it is a source agreement bug, not a punctuation one, and the same shape likely recurs elsewhere. |
| `onboarding.templateMeta` | "{tasks} tasks · {questions} questions" is flat where both counts are live, so every locale has to invent the plural. de/fr were rendering "1 Aufgaben" / "1 tâches" until this batch; en itself still says "1 tasks · 1 questions". Should be ICU plural in the source. |
| `pipeline.board.active` | The en plural has identical one/other branches ("{count, plural, one {# active} other {# active}}"), which is a plural wrapper doing no work in English but forcing every locale to guess whether agreement was intended. Same shape at pipeline.tab.previewAdvances ("Would advance #" in both branches). A flat string plus a note, or genuinely differing branches, would tell translators which it is. |
| `pipeline.board.active / pipeline.tab.previewAdvances` | ICU plural whose one and other branches are byte-identical ("# active" / "# active", "Would advance #" twice). The plural wrapper carries no information in en, which invites a translator to collapse it — but cs genuinely needs three branches. Harmless, worth a comment. |
| `pipeline.controlCenter.needYou` | A bare verb fragment ("need you") deliberately split from its count, which lives in a separate badge element (SimControlDockOpsFace.tsx:53-57). Every inflecting target language must invent a count-invariant workaround because the string has no {count} to hang a plural on. Passing the count into the message would remove the whole class of defect. |
| `pipeline.controlCenter.passLine` | "{advanced} advance · {rejected} reject · {held} hold · {alerts} flag" labels counts with bare verb stems. English gets away with it as a telegraphic dashboard tape; every target language must invent a convention (cs uses count-invariant neuter participles, de predicative participles, fr now needs "(s)"). Deverbal nouns in the source ("advanced · rejected · held · flagged", or "advances · rejections · holds · flags") would translate cleanly everywhere. |
| `pipeline.controlCenter.titleAwaiting` | "Control center · {count} need you" is a flat non-ICU message whose verb disagrees at count = 1 ("1 need you"). cs and de already had to add plural branches the source lacks; the source should be an ICU plural. |
| `pipeline.tab.bulkAwaiting` | Flat "{count} awaiting a decision" with no plural; the verb agreement it hides breaks cs and de, both of which had to add branches the source lacks. |
| `pipeline.tab.bulkDrafted` | Flat "{count} outreach letters sent" with no ICU plural — "1 outreach letters sent" in English. |
| `pipeline.tab.passAdvanced` | "{n} advanced" / "{n} rejected" / "{n} held for review" / "{n} aging alerts logged." are flat strings with an interpolated count, so every inflecting locale has to either invent an ICU plural (fr and de did on some, not others) or fall back to a count-invariant form (cs did). The four siblings should be plural-formatted consistently in en so the locales are not split key-by-key. |
| `pipeline.tab.passAdvanced / passRejected / passHeld / bulkMoved / bulkAccepted / bulkRejected / bulkInvited / selectedCount` | A family of count strings with a bare {n}/{count} and no plural block. English gets away with a participle that does not inflect; fr must agree the participle and cs must pick a count-invariant form. Marking these as plural messages in en would make the intent explicit rather than leaving each locale to guess. |
| `pipeline.tab.passAlerts` | Flat "{n} aging alerts logged." with no ICU plural. English itself renders "1 aging alerts logged." Needs {n, plural, one {…} other {…}} in en; every locale had to invent the branches. |
| `pipeline.tab.previewRejectsQueued` | The en `one` and `other` branches are byte-identical, so the ICU plural does no work in the source. It exists only so translators can inflect — which is fine, but it reads as a copy-paste bug and invites someone to 'simplify' it back to a flat string, which would break cs/de/fr agreement. Same shape in pipeline.tab.previewHeld. |
| `pipeline.tab.previewSubtitle` | Flat string needing a plural: `{evaluated} active candidates evaluated` hardcodes the English plural noun, so `1 active candidates evaluated` is reachable. cs already works around it with its own plural block; en/de/fr cannot. |
| `pipeline.tab.selectedOutsideFilter` | Flat count with English plural agreement baked in (`{count} selected are hidden`). Breaks at count=1 in en and de. Same defect in pipeline.tab.bulkFailed, bulkDraftFailed, bulkInvitedQueued, bulkDraftedQueued, pipeline.controlCenter.openAwaiting, pipeline.command.doneSummary and pipeline.tab.previewScope — all interpolate a live count with no ICU plural. |
| `profile.empty.filed` | Declared as an ICU plural but the only call site (`ProfileEmptyStates.tsx:144`) hardcodes `{ count: 0 }`, and both en branches are byte-identical ("# filed"). Dead branch structure: it forces every locale to guess the referent noun for a count that is always zero. |
| `profile.empty.moreArchetypes` | Both en plural branches are identical ("+# more"), so the plural carries no information in the source and gives translators no cue about the elided head noun (archetypes). de/cs/fr each had to infer it. |
| `profile.matrix.band_strong` | The band chips ("{count} strong", "{count} mid", "{count} weak", "{count} not assessed") are elliptical adjective fragments with the head noun omitted. English gets away with it; Czech has to choose a case and gender for the missing noun (the catalog currently uses a genitive-plural escape, "silných: {count}"). Spelling the noun out in en, or documenting the intended head noun, would let all three locales agree properly. |
| `profile.matrix.groupCount` | **RESOLVED 2026-08-12** — en/cs/de/fr all expanded to ICU plurals. "{count} candidates" is a flat string with no ICU plural, so it renders "1 candidates". The call site (CandidateMatrixFilterBar.tsx:93) passes the real group total, which can be 1. Needs `{count, plural, one {# candidate} other {# candidates}}` in en before the locales can expand correctly (cs needs one/few/other). |
| `profile.roster.count` | **RESOLVED 2026-08-12** — en/cs/de/fr all expanded to ICU plurals. Same defect: "{count} saved profiles" renders "1 saved profiles". Should be an ICU plural in en. |
| `report.archetype.fillGaps` | Flat '({count})' beside a noun phrase; no plural branches. Czech and the German article would both want agreement if the count ever moves inline. |
| `report.compare.reasonSkillsLine` | '{count} skills indexed' is flat with no ICU plural; breaks at count=1 in every locale. cs supplied its own branches, de/fr did not, so the three locales now disagree structurally on the same source string. |
| `report.compare.reasonSummary` | '{yrs} yrs surfaced' is a flat string with no ICU plural and an abbreviation ('yrs') that does not survive translation. cs had to invent the plural branches itself; de/fr rendered a bare number that will read wrong at yrs=1. Should be a `{yrs, plural, …}` message in en. |
| `report.panel.moreHidden` | '+{count} more not shown' has no plural AND no head noun. Translators must guess what 'more' counts (cs supplied 'klíčové slovo' from the call site, JobFitTab.tsx:258). Name the noun in en and make it a plural message. |
| `results.github.finding.basis.files` | "Up to {count} root-level file and directory names per repo" interpolates a live count with no ICU plural. Czech needs one/few/other agreement on "názvů"; the flat form is ungrammatical for count=1. |
| `results.github.finding.summary.text` | "{active} active" is a flat count inside a sentence that already contains a plural on {repos}. English gets away with an invariant adjective; cs needs one/few/other and fr needs one/other, so both had to add a block the source does not signal. Same defect in results.github.finding.summary.noLanguages. |
| `scheduleTab.lifecycle.attention / upcoming / today / awaiting / rescheduled / calendarDropped` | Several of these plural messages have identical `one` and `other` branches in en ("Today & in progress (#)", "Awaiting booking (#)", "rescheduled ×#"), i.e. the plural wrapper carries no English meaning. That is fine for en/de/fr but gives translators no signal about which of these actually need Czech `few`/`many` expansion — the count sometimes governs an elided verb ("Čeká / Čekají na rezervaci") and sometimes not. A comment or a non-plural key for the genuinely count-invariant ones would remove the guesswork. |
| `scheduleTab.lifecycle.today` | An ICU plural whose one and other branches are byte-identical ("Today & in progress (#)"). Same in lifecycle.awaiting and lifecycle.rescheduled. A no-op plural invites translators to invent a distinction that the UI does not want, and it costs a compile for nothing — either make the branches differ or drop the plural wrapper. |
| `setup.checklist.progress` | '{done} of {total} done' repeats 'done' in two different senses (count vs. state) and has no plural. Reads as a typo in en and forces every locale to invent a different second word. |
| `simulation.wave.subtitle` | "{cohort} matched · {rejected} auto-rejected · {kept} advanced" — three bare counts with participles and no plural blocks. Every number-agreeing language has to guess (fr must agree the participle, cs the noun and verb). Same defect in simulation.log.screenWave. |
| `tasks.backup.restored` | Flat counts in a sentence that interpolates two numbers ("Restored {tables} tables · {rows} rows"). Any language with real plural morphology has to reconstruct the ICU plural from scratch; de renders "1 Tabellen" today. Same defect in tasks.backup.plan.title and tasks.backup.plan.applyReplace ("Replace {count} tables & restore"). |
| `tasks.outcome.advanced` | "{count} advanced" / "{count} held" / "{count} advisory" are flat participle stat chips with no plural and no head noun. English gets away with it; cs needs the neuter-invariant escape hatch and fr cannot agree the participle at all. |
| `tasks.system.queue` | "queue {running} running · {queued} queued" is a flat two-count metric with no ICU plural and no punctuation between the label and the first count, forcing every locale to invent both the separator and the agreement strategy. |

## Sentence assembled by concatenation (39)

| key | problem |
|-----|---------|
| `analytics.calibration.exclusionAnalysis` | Same shape: lowercase-initial fragment concatenated onto an unseen prefix. |
| `analytics.calibration.exclusionPipeline` | A 45-word two-sentence legal-ish blurb that starts lowercase, meaning it is concatenated onto a label elsewhere in the UI. The leading fragment forces every locale to guess the sentence's head. |
| `analytics.metricPack.caveatNotMeasurable` | String concatenation: {basis} is a whole other translated sentence spliced in after "no data yet". The joined result's punctuation and capitalisation can only be right by luck across locales; I made the join a full stop so any {basis} sentence reads correctly, but a sentence assembled from two catalogs is untranslatable in principle. |
| `analytics.roi.csvMinsEach` | "Mins each" is an elliptical fragment with no head noun; as a CSV column header it forces each locale to invent the missing complement (per action / per row). "Minutes per action" would be unambiguous. |
| `channels.cvSim.stub` | Not a string, a fragment. ChannelsCvSimCard.tsx:165 concatenates it onto another message as `suffix`. It has to open with a leading space and a parenthesis and can never be re-ordered by target grammar. Should be a real message with a {suffix} or {archetype} placeholder, or a select over degraded/archetype. |
| `comms.scheduleInvite.body` | {length} is spliced mid-paragraph directly after a full stop with no separator, so the sentence that follows it depends on whatever {length} ends with. Same pattern in comms.interviewConfirmation.short, comms.interviewReminder.body and comms.interviewInvite.body. |
| `comms.there / comms.theRole / comms.aRole / comms.yourNewRole / comms.devcaseFeedback.greetingFallback` | Five bare noun-fragment fallbacks are string-spliced into a dozen prose templates (app/_lib/comms-dispatch.ts, app/_lib/devcase-feedback.ts). English survives this because its nouns do not inflect and "there" happens to fit any greeting; every inflecting target language breaks on at least one call site (see the cs comms.theRole entry in nativeReview). The durable fix is a no-name / no-title VARIANT of each template rather than a substitutable fragment. |
| `control.calibration.rationale.lower` | Builds a numeric range by gluing two independently formatted placeholders with a literal en dash ({suggested}–{current}). cs and fr both had to spell it out as "od X do Y" / "de X à Y" because a glued range is not idiomatic there. A real range value (or two clearly labelled numbers) would translate cleanly everywhere. |
| `decisions.rules.ruleLog` | The value is a sentence fragment beginning with ". " that is concatenated onto decisions.rules.ruleSentence (+ optionally ruleFamilyAppend) at render time. Cross-key sentence assembly fixes English clause order for every locale; a language wanting the log caveat elsewhere in the sentence cannot express it. |
| `decisions.rules.ruleWeakest` | Sentence fragment concatenated at the call site with ruleSentence + ruleFamilyAppend + ruleLog (DecisionsRulesModal.tsx:130-145). Four keys assemble one sentence, so no locale can control word order or clause type; German verb-final and Czech word order are both unreachable. Should be one ICU message with optional select branches. |
| `decisions.summary.decisionNoteOptional` | The value begins with a bare "· " separator, so the string is not self-contained and its leading punctuation cannot be dropped by locales that would punctuate the concatenation differently. Same shape in decisions.groupEval.subtitleRan (" · ran {when}"). Prefer composing the separator in the component. |
| `devApply.workSurface.intro` | Five independent statements concatenated into one catalog value (edit instruction + recording disclosure + grading rationale + tool permission + privacy promise). It cannot be reordered or partially reused per surface, and the dashed aside forced a full restructure to translate. Should be 2-3 sibling keys. |
| `devcase.processTrace.noCommits` | Lowercase fragment appended after a "·" separator in DevEvalPanelProcessTrace. Not a sentence, so no locale can punctuate it properly; cs again needs the genderless "pracoval/a" form because the implied subject is off-string. |
| `landing.hero.subtitle` | A 40-word sentence that concatenates five parallel clauses after a colon, then a second sentence. It fits the English rhythm; German runs it to ~55 words and it is the longest single value in the batch. Consider splitting the funnel list into its own key. |
| `landing.previews.gates.signed` | The English is a bare fragment ("{time} — shortlist approved, signed {name} ✓") whose only sentence spine was the dash. Recast to "shortlist approved at {time}, signed {name} ✓", but the underlying issue is that the log-line shape leaves {name} in a slot that inflecting languages cannot case (Czech would want a dative). Worth restructuring at the component. |
| `match.shared.cachedSuffix` | " · cached" is a suffix fragment with a leading space, concatenated onto another message. Fragment concatenation blocks correct word order in any locale. |
| `match.shared.weightPercent` | "· {weight}%" bakes a leading separator glyph into the value — a concatenation fragment, not a message. cs has already compensated by adding a leading space, which risks a double space at the call site (app/features/shared/MatchPresentation.tsx:79). The separator belongs in the JSX. |
| `matrix.selectedSuffix / matrix.selectableSuffix / matrix.inPipelineSuffix` | An aria-label sentence is assembled by concatenating fragments into matrix.cellAria ('{cand} to {pos}: {val}{ring}{sel}'). The suffixes are bare comma fragments with no grammatical relation the translator can control; cs/de cannot inflect them to the preceding noun. Should be one ICU select per state inside cellAria. |
| `pipeline.drawer.current` | The value is a concatenation fragment with a hard leading space (" (current)"), appended to a stage name in code. Locales that need a different attachment (or a non-parenthetical form) cannot express it, and the leading space is invisible to review. Better as a full ICU message taking {stage}. |
| `pipeline.events.* (whole namespace)` | The values are predicate fragments concatenated after a name (PipelineActivityFeed.tsx:52-53: `{candidateLabel} {eventVerb(ev)}`), but nothing in the key name or the en value says so, and the en resultatives ("had their interviewer briefed", "had the onboarding dispatch fail") are an English-only construction with no equivalent in cs/de/fr. Four values in this batch had already drifted into self-contained clauses that render ungrammatically after the name. A comment on the namespace, or renaming the keys to a `…Predicate` convention, would stop it recurring. |
| `pipeline.events.outreach_halted` | Event labels are verb-phrase fragments concatenated after a candidate name. That forces every locale into a headless predicate and is exactly the CS-HADDONE trap documented in constructions-cs.md (cs 'má zastavené oslovování'). ~13 pipeline.events.* keys share the shape and need one coordinated rewrite plus a decision on how the feed joins {name} to the phrase. |
| `pipeline.result.confidence` | " · {confidence}% confidence" is a concatenation fragment with a leading space and separator baked into the value. Fragments like this force every locale to guess the surrounding sentence and prevent per-locale reordering; the separator belongs in JSX. |
| `pipeline.result.matchSuffix` | "· {score} match" is the same concatenation-fragment pattern (leading separator inside the value), assembled with an adjacent title string at PipelineCandidateRow.tsx:108. Locales that need a different clause order cannot express it. |
| `pipeline.tab.degradedBannerBody` | The string is a sentence FRAGMENT concatenated at render time onto a separately-translated count headline (`degradedBannerCount`). The two halves are separate <p> elements, so no locale can make the body's verb agree with the number. Czech and German have to fall back to a count-invariant recast. Same structural defect in pipeline.tab.approvalsBody. The fix is to make each banner one message that owns its own count. |
| `profile.archetypes.earlyModelNote` | Same concatenation defect: scoringModelLabel + earlyModelNote/expModelNote + applyClause build one sentence in ArchetypeManagerViewPanel.tsx:57-59, each fragment shipped as its own key. |
| `profile.archetypes.weightFieldLabel` | `{slot}` interpolates the raw internal slug ("skills" / "career" / "personal") from `SLOTS` in ArchetypeManagerTypes.ts — it is never translated, so every non-English locale renders a mixed-language label ("skills váha %", "skills Gewicht %"). Same defect in `dimFieldLabel`, `validation.weight_not_number` and `validation.dimension_label_required`. Fix in code: map the slot to a `profile.archetypes.slot.<slot>` message before interpolation. |
| `profile.editor.draftHint` | Fragment glued to draftToggle inside the same button label (ProfileEditorAiDraft.tsx:64-66). The hint cannot be reordered relative to the toggle text in any locale. |
| `report.compare.fromLabel` | A bare preposition ('from') rendered in its own <span> and concatenated with a source label in CompareTab.tsx. Czech and German case government cannot be expressed: 'z' / 'aus' need a genitive/dative noun the string never sees. Should be a single message with a {source} placeholder. |
| `report.compare.narrativeTie / narrativeDelta` | {metric} is injected as a pre-translated nominative label ('overall score' -> cs 'celkove skore'), then placed after a preposition ('on {metric}', de 'bei {metric}', fr 'sur le {metric}'). Czech needs a locative and German needs 'beim'; French needs the article to agree with the label's gender. The metric should be a select on the enum, not an interpolated label. |
| `report.compare.subtitle` | Three claims concatenated into one string ('N variants scored; winner crowned; delta baseline'), joined by a semicolon and a dash. It reads as three sentences and should be three keys, or at least stop hiding a definition of {baseline} inside a subtitle. |
| `report.verdict.outOf` | '/ 100' is a bare fragment concatenated next to a rendered number. It is not a translatable sentence and locks the score/max presentation to a slash form; fold it into the score message with a placeholder instead. |
| `results.github.finding.contribution.activity` | "in the last {activeMonths} months" / "in the last {recentMonths} months" interpolate a window that is 12 and 3 respectively (app/_lib/repo-activity.ts:15-16). English is invariant; Czech is not ("3 měsíce" vs "12 měsíců"). A flat {n} months construction is a standing trap for every inflecting locale. |
| `rubricCoverage.no_family` | Concatenation-by-prose: the leading fragment "Generic rubric" is a label glued onto two full sentences of explanation. The label and the body want to be separate keys so a surface can show the label as a badge and the body as help text; as one string every locale must guess the register of the joint. |
| `schedule.planFor` | Value ends in a trailing space ("…{min} minutes}}. ") because it is concatenated with a following sentence at the call site. Concatenated UI sentences are unlocalizable in the general case (word order and case government differ per locale); make it one complete message or an ICU-composed one. |
| `scheduleTab.lifecycle.needsMoreSlots` | Sentence fragment concatenated at runtime after a candidate name and a job title (ScheduleInviteAttentionSection). The locale cannot control capitalisation or verb agreement with the name it follows; cs is forced into the "nenašel/a" slash form with no subject. Same shape in scheduleTab.lifecycle.proposedTimes. |
| `scheduleTab.prep.plan.forCandidate` | The value is a bare preposition ("for") concatenated at runtime with a candidate name, per the {who} slot in prep.plan.scenario / studentPlan.scenario. Czech and German require the name to carry a governed case ("pro Jana Nováka", "mit Frau Nováková"), and Czech surnames decline. No locale can produce correct grammar from a preposition fragment plus an uninflected name. The phrase should be a single ICU message owning the whole "{name}" clause. |
| `scheduleTab.prep.plan.scenario / scheduleTab.prep.studentPlan.scenario` | The sentence is assembled in code from a separate one-word fragment (`prep.plan.forCandidate` = "for") plus the raw candidate name: `who = " ${forCandidate} ${candidateLabel}"` (app/_lib/run-of-show.ts:146, app/_lib/student-interview.ts:134). Inflecting locales are forced to put an undeclinable interpolated proper name into an oblique case (cs "pro Jan Novák" instead of "pro Jana Nováka"), and they cannot move or restructure the fragment because the join happens outside ICU. A single ICU message with a `{who}`-shaped optional clause, or a name-first frame ("Kandidát: {name}"), would let each locale pick a grammatical construction. |
| `setup.handoff.readyTitle` | An organization name is interpolated straight into a predicate ("{org} is ready to go"). Any locale whose adjective agrees in gender has to guess the gender of an arbitrary company name. cs and fr were forced into verb constructions ("může začít", "peut démarrer") to dodge it. A gender-free frame such as "Ready to go: {org}" would remove the trap. |
| `table.filters.allOf` | The English builds a label by interpolating a column title ({column}) into a fixed frame. Column titles are themselves translated nouns in nominative case, so any locale wanting a case-inflected or gendered frame ("Vše podle role", "Alle nach Rolle") cannot have one. Parentheses are the only frame that survives all four locales, which is why I used them — but the underlying pattern is a source constraint worth noting. |

## Hardcoded currency, amounts, dates or latencies (27)

**Left open, needs a product decision:** the `analytics.roi.*` and
`landing.pricing.tiers.*` / `landing.pricing.footnote2` rows below hardcode
`CZK`/`Kč` as literal text rather than a formatted placeholder. Unlike the
`spendNote`/`unpriced`/`backdatedTitle`/`groupEvalTimeout` keys fixed in the
2026-08-12 pass (which only needed the *number* run through Intl), these
name the *currency itself* in prose — and `app/_lib/format.ts` documents
`APP_CURRENCY = "CZK"` as a deliberate single-currency product decision
("The app does not do FX"). Reformatting them to a locale-neutral currency
placeholder is only worth doing once multi-currency billing is a real
question; until then the literal CZK/Kč is an accurate statement of how the
product works, not a formatting bug. Flagging here rather than fixing.

| key | problem |
|-----|---------|
| `about.students.scoringFootnote` | ~60-word footnote concatenating four independent claims (hover behaviour, Adéla's average, Bára's band, Cyril's delivery) plus hardcoded person names and the literal "4.3" / "4/5" numbers. cs and de must carry the whole block in one value and run 15-30% longer. Worth splitting into 3-4 keys; the decimal should come through Intl, not as literal "4.3". |
| `aboutPage.art.design.bandValue` | Hardcoded salary range "120k — 165k" with no currency and no Intl formatting. cs already diverged to "120–165 tis."; de/fr keep the English "k" abbreviation, which is not the local convention. Should be a formatted number pair, not a literal. |
| `agentsWorkforce.spendNote` | **RESOLVED 2026-08-12** — "$0" replaced with a `{zero}` placeholder, formatted via `fmtUsd()` at the call site (AgentsWorkforceRow.tsx, AgentsWorkforceRoster.tsx). Hardcoded currency symbol: "$0" is baked into the string in all four locales. Per contract §9 currency must be formatted at runtime via ICU/Intl; a CZK/EUR-market operator sees a dollar amount that no locale controls. Needs a placeholder plus a number/currency format, or the amount removed from the copy. |
| `analytics.compute.perHireNote` | USD and CZK are hardcoded currency codes in prose. Correct as a statement of fact today, but it hard-codes the ledger's pricing currency into copy that will drift if a second billing currency is ever supported. |
| `analytics.compute.unpriced` | **RESOLVED 2026-08-12** — "$0" replaced with a `{zero}` placeholder, formatted via `useFormatter()` at the call site (AnalyticsComputeCostPanel.tsx). "$0" is a hardcoded currency literal inside the message, in every locale. A CZK-market workspace sees a dollar amount. It should be an Intl-formatted placeholder, not a baked symbol. |
| `analytics.roi.basis` | "{actions} automated actions, at {rate} CZK/hour." is flat and renders "1 automated actions" in English when totalActions is 1 (reachable: automation-roi.ts sums per-kind counts with no floor). Needs a plural in the source. |
| `analytics.roi.headline / analytics.roi.basis / analytics.roi.rateSuffix / analytics.channels.colSpend` | Currency is hardcoded as "CZK" in the source string rather than formatted at runtime, so no locale can render EUR/USD for a non-Czech tenant. contract.md §9 says never hardcode currency; here the currency code is baked into en itself, which caps every locale. |
| `analytics.roi.perHire` | Hardcoded unit abbreviation "h" and the "~" / "≈" symbols are baked into the message rather than formatted; "h" is not the natural hour abbreviation in every target locale (de wants "Std."). |
| `analyze.ready` | Hardcoded latency "15–25 seconds" and a hardcoded provider name (Gemini) in user-facing copy. Both go stale silently when the model or provider changes. |
| `channels.empty.comms.effort` | Sibling keys channels.empty.email.effort and channels.empty.ads.effort share the same shape with the duration hardcoded as a literal numeral in prose ("About 2 / 3 / 5 minutes"). No placeholder, so the number cannot be formatted per locale and there is no plural branch for a hypothetical "1 minute". |
| `decisions.compliance.anyAdverse` | Hardcoded literal "80%" (the four-fifths threshold) inside the copy rather than a placeholder fed from the same constant the check uses. If the threshold is ever configurable the string silently lies, and each locale hand-writes the percent spacing. |
| `decisions.wave.reasons.holdout` | Hardcoded "%" glued to {pct}. Czech wants a space before the percent sign when it is a noun ("{pct} %"), French wants a narrow no-break space; the source shape forces each locale to hand-edit the symbol instead of formatting the number. |
| `devcase.integrity.backdatedTitle` | **RESOLVED 2026-08-12** — the glued "s" unit removed; the call site now formats the whole duration via `Intl.NumberFormat(locale, {style:"unit"})` (DevEvalPanelIntegrity.tsx). Hardcoded unit suffix "{seconds}s". Every other locale needs a space and a different abbreviation (cs/de/fr use "{seconds} s"); the unit belongs in the value per locale or in an Intl unit format, not glued to the placeholder in the source. |
| `diagrams.items.tobe.blurb` | The phase list "Design JD · Source · Intake · Screen · Interview · Offer · Hired" is hardcoded English inside a translatable prose value. Every locale ships untranslated English stage names mid-sentence, and they will silently diverge from the real stage labels if a stage is ever renamed. Should interpolate the stage-label keys instead. |
| `integrations.personas.timeoutBody` | Hardcoded duration: "The request expires after 5 minutes". Per contract.md §9 numbers should come through a placeholder so the copy cannot drift from the real pairing TTL; all four locales now carry the literal 5. |
| `jobs.posting.lifecycle.jd` | "JD source" uses the internal abbreviation JD in a user-visible lifecycle segment. Every other user-facing string in the namespace spells out "Job descriptions" / "job description"; the abbreviation has no currency outside English-speaking recruiting. |
| `landing.previews.gates.queue.offer.sub` | "Band 85–105k" hardcodes an English magnitude suffix (k) and omits the currency entirely, so every locale has to invent one (cs chose "tis.", de/fr kept "k"). Either state the currency in the source or move the figure into a formatted placeholder; per contract §9 a localized number should never be frozen into the string. |
| `landing.previews.schedule.confirmed` | Hardcoded weekday and clock time ("Wednesday 14:00") inside the copy. Translators have to render a date by hand; the German value already had to add "Uhr" and no locale can apply its own time format. Should be placeholders fed from the preview component. |
| `landing.pricing.footnote2` | Hardcoded currency in the source: "790 Kč". Per contract §9 the amount should be a placeholder formatted at runtime; as written, every locale ships CZK regardless of market, and de/fr copy reads "790 Kč" to a DE/FR audience. |
| `landing.pricing.tiers.{free,starter,growth,byom}.price / .usd` | Currency is hardcoded in the source string ("0 Kč", "240 Kč", "≈ $10 a month"), which contract.md §9 ("Numbers, dates and currency are formatted at runtime; never hardcode a localized number") forbids. Every locale is forced to carry a CZK/USD literal, so a fr/de reader sees Kč with no conversion and the number cannot be Intl-formatted per locale. |
| `landing.pricing.tiers.free.price / starter.price / growth.price / byom.price and the matching .usd keys` | Currency is hardcoded into the catalog value ("0 Kč", "240 Kč", "≈ $10 a month"). contract.md §9 requires numbers and currency to be formatted at runtime via ICU/Intl with a placeholder. As written, every locale is forced to display CZK and USD literally, with no way to localize the currency, the grouping or the symbol position, and the translator has to hand-place a non-breaking space per locale (cs/de did, fr did not). |
| `login.eyebrow` | The value is the literal brand string "kp", carried through all four catalogs as an untranslatable key. It is a hardcoded brand token wearing a message key — it can never diverge per locale and only adds parity surface. |
| `pipeline.candidateRow.aging` | Hardcoded unit abbreviations inside the copy ("{days}d", "{d}d ago", "{w}w ago", "{mo}mo ago") push a locale-specific unit shorthand into a translated string rather than using Intl.RelativeTimeFormat / a unit-formatted number. Every locale is inventing its own abbreviation (cs "d"/"týd"/"měs", de "T"/"Wo"/"Mon", fr "j"/"sem."/"mois") with no shared convention. |
| `pipeline.result.band` | `{currency}` is a separate trailing placeholder appended after a pre-formatted `{min}–{max}` range. Currency position and spacing are locale-dependent and cannot be moved by a translator without breaking the digits-only en dash. Should be one Intl-formatted value, not three placeholders. |
| `pipeline.result.perMonth` | "/ mo" is a hardcoded period abbreviation glued to a number formatted separately (PipelineCandidateResultView.tsx:117 uses toLocaleString). Locales cannot move the unit relative to the figure, and the space handling before "/" is not expressible. |
| `report.anchorBand` | Currency and period are hardcoded English-side ('CZK/mo'), not formatted at runtime. Contract §9 forbids hardcoded localized currency; this pins the product to CZK in every locale. |
| `simulation.error.groupEvalTimeout` | **RESOLVED 2026-08-12** — same fix as backdatedTitle, applied in useSimulationEngine.ts. '{seconds}s' hardcodes the unit suffix into the string instead of formatting a duration. Also forces the translator to hand-insert a non-breaking space (CS-NBSP / DE-NBSP) that a formatter would supply. |

## One value carrying several distinct claims (3)

| key | problem |
|-----|---------|
| `channels.email.notWiredHowTo` | **RESOLVED 2026-08-12** — split into `notWiredHowTo` (lead sentence) + `notWiredHowToSetup` (setup instructions); ChannelsEmailIntakeWizard.tsx now renders two paragraphs. One string carries four distinct instructions (what already works, how to route a provider, which env var to set, what appears afterwards). At 60+ words it is the longest value in the batch and forces every locale into a wall of text in a hint slot. Should be split into a lead sentence plus a list. |
| `data.eraseExplainer` | One 70-word GDPR paragraph carrying four distinct claims (what is deleted, what is kept and why, the sealed decision record, the legal-defence basis). It is a single flat string, so no locale can re-order or split it for readability and no surface can render the list as a real list. Splitting it into 2-3 keys (or making the deleted-data list a structured array) would raise quality in every locale; I only recast the dash into a colon rather than restructuring. |
| `jobs.tab.intro` | One 55-word sentence carrying a parenthetical, a rich tag, and (before this rewrite) a dashed aside. Even recast to a colon it is at the limit of what a tab intro should carry; the second half is really a second paragraph. Reported, not split, since splitting would change the key shape. |

## How to act on these

- **Hardcoded currency/amounts**: replace the literal with a placeholder and let
  `app/_lib/format.ts` render it per locale. A CZK-market workspace currently
  sees `$0` in the compute ledger.
- **Missing plurals**: an English string reading "1 need attention" is already
  wrong in English, and forces every translator to guess. Czech additionally
  needs a `few` branch (2-4) that a flat string cannot express.
- **Concatenation**: a value that is a sentence fragment glued to a sibling at
  render time cannot be reordered, and target grammar almost always wants a
  different order. Prefer one ICU message with placeholders.
- **Several claims in one value**: split into sibling keys so a translator can
  set each one's register and length independently.
