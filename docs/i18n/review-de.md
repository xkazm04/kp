# German (de) — native-review queue

Strings the `/i18n-translate review de` pass left for a **native speaker** to confirm — subjective calls, unsettled domain/legal terms, or house-style decisions. High-confidence fixes were already applied to `messages/de.json`; these are the remaining judgment calls (AI-flagged, need human sign-off).

| key | note |
|-----|------|
| `apply.script.greeting` | 'Bewerben wir Sie für {jobTitle}…' is non-idiomatic: 'bewerben' is reflexive (sich bewerben) or transitive 'to advertise', so this reads as 'let's advertise you'. Suggest 'Starten wir Ihre Bewerbung für {jobTitle} bei {company}.' Same issue in apply.script.greetingCompany. Left as-is because the natural rewrite is a copywriting/tone choice. |
| `match.dims.skills` | 'skills' is rendered 'Skills' throughout match (dims.skills, csv.matchedSkills/missingSkills, card 'Skill'), but 'Fähigkeiten' in profile/analyze/apply/jobMarket for the same concept. Style guide's kept-loanword list omits Skills, yet 'Skills' is idiomatic in DE tech/HR — pick one term app-wide. |
| `matrix.probes` | 'Interview probes' = 'Interview-Fragen' here but 'Interview-Nachfragen' in match.shared.interviewProbes. Same concept, two renderings — unify (both are acceptable German). |
| `match.tab.candidate` | Whole DE consistently uses generic-masculine 'Kandidat/Kandidaten', but glossary specifies inclusive 'Kandidat/in' when gender is unknown, and apply.json example placeholders mix in '/in' forms ('Entwickler/in', 'Analyst/in'). Decide one gender-form policy and apply it app-wide. |
| `channels.intro / channels.received / channels.items.*` | Pipeline stage labels render as „Angenommen" (Accepted) and „Geprüft" (Screened). These read consistently within channels, but the enums.stage.* namespace was not in this audit set — verify these match the canonical enum labels so prose and chips agree app-wide. |
| `report.factorRole` | "Role" scoring factor is "Rolle"; glossary prefers Position/Stelle over "Rolle". As a scoring-dimension label (role fit) it may be intentional — confirm with a native whether "Rolle" or "Position" is wanted here. |
| `interviewSim.simulating` | "Simuliere <b>{name}</b>{job}" uses a first-person/casual progress form, while the rest of this file uses passive ("… wird erstellt/geladen"). Consider "Simulation: <b>{name}</b>{job}" or "Simuliert <b>{name}</b>{job}" for register consistency. |
| `scheduleTab.humanLedChip` | "Human-led" → "Persönlich geführt" may be misread as "in person"; the concept (human vs AI) is elsewhere "Von Menschen geführt" (jobs.compare). Native check on the shortest chip-safe wording. |
| `jobs.tab.allModes / jobs.table.colMode / jobs.tab.filterWorkMode` | Work mode (remote/hybrid/onsite) is "Modell"/"Alle Modelle"/"Arbeitsmodell". Standalone "Alle Modelle" is loose and could be confused with AI "Modelle"; consider "Arbeitsmodus"/"Arbeitsweise". Judgment call for a native. |
| `pipeline.board.rankCandidates` | 'ranken' is an anglicism/false friend (standard German 'ranken' = plants climbing). Consider 'reihen', 'einstufen' or 'nach Eignung ordnen'; replacement is a judgment call. |
| `comms.there` | Fallback 'zusammen' produces 'Hallo zusammen,' — a group greeting — in a 1:1 candidate email. Template forces a token, so it needs a native-approved singular fallback. |
| `comms.interviewerBrief.body` | 'Live-Bewertungsmatrix' renders 'rubric' differently from pipeline (fixedRubric/humanScorecardNote use 'Bewertungsraster'). Pick one term for 'rubric'. |
| `pipeline.drawer.humanScorecard` | 'Manuelle Scorecard' vs decisions.aiReview.tagHumanScorecard 'Menschliche Interview-Scorecard' — 'human (scorecard)' rendered two ways; the app elsewhere uses 'menschlich/Mensch' for 'human'. |
| `analytics.declinedSub` | 'declined' = 'abgesagt' here but 'abgelehnt' in log.kinds.offer_declined; 'abgesagt' usefully disambiguates from 'rejected'='abgelehnt' in the adjacent stat, but confirm the intended term. |
| `status.notSelectedBody` | Doubled preposition: 'sich für diese Position für andere Kandidatinnen … entschieden' reads awkwardly; consider 'bei dieser Position'. Stylistic. |
| `decisions.row.candidateCount` | Gendering inconsistency across namespaces: pipeline uses inclusive 'Kandidat/in', but decisions/analytics use generic 'Kandidat', and status/comms use 'Kandidatinnen und Kandidaten'. Glossary asks for inclusive form; needs one contentious house-style decision (pervasive, not fixed here). |
| `pipeline.tab.searchLabel` | 'role/position' is 'Position(en)' in pipeline but 'Stelle(n)' in decisions/analytics. Glossary sanctions both, so confirm whether the split is intentional or should be unified. |

_17 items._

## Queued by the 2026-08-04 `aboutPage` (/about) review

Namespace-scoped pass over `aboutPage` only. Framing verdict: **product**, not
company. One key fixed (`steps.screen.body`: Rolle → Stelle, and the colloquial
"rausgehen" → "verschickt werden"). Left for a native:

| key | note |
|-----|------|
| `aboutPage.steps.source.title/body` | "eine gerankte Shortlist" / "rankt sie" — the same *ranken* anglicism already queued for `pipeline.board.rankCandidates`. This is a public marketing page, so it is the strongest argument for settling that row ("sortierte Shortlist" / "stuft sie ein"). Not changed unilaterally. |
| `aboutPage.steps.design.title` | "Stelle beschreiben. Rubrik erhalten." is infinitive-headline German while the rest of the page uses Sie ("Begleiten Sie", "Scrollen Sie"). Idiomatic as a headline; confirm the register mix is wanted. |
| `aboutPage.steps.interview.title` | "Dann spricht es mit Menschen" — the *es* has no antecedent in German (en's "it" = the system). fr has the same issue. Consider naming the subject. |
| `aboutPage.steps.screen.body` | "Jede Bewerbung" where en says *each candidate*; fr made the same swap independently. Minor accuracy drift that reads better — confirm. |

_4 items._

## Queued by the 2026-08-04 landing-page review (namespace `landing` only)

8 keys fixed in `messages/de.json`; these were deliberately left alone.

| key(s) | severity | note |
|--------|----------|------|
| **AI vs KI, catalog-wide** | major | `landing` was unified on **AI** this pass (nav.trust, features.cases.body, previews.cases.note), because the same page already said AI in ~8 places and the section rail put "Verantwortungsvolle KI" directly beside "Leistungsstarke AI". But the split is app-wide: 92 AI vs 26 KI across `messages/de.json` (`library.tab.*`, `devcase.*`, `agentFit.*`, `setup.*`, `decisions.*` all say KI). Needs one house decision plus a single sweep — do not half-sweep further. |
| `landing.nav.trust` | minor (length) | "Verantwortungsvolle AI" is 22 chars in the SectionRail, whose label column is capped at `max-w-[12rem]` (192px) with `overflow-hidden`. It fits at 15px bold but with no margin. "Verantwortliche AI" (18) is shorter but reads as *liable* rather than *responsible*. |
| `landing.nav.market` | minor | Changed "Market Pulse" → **"Marktpuls"** (cs/fr both localize it; it is descriptive, not on the glossary Do-Not-Translate list). Revert if "Market Pulse" is meant as a sub-brand — in which case cs/fr need reverting too. |
| `landing.trust.human.title` | minor | "Human in the Loop" left in English. Standard in German AI-governance writing, but cs/fr both translate it. |
| `landing.features.score.body` | minor | "mit belegbaren Nachweisen" is a pleonasm (provable proofs); "mit den Nachweisen dazu" is tighter. |
| `landing.features.offer.body` ("Rollenband") | minor | "Passung" → "Eignung" was fixed; "Rollenband" still uses *Rolle* where the glossary prefers Position/Stelle (de uses "Stelle" elsewhere on this page). Same open question as `report.factorRole`. |
| `landing.hero.pileHint` | minor | Mixes a Sie-imperative with a bare infinitive ("Probieren Sie's — über den Stapel fahren"). Fine as a handwritten aside; flag only. |
| `landing.pricing.heading` | minor | "Winzige Preise" is a literal read of "Tiny prices"; "Mini-Preise" is the more usual German marketing form. |
## Queued by the 2026-08 `jobMarket` (/market) review

| key(s) | severity | note |
|--------|----------|------|
| `jobMarket.map.legendScale` | critical (fixed) | "(Mittelwert {mid})" claimed an **arithmetic mean**; `{mid}` is the midpoint of the colour scale (`(min+max)/2` in `MarketPulseAtlas.tsx:56`). Changed to "(Mitte {mid})". Confirm the wording; "Mittelwert" on a page publishing real wage statistics is a factual claim. |
| `jobMarket.orgTypes.subtitle`, `footer.coverage` | major (rewritten) | Both were rewritten for the ÚP-counts / ISPV-earnings split. The ISPV wage spheres (Czech *mzdová sféra* / *platová sféra*) are rendered "Unternehmens-" / "öffentliche Lohnsphäre"; RSCP is glossed with its Czech name. A native economist should confirm the German sphere terminology. |
| `jobMarket.map.median` "Medianverdienst" vs `map.a11yMedian` "Mediangehalt" | minor | Same figure, two words — inherited from the en source, which likewise says "Median earnings" on the tile and "median salary" in the a11y label. Left aligned to en. |
| `jobMarket.map.topRegions` | minor | "Meiste Stellen" is telegraphic; "Die meisten Stellen" or "Top-Regionen" may read better in the card heading. |
| `jobMarket.families.data_ai` | minor | "Daten & AI" here, "Data / AI" in `enums.family`. Same open AI/KI + loanword question as elsewhere. |

## Queued by the 2026-08 `simulation` (public guided demo) migration

| key(s) | severity | note |
|--------|----------|------|
| `simulation.phase.design` | minor | "Ausschreibung", not the glossary's "Stellenbeschreibung": the phase pills sit seven across and the glossary term overflows the row. The style guide sanctions shortening the label rather than the meaning, but this is the one place de departs from the termbase — confirm, or pick a shorter faithful alternative. |
| `simulation.status.done` | minor | "Fertig — Stelle besetzt 🎉" reframes en's "candidate hired" from the role's side, matching the cs call. `pipeline.controlCenter.hired` still says "Eingestellt 🎉". |
| `simulation.diagram.*` | minor | Diagram node labels are capped by the SVG box width, so compounds were kept short ("Multifaktor-Scoring", "Stellen-Entwurf", "Pool: Angenommen"). A native may prefer fuller forms if the renderer wraps them acceptably. |
| `simulation.criteria.*` | minor | Keeps the English loans the de style guide sanctions ("Skills-Match", "Karriere-Fit", "Kontext-Fit", "Interview-Scorecard") in a table that otherwise reads German. Consistent with the glossary, but a dense cluster. |

## Queued by the 2026-08-12 full sweep (no-dash recast + full MQM review)

Raised while recasting every dash-as-punctuation string in all four catalogs
and then auditing the whole catalog against the English source. High-confidence
fixes were already applied; these are the calls a native speaker should make.
Each row carries its typed MQM category and severity.

| key | category | severity | note |
|-----|----------|----------|------|
| `comms.devcaseFeedback.greetingFallback` | style | major | I set this to "zusammen" to stop the empty-string break and to match the sibling comms.there (de). But "Hallo zusammen," addresses a GROUP, and both keys are 1:1 candidate correspondence — style-de.md §Register requires addressing the candidate with Sie, singular. A German recruiter would write "Guten Tag," with no filler. Same structural problem as the cs entry above: it needs the greeting template forked for the no-name case, in both keys at once. |
| `match.shared.koReasonsNote` | grammar | major | I added a second {count} plural block to agree the verb (kam/kamen); the previous value had a dangling dash and no finite verb at all. Confirm "kam/kamen nicht in die engere Wahl" over "hat/haben es nicht in die engere Wahl geschafft". |
| `scheduleTab.prep.studentPlan.signalHint` | terminology | major | Was "Feedback-Phase" (drifted from en "coachability phase"); now "Phase zur Feedbackfähigkeit" to match studentInterview.coachability.label. Long for an inline hint — a native should confirm the length is acceptable or approve a shorter form that keeps the construct name. |
| `pipeline.tab.degradedBannerBody` | mistranslation | major | Pre-existing defect: the German value carried the English ICU branch literals `one {is} other {are}` verbatim. I restructured to `{count, plural, one {konnte nicht automatisch profiliert werden und ist ein nicht abgleichbarer Platzhalter} other {konnten … sind nicht abgleichbare Platzhalter}}` so the verb and the noun both agree. A native should confirm the fragment still reads correctly under the separately rendered 'N Bewerbungen' headline. |
| `about.students.scoringFootnote` | terminology | major | "gemintet" / "das Minting" is not German. The de catalog verbs an English noun that has no currency outside crypto. Candidates: "vergeben" / "Vergabe" (matching fr's attribution) or "prägen" if the coin metaphor is wanted. Same token appears in about.students.caseBadgeTitle and about.students.scriptFootnote — fix all three together, not one. |
| `pipeline.events.intake_degraded` | fluency | major | "die Erfassung konnte nicht automatisch profiliert werden" calques the English passive onto a noun that cannot be "profiliert" in German (you profile a person, not an intake). A native rendering would be closer to "aus der Erfassung konnte kein Profil erstellt werden". Left untouched because it also carries the contested Erfassung/Eingang term decision (see termNotes) and rewriting it half-sweeps that split. Same wording in pipeline.events.intakeDegradedDetail. |
| `library.tab.intake.tabLabel` | terminology | major | The whole de intake sub-cluster renders "role" as "Rolle" (tabLabel "Rollenaufnahme", ledgerTitle "Rollenaufnahme-Gespräche", intake.untitled / draft.untitled "Unbenannte Rolle", edit.titleField "Rollenbezeichnung", brief.role "Rolle", draft.aboutRole "Über die Rolle"), while the outer library namespace follows the glossary row "role / position / job -> Position / Stelle. Prefer this over 'role'" (library.tab.colRole = "Position"). cs ("pozice") and fr ("poste") are consistent; only de is split. This is a 6+ string sweep with no obviously good German for "Rollenaufnahme", so per glossary.md ("a half-sweep is worse than none") it needs one house decision before anyone touches it. |
| `comms.there` | accuracy | major | "zusammen" renders "Hallo zusammen," — a GROUP salutation ("Hi everyone") at the top of a 1:1 candidate email, and it is also substituted into the {candidate} slot of comms.interviewerBrief.body ("das Interview mit zusammen"). German for an unknown single recipient is a bare "Hallo," / "Guten Tag,", which the "Hallo {name}," template cannot produce. Same cross-key structural constraint as cs; needs a coordinated template change, not a value swap. |
| `jobs.compare.confidence.wide` | fluency | major | "breite Konfidenz" (and its sibling "moderate Konfidenz") is a collocation calque: in German the *Konfidenzintervall* is wide, not the Konfidenz. Natural options are "breites Konfidenzintervall" or, mirroring cs's transcreation, "breite/mittlere/enge Spanne". NOT fixed here because jobs.compare.confidence.tight ("enge Konfidenz") sits in another batch and a half-sweep of a three-value band set is worse than the split (glossary.md house note). Needs one coordinated decision across all three levels. |
| `decisions.aiReview.jdEditedBadge / decisions.wave.jdEditedBadge` | terminology | major | Same split in German: "Stellenanzeige {date} geändert" vs the glossary's Stellenbeschreibung (14 vs 110 hits catalog-wide). Left alone pending the same sweep decision. |
| `library.tab.intake.side.draft` | terminology | major | Same split in German: side.draft = "Anzeigenentwurf" vs draft.title = "Entwurf der Stellenbeschreibung", and glossary.md says Stellenbeschreibung. Left untouched for the same reason as cs (and because the key looks dead). Suggested value if swept: "Entwurf" — the tab spine is very tight (JdsIntakeLayoutTriptych.tsx renders the label vertically). |
| `library.tab.intake.tabLabel` | terminology | major | The whole de intake cluster renders "role" as Rolle (tabLabel "Rollenaufnahme", ledgerTitle "Rollenaufnahme-Gespräche", untitled "Unbenannte Rolle", edit.titleField "Rollenbezeichnung", brief.role "Rolle", draft.aboutRole "Über die Rolle", export.fileTitle "Rollenaufnahme"). glossary.md says role/position/job → Position / Stelle, and DE-ONE-WORD names exactly this as open drift, but unlike cs the German sweep is NOT recorded as done, so I did not half-sweep it. Note that "Positionsaufnahme" is clumsy; a native may prefer "Bedarfsaufnahme" for the intake feature. One decision + one sweep. (fr is already clean: Poste / Poste sans titre / Intitulé du poste.) |
| `scheduleTab.transcript.readbackHeading` | fluency | major | "Zurückgelesene Technologien" / "Zurücklesen" is a particle-for-particle calque of English "read back" (constructions-de.md DE-CALQUE-PREP); `zurücklesen` is not standard German in this sense. Left unchanged deliberately: the same coinage sits in three keys (readbackHeading, readbackNote, readbackUnconfirmedHint) and only two are in this batch — glossary.md warns "a half-sweep is worse than none". A native should pick one term (Wiederholung? Rückbestätigung? keep the Anglicism "Read-back"?) and it should be swept in one pass, with a glossary row added. |
| `diagrams.status.gap` | style | minor | "To build" → "Zu bauen" in a three-item status legend (Automatisiert / Menschliches Gate / Zu bauen). Grammatical but reads translated; "Noch offen" or "Geplant" may be the idiomatic German status-chip word. No anchorable rule, so leaving as-is. |
| `integrations.calendar.callback.state_mismatch.body` | terminology | minor | Out of batch, but it is the 4th member of the consent-screen cluster I just aligned ("Zustimmungsdialog"). It is already consistent with my fixes, so no action needed — flagged only so the cluster is not later swept to "Einwilligungsbildschirm" in three places and left here. |
| `aboutPage.steps.source.title` | style | minor | "eine gerankte Shortlist": "ranken/gerankt" is not Duden-standard and reads as a stylistic anglicism (constructions-de.md DE-LOANWORD: the word may stay English, but the grammar must be German). "sortierte" / "nach Rang sortierte Shortlist" would be safer, but HR-tech German does tolerate "gerankt". Native call. |
| `analyze.detach` | terminology | minor | "Ablösen" for "Detach" (unlinking a saved JD from the analysis form) reads as relieve/replace rather than detach. "Trennen" or "Lösen" would be the usual UI verb, but "Entfernen" is taken by analyze.remove in the same cluster. No glossary row for attach/detach — needs one decision, then one sweep. |
| `matrix.reasoningLoading` | fluency | minor | 'Eignung wird gelesen…' is a literal rendering of en 'Reading the fit…'; German does not 'read' a suitability score. 'Eignung wird ausgewertet…' would be idiomatic. No constructions-de ID covers it, so left as-is pending a native call. |
| `enums.commKind.schedule_invite` | length | minor | "Terminplanungseinladung" is a 22-char compound rendered in an uppercase pill (PipelineCommsList.tsx, text-meta). It wraps rather than overflows, so it is not broken, but style-de.md's length rule would prefer "Einladung zur Terminwahl" or "Terminvorschlag". Left alone — needs a native call on which term German recruiters use. |
| `agentsWorkforce.intro` | style | minor | The colon now governs a three-part list whose last member is a subordinate clause ("und ob sie die Erwartungen erfüllen, für die sie eingestellt wurden"). Mixing noun phrases and a dass/ob-clause after a colon is grammatical but heavy in German; a native should judge whether the last member wants to become its own sentence. |
| `data.erasedBody` | grammar | minor | The dash became a comma before an appositive participle phrase ("…der Entscheidung, ausschließlich aufbewahrt, um…"). The doubled comma-parenthesis around "ausschließlich aufbewahrt" is correct but reads bureaucratic (cf. DE-NOMINALSTIL); a native should weigh a second sentence ("Beides bewahren wir ausschließlich auf, um…") instead. This is candidate-facing GDPR copy, so the register matters. |
| `table.filters.allOf` | style | minor | "Alle ({column})" — with a long German column title this is the batch's tightest length budget (a dropdown clear-option). A native should eyeball it against the real column headers; if it wraps, the fallback is a bare "Alle". |
| `channels.comms.deadLetters` | terminology | minor | "Dead-Letter" / "Dead-Letters" is kept as an English loan with a German plural -s. A German ops team may prefer "unzustellbare Nachricht(en)". Decide once; the chip is short either way. |
| `setup.checklist.steps.company.body` | style | minor | "Name, Sprache und Marke: So sehen Kandidaten Sie." — the fronted "So" plus final "Sie" is correct but the pronoun stack at the end is easy to misread. "So nehmen Kandidaten Sie wahr." may land better. |
| `models.quality.na` | terminology | minor | Chose "k. A." (keine Angabe), the standard German table abbreviation, over "n/a". It carries an internal space, which could wrap in a narrow numeric column in ModelsQualityOverview.tsx (app/features/settings/models/ModelsQualityOverview.tsx:97). A native should confirm "k. A." over "–"/"n. v.", and if kept it may want U+00A0 rather than a plain space. |
| `landing.features.hint` | style | minor | This is deliberately lowercase handwriting in en/cs/fr; German capitalises the second sentence ("Alles schon heute verfügbar") because dropping the dash created one. The mixed casing across locales in the same visual element may look inconsistent — a native/designer call. |
| `models.useCaseDesc.role_intake_voice` | terminology | minor | Kept "Rollenaufnahme" for "role intake", but the glossary maps role/position to Position/Stelle and constructions-de.md already flags Rolle/Rollenband as open drift. Not swept here (not dash-related). Needs one coordinated decision plus a sweep. |
| `interview.voice.micTestListening` | style | minor | Changed "Hört zu" to "Wir hören zu." to match kp's 1st-person-plural system voice. A native should confirm the register is right for a mic-test readout rather than a neutral "Aufnahme läuft". |
| `analyze.almostThere` | style | minor | "Fast geschafft, Ihr Bericht wird verpackt" — the comma splice is faithful to the recast but a German progress line often prefers "Fast geschafft. Ihr Bericht wird verpackt…". Confirm whether this element takes a trailing ellipsis (sibling stage strings do). |
| `analytics.calibration.exclusionPipeline` | style | minor | The tail "Noch ausstehende, abgesagte, zurückgezogene und unbewertete Kandidaten sowie Kandidaten, deren Stelle geschlossen wurde, …" splits the English's flat five-item list into a list plus a relative clause because "role-closed" has no German adjective. It parses, but a native would probably prefer "Kandidaten mit geschlossener Stelle" inside the list. Carried over unchanged from the existing translation. |
| `analytics.compute.intro` | terminology | minor | "Nutzungs-Ledger" keeps the English "ledger" with a German hyphen compound; DE-HYPHEN/DE-LOANWORD would prefer either a fully German "Nutzungsjournal"/"Nutzungsprotokoll" or a closed "Nutzungsledger". Appears in three keys, so it needs one decision, not a per-string call. |
| `analytics.compute.perHireMultiWorkspace` | terminology | minor | Bare "Compute" as a German subject noun is un-declined and un-gendered (DE-LOANWORD). "Die Rechenleistung lässt sich nicht je Arbeitsbereich zuordnen" is likely better, and matches analytics.compute.intro which already says "Rechenleistung". |
| `comms.dataFooter` | style | minor | "Ihre Daten: Sehen Sie jederzeit ein, welche Daten wir über Sie speichern…" repeats "Daten" twice in one line. "Ihre Daten: Sehen Sie jederzeit ein, was wir über Sie speichern…" avoids the echo (DE-PLEONASM adjacent). Carried over unchanged. |
| `analytics.funnelGuide.body` | style | minor | "# Kandidat/in ist eingetroffen" / "# Kandidat/innen sind eingetroffen" uses the slash form throughout while the rest of the German catalog mostly says plain "Kandidaten". Needs one house call on German gender-inclusive forms rather than a per-string one. |
| `analytics.channels.pauseLine` | style | minor | I normalised "{sharePct}%" to "{sharePct} %" to match every other German percentage in this batch. Confirm the spaced form is the house convention for de (it is correct German typography, but the catalog was inconsistent). |
| `library.tab.ingestAsJobTitle` | fluency | minor | "…damit sie gesourct, gerankt und gematcht werden kann und Bewerbungen empfängt" chains four verbs; the final "und Bewerbungen empfängt" breaks the passive series. Correct German, slightly heavy — worth a native trim. |
| `library.tab.intake.lede` | style | minor | "Die Rolle nicht schreiben, sondern besprechen." is a verbless-imperative fragment carried over from the source's rhythm. Register-correct for a lede, but check it does not read as clipped in Sie-form context. |
| `library.tab.intake.attachments.empty` | terminology | minor | "eine Notiz aus dem Team" replaces the source's "a colleague's note" to avoid a gendered Kollegin/Kollege pile-up in a short empty state. Slight loss of specificity — native call. |
| `report.jdEditedTitle` | style | minor | 'nach der Bewertung dieser Kandidatin bzw. dieses Kandidaten' is heavy for a title tooltip. A native may prefer 'nach der Bewertung dieser Person'. Left as-is because gender-inclusive form is the catalog's existing convention here. |
| `simulation.step.offer.clickCaption` | style | minor | 'Der Recruiter klickt' forces masculine on an unknown operator. The cs sibling uses 'Naborar/ka'. A German inclusive form ('Die Recruiterin bzw. der Recruiter') is long for a caption; flagged for a house call rather than changed here. |
| `jobs.shared.skippedSummary` | style | minor | "Die Übrigen wurden normal gereiht" — reihen for ranking is Austrian-leaning; DE-DE would more likely say "normal sortiert" or "normal eingeordnet". The same verb recurs in jobs.candidates.earlyCareerNote and jobs.candidates.fairRankNote, so this is one decision affecting three strings. I kept the incumbent verb rather than sweeping it in a punctuation pass. |
| `jobs.posting.reopenTitle` | style | minor | "sourct die Pipeline neu" conjugates the English verb sourcen — grammatical in German HR jargon but jarring in a tooltip. Consider "sucht erneut Kandidat/innen für die Pipeline". Left as-is because "sourcen" is the glossary's kept loanword. |
| `jobs.posting.closeTitle` | style | minor | Capitalisation after the colon: I capitalised ("…stilllegen: Der Bewerbungslink…") because a full clause follows, but lowercased in the sibling reopenTitle where the clause has no subject. A native should confirm the pair reads consistently side by side in the footer. |
| `decisions.empty.hintPipeline` | style | minor | "alle Kandidat/innen" (inherited) uses the slash form, while every other de string in this batch says "Kandidaten" (e.g. decisions.groupEval.*, decisions.waveComms.banner). One inclusive-form convention should win catalog-wide; I did not change it to avoid a half-sweep. |
| `decisions.wave.commsFailedBadge` | fluency | minor | "Benachrichtigung fehlgeschlagen, manuell anstupsen" sits in a small inline badge next to an icon (DecisionsScreenWaveLists.tsx:93). German is the longest locale here; a native should confirm it does not wrap the badge, and whether "anstupsen" is the right register for a B2B tool (it renders "nudge" literally and recurs in decisions.waveComms.banner and decisions.wave.commsFailures). |
| `devcase.emptyLedger.control.hashChain.proves` | style | minor | The dash pair became a comma pair around a long "oder"-coordinated subject ("Ein Glied, das …, oder eine Client-Uhr, die …, macht …"). Correct German but comma-dense; a native may prefer splitting into two sentences. |
| `scheduleTab.aiRound.doneTitle` | style | minor | "Abgeschlossen (zu prüfen)" on a Station heading that also carries a count badge. Confirm the parenthetical does not overflow the station header at narrow widths; "Abgeschlossen" alone would fit but drops the call to action. |
| `devcase.checks.promptsNeverPenalty` | style | minor | "Es zählt, wie sie geführt wurde, nicht ob." — the elliptical trailing "nicht ob" mirrors en's clipped rhythm. Confirm a native does not want "nicht, ob sie genutzt wurde". |
| `devcase.integrity.markerAbsentTitle` | style | minor | Colon followed by a capitalised full clause ("…beweist das nichts: Die Zeile kann…"). German permits both cases after a colon; capitalised was chosen for a full sentence. Flagging for house consistency with the other colon recasts in this batch (baseline.proves also capitalises, honestDarkness does not because it follows with a noun phrase). |
| `pipeline.command.doneDroppedOut` | style | minor | The trailing clause was made count-invariant ('Beim Bestätigen war die Schwelle nicht mehr unterschritten, daher wurde niemand abgelehnt') so it works for =1 and other. fr took the same treatment ('le seuil n’était plus franchi'). Check the passive reads naturally in a toast. |
| `pipeline.command.doneHeldAtOffer` | terminology | minor | 'Weiterleiten stellt niemals ein' was ungrammatical-ish; rewritten as 'Weiterleiten stellt niemanden ein.' Confirm this carries the intended warning (advancing a candidate is not the same as hiring them). |
| `pipeline.tab.previewHeld` | style | minor | 'Zur Prüfung zurückgehalten (#). Gründe ansehen' is long for what renders as a single inline row next to a PauseCircle icon. German length budget worth an eyeball; the cs/fr equivalents fit. |
| `pipeline.controlCenter.getStarted` | style | minor | 'Legen Sie los: mit Ihren eigenen Positionen' keeps the colon-expansion shape of the new English. As a CTA the colon may read oddly in German; 'Legen Sie los, mit Ihren eigenen Positionen' or a single clause may be better. Same question for fr 'Lancez-vous : faites-le avec vos postes'. |
| `interviewSim.modes.student-case.label` | terminology | minor | Label says "fallbasiert" while interviewSim.caseDescCase and about.students.* say "case-basiert" / "case-designt" / "Demo-Fall". Two words for one concept inside one screen (DE-ONE-WORD). Native call needed on whether de keeps the English "Case" as a noun (it already does: "Demo-Fall" vs "Case der Stelle") or goes fully to Fall. |
| `agentFit.status.terminal.failed` | terminology | minor | "Entsendung" for dispatching a software agent reads military/HR-posting (Arbeitnehmerentsendung). Consider "Start fehlgeschlagen" or "Übermittlung fehlgeschlagen". Also agentFit.notConnected uses "Entsendung eines Agenten" — same decision covers both. |
| `onboarding.recordStampedFrom` | fluency | minor | "Erstellt aus der Vorlage <b>{template}</b>, der bisher einzigen." — the trailing elliptical dative apposition is grammatical but clipped. Check whether "…, der bisher einzigen Vorlage." reads better in the panel, or whether the appositive should become its own sentence as the other three locales' siblings do. |
| `report.panel.bucketBehavioral` | style | minor | The four bucket chips mix parts of speech: "Verhalten" (noun) / "Technisch" (adjective) / "Red-Flag-Abwehr" (compound) / "Sonstiges". Each is comprehensible, but "Verhalten (4)" names the concept rather than the question type; "Verhaltensbasiert" would parallel "Technisch". Not changed — no anchored error, and the chip length budget favours the short form. |
| `report.panel.tierMust` | style | minor | "Zuoberst gelistet" is correct but regionally marked and somewhat literary for a small pill; "Oben gelistet" is shorter and more neutral (style-de.md length rule). Left untouched as defensible. |
| `pipeline.result.task.rematch` | fluency | minor | "Neu-Matching" is a coined hyphenated compound. glossary.md pins de match(activity) = "Matching / abgleichen" but says nothing about the re- prefix. "Neues Matching" or "Rematching" may read better on the task chip; a native should rule. |
| `jobs.compare.confidence.tight` | terminology | minor | 'enge Konfidenz' is a calque; German statistics says 'enges Konfidenzintervall'. The call site is a tight inline qualifier, so the full term may not fit. A native statistician-recruiter should rule between 'enge Konfidenz', 'enges Intervall', and leaving it. |
| `jobs.table.colFamily` | terminology | minor | Bare 'Familie' as a column header has the same ambiguity class as the 'Modell' I fixed, but unlike 'Modell' it collides with no competing concept in the app and the adjacent filter label already says 'Berufsfamilie'. Left as-is; a native should decide whether 'Berufsfamilie' / 'Alle Berufsfamilien' is worth the extra width. |
| `jobs.ingest.resFailed` | accuracy | minor | 'nicht lesbar' (not readable) is a slight sense shift from "couldn't parse" — the ad was legible but unparseable. 'nicht auswertbar' is closer; both fit the result chip. |
| `jobs.coach.stat.eligible` | style | minor | 'Hürden genommen' as a stat-tile label reads as a completed action rather than a count category ('clear gates'). 'Erfüllen die Kriterien' is clearer but too long for the tile. Native call needed. |
| `pipeline.board.rankCandidates` | fluency | minor | "Kandidat/innen ranken" uses "ranken", which collides with the everyday verb meaning "to climb/vine". German HR-tech tolerates it colloquially, but a reviewer should rule between it and "Kandidat/innen reihen" / "Ranking der Kandidat/innen". Not anchorable to any current guide row, so not changed. |
| `library.tab.intake.columns.materials.cue` | fluency | minor | EN "They can be attached as materials" refers back to three plural/singular items; de uses a singular demonstrative and singular noun, "Das lässt sich als Unterlage anhängen". Generic "das" is idiomatic German for a whole set, so I did not touch it, but a native should rule between that and "Die lassen sich als Unterlagen anhängen". |
| `scheduleTab.prep.plan.signalDepth` | fluency | minor | "Tiefe hinter selbst angegebenen / Projektkenntnissen geprüft" — the first coordinate of the slash pair has no head noun and no suspension hyphen, so "selbst angegebenen" dangles. Candidate: "Tiefe hinter selbst angegebenen Kenntnissen und Projekterfahrung geprüft". Only anchor available is style-de's soft "readability wins" line, so left untouched. |
| `scheduleTab.interviewed` | fluency | minor | "Interviewt" as a bare participle section heading (ScheduleTabInterviewedList.tsx:38, followed by "· {count}"). Its sibling heading is the noun phrase "Ausstehende Interviews"; a native may prefer "Geführte Interviews". Grammatically valid, so not changed. |
| `scheduleTab.transcript.humanScorecard` | style | minor | "Menschliche Scorecard" is a literal pairing with "AI-Scorecard". A native may prefer "Scorecard (Mensch)" or "Scorecard des Recruiters" (the note key already says "Von einem Recruiter … erfasst"). Left alone because the AI/human parallelism is deliberate and cs/fr use the same pairing. |
| `library.tab.candidatesEmpty` | style | minor | "Generierte Positionen sourcen ebenfalls Kandidat/innen in die Pipeline." — "sourcen" as a conjugated German verb is recruiter jargon; the glossary only pins the noun "Sourcing". Left as is because the audience is recruiters, but a native German recruiter should rule between "sourcen" and "bringen … in die Pipeline". |
| `analytics.daysAvg` | style | minor | "Tage Ø" as a suffix under a number. The German convention is usually "Ø" in front (the sibling analytics.stageDwellRow already writes "Ø {days} T"). No anchored rule forbids the trailing form; left as is. |
| `diagrams.status.gap` | fluency | minor | "Zu bauen" for the roadmap status "To build" is grammatical but non-idiomatic as a status chip; German roadmap boards normally say "Offen", "Geplant" or "Noch umzusetzen". No anchored rule, and the neighbouring statuses ("Automatisiert", "Menschliches Gate") are already nominal, so a native should rule whether the nominal set should be completed. |
| `about.eyebrow` | fluency | minor | Bare "Über" as a nav eyebrow is the German About-dialog convention but reads as a dangling preposition outside that context; cs solved it as "O aplikaci". A native should decide between keeping "Über" and "Über die Plattform" / "Info" (length budget in the eyebrow is tight). |
| `analytics.advancedPct` | fluency | minor | "{pct}% über das Screening hinaus weitergeleitet" keeps English's "advanced past" preposition (constructions-de.md DE-CALQUE-PREP territory). "{pct}% nach dem Screening weitergeleitet" reads more German, but the current value is parseable and glossary-compliant (advance → weiterleiten), so I did not churn it. Native call wanted. |
| `analytics.automation.headline, analytics.advancedPct` | locale-convention | minor | German writes a space before % (DIN 5008); messages/de.json is split 31 spaced vs 13 unspaced catalog-wide, and cs/fr both use the spaced form in the same cluster. These two keys are unspaced. Not fixed here because a 2-of-13 edit is a half-sweep; needs one catalog-wide decision, mirroring the style-cs.md percent rule. |
| `analytics.automation.holds, analytics.automation.reviewHolds` | terminology | minor | "Zurückhaltung(en)" as a countable noun for a pipeline hold is unusual German (the everyday sense is restraint/reticence). glossary.md pins only the verb (hold → zurückhalten). Consistent across the cluster, so not touched; if a native rejects the noun it needs one catalog-wide sweep (e.g. Zurückstellungen), not a per-key fix. |
| `pipeline.controlCenter.stop` | style | minor | "Stopp" is a noun/interjection while every sibling control in the cluster is a verb (Pausieren, Fortsetzen, Zurücksetzen). "Stoppen" would align it. Not mechanical, and "Stopp" is attested on German buttons, so left for a native call. |
| `jobs.compare.confidence.tight` | fluency | minor | "enge Konfidenz" — *Konfidenz* standing alone is unusual German outside statistics prose; "enges Konfidenzintervall" is correct but far too long for the cell. Same re-labelling call as the French row; pair the two decisions. |
| `jobs.posting.rankInMatrix` | fluency | minor | "In der Matrix reihen" — *reihen* as a bare transitive is stilted; a German operator would expect "einordnen" or "In der Matrix ranken". No style-guide row anchors the lexical choice, so flagged rather than changed. Button in JobsPostingModalFooter.tsx, so the replacement must stay short. |
| `jobs.tab.allModes / jobs.tab.colMode` | terminology | minor | work mode rendered as "Modell" / "Alle Modelle". Internally consistent with filterWorkMode ("Arbeitsmodell"), but a bare "Alle Modelle" in a filter row beside families and seniority reads as "all models". "Alle Arbeitsmodelle" is clearer if the select can take the width. |
| `analytics.advancedPct` | fluency | minor | "{pct}% über das Screening hinaus weitergeleitet" calques "past"; "{pct}% nach dem Screening weitergeleitet" reads better. Left alone because "weiterleiten" is glossary-mandated and the current form is understandable. |
| `analytics.funnelGuide.waiting` | style | minor | The two ICU branches use two different inclusive-form styles ("die erste Kandidatin oder den ersten Kandidaten" vs "ihre ersten Kandidat/innen"). Harmonizing needs a house call on which inclusive form German uses; both are correct. |
| `pipeline.tab.saveViewModalSubtitle` | fluency | minor | "die aktuelle Suche, Filter und Sortierung" shares one article across three different heads; cleaner German repeats it ("die aktuelle Suche, die Filter und die Sortierung"). Left untouched as taste-adjacent. |
| `pipeline.drawer.timeline.byName` | fluency | minor | "per Name" as an uppercase chip (PipelineTimelineItemRow.tsx:76) — "nach Name" is the idiomatic German for a match performed by name. Chip is tight, both fit. |
| `pipeline.drawer.timeline.inviteDeclined` | style | minor | The de timeline alternates "Kandidatin oder dem Kandidaten" (3 keys, long) with "Kandidat/in" everywhere else, and uses "Gespräch" where siblings use "Interview". Both renderings are glossary-legal, so this needs one house call on the inclusive form before a sweep; the long form also strains the timeline row width. |
| `jobs.candidates.fairRank` | style | minor | "Faire Reihung" — *Reihung* is idiomatic in AT/CH but reads slightly unusual in DE-DE B2B software, where "Faires Ranking" is more common. The catalog is self-consistent (jobs.candidates.fairRankNote also says "gereiht"), so no change was made. Needs a DE-DE vs DE-AT house call before any sweep. |
| `decisions.compliance.covered5` | fluency | minor | "Aufbewahrungsfrist von {months} Monaten" breaks at months=1 ("von 1 Monaten"). cs already expands the plural; fr is safe (invariant "mois"). Default is 12 (decisionsComplianceState.ts:19) but the value is tenant-configurable. Suggested: {months, plural, one {# Monat} other {# Monaten}}. Not applied — below the major bar and not mechanical. |
| `decisions.wave.confirmTitle / confirmBody vs decisions.row.candidateCount` | terminology | minor | Two renderings of *candidate* inside one cluster: "Kandidat/in · Kandidat/innen" in the wave modal vs plain "Kandidat · Kandidaten" in the row count. The glossary prescribes the inclusive form, the catalog mostly uses the plain one. Needs a house call before either is swept. |
| `decisions.summary.notePlaceholder` | style | minor | "aber dünn beim Must-have-Stack" is a literal rendering of English "thin on". German would more likely say "aber schwach beim Must-have-Stack". No de constructions ID covers lexical calque, so not fixed under the gate — needs a native call. |
| `decisions.summary.unprovenProvenance` | terminology | minor | de collapses two distinct source concepts onto one word: unprovenTitle "Claimed but unproven" → "unbelegt" and unprovenProvenance "unverified" → "unbelegt". Candidates for the second: *ungeprüft* / *nicht verifiziert*. Not swept because the pair also appears in unprovenBoth. |
| `decisions.groupEval.skillStrongTitle` | locale-convention | minor | Percent spacing is split catalog-wide, not just here: cs 29 NBSP vs 48 plain space before %, de 24 vs 26, fr 49 narrow-NBSP vs 12 plain. Within this batch de/fr run "{pct} %" in unprovenStrengthTitle and "{pct}%" in skillStrongTitle/weight/salaryOver. Needs one house decision plus a single clean sweep; a partial fix here would deepen the split. |
| `analytics.bottleneck` | locale-convention | minor | After the colon in "<b>Engpass:</b> die <m>{count}</m> aktiven Kandidaten …" a complete sentence follows, which German orthography capitalizes ("Die"). No rule in style-de.md or constructions-de.md covers post-colon casing, so it is unanchored here; add the row if the house agrees. |
| `library.tab.intake.thinking` | style | minor | de progress labels are split in this cluster: "Denke nach…" (thinking) and "Verbinde…" (voice.connecting) are 1st-person singular, while every sibling uses the passive ("Wird gestartet…", "Wird erstellt…", "Wird gespeichert…", "Anruf wird ausgewertet…", "Wird dupliziert…"). cs has an explicit rule banning the 1st-person leak; de does not, so I left these. Suggested: "Denkt nach…" / "Wird verbunden…". |
| `library.tab.coachStageEyebrow` | leftover-source | minor | de keeps "Winnability-Coach" in English while cs ("koučem obsaditelnosti") and fr ("coach de recrutabilité") translate it. "winnability" is not in the do-not-translate list and has no glossary row, so this is a term decision rather than a defect I can anchor. Candidate: "Besetzbarkeits-Coach". Add a glossary row either way so the three locales stop diverging. |
| `pipeline.tab.statNeedsIntake and the whole intake cluster` | terminology | minor | Glossary pins intake → "Eingang", but every German string in this batch says "Erfassung" (statNeedsIntake, filterIntake, candidateRow.degradedSuffix, events.intake_degraded, events.intake_resolved). Internally consistent and arguably better German than "Eingang"; either update the glossary row or sweep the cluster, but not half of it. |
| `pipeline.modeFocus (de/fr, outside this batch)` | terminology | minor | Stragglers from the inclusive-form sweep I completed inside this batch: de.json:5842 "Kandidat:innen-Fokus" and fr.json:5842 "Focus candidat·e" are the last two colon/midpoint forms in the catalogs. The central writer should finish them so the sweep is whole. Same for de.json:4640 "Alle Senioritätsstufen", the last *Senioritätsstufe* left after the Seniorität unification. |
| `profile.matrix.filterAllSeniorities` | terminology | minor | "Alle Stufen" drops the term entirely while its filter-bar siblings name theirs ("Alle Rollenfamilien", "Alle Quellen"). Left alone because "Alle Senioritäten" is an awkward plural and the only catalog precedent is "Alle Senioritätsstufen" (de.json:4640), which conflicts with the Seniorität unification. Needs a native call. |
| `devcase.emptyLedger.controlsHeading` | style | minor | "Was eine versiegelte Einreichung mitbringt" — *mitbringen* for a document/record is colloquial; "Was eine versiegelte Einreichung enthält/enthalten wird" may read better as a META_LABEL heading. Accurate either way, so not rewritten (style-de has no rule against it); a native ear should rule. |
| `devcase.emptyLedger.control.canaries.name` | style | minor | "Platzierte Kanarienvögel" keeps the literal-bird metaphor of the EN source. German security writing often keeps "Canaries" / "Canary-Token". cs ("kanárky") and fr ("Canaris") also keep the metaphor, so the trio is at least consistent; left untouched because changing only de would split the cluster. |
| `devcase.emptyLedger.cta` | accuracy | minor | EN "Open the first case"; de "Ersten Fall anlegen" (= create), while cs/fr say Otevřít/Ouvrir. The call site (DevCasesEmptyLedger.tsx button → onDefine, shown at 0 cases) makes *anlegen* the truthful action, and "Fall öffnen" for a case that does not exist yet would be odd German. Left as-is deliberately; flagging in case the house wants literal cross-locale parity. |
| `report.panel.hideSofterGaps` | style | minor | "Weiche Lücken ausblenden" is as literal as the Czech was, and "weich" collides with Soft Skills; "Kleinere Lücken ausblenden" reads better. No German construction ID covers a plain calque, so left untouched rather than changed on taste. |
| `report.panel.tierMust` | style | minor | "Zuoberst gelistet" is grammatical but "zuoberst" is southern/CH-leaning and stilted in a badge; "Oben gelistet" / "Zuerst gelistet" are the neutral DE-DE options. Needs a native call. |
| `report.panel.kwMatched` | terminology | minor | The three keyword-status siblings mix word classes: "Treffer" (noun) · "Fehlt" (finite verb) · "Zu oft" (adverbial). Rendered uppercase so nothing breaks, but a parallel set (all nouns or all verbs) would read cleaner. Not changed: cs and fr mix the same way and the source does too. |
| `tasks.kind.batchScreen` | fluency | minor | "Alle gematchten Kandidat/innen" uses the declined anglicism "gematcht". Glossary gives de match(activity) = "Matching / abgleichen", but "abgeglichene Kandidat/innen" reads worse. "gematcht" is common in German recruiting SaaS; leaving as-is pending a house call. |
| `profile.archetypes.applyField` | fluency | minor | "Selbstauskunft bei Bewerbung (optional)" drops the article; "bei der Bewerbung" / "in der Bewerbung" reads more naturally. NOT changed on purpose: the sibling `profile.archetypes.applyClause` uses the identical telegraphic phrase, so fixing one of the two would create namespace drift. Needs one coordinated decision. |
| `report.panel.talkingPoints` | style | minor | 'Gesprächspunkte fürs Interview' — 'fürs' is a spoken contraction in a B2B panel heading (style-de.md formal-Sie register). 'für das Interview' or the compound 'Interview-Gesprächspunkte' would read more formal; not mechanical, so left for a native call. |
| `channels.title` | style | minor | "Wo Kandidat/innen hereinkommen" renders "come in" as physically walking through a door. "Wo Kandidat/innen ankommen" or "Woher Kandidat/innen kommen" may read better as a section headline. No rule anchors the choice. |
| `channels.comms.bouncedAt` | terminology | minor | "einen Bounce gemeldet" keeps mail-server jargon while its sibling channels.comms.statusBounced says "Unzustellbar". The glossary's *dead letter* row keeps "Dead-Letter" for de, so de may deliberately tolerate the jargon. Needs the house call. |
| `devcase.checks.canary.flagged` | terminology | minor | Same question as cs: "benannt" vs the glossary's "markiert". Decide once for the canary state triple (addressed / flagged / propagated) rather than per locale. |
| `devcase.probeStatus.handled` | style | minor | "gemeistert" / "nicht gemeistert" for handled / not handled reads slightly triumphal for a neutral status chip; "bewältigt" or "berücksichtigt" would be flatter. No rule anchors the register of a status chip, so left as written. |
| `devcase.lifecycle.stalledBadge` | locale-convention | minor | "stockt {days} T" abbreviates Tage as a bare "T"; German UI more usually writes "Tg." or "d". The badge (DevLifecycleRow.tsx:118, next to an 11px icon) is very tight, so the abbreviation may be deliberate. |
| `devcase.review.previewTitle` | style | minor | "Kandidatensichere Vorschau" coins an adjective for en's coinage "candidate-safe"; cs and fr both paraphrase ("Náhled pro kandidáta", "Aperçu côté candidat"). A German native should say whether the coinage lands or wants "Vorschau wie für Kandidaten". |
| `devcase.stage.ranked` | accuracy | minor | "sortiert" renders "ranked" as merely sorted; "eingestuft" carries the ordering. Left alone deliberately, and devcase.lifecycle.reSourceTitle was rewritten to "sortieren" to match — if a native prefers "eingestuft", both keys move together. |
| `landing.pile.jana.role` | terminology | minor | "React-Entwickler" here vs "React Developer" at landing.previews.salary.role — one job title, two renderings (DE-ONE-WORD). Both are idiomatic in the German market and I could not pick a direction without a house call; German postings commonly keep the English title, which argues for "React Developer" in both. Not changed. |
| `landing.pricing.tiers.free.name` | leftover-source | minor | de keeps "Free" while cs says "Zdarma" and fr "Gratuit". Defensible if "Free" is treated as a tier proper name alongside Starter / Growth / BYOM (which all stay English in every locale), but then cs and fr are the outliers. One house call, applied to all three. |
| `nav.tabs.about` | fluency | minor | Bare "Über" is a preposition with no object. German UI convention is "Info" (MS/Apple) or "Über die App"; cs went "O aplikaci", fr "À propos". No style-guide anchor covers it, so not rewritten. Nav-rail item, ~12 char budget. |
| `simulation.diagram.screen.reject` | locale-convention | minor | "Untere X% ablehnen" / "< Y% Match": DIN 5008 wants a space before %. cs and fr both spell it spaced ("spodních X %", "les X %"), so de is the odd one out, but style-de.md has no percent rule to cite. |

_116 items (13 major, 103 minor)._
