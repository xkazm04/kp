# Czech (cs) — native-review queue

Strings the first `/i18n-translate review cs` pass left for a **native speaker**
to confirm — subjective calls, unsettled domain/legal terms, or house-style
decisions. High-confidence fixes were already applied to `messages/cs.json`;
these are the remaining judgment calls (AI-flagged, need human sign-off).

| key | note |
|-----|------|
| `report.factorRole` | Scoring-factor chart label "Role" is left as the bare word "Role" (identical to en) — valid, naturalized Czech noun, but fr renders the same concept "Poste" (glossary's preferred role/position term) and de's "Rolle" was already flagged as ambiguous in the de review. Confirm whether this chart axis means "position fit" (→ "Pozice") or a narrower "role" concept distinct from the Jobs tab's "Pozice". |
| `report.panel.parserAi` | "AI parser" kept as an English compound (two words, no hyphen — fine in Czech). de compounds it "AI-Parser", fr translates to "Analyseur AI". "Parser" is common Czech tech jargon; native call on whether to keep as-is or use "AI analyzátor". |
| `enums.family.*` vs `jobMarket.families.*` | The 14 role-family names are translated slightly differently between the internal taxonomy (`enums.family`, e.g. "Zdravotnictví / klinické", "Služby v první linii") and the public market page (`jobMarket.families`, e.g. "Zdravotnictví", "Služby a obsluha") for the same underlying codes. The English source itself already diverges between these two namespaces ("Healthcare / Clinical" vs "Healthcare & Clinical", "Frontline / Service" vs "Frontline Service"), so this may be an intentional per-surface wording choice inherited from en rather than a cs-specific bug — flagging in case the source divergence itself should be reported upstream. |

_3 items._

## Queued by the 2026-08-04 coverage wave (hardcoded-EN-string sweep)

Externalized 43 previously-hardcoded English UI strings across 4 surfaces (see
run report). Two calls left for a native/house decision — high-confidence
translations were still applied so nothing regresses to English:

| key(s) | severity | note |
|--------|----------|------|
| `onboarding.hireLabel` | minor | Translated "Hire" (a meta-label above the waiting candidate's name in the empty-record card) as "Nový kolega" ("new colleague/hire"). Reads fine but is one word longer than the English label and slightly informal for a stat-card caption; a native may prefer a bare noun like "Nováček" or reuse "Kandidát". |
| `onboarding.firstDayTitle`, `onboarding.preboardQuestions` | minor | New ICU plurals using the "new hire" concept render it as "přijatý kandidát" / "nový kolega" respectively (two different nouns for the same referent, one per string, chosen for what read most naturally in each sentence). Confirm whether the catalog wants one fixed noun for "new hire" across onboarding, or per-sentence flexibility is fine. |

_2 items._

## Deferred to a future wave (not converted this pass)

Found during the same sweep, intentionally left hardcoded because the fix is a
bigger lift than a namespace-scoped externalization (see the run report for
detail):

- `app/_components/GithubAnalysisPanel.tsx` — no i18n wiring at all; extensive
  hardcoded English (headings, status copy, dynamic gap-analysis sentences).
- `app/_components/ErrorBoundary.tsx` — class component; React error boundaries
  can't call hooks directly, needs a hook-wrapper pattern first.
- `app/features/shell/tasks/TasksDoneRow.tsx`, `TasksFilterBar.tsx`,
  `tasksTabHelpers.ts` — no i18n wiring; deep-link labels into pipeline/decisions
  outcomes plus a `STATUS` label map.
- `app/features/hiring/onboarding/OnboardingTab.tsx` — a "prototype scaffold
  (throwaway)" empty-state variant switcher, hardcoded and possibly not meant to
  ship to all recruiters; flagged for a product call before localizing.
- `app/features/hiring/pipeline/PipelineAiActionsGrid.tsx` `ACTIONS.label`/`.note`
  — hardcoded but dead for rendering (real button text comes from
  `tActions(act.id)`); a cleanup candidate, not a live localization bug.
- Admin/dev-tools tree (`app/features/settings/organization/*`,
  `app/control/ControlRoom.tsx`, `app/features/tools/devcases/*`,
  `app/interview-lab/page.tsx`, `app/diagrams/PipelineExplorer.tsx`,
  `app/_components/puml/PlantUml.tsx`) — lower priority per the wave's scope
  order; not swept in depth this pass.

## Queued by the 2026-08 full-catalog review wave (Pass B + Pass C)

Severity is the MQM severity of the underlying error record. Everything here was
**deliberately not applied** — it needs a house/native decision, or the fix
belongs at a call site rather than in the catalog.

| key(s) | severity | note |
|--------|----------|------|
| `pipeline.command.placeholder`, `pipeline.command.examples` | critical | The localized Czech example commands are dead copy: `app/_lib/pipeline-command.ts:44,54,58` matches English keywords only (`/reject\b.*?\bbelow\s+(\d+)/` etc.), so a Czech command never parses. Fixing this is a product decision — either per-locale parser patterns, or mark the two strings non-translatable and say "(anglicky)". Not a translation fix. |
| `comms.theRole` | critical | The generic `{role}` fallback has no value that works in both frames. 10 cs templates frame it as "na pozici {role}" / "o pozici {role}" (needs accusative/locative — today's "danou pozici" fits), while 3 subject lines are bare appositives "Vaše přihláška — {role}" (needs nominative). en/de/fr leave `{role}` bare and dodge this. Needs split subject/body fallback keys, or dropping the "na pozici" frame. |
| `comms.*` greetings (13 keys) | major | All render "Dobrý den {name}," — Czech orthography sets the address off with a comma: "Dobrý den, {name},". Uniform across the namespace, so it is one sweep, and it changes every candidate email. Confirm before applying. |
| `comms.there` (second use) | major | Value is settled, but `comms-dispatch.ts:439,441` reuses the key as the *interviewer's* name fallback and as the `{candidate}` fallback in `interviewerBrief` — producing "Dobrý den kandidáte," addressed to a hiring manager. Needs a separate key, not a value change. |
| **`workspace`** — glossary row vs catalog (~10 keys: `*.eyebrow`, `nav.openWorkspace`, `channels.empty.comms.effort`, `workspaceAdmin.*`) | major | `glossary.md` says *pracovní plocha*; the catalog says *pracovní prostor* almost everywhere ("pracovní plocha" also reads as *desktop* in Czech computing). Four independent auditors flagged the glossary row as the stale side. Decide, then sweep once. |
| **`scorecard`** — glossary row vs catalog (~30 keys across `pipeline`, `decisions`, `scheduleTab`, `jobs`, `analytics`, `interviewSim`) | major | `glossary.md` keeps the loanword; the product UI says *hodnoticí karta* and only `landing`/`aboutPage` marketing copy keeps "scorecard". Same shape as the workspace row. |
| `enums.stage.Accepted` vs `enums.stage.Hired` | major | "Přijato" (intake) and "Přijat/a" (terminal) render as near-identical badges at both ends of the same stage strip. Candidate replacements for the intake end: "Doručeno", "Zaevidováno", "Nová přihláška". Highest-ripple enum in the catalog — needs sign-off. |
| `profile.evidence.*`, `profile.editor.intro`, `devcase.*` | major | The false friend **evidence**: Czech *evidence* = record-keeping/registry, not proof. The catalog is split — `report.panel.*` and `jobs.compare.*` were unified on **důkaz** this pass (a glossary row now exists), but `profile.evidence.*` and the devcase module keep the anglicism, possibly as deliberate jargon. Cross-namespace call. |
| `devcase.evalPanel.authenticityTitle` | major | The interpolated `{reasons}` are English sentences built in `app/_lib/devcase-authenticity.ts:88,97`, so a Czech tooltip wraps an English body. Not fixable in the catalog — needs a codes+params contract with the Python evidence producers. Already a named gap in `DevEvalPanelScores.tsx:61-66`. |
| `offer.deadline` + `offer.deadlineHours` | major | Both keys are correct in isolation (`deadlineHours` is exemplar 6), but `OfferClient.tsx:299` concatenates them, so cs renders "…14:00. zbývá 5 hodin." — a sentence starting lowercase, because the gold Czech form fronts the verb. Fix belongs at the call site. |
| `analytics.calibration.familyLabel` + 4 siblings | minor | "Rodina rolí" vs the glossary's role→pozice. Blocked on the existing `report.factorRole` question above — the taxonomy families are the same surface, settle both together. |
| `fit (result)` — `match.*`, `report.*`, `jobs.compare.*` | minor | Split between **shoda** (glossary) and **vhodnost**. Both are live in scored surfaces; too pervasive for a line-item fix. |
| `pipeline` grammatical gender | minor | The catalog treats the loanword as masculine in a handful of places ("samostatný pipeline", "do stejného pipeline") though standard Czech takes it as feminine ("ta pipeline" — the majority form, and what `aboutPage.steps.intake.*` was corrected to this pass). One sweep once confirmed. |
| `scheduleTab.lifecycle.reminderSent/reminderQueued/reminderPending` | minor | "připomínka" (recruiter side) vs "připomenutí" (candidate side, and `enums.commKind.*`). The `devcase.outboxKind.*` copies were unified on "připomenutí" this pass; the scheduleTab set was left alone pending a call. |
| `integrations.calendar.*`, `scheduleTab.calendar.google` | minor | Unified on **"Google Calendar"** this pass (glossary Do-Not-Translate: product proper nouns). The defensible alternative is Google's own Czech branding **"Kalendář Google"** (cf. "Disk Google"); the one thing that was certainly wrong — the calqued "Google Kalendář" — is gone either way. |
| `report.dispPass` | minor | "Pass" → "Zamítnout", which is the glossary's word for *reject*. Advance/Hold/Pass and Advance/Reject are distinct disposition sets elsewhere; confirm the collapse or pick a softer verb ("Nepokračovat"). |
| `skillProfile.eyebrow`/`.revoked`/`.stale` | minor | "doklad" for a signed, verifiable credential reads like an ID document or a receipt; "osvědčení" is the usual Czech. Public third-party-facing page, namespace-wide call. |
| `shortlist` | minor | Rendered "užší výběr" (`jobs.coach.verdict.*`) and "vybrat" (`match.results.*`, `match.card.*`). Both defensible; needs one call. |
| `landing.*` transcreation leftovers | minor | Landing is the most subjective surface, so only the outright errors were applied (`pricing.enterprise.blurb` "metrovaných", plus three terminology fixes). Left for a native: the headline calques `pricing.heading` ("Nulová matematika tokenů.") and `features.heading` ("jedno šťastné místo"), "stvrzenky" for *receipts* (`features.gates.*`), "Člověk v řídicí smyčce" vs the AI-Act register "lidský dohled" (`trust.human.title`), "ceny, které si zamknete" (`pricing.headingNote`), and the mixed EN/CS job titles in `landing.pile.*`. |
| `about.students.*` — "ražba"/"vyrazit" | minor | The coin-minting metaphor for skill provenance is carried literally into Czech. Comprehensible but unusual; a native may prefer "potvrdit"/"udělit". Cross-cutting, so not changed unilaterally. |
| `pipeline.drawer.fixedRubric`, `humanScorecardNote` | minor | "rubrika" in the assessment sense — in ordinary Czech it means a newspaper column. Confirm Czech HR usage accepts it, or move to "hodnoticí škála"/"kritéria". |
| `landing.pricing.tiers.*.price` | minor | "0 Kč / 240 Kč / 480 Kč / 120 Kč" are hardcoded currency strings, which `style-cs.md` forbids — but the **en source hardcodes them identically**, so this is a source-architecture issue, not a cs defect. See the en-source list in the run report. |

_22 items._

## Fixes applied this pass (for reference — no action needed)

- `comms.there` ("dobrý den" → "kandidáte"): the fallback substitutes directly
  into `"Dobrý den {name},"` when no name is known; the old value produced a
  doubled "Dobrý den dobrý den,". De/fr fallbacks were checked and don't have
  this bug (they use "zusammen"/"à vous", which fit their templates).
- Unified the "Hired" stage on the recruiter/internal side, which consistently
  rendered as "Najat/a" (enums.stage.Hired, pipeline.stageHelp.Hired,
  analytics.statHired/noHires/totalHired/colHired), with "Přijat/a" — the term
  already used on the candidate-facing status page and the one the glossary
  specifies. De and fr each use one root word for both surfaces; cs was the
  only locale split across two.
- `analytics.statTimeToHire` / `analytics.colHireRate`: same "najmutí" →
  "přijetí" root unification, for the same reason.
- `jobMarket.nav.menu` ("Nabídka" → "Menu"): the hamburger-menu aria-label
  collided with the app-wide term for a job offer ("Nabídka" = offer
  everywhere else). De/fr both keep "Menü"/"Menu" as a loanword here.
- `analyze.almostThere` / `analyze.workingOnProfile`: fixed two *tykání*
  (informal "ty") leaks — "tvoji zprávu" → "vaši zprávu", "tvůj profil" →
  "váš profil". Everywhere else in the catalog, and in de/fr, this flow
  addresses the candidate formally.
- `pipeline.tab.degradedBannerBody`: added the missing Czech "few" (2–4)
  plural category and corrected "other" (5+/0) to genitive-plural agreement
  — the existing string reused the "few" form for "other" too, which is
  ungrammatical Czech ("5 je nespárovatelné útržky" → should read "je
  nespárovatelných útržků").
- `analytics.exportCsv` (root) and `analytics.log.exportCsv`: "Export CSV" →
  "Exportovat CSV", matching the other 4 export-button instances in the
  catalog and the de/fr pattern of localizing the verb, not just the noun.
- `channels.relay.*` (6 keys) and `pipeline.tab.bulkDraftOutreachConfirm`:
  unified the untranslated English "relay"/"relay on" with the Czech "relé"
  already used two keys over in `channels.comms.relayNotConfigured` and
  `channels.comms.bouncedAt` — same delivery-relay concept, same Channels tab.
- `decisions.groupEval.interviewProbes`: "Otázky na pohovor" → "Otázky k
  pohovoru", to match the other two renderings of the same concept
  (`matrix.probes`, `match.shared.interviewProbes`).
- `jdPublic.archivedBanner/editJd/archiveTitle/unarchiveTitle/historyTitle`:
  5 keys drifted to "inzerát" (posting/ad) for the JD-editing UI, while the
  namespace's own eyebrow and every other namespace covering the *same*
  "editing a job description" concept (`library.tab.*`, `scheduleTab.prep.*`)
  consistently say "popis"/"popis pozice" — unified on "popis". (The separate
  "JD edited since this candidate was scored" notices in `report`, `pipeline`,
  `decisions`, and `library` correctly keep "Inzerát upraven" — checked against
  de/fr, which independently make the same posting-vs-description split at
  exactly those same points, so that one is a pre-existing, intentional
  cross-locale pattern, not a cs bug.)
- `onboarding.readyEmpty`: "jako Přijatý" → "jako „Přijat/a"" — this sentence
  tells the recruiter to look for the literal stage-badge text, so it now
  quotes the actual enum label instead of a paraphrase (matches de's approach
  of quoting the stage name here).
- `jobs.rediscoveryFeed.swept`: rewrote the plural block for the "jobs
  checked" count — the previous string paired a frozen genitive-plural
  adjective ("publikovaných") with a noun that only correctly declines for
  the genitive-plural count (5+), producing bad Czech for count=1 and
  count=2–4 ("1 publikovaných role" instead of "1 publikovanou roli"). The
  adjective now sits inside the plural block too, with one/few/other each
  getting the grammatically correct case.

## Fixes applied by the 2026-08 review wave (455 keys)

Full Pass B (typed MQM audit, every key, anchored findings only) + Pass C (gated
refine) over all 4,846 keys. Themes, largest first:

- **ICU number agreement (~55 keys)** — flat genitive plurals against live counts
  ("1 aktivních", "3 čeká", "4 týdnů"), and verbs/adjectives frozen *outside* a
  plural block while the noun declined inside it. Expanded to one/few/other with
  the verb or adjective moved into each branch, per exemplar 6.
- **Progress/busy labels (~30 keys)** — 1st-person-singular leaks ("Generuji…",
  "Ukládám…", "Zamítám…") converted to the verbal noun the rest of the catalog
  and `style-cs.md` use. New rule written into the style guide.
- **Terminology unification against the glossary (~90 keys)** — reject→zamítnout
  (odmítnout reserved for the candidate declining), advance→postoupit,
  role→pozice, sourcing→sourcing, board→nástěnka, relay→relé,
  per-hire→"na jedno přijetí", evidence→důkaz, tamper-evident→"odolný proti
  manipulaci", onboarding kept as the loanword, Google Calendar kept verbatim.
- **Percent typography (~12 keys)** — "{pct}%" → "{pct} %" for the nominal
  reading; rule added to `style-cs.md`.
- **Unknown-gender participles** — "nedostavil se" → "nedostavil/a se",
  "navrhl(a)" → "navrhl/a", "postoupil"/"zamítnut" in per-candidate rows.
- **False friends and near-misses caught by the audit** — "metrovaných plánů"
  (metered → *measured in metres*, on the enterprise pricing pitch);
  "Vedoucí" as the group-eval crown pill (reads as *manager*); "evidence" for
  proof; "nezfalšovatelný" for tamper-evident; "Fairness pojistka";
  "rozsahování"; "pokles" inverting a lower-is-better metric;
  "diplomová práce" for a third-year bachelor student's thesis.
- **Call-site-anchored predicate fixes** — 8 `pipeline.events.*` strings that
  render as `{candidateLabel} {verb}` had started a new clause instead of
  continuing the candidate as subject.
- **Placeholder-case defects** — `apply.script.koAuth`/`koModeLocation` put a
  nominative `{location}` behind "v" ("v Praha"); `koLang` used a singular frame
  for a joined language list.
- **`devcase.integrity.backdatedTitle`** — the one plural in the catalog whose
  branches differ by count but had no Czech `few`, so 2–4 fell through to the
  genitive-plural branch.

## Scope note

This pass combined a full-catalog automated sweep (ICU plural-category
completeness, *tykání* leaks, stray literal English, quote/ellipsis
typography, en-identical-value detection) with a complete manual read-through
of every namespace in `messages/cs.json` (~4,100 keys, cross-checked against
`en`/`de`/`fr` wherever a term choice looked inconsistent) — including the
large recruiter-facing ones (`pipeline`, `decisions`, `scheduleTab`, `library`,
`jobs`, `match`, `profile`, `channels`, `analytics`, `matrix`, `analyze`,
`models`, `setup`) that a prior pass had only covered with the automated
sweep. This is now a complete first full-catalog review; a future `sync` pass
only needs to cover keys added or changed after this point.

## Queued by the 2026-08-04 `aboutPage` (/about) review

Namespace-scoped pass over `aboutPage` only, after the page was relabelled
"About the app" / "O aplikaci". Framing verdict: **product**, not company — no
about-us copy in any locale. Six keys fixed; left for a native:

| key(s) | severity | note |
|--------|----------|------|
| `aboutPage.steps.*` — "role" | minor | The page says *role* seven times ("Popište roli", "pásmo role") where the glossary says **pozice**. NOT swept: 150 cs strings use the *role* form catalog-wide and the sibling `landing.features.offer.body` carries the identical "pásmo role × vhodnost". Same shape as the queued *workspace*/*scorecard* rows — one decision, one sweep, or leave. |
| `aboutPage.hero.title` | minor | Changed "celým náborem" → "celou pipeline" so the page's central emphasized term matches de/fr, `steps.intake` ("jedna pipeline") and the glossary's inline-loanword rule. The verb moved with it ("Proveďte jednu posilu" → "Projděte s jednou posilou", instrumental). Confirm it still reads as punchy Czech marketing. |
| `aboutPage.hero.title` — "posila" | minor | *one hire* as "posila" (a new addition to the team) is warm and idiomatic but slightly sportish; confirm it fits a public B2B page. |
| `aboutPage.hero.subtitle`, `closing.title` | minor | "podepsaný nástup" for *a signed hire*. Comprehensible, and de/fr carry the same conceit, but a native may prefer "k podpisu smlouvy". Left alone. |
| `aboutPage.steps.design.title` — "rubrika" | minor | Same open question as `pipeline.drawer.fixedRubric` above; not touched here. |
| `aboutPage.steps.interview.body` — "strukturovaný scorecard" | minor | Loanword kept per the still-open scorecard row; the masculine agreement rides on that decision. |

_6 items._

## Queued by the 2026-08-04 landing-page review (namespace `landing` only)

12 keys fixed in `messages/cs.json`; these were deliberately left alone.

| key(s) | severity | note |
|--------|----------|------|
| `landing.hero.title` | minor | "Nábor, který konečně **funguje**" swaps en's motion metaphor ("actually **moves**") for "works". de ("vorankommt") and fr ("avance") both keep the motion; cs is the only locale that drops it, and the whole page is built on momentum ("Hromada zmizí"). A punchier alternative: "Nábor, který se konečně **hýbe**." Defensible transcreation, so not changed unilaterally. |
| `landing.pile.jana.role` vs `.petr.role` | minor | "React Developer" (EN) next to "Datový analytik" (CS) inside the same three-card hero cluster. Realistic for Czech IT ads, but visibly mixed. de/fr both localize both. Already on this queue from the previous wave; still unsettled. |
| `landing.steps.drop.body` ("rubriky") | minor | Same "rubrika" question as `pipeline.drawer.fixedRubric` — settle together. |
| `landing.features.heading`, `landing.pricing.heading`/`.headingNote`, `landing.features.gates.*` ("stvrzenky"), `landing.trust.human.title` | minor | Carried over unchanged from the previous wave's landing entry — still native calls. |
| `landing.steps.call.body` ("změňte") | minor | "override" → "změňte" (change) loses the override sense; "přehlasujte" is stronger but stiffer on a CTA-ish list. |
| `landing.features.salary.body` / `steps.drop.body` | minor | "mzda" and "plat" both used for salary within one page ("Platový radar" vs "odhadu mzdy", "Mzdová rozpětí"). Both correct Czech; pick one for the landing surface. |
## Queued by the 2026-08 `jobMarket` (/market) review

| key(s) | severity | note |
|--------|----------|------|
| `jobMarket.hero.updated`, `hero.updatedNoPct`, `demand.openings` | critical (worked around) | The call sites pass `n` as an already-formatted **string** (`fmtInt()`, NBSP thousands separator), so an ICU `{n, plural, …}` block evaluates `"38 553" - 0 = NaN` and Czech rendered literally **"NaN volných míst"** on the hero and in the demand list. The plural blocks were replaced with the count-invariant genitive plural ("{n} volných míst", "{n} míst"), which is correct for every value the snapshot actually carries (min 20, national total 38 553) but wrong for a future 1–4. The real fix is at the call site (`MarketPulseApp.tsx:110-111`, `parts.tsx:232,303`): pass the raw number and let ICU format it, then restore one/few/other. |
| `jobMarket.map.median` vs `map.a11yMedian`/`legendSalary`/`hintBody` | minor | The visible tile says "Medián výdělku" (mirroring en's deliberate "Median earnings") while the screen-reader label and legend for the *same* figure say "medián mzdy" (mirroring en, which still says "median salary" there). The split is inherited from the en source; left in place rather than diverging from it. Czech ISPV usage sanctions both ("hrubá měsíční mzda" is the survey's own term). Settle together with the en source. |
| `jobMarket.salary.eyebrow` | minor | "Přehled" → "Průvodce" (en "The field guide", de "Der Wegweiser", fr "Le guide de terrain"). A native may want to carry the field-guide metaphor further ("Terénní průvodce", "Malý průvodce"). |
| `jobMarket.families.hr_people` | minor | "HR a lidé" (a calque of "HR & People") → "HR a personalistika". `enums.family.hr_people` says "HR / lidské zdroje"; the two surfaces still differ, as they do in en. |
| `jobMarket.families.*` vs `enums.family.*` | minor | Still the divergence already queued above; this pass only shortened `life_sciences_research` ("Vědy o živé přírodě a výzkum" → "Věda a výzkum") because the old value overflowed the JD filter chip. |

## Queued by the 2026-08 `simulation` (public guided demo) migration

The guided demo (`/api/demo` → `/?sim=auto`) moved off English into the new
`simulation` namespace. High-confidence translations were applied; these are the
judgment calls.

| key(s) | severity | note |
|--------|----------|------|
| `simulation.status.done` | minor | Transcreated rather than translated: en "Done — candidate hired 🎉" → "Hotovo — pozice obsazena 🎉". A literal read needs the gendered participle ("kandidát/ka přijat/a"), which is heavy on a celebratory one-liner; naming the outcome from the role's side dodges it. Confirm the register — the sibling `pipeline.controlCenter.hired` still says "Přijat/a 🎉". |
| `simulation.phase.*` | minor | The 7 stepper pills are tight, so the chronology is nominalized ("Popis pozice · Sourcing · Příjem · Screening · Pohovor · Nabídka · Přijetí") where en mixes verbs and nouns ("Design JD · Source · Intake…"). Confirm "Příjem" for *Intake* — it is also the everyday word for reception/admission. |
| `simulation.weight.*` | minor | "Klíčová / Střední / Malá" agree with the column header "Váha" (f.). If that header is ever renamed the adjectives must move with it. |
| `simulation.criteria.*.source` | minor | The narrow `whitespace-nowrap` column keeps "CV ↔ JD" untranslated and shortens the qualified forms ("CV ↔ dovednosti", "CV ↔ role"). Native call on whether "JD" is transparent enough in a Czech UI that otherwise says "popis pozice". |
| `simulation.log.*` participles | minor | Uses the house "/a" form for unknown gender ("reagoval/a", "prošel/a", "uvázl/a"). Dense in a log line that already carries a name; an impersonal rewrite is possible but loses the subject. |

## Queued by the 2026-08-12 full sweep (no-dash recast + full MQM review)

Raised while recasting every dash-as-punctuation string in all four catalogs
and then auditing the whole catalog against the English source. High-confidence
fixes were already applied; these are the calls a native speaker should make.
Each row carries its typed MQM category and severity.

| key | category | severity | note |
|-----|----------|----------|------|
| `billing.meters.case_designs` | terminology | major | The devcase "case" concept is rendered three ways across this batch: "Návrhy úloh" here, "praktický úkol" in models.useCaseDesc.devcase_case_design, "Případ" in models.quality.colCase, and style-cs.md itself cites "Vývojové případy" as the shipped nav label. glossary.md has no row for it. This needs one decision plus one full sweep across namespaces; a fix inside this batch alone would be the half-sweep glossary.md explicitly bans. |
| `integrations.calendar.connectHint` | fluency | major | "souhlasná obrazovka Google" is an adjective calque; *souhlasný* means "concurring/agreeing", not "of consent". Google's own Czech docs say "obrazovka souhlasu (OAuth)". NOT fixed here because the rendering is internally CONSISTENT across four keys (connectHint, partialBody, callback.cancelled.body, and the out-of-batch callback.state_mismatch.body) and glossary.md warns "a half-sweep is worse than the split". Needs one coordinated sweep of all four to "obrazovka souhlasu". |
| `comms.theRole` | fluency | major | Cross-key construction break I could not fix inside this batch. cs renders "the role" as the ACCUSATIVE "danou pozici", but the cs templates that consume it (comms-dispatch.ts lines 177/265/318/372/422/468/499/534 all resolve `entry.jobTitle ?? t("theRole")`) already supply the head noun themselves: comms.rejection.opening says "o pozici {role}", comms.interviewConfirmation.normal and comms.ack.bodyRole say "na pozici {role}". For an anonymous entry that renders "o pozici danou pozici" / "na pozici danou pozici". No single fallback string satisfies all consuming templates (comms.onboarding.body wants "jako {role}", comms.outreach.subjectFallback wants a bare nominative), so the fix is a coordinated pass over the cs comms templates plus this fallback, not a one-value rewrite. Left untouched deliberately. |
| `comms.there` | fluency | major | CS-GENDER (constructions-cs.md: "A bare masculine is a defect, not a default"). The anonymous-greeting fallback "kandidáte" is a masculine vocative rendered into "Dobrý den, {name}," for a candidate of unknown gender. The slash form ("kandidáte/ko") is unusable in a vocative, and Czech has no clean neutral vocative here; the idiomatic solution is to drop the address entirely ("Dobrý den,"), which the fixed template shape does not permit. Needs a native/product call on whether the cs greeting template should be forked for the no-name case. |
| `jobMarket.families.life_sciences_research` | accuracy | major | en "Life Sciences & Research" renders as generic "Věda a výzkum", losing the life-sciences specificity that de ("Life Sciences & Forschung") and fr ("Sciences du vivant et recherche") keep. Counter-argument: "Věda a výzkum" is the established Czech job-portal category and pairs cleanly with the separate "Zdravotnictví" family. Left untouched pending a native call — a 16-family locked catalog must not get two overlapping research buckets. |
| `enums.stage.Accepted` | terminology | major | Confirms the item already queued in review-cs.md line 68: "Přijato" (intake) and "Přijat/a" (Hired) sit at both ends of the same stage strip and read as the same word, while de (Angenommen/Eingestellt) and fr (Accepté/Recruté) stay distinct. Not fixed here — the review file marks it "highest-ripple enum in the catalog, needs sign-off". Candidates: Doručeno / Zaevidováno / Nová přihláška. |
| `landing.previews.gates.signed` | grammar | major | Carried over the pre-existing "podepsala {name}" — a hard feminine participle for an interpolated name. The sample data is "M. Horáková", but {name} is a placeholder and CS-GENDER forbids forcing a gender on an unknown person. Should be "podepsal/a {name}". Left unchanged because the dash rewrite did not touch that clause; flagging rather than doing a drive-by. |
| `analytics.calibration.exclusionPipeline` | grammar | major | CS-GENDER: the chain "postoupil (došel k pohovoru, nabídce nebo byl přijat), nebo v něm byl zamítnut" forces masculine on a candidate of unknown gender. The correct forms are postoupil/a, došel/došla, byl/a přijat/a, byl/a zamítnut/a, but four slash-forms in one clause make the sentence unreadable. This needs a native rewrite into an impersonal frame ("jakmile je o kandidátovi rozhodnuto: buď postup dál…, nebo zamítnutí"), which is outside a punctuation pass. |
| `library.result.lintManyMustHaves` | grammar | major | "{count} „povinných“ požadavků" is frozen genitive plural and is wrong for 1 and for 2–4 (CS-NUM). Left as-is because the en source is flat; reported as a source defect. Fix needs an ICU plural added to en first. |
| `report.compare.fromLabel` | grammar | major | Reduced to a bare 'z' before an uninflected variant label ('Shrnuti z CV_v2.pdf'). Grammatically loose because the source label is never inflected; this is a source defect (see sourceDefects) rather than a translation choice, but a native should judge whether 'z' or 'zdroj:' reads better under the concatenation. |
| `scheduleTab.lifecycle.proposedTimes` | terminology | major | Changed "odmítněte" → "zamítněte" per the glossary's recruiter/candidate split. This is a recruiter refusing candidate-proposed slots, so "zamítnout" is right, but the sibling ScheduleInviteAttentionSection decline button copy should be checked for the same word. |
| `about.students.scoringFootnote` | terminology | major | "ražba" / "vyrazil dovednosti" is literally minting metal coins and reads as a machine rendering of the English metaphor (CS-CALQUE). A Czech HR reader will not decode it. Suggest "udělit / udělení dovedností" or "přiznat". Affects about.students.caseBadgeTitle, scoringFootnote and scriptFootnote consistently, so it is one sweep, not three edits. |
| `scheduleTab.prep.studentPlan.scenario` | fluency | major | Composed with {who} = " pro <jméno> (<pozice>)" (app/_lib/student-interview.ts:134 + prep.plan.forCandidate = "pro"), so the sentence renders "Šestifázový screening pro začínající talenty na 45 minut pro Jan Novák (…)" — two "pro" with different referents in one clause. Proposed fix: open with the genitive, "Šestifázový screening začínajících talentů na … {who}". No constructions-cs ID covers repeated-preposition clash, so I did not act; if a native confirms, this is a new CS-rule row rather than a one-off. |
| `comms.there` | fluency | major | Fallback for BOTH the greeting name and the {candidate} slot (app/_lib/comms-dispatch.ts: `input.name ?? t("there")`, `extractRecipientName(assigned) ?? t("there")`, and `candidate = entry.candidateLabel \|\| t("there")`). cs "kandidáte" is a masculine-only vocative: it renders "Dobrý den, kandidáte," (gendered, against style-cs.md "prefer the neuter/impersonal or the /a form") and, in the interviewer brief, the wrong case: "Kandidát/ka: kandidáte". No single Czech word satisfies both a vocative greeting slot and a nominative name slot; fixing it needs the templates changed together, which is out of this batch's reach. |
| `comms.theRole` | fluency | major | theRole/aRole are the fallback for {role} in ~10 dispatchers. The cs (and de/fr) templates wrap the placeholder with scaffolding the English lacks, so the fallback double-prints and lands in the wrong case: comms.rejection.opening "o pozici {role}" → "o pozici danou pozici"; comms.ack.bodyRole "na pozici {role}" → "na pozici danou pozici"; comms.onboarding.body "jako {role}" → "jako danou pozici". de "die Stelle" → "Interesse an die Stelle" (needs dative); fr "le poste" → "pour le poste de le poste". Every candidate fallback value is wrong in at least one slot, and most of the affected templates are outside this batch, so this needs one coordinated decision (either drop the scaffolding or drop the fallback) rather than a local edit. |
| `jobs.tab.eyebrow / jobs.tab.launchpad.body` | terminology | major | Intra-namespace split on *workspace*: eyebrow says "Pracovní prostor", launchpad.body says "zbytek pracovní plochy". This is exactly the contested row glossary.md flags ("glossary says pracovní plocha, the catalog overwhelmingly says pracovní prostor … Do not unify either until it is settled — a half-sweep is worse than the split"). Left untouched by design; needs the house decision, then one catalog-wide sweep. |
| `pipeline.drawer.fixedRubric` | terminology | major | "Pevná rubrika" (and "podle rubriky" in the sibling humanScorecardNote): Czech *rubrika* primarily means a newspaper column/section, not a scoring rubric. de/fr correctly say Bewertungsraster / grille. Candidate: "Pevné hodnoticí schéma" or "hodnoticí rubrika". Two keys, so it needs one decision + a glossary row, not a one-off. |
| `jobs.compare.offRubric` | terminology | major | cs "rubrika" (and de "Rubrik") for a *scoring rubric* is a false friend: Czech rubrika / German Rubrik mean a section or column heading, not an assessment grid. fr got it right with "grille". BUT the chip already matches its own tooltip (jobs.compare.offRubricTitle: "…není v aktuální rubrice kohorty" / "…nicht in der aktuellen Rubrik der Kohorte"), so fixing the chip alone creates drift. Needs one coordinated cs+de sweep across offRubric / offRubricTitle / humanOnlyTitle ("rubriky přípravy", "Vorbereitungsrubrik") plus a glossary row for *rubric*. Bonus: uppercased "AUSSERHALB DER RUBRIK" is long for the tight sticky-left cell in JobsCompareInterviewsCohortTable.tsx:141. |
| `decisions.aiReview.jdEditedBadge / decisions.wave.jdEditedBadge` | terminology | major | cs says "Inzerát upraven {date}" (job ad) where glossary.md rules JD → "popis pozice". NOT fixed: the whole catalog is split (56 hits of inzerát* vs ~85 of popis pozice*), so touching 2 keys is the half-sweep the glossary explicitly warns against. Needs one house decision then one sweep. Note the badge budget is tight (History icon + text-meta pill, DecisionsAiReviewCard.tsx:145). |
| `analytics.channels.colCpa` | terminology | major | glossary.md says cs *uchazeč* is reserved for the PORTAL name and "the person inside the pipeline is always kandidát", but the whole cs cost-per-applicant cluster uses uchazeč (colCpa, channels.intro, channels.cpaWindowedNote, analytics.koDeclinedLine, analytics.funnelEmpty, analytics.funnelGuide.questionScreened). This is a glossary-vs-catalog split like workspace/scorecard: needs one ruling then one clean sweep, not a half-sweep inside one batch. |
| `library.tab.intake.side.draft` | terminology | major | cs renders JD two ways INSIDE one tri-pane: side.draft = "Návrh inzerátu", draft.title = "Návrh popisu pozice". Not fixed here because it is a catalog-wide split, not a local slip: messages/cs.json has 56 strings using "inzerát" (errors.JD_LIST_FAILED, errors.JD_LOAD_FAILED, report.jdEditedBadge, jobs.ingest.pasteAd, jobs.tab.launchpad.route2Title…) against 56 using "popis pozice". glossary.md says popis pozice, but a half-sweep is explicitly worse than the split. Needs one house decision then one clean sweep. (Also: library.tab.intake.side.* appears DEAD — no useTranslations("library.tab.intake.side") anywhere in app/; the triptych uses library.tab.intake.columns instead. Worth grepping before spending a sweep on it.) |
| `profile.evidence.evidenceTitle` | terminology | major | The known false friend. Czech *evidence* means record-keeping, not proof; glossary pins evidence → **důkaz** but flags `profile.evidence.*` as "queued". The cs catalog is genuinely split: evidenc* 43 / důkaz 50 / doklad 11, and this very batch shows all three (evidenceTitle "Evidence (…)", addEvidence "+ evidence", checks.has_project_or_thesis "váš nejsilnější doklad", editor.intro "každou evidenci … proveniencí"). Fixing only the ~7 keys in this batch would be exactly the half-sweep the docs warn about. Needs one house decision (důkaz vs doklad) plus one 43-site sweep. Affected here: profile.evidence.evidenceTitle / addEvidence / evidenceKindAria, profile.editor.intro. |
| `profile.archetypes.fairnessProtectedAria` | accuracy | major | "Chráněno spravedlností" (and fr "Protégé par l’équité") reads as *protected BY fairness*; the concept is a dimension protected FOR fairness reasons (a protected attribute). de "Fairness-geschützt" keeps the source's compression and is fine. No anchor lets me pick a replacement confidently; a native should rule between e.g. "Chráněno kvůli spravedlnosti" and "Chráněný atribut". Same for profile.archetypes.fairnessProtected. |
| `nav.ariaLabel` | terminology | major | workspace rendered two ways inside ONE namespace: nav.ariaLabel = "Pracovní plocha", nav.openWorkspace = "Otevřít pracovní prostor". glossary.md explicitly forbids unifying until the house settles plocha vs prostor ("a half-sweep is worse than the split"), so deliberately left untouched. Cleanest evidence yet for settling it: both variants sit in the same nav component. |
| `resilience.notFoundCta / resilience.home` | style | minor | "Zpět domů" is a literal rendering of "Back to home"; de/fr both name the page ("Startseite" / "l’accueil"). "Zpět na úvodní stránku" would be the MS-Czech idiom. Left alone because the catalog is consistent at 3/3 occurrences and the third (messages/cs.json:1093) is outside this batch — changing two of three would create exactly the half-sweep drift CS-ONE-WORD warns about. |
| `resilience.errorDigest` | accuracy | minor | "Kód chyby: {digest}" reads as an error *code* (a 404-style enum) whereas {digest} is a Next.js trace reference. "Reference chyby" is more accurate but stiffer; a native should rule whether the accuracy gain is worth the register cost. |
| `branding.previewBadge` | terminology | minor | "Badge" rendered as "Odznak" (a physical pin/insignia). de and fr both keep "Badge". Czech UI practice is split between "odznak" and "štítek"; there is no glossary row, so this needs a house call rather than my taste. Sibling `about`/`profile` badge strings should be swept together once decided. |
| `offer.declineConfirmBody` | style | minor | "Tuto akci nejde vrátit zpět." — "nejde" is neutral-to-colloquial; "nelze" is the register a B2B legal-consequence confirmation usually takes. style-cs pins register only for address (vykání), not lexical formality, so this is a house-voice call. |
| `offer.prompt` | style | minor | "kdybyste se přidali k nám do společnosti {company}" doubles the destination ("k nám" + "do společnosti X"), which the English does not. "kdybyste se přidali do společnosti {company}" is tighter, but the redundancy is idiomatic enough that I did not want to churn a clean-meaning candidate-facing string. |
| `models.routing.reset` | terminology | minor | "Obnovit" for Reset sits in a row next to "Uložit" and "Otestovat" and can read as Refresh rather than Reset-to-default. "Resetovat" or "Vrátit výchozí" would be unambiguous, but there is no glossary row for reset and the sibling models.routing.resetTitle already explains the action. Needs a house call, not a unilateral change. |
| `models.useCases.grounded_salary` | accuracy | minor | "Tržní odhad mzdy" (market estimate of pay) adds "market" and "estimate", neither in "Grounded salary"; de "Fundiertes Gehalt" and fr "Salaire étayé" stay literal. The transcreation may well be the better label for the cs market (mzda is glossary-correct), but it is an addition a native owner should confirm. |
| `workspaceAdmin.intro` | terminology | minor | "tenant" is rendered as the raw loan "tenantů" in cs while de says "Mandanten" and fr "locataires". There is no glossary row for tenant/tenancy and fr "locataire" (= renter) is itself debatable for multi-tenant SaaS. Needs one glossary decision, then one sweep; not touched. |
| `workspaceAdmin.createPlaceholder` | terminology | minor | "Název nového prostoru" drops "pracovní" while every sibling in the cluster says "pracovní prostor". Left alone: it is an input placeholder with a real length budget, and the workspace term itself is the contested row glossary.md tells us not to unify yet (glossary says *pracovní plocha*, catalog says *pracovní prostor*). |
| `integrations.personas.title` | style | minor | "Personas bridge" → cs "Most Personas" (de "Personas-Brücke", fr "Pont Personas"). *Most* is the civil-engineering bridge; a Czech native may prefer "Můstek Personas" or keeping the English feature name. No anchor exists, and the three locales are at least consistent, so unchanged. |
| `workspaceAdmin.members.inviting` | accuracy | minor | en "Inviting…" → cs "Odesílání…" (= "Sending…"), where de/fr both keep the invite verb. It is a correct verbal-noun progress form per style-cs.md and defensible in context (an invite email is being sent), so not rewritten; flagging in case the house wants the cluster verb kept. |
| `apply.script.skillsPrompt` | terminology | minor | Three keys carry English "(comma-separated)": this one says "(oddělené čárkami)", apply.gapFields.hasLanguages.prompt and min3Skills.prompt say "(oddělte čárkami)". Both are correct Czech; CS-ONE-WORD wants one. Left alone as a non-mechanical minor. |
| `legal.privacy.subprocessors.trustLink` | style | minor | "na stránce důvěry" is a literal rendering of "the trust page". No surface in any locale is actually named that: landing.nav.trust is "Odpovědná AI" / "Verantwortungsvolle AI" / "AI responsable". de "Trust-Seite" and fr "page de confiance" have the same problem. Fixing it properly means naming the real section, which is a copy decision above the translator (see sourceDefects). |
| `legal.privacy.consent.expiry` | accuracy | minor | "jméno se zkrátí na neidentifikující zkratku" renders "handle" as "zkratka" (= abbreviation). The sibling legal.privacy.rights.carveout uses "maskovanou, neidentifikující značkou" for the same masked reference. Possibly one concept, two words, but the two sentences describe different artifacts; left alone pending a native read. |
| `data.appliedOn` | fluency | minor | "Přihlášku jste podali {date}." uses the plural participle as a gender-neutral vykání workaround, while style-cs.md prescribes the "/a" form for unknown gender ("podal/a jste"). The whole candidate-facing data.* cluster (also data.held.answers "jste uvedli") is consistent on the plural, so this is a house convention to confirm or sweep, not a one-off. |
| `results.softSignals.probe` | accuracy | minor | "Otázka:" flattens "Probe:" to "Question:", losing the follow-up/dig-deeper sense that de "Nachfrage:" and fr "À creuser :" both keep. "Doptat se:" or "Na co se doptat:" would carry it, but the label sits inline before the probe text and length matters. |
| `analyze.blindTitle` | terminology | minor | "trust ledger" is rendered three different ways across locales: cs "kontrolní záznam", de "Trust-Ledger" (loanword kept), fr "registre de confiance". No glossary row exists. Also cs "odstranit" renders "redact" as plain removal, where de "schwärzen" and fr "masquer" keep the redaction sense. |
| `agentFit.coverage.automatable / .assisted / .unknown` | fluency | minor | These render inline immediately after a free-text responsibility string (JobsAgentFitCoverage.tsx:61-64), so the head noun's gender is not knowable. cs mixes long neuter adjectives ("automatizovatelné", "asistované") with a short neuter passive ("nezařazeno"); fr agrees them all feminine with *responsabilité*. constructions-cs.md CS-ABLE warns that a bare -able adjective dangles in the neuter. Impersonal neuter is probably the safest choice for cs, but the mixed long/short forms want a native ruling before I unify them. |
| `schedule.booking` | style | minor | Busy label "Rezervace…" is a deverbal noun but reads as "reservation", not as progress. style-cs demands a verbal noun ("Ukládání…") and CS-PROG allows the reflexive ("Rezervuje se…"). Both replacements are clunkier than the incumbent; not changed on my own taste. |
| `schedule.rsvpPrompt` | fluency | minor | "Můžete stále dorazit?" reads as an SVO calque of "Can you still make it?". Native alternatives ("Platí to stále?", "Můžete se stále zúčastnit?" — the latter pairs with the sibling answer "Nemohu se zúčastnit") are judgement calls, not anchorable errors. |
| `errors.SESSION_TOKEN_REQUIRED` | accuracy | minor | cs drops "apply" from "a different apply link" ("patří k jinému odkazu"), where de says "Bewerbungslink" and fr "lien de candidature". The next sentence disambiguates, so this is a minor omission, not mechanical to fix. |
| `matrix.popoverAria` | fluency | minor | 'Proč {cand} skóruje na {pos}' — 'skórovat' reads as scoring a goal in sport; the data-scoring sense is emerging in Czech business usage but is not settled. Candidate rewrite: 'Proč má {cand} toto skóre na pozici {pos}'. No constructions-cs ID covers a bare verb-choice calque, so not fixed. Low visibility (aria-label). |
| `matrix.staleBody` | style | minor | 'poté, co byl tento odkaz sdílen' is a short passive with a real subject (CS-PASS territory), but the conjunction 'poté, co' is standard and every reflexive/active recast tested worse. Flagged rather than churning a clean-enough sentence. |
| `setup.steps.company.title` | style | minor | 'Ať je to vaše společnost' renders en 'Make it your company' as a wish rather than an instruction. Understandable but limp for a wizard step title; de/fr both use a direct imperative. Native call on whether to transcreate (e.g. 'Nastavte si svou značku'). |
| `setup.welcome.values.pipeline.title` | terminology | minor | 'Řiďte celý proces' drops the glossary loanword 'pipeline' (de 'Pipeline steuern', fr 'Pilotez le pipeline'). CS-ONE-WORD would call it terminology drift; CS-CALQUE licenses transcreation on a welcome-screen value card. Needs a house call on which rule wins for onboarding marketing copy. |
| `rubric.competency.coachability.description` | terminology | minor | "odmítnou" renders "deflect (a hint)", but the glossary reserves cs *odmítnout* for the CANDIDATE declining an offer/role. Here the candidate is the subject but the object is a hint, so it is not strictly the reserved sense; *obejdou ji* / *odbudou ji* would be sharper. Only the stray comma was fixed mechanically. |
| `enums.archetype.career_switcher` | style | minor | The archetype set mixes person nouns (Zkušený, Student) with an abstract noun (Změna oboru) and a state (Nezařazeno), where de (Quereinsteiger) and fr (Reconverti) stay person-shaped throughout. "Změna oboru" dodges the gender problem, which may be why it was chosen — worth a native ruling before unifying. |
| `match.drivers.fewSkills` | fluency | minor | "Uvedeny méně než 3 dovednosti" — the short passive plural sits uneasily on a "méně než"-quantified subject; a native may prefer "Méně než 3 uvedené dovednosti" or "Uvedeny jsou méně než 3 dovednosti". Sibling match.drivers.noLanguages ("Neuvedeny žádné jazyky") has the same telegraphic shape, so both should be ruled on together. Not changed. |
| `match.weights.intro` | style | minor | "Omezeno, aby řazení zůstalo spravedlivé; při použití normalizováno na 100 %." — two subjectless short passives in a modal body. constructions-cs.md CS-PASS would push to "Váhy jsou omezené … a při použití se normalizují na 100 %", but CS-PASS explicitly exempts elliptical fragments, so the call is genuinely ambiguous. Not changed. |
| `match.shared.sourceRuleBased` | fluency | minor | "pravidlové" is a neuter adjective agreeing with nothing (rendered as a source chip after "Zdůvodnění ·"). CS-ABLE names this shape; "podle pravidel" would fix it. Left untouched because the catalog is consistent (also decisions.*.ruleBased) and a fix must sweep both sites. |
| `hiringPlan.presetLean` | style | minor | "Sólo / štíhlý" for "Solo / lean" — štíhlý is the established calque in "štíhlá výroba" but reads oddly as a hiring-process preset name; "úsporný" or "jednoduchý" may land better. Preset label, tight budget. |
| `feedback.sent` | style | minor | Changed "Vaše zpětná vazba byla odeslána" to "Zpětnou vazbu jsme odeslali" — CS-ACTIVE (name the actor) plus CS-POSS (drop the English possessive). Both rules point this way, but a native should confirm the 1st-person-plural does not read too chatty for a confirmation toast, since the sibling login/branding errors in this batch stayed impersonal ("nepodařilo se"). |
| `nav.attentionBadgeGo` | grammar | minor | The plural block now carries the whole clause and the trailing ". Zobrazit." sits outside it — count-invariant, so CS-AGREE is satisfied. But the string is an accessible link name (NavSectionRail.tsx:86), and two sentences in an accessible name is unusual; a native should judge "3 vyžadují pozornost. Zobrazit." against a single-clause alternative such as "Zobrazit 3, které vyžadují pozornost" (which would need the plural rebuilt around the relative clause). |
| `rubric.competency.learning_agility.anchor.4` | terminology | minor | "smyčku diagnostiky, experimentu a úpravy" replaces the dashed triplet. Three chained genitives is at the edge of what CS-NOM tolerates (it allows two prepositionless genitives; here they are coordinated rather than chained, which I read as compliant). A native should confirm, and confirm "úprava" over "korekce"/"doladění" for the adjust step of the loop. |
| `candidateOnboarding.intro` | style | minor | "Než nastoupíte, jeden rychlý krok:" is a verbless fragment before the colon, mirroring English's "One quick step before day one:". Warm public-surface register, but a native should confirm the fragment reads as deliberate rather than clipped, and that "ať pro vás můžeme vše připravit" is not too colloquial beside the formal vykání. |
| `channels.comms.deadLetters` | grammar | minor | I added the head noun ("# nedoručená zpráva / # nedoručené zprávy / # nedoručených zpráv vyžaduje/vyžadují pozornost") so the participle has something to agree with, and moved the verb inside the branches per CS-AGREE. Confirm the `other` branch verb is singular ("vyžaduje") as written — that is the correct genitive-plural government, but it reads odd to some ears next to the plural noun. |
| `setup.checklist.steps.channels.listening` | style | minor | "Nasloucháme." keeps the pre-existing 1st-person-plural system voice (permitted by style-cs.md) but reads slightly poetic for a checklist state. "Připraveno k příjmu." would match the impersonal status-chip convention. Not changed because the dash rule did not reach it. |
| `setup.handoff.readyTitle` | accuracy | minor | "{org} může začít" ("{org} can get started") replaces "{org} is ready to go" specifically to avoid agreeing an adjective with an unknown-gender company name. Confirm the shift from state to capability is acceptable in a handoff headline. |
| `channels.cvSim.stub` | terminology | minor | "jako neúplný záznam, protože text životopisu byl příliš strohý" — inherited wording. "strohý" reads as terse-in-tone rather than short-on-content; "protože v životopisu bylo málo textu" is plainer. Left as-is to avoid drive-by rephrasing. |
| `channels.email.gmail3` | style | minor | Splitting the em-dash aside into its own sentence puts the brand "kp" in lowercase at a sentence start three times across the batch (also empty.email.step1, checklist.steps.firstRole.body). That is correct per the do-not-translate rule but a native proofreader will flag it as a typo — worth a house note. |
| `channels.receivers.confirmLive` | grammar | minor | The `few` and `many` branches are byte-identical (both "# příchozí přihlášky"), inherited from the existing value. That is technically right for integer counts (many only fires on decimals) but confirm the team wants the redundant branch kept rather than dropped. |
| `landing.previews.gates.signed` | grammar | minor | "schválen v {time}" — with a time like 14:02 Czech usage splits between "v 14:02" and "ve 14:02". Chose "v". A native should settle it; alternatively drop the preposition by fronting the time. |
| `landing.previews.gates.queue.pass.title` | style | minor | The cs value carried a dash the English never had ("Poslat laskavé zamítnutí – {name}"). Recast to "pro {name}" to match the sibling key offer.title ("Schválit návrh nabídky pro {name}"). "zamítnutí pro X" is slightly stiff; a dative would be better but {name} cannot decline. Confirm the phrasing. |
| `landing.proof.footline` | accuracy | minor | The old cs said "dnes" where the English says "every 2026 hire". Restored the year ("kterou v roce 2026 vyvolá každý nábor") while removing the dash. Confirm the year should stay explicit in Czech marketing copy rather than being softened to "dnes". |
| `landing.previews.inbox.sameStartingLine` | style | minor | "Každé dveře ústí…" is grammatical (dveře is plurale tantum) but reads a little odd as a metaphor head; "Všechny dveře" may be smoother. Left as-is to avoid drive-by rewriting. |
| `workspaceAdmin.members.removed` | style | minor | "Odebráno: {name}" is grammatically safe (impersonal neuter, no gender guess) but reads slightly clerical for a toast. A native may prefer "{name} odebrán/a"; that reintroduces the slash-participle. Needs one house call, then apply catalog-wide to the other {name}-toasts. |
| `aboutPage.steps.source.body` | style | minor | "nikdy ne bingo klíčových slov" — the "keyword bingo" metaphor was rendered literally. CS-CALQUE says transcreate; Czech HR does not use "bingo" this way. A native should confirm or replace (e.g. "ne shoda podle klíčových slov"). |
| `aboutPage.steps.interview.body` | terminology | minor | "strukturovaný scorecard" (masculine) is inherited, not introduced here, but it sits in the open scorecard/hodnoticí karta split flagged in glossary.md. Do not unify in this pass; listed so the eventual sweep catches it. |
| `aboutPage.steps.design.body` | fluency | minor | The double-dash aside was split into two sentences with "Vznikne i hodnoticí rubrika…". Meaning is preserved but the marketing rhythm of the original changed; a native marketing read is worth one pass since this is aboutPage copy. |
| `results.quality.trustTitle` | fluency | minor | "životopisů generovaných AI / přikrášlených" is awkward Czech because the source slash-coordinates two adjectives with different natural positions. Left as-is to avoid meaning drift; fixing it properly needs the source defect resolved first. |
| `analytics.calibration.blurb` | style | minor | "jak jsme kalibrovaní" renders English's first-person-plural "how calibrated we are" literally. Czech would more naturally say "jak přesně je model nakalibrovaný" — the product, not "we", is what is calibrated. Flagged rather than changed, since it is outside the dash rewrite. |
| `analytics.calibration.bandsBlurb` | style | minor | "kdo tam skóroval" is a direct borrowing of the verb "to score"; "kdo se do něj vešel" or "kdo v něm skončil" reads more like Czech. Also "Otevřete jej" — the pronoun refers back to "pásmo" (neuter), so it should be "Otevřete ho/je", not masculine "jej". Worth a native check. |
| `analytics.roi.title` | terminology | minor | The heading translates ROI to "Návratnost" while the de/fr keep "ROI". Both are defensible, but the concept should get one glossary row rather than three per-locale calls. |
| `comms.ack.subjectRole` | accuracy | minor | "Přijali jsme vaše řešení" narrows English's "submission" to a solved exercise. Correct for the devcase flow this key serves, but if the ack template is ever reused for a plain application the Czech will be wrong where the English still fits. |
| `comms.interviewInvite.subject` | style | minor | "Vaše první kolo s AI: {role}" reads as an inbox subject where the {role} appendix is a bit abrupt after "s AI". A native might prefer "První kolo s AI na pozici {role}" and drop the colon entirely. Same shape in de/fr, so decide once. |
| `analytics.goals.saveFailed` | style | minor | "(nebo pole vymažte a hodnotu odstraňte)" expands the terse English parenthetical into two imperatives. Shorter alternative: "(nebo pole vymažte)". Check against the field's actual behaviour before shortening. |
| `library.tab.ingestAsJobTitle` | style | minor | Long infinitive tooltip with a purpose clause: "Převést tento popis na párovatelnou pozici, aby ji šlo nasadit do Pipeline, řadit, párovat a hlásit se na ni". The trailing "a hlásit se na ni" switches subject (candidates apply, not the operator). Grammatical, but a native may prefer splitting into two clauses. |
| `match.weights.weightAria` | grammar | minor | Rewrote "Váha – {label}" as "Váha: {label}". A colon in an aria-label is read aloud as a pause; alternative "Váha pro {label}" avoids the case problem of a bare nominative dimension name. Native call. |
| `library.tab.intake.thinkingSlow` | style | minor | Changed 1st person singular "Stále přemýšlím" to plural "Ještě přemýšlíme" per style-cs.md (no 1st person singular for progress). Confirm the intake assistant is meant to speak as "we" and not as a named agent. |
| `library.tab.chipLiveAria` | style | minor | Sibling chip aria-labels (chipLive/chipDraft/chipLinked/chipUnlinked) were unified on a colon. Check the colon reads well in a screen reader across all four. |
| `report.compare.narrativeDelta` | construction | minor | Recast the trailing '– {metric}' as a parenthetical '({metric}: {bestScore} vs {otherScore})' to dodge the case problem, since {metric} arrives nominative ('celkove skore'). Reads fine, but a native should confirm the parenthesis is not too terse for a driver sentence. The alternative 'v kategorii {metric}' adds a word the source lacks. |
| `report.compare.narrativeTie` | construction | minor | Rewrote as '„{best}“ ma stejne {metric} jako „{other}“ ({score}).' 'stejne' agrees with neuter 'skore', which both metric labels are today. If a third metric label with another gender is ever added, this breaks. Cites CS-AGREE. |
| `matrix.selectableSuffix` | style | minor | ', lze vybrat' replaces the old ', vybiratelne' (an -able adjective dangling in the neuter, CS-ABLE). 'lze' sits slightly high in register but is the shortest verb form that fits an aria suffix. A native may prefer ', k vyberu'. |
| `report.jdEditedTitle` | construction | minor | Changed the short passive 'byl upraven' to reflexive 'se zmenil' per CS-PASS. Confirm the reflexive does not read as if the JD changed by itself; if it does, 'Popis pozice jste upravili {date}' is the active alternative when the editor is always the operator. |
| `simulation.log.noScore` | terminology | minor | 'n/a' now stands where a Czech log line reads 'skore n/a'. Acceptable but anglophone; a native may prefer 'neuvedeno'. Tied to the termNotes decision. |
| `matrix.coverageGap` | grammar | minor | The plural block now carries the verb ('nema' / 'nemaji' / 'nema') and the tail 'silnou shodu' is count-invariant accusative, so all three branches render. Worth a native re-read of the one-branch case ('1 z 5 otevrenych pozic nema silnou shodu.'). |
| `apply.script.experiencePrompt` | construction | minor | Split into 'Jaka je vase nejrelevantnejsi nedavna zkusenost pro tuto pozici? Staci jedna dve vety.' to remove the dash after the fronted 'V jedne dvou vetach'. This reorders the source's own clause order; confirm the chat register still opens well. |
| `apply.subtitle / apply.networkFailed` | other | minor | Both strings are quoted verbatim WITH em dashes as gold pairs #7 and #4 in docs/i18n/exemplars-cs.md. That doc now contradicts the shipped catalog and contract.md §5, and needs updating in the same change as the merge. pipeline.tab.emptyBody (exemplar #5) has the same problem. |
| `jobs.candidates.fairnessShielded` | style | minor | "Chráněno spravedlností" for "Fairness-shielded" is a literal calque and reads as a machine rendering — spravedlnost is not what shields anything here. Candidate rewrite: "Chráněno proti zkreslení: hodnoceno podle potenciálu…". I left the existing term standing because it also appears outside this batch and a unilateral change would split the catalog. |
| `jobs.compare.singleCohortNote` | fluency | minor | "Porovnáváte srovnatelné" for "Compare like for like" is terse to the point of sounding like a slogan, and it is indicative where the English is imperative. A native should judge "Porovnáváte tak srovnatelné s porovnatelným." vs. the shorter form kept here. |
| `jobs.coach.gateEducation` | terminology | minor | "rozšiřte fond kandidátů" — fond is correct but slightly financial-sounding; jobs.candidates.poolFitNote says "databáze kandidátů" for the same concept. One of the two should win (see termNotes: candidate pool). |
| `jobs.compare.cohort.early_career` | terminology | minor | "Junioři" renders "Early-career" compactly for the table header, but jobs.candidates.earlyCareerNote also says Junioři while jobs.posting.doc.earlyDefault says "začínajícím kandidátům". Two renderings of one cohort name; a native should pick one for the cohort label specifically since it is a heading. |
| `decisions.wave.reasons.holdout` | fluency | minor | Long single-clause reason line: "ponecháno jako kalibrační kontrolní vzorek: {pct} % kandidátů určených k automatickému zamítnutí se záměrně ponechává, aby jejich skutečné výsledky ověřily správnost skóre (...)". "ponecháno … se ponechává" repeats the verb inside one sentence. A native may prefer "…se záměrně nechává v řízení" for the second occurrence. Meaning is exact; only the repetition is in question. |
| `decisions.rules.ruleWeakest` | terminology | minor | "v každém neprázdném poolu" keeps the English loanword *pool* declined as a Czech masculine. It was already in the catalog and is not in glossary.md. A native should decide between *pool* (kept) and *skupina kandidátů* / *výběr*; if kept, add the row with its gender per CS-LOANGENDER. |
| `profile.matrix.openProfileTitle` | grammar | minor | "Upravit profil: {name}" replaces the previous dash form. The colon avoids the impossible genitive of an interpolated name, but a native may prefer "Upravit profil kandidáta {name}" — which reads better yet forces masculine (CS-GENDER). Flagged rather than decided. |
| `decisions.compliance.covered1` | style | minor | Changed the inherited "přezkoumává a činí člověk" to "posuzuje a rozhoduje člověk" per CS-FORMAL (činit → dělat / name the real verb). Confirm the compliance register still reads precise enough — this string is an EU AI-Act oversight claim, and aiDisclosure.body nearby still uses "přezkoumává a dělá člověk". |
| `devcase.probeAudit.strong` | style | minor | Rendered as "Nosných sond: {loadBearing} z {total}. Tento případ rozlišuje." to dodge the missing ICU plural (see sourceDefects). Reads fine but is structurally different from en/de/fr; a native should confirm the colon-fronted count is idiomatic here rather than pushing for an ICU plural fix in the source. |
| `devcase.probeAudit.weak` | terminology | minor | "většina silné od naivních neodliší" nominalises the adjectives (strong/naive submissions). Grammatical, but confirm a native prefers this over spelling out "silná řešení od naivních". |
| `devcase.cohort.miscalibration` | accuracy | minor | "Švy mohou být příliš skryté" translates "seam" as the sewing/joint sense used elsewhere in the devcase module (seedEngagementTitle uses "šev" too). Consistent, but a native engineer should confirm "šev" carries the intended "decision point planted in the code" meaning in Czech dev-speak. |
| `devcase.emptyLedger.control.hashChain.proves` | style | minor | Second sentence was restructured ("Celý záznam je nedůvěryhodný, ne jen podivný, pokud …") rather than kept as a comma-pair parenthetical, because the literal shape stacked four relative clauses before the main verb. Meaning is preserved; the clause ORDER differs from en/de/fr. Worth a native read for whether the emphasis still lands on the failure condition. |
| `pipeline.tab.approvalsBody` | style | minor | The cs value previously opened with a bare dash as a continuation of the count headline. Recast to a count-invariant standalone sentence, 'Rozhodnutí je na vás: postoupit, zamítnout nebo potvrdit termín pohovoru.' de took the same treatment ('Die Entscheidung liegt bei Ihnen: …') because 'brauchen' cannot agree with count=1. Confirm the two halves still read as one banner. |
| `pipeline.command.doneCount` | grammar | minor | Moved the participle inside the plural branches (Zpracován 1 kandidát / Zpracováni # kandidáti / Zpracováno # kandidátů) per CS-AGREE, instead of the previous count-invariant neuter 'Zpracováno {count…}'. Both are defensible; a native should pick one and it should then be applied to the sibling done* keys. |
| `pipeline.scheduler.historyTitle` | style | minor | Heading recast to 'Co každý průchod udělal a proč, po jednotlivých kandidátech' (the dash became a comma). The trailing adverbial on a heading is slightly heavy in Czech; a native may prefer fronting it as 'Po jednotlivých kandidátech: co každý průchod udělal a proč'. |
| `pipeline.drawer.jdEditedTitle` | terminology | minor | Changed *Inzerát* → *Popis pozice* to follow the glossary's JD row, and joined the clauses with 'takže' instead of the dash. Confirm the term is right for this drawer (it refers to the internal JD text that was scored against, not the public advert). |
| `about.students.scriptIntro` | terminology | minor | "kontrafaktuál" as a bare noun is academic-philosophy jargon in Czech and appears in the middle of an arrow chain a recruiter reads at a glance. Consider "co kdyby" or "kontrafaktuální otázka". Also in interviewSim.caseDescGeneric — same chain, keep them identical whichever way it goes. |
| `onboarding.recordBodyStarved` | terminology | minor | cs renders en's "a run" as "Onboarding", so the sentence defines the tab's own name ("Onboarding je záznam jednoho předání") rather than defining a sub-object of it. Reads circular. Ask a native whether "Záznam onboardingu" or "Průběh onboardingu" is better. Same in onboarding.recordBodyReady — keep the pair aligned. |
| `skillProfile.tampered` | style | minor | "Neplatný podpis, nedůvěřujte mu" puts a formal imperative inside a status badge, which Czech UI convention avoids (badges are neuter state labels — CS-PASS exception 1). A native may prefer "Neplatný podpis (nedůvěryhodné)" or dropping the warning half entirely; see the matching source defect. |
| `tasks.backup.intro` | terminology | minor | Uses "pracovní prostory" (workspace) while the glossary row says "pracovní plocha". Known, tracked drift — carried through unchanged rather than half-swept. Same word in signup.subtitle and signup.success in this batch, all three consistent with each other and with the catalog majority. |
| `scheduleTab.transcript.telemetryDuration` | accuracy | minor | cs "Délka hovoru" (call length) vs en "Spoken duration" / de "Sprechdauer" / fr "Durée de parole". The underlying value is the span between first and last utterance timestamp (app/_lib/interview-telemetry.ts:107), so "délka hovoru" is factually what is measured; left untouched, but a native may prefer "Délka mluvení" for parallelism with the sibling "Podíl mluvení kandidáta". |
| `scheduleTab.transcript.readbackNote` | fluency | minor | "kandidát potvrdil nebo opravil" is a bare generic masculine where style-cs.md asks for the "/a" form for unknown gender. The "/a" expansion here ("kandidát/ka potvrdil/a nebo opravil/a") is heavy in a meta note and the catalog uses generic "kandidát" broadly, so left as shipped. |
| `scheduleTab.relay.noteWaiting` | style | minor | "Čeká tady" / "Jste tady" use colloquial *tady* in an uppercase meta label (ScheduleEmptyRelay.tsx line 42). *zde* would read more formally, but the pair is internally consistent and "Jste tady" is the standard you-are-here idiom. Native call. |
| `integrations.calendar.disconnectedNotRevoked` | style | minor | "Odpojeno tady, ale Google odebrání nepotvrdil." — "tady" is the colloquial deictic; "zde" is the formal register this catalog otherwise keeps (style-cs.md formal vykání). Fronting would also read better ("Zde je kalendář odpojen, ale …"). Left alone because the register rule targets person/address, not deictics, so this is taste rather than an anchored error. |
| `integrations.personas.baseUrlLabel` | terminology | minor | "Adresa Personas" vs the sibling integrations.ats.baseUrlLabel "Základní URL API" — the batch renders the same field concept two ways (adresa / URL). Different panels, so arguably fine; a native reviewer should decide whether kp says "URL" or "adresa" for endpoint fields app-wide. |
| `workspaceAdmin.members.inviting` | accuracy | minor | en "Inviting…" → cs "Odesílání…" (= "Sending…"). Defensible (the invite is being sent) and it obeys the verbal-noun rule in style-cs.md, but it drops the invite concept that de ("Wird eingeladen…") and fr ("Invitation…") keep. "Zvaní…" is awkward Czech, so a native should confirm "Odesílání…" is the right escape. |
| `report.panel.bucketRedFlag` | accuracy | minor | cs "Varovné signály" and fr "Signaux d’alerte" drop the "defense" half of en "Red-flag defense" (these questions exist so the candidate can answer the red flag), which de keeps as "Red-Flag-Abwehr". Plausibly a deliberate first-pass call for the chip length budget; flagging rather than re-litigating. |
| `landing.trust.human.title` | style | minor | "Human in the loop" → "Člověk v řídicí smyčce" borrows a control-engineering metaphor; de deliberately keeps the English term. A native HR reader should rule whether "Člověk ve smyčce" or keeping the English term is stronger. Left as-is. |
| `pipeline.scheduler.outcome.fairness_blocked` | style | minor | "férová pojistka" for "fairness backstop". "férový" is a colloquial anglicism; style-cs.md pins formal B2B register but does not rule on colloquial lexis, so this is a house call rather than an anchored error. A native should decide between keeping it and something like "pojistka spravedlnosti" / "pojistka rovného zacházení" (both noticeably longer in the badge). |
| `pipeline.controlCenter.phasesLabel` | terminology | minor | cs renders "Pipeline phases" as "Fáze náboru" while de/fr keep the loanword ("Pipeline-Phasen" / "Phases du pipeline"). glossary.md keeps inline "pipeline" as a loanword and reserves "Nábor" for the nav TAB, so this is arguably tab-name drift, but "Fáze pipeline" reads worse. Left untouched pending a house call. |
| `devcase.emptyLedger.eyebrow` | style | minor | "Kniha otevřena" for "Ledger open": *kniha* alone reads as a book. de/fr chose Journal/Registre. "Záznamník" or "Kniha záznamů" may be better, but *ledger* has no glossary row and the sibling title ("Zatím nic zapečetěno") already carries the metaphor. Not changed. |
| `devcase.outboxKind.* (13 keys)` | locale-convention | minor | The en enum labels are lowercase ("acknowledgement", "offer", "interview invite") and the sibling enum clusters devcase.stage.*, devcase.probeStatus.*, devcase.checks.canary.* keep lowercase in cs/fr. outboxKind is capitalized in all three locales. Internally consistent, so left alone, but the catalog-wide convention for enum chips should be settled once. |
| `jobs.campaign.beatHook` | style | minor | 'Háček' for the marketing 'Hook' beat is understood but folksy; Czech marketing decks often keep 'Hook'. Left untouched because the sibling beats (Nabídka / Důkaz / Výzva) are all translated and a lone loanword would break the set. |
| `pipeline.relTime.daysAgo` | locale-convention | minor | "před {d} d" (and the parallel pipeline.candidateRow.aging "> {days} d", pipeline.relTime.weeksAgo "před {w} týd") use abbreviations that are not standard Czech unit shorthand. A native should decide between keeping the ultra-compact chip form and spelling out ("před {d} dny" needs an ICU plural). Left as is because the call sites are tight relative-time chips and the change would require restructuring, not just wording. |
| `pipeline.candidateRow.degradedSuffix` | accuracy | minor | en "intake degraded" vs cs "příjem selhal" (= intake failed). de "eingeschränkt" and fr "dégradée" both keep the softer claim. Given the repo's truthful-claims convention this may want "příjem omezen", but the sibling event string already says the intake could not be processed, so the stronger word is defensible. Left for a native call. |
| `pipeline.tab.loadFailed` | terminology | minor | cs renders "pipeline" as "nábor" in the board/tab cluster (loadFailed, boardAria, board.scrollHint) but keeps the loanword in the events cluster ("do pipeline", "už je v pipeline"). glossary.md sanctions both (tab = product area "Nábor"; inline = technical funnel "pipeline"), so this is on the boundary rather than clearly wrong. Needs one ruling, then one sweep. |
| `library.builder.generateWithAi` | style | minor | cs renders the eyebrow as a finite clause, "Popis vygeneruje AI", where de/fr use a label ("Mit AI generieren" / "Générer avec l'AI"). Call site is app/features/library/jds/JdsBuilder.tsx:75, an EYEBROW recipe (uppercase, tracking-wide), so it ships as "POPIS VYGENERUJE AI". CS-ACTIVE ("name the actor") actively supports the clause and CS-CTRL does not apply (it is not a control), so I left it untouched. A native should confirm it reads as a section title rather than a slogan; the alternative is "Generování pomocí AI" (though CS-POMOCI discourages pomocí). |
| `library.tab.intake.columns.badge.turns` | terminology | minor | EN says "# messages" here but "# turn" at the sibling library.tab.intake.turns; cs renders both as "replika/repliky/replik", which is catalog-consistent (errors.INTAKE_VOICE_TURN_FAILED, scheduleTab.transcript.coverageCaveat, models.useCaseDesc.role_intake_voice all use replika) while de/fr follow the EN word ("Nachricht" / "message"). Left as-is: CS-ONE-WORD favours the cs consistency, but a native should rule whether "replika" or "zpráva" belongs on a badge under the column headed "Rozhovor". |
| `library.result.openPipeline` | terminology | minor | glossary.md pins the Pipeline NAV TAB as localized while inline "pipeline" stays a loanword. This is a navigation button ("Open Pipeline"), so by that row it would be the localized form, but the whole surrounding cluster (sourceIntoPipeline, sourcingIntoPipeline, liveSourced, library.tab.intro) says "Pipeline" and nav.tabs.pipeline is now "Přehled" anyway. Changing this one key alone would be exactly the half-sweep contract.md warns against, so I left it; needs one house decision then one sweep. |
| `decisions.batch.rejectConfirm` | style | minor | "uvědomit" for "notify" reads as administrative/officialese register (CS-FORMAL, plain word not the bookish one); "informovat" is the everyday word. NOT changed deliberately: "uvědomit" is the catalog-wide rendering at 6 sites (cs.json:2025, 2026, 2404, 2422, 2423, 2566), three of them outside this batch. A half-sweep would be worse than the current consistent split-free state — needs one house decision then one sweep. |
| `decisions.compliance.jurisdictionLabel` | fluency | minor | "Jurisdikce souladu" is a literal two-noun rendering of "Compliance jurisdiction" and is hard to parse as a settings label; a Czech HR reader would expect something like "Jurisdikce pro compliance" or "Právní rámec". Blocked on a house call: "compliance" is not in glossary.md for cs (de keeps the loanword), and decisions.compliance.title currently renders it "Soulad". |
| `decisions.compliance.aiCheckIntro` | fluency | minor | "z odděleného EEO průzkumu": "oddělený" reads as "detached" rather than "separate/standalone" (samostatný), and the acronym-as-premodifier "EEO průzkum" is English word order (Czech prefers "průzkum EEO"). Left untouched — no ID covers acronym premodification and the string is comprehensible. |
| `decisions.aiReview.noBand` | fluency | minor | "Pro tuto pozici ani trh není nastavené mzdové pásmo" compresses the second prepositional phrase ("…ani pro tento trh"). Grammatical but slightly clipped; a native ear should rule whether it is worth the extra words in a body-copy hint. |
| `decisions.summary.scoringRole` | fluency | minor | "Hodnocení vůči této pozici…": "hodnotit vůči" mirrors English "scored against"; "podle této pozice" / "vzhledem k této pozici" may be the idiomatic government (cf. DE-CALQUE-PREP, which has no cs twin rule). Not changed — no cs ID to cite. |
| `scheduleTab.lifecycle.calendarDropped` | fluency | minor | The `many` branch "# termínu skryto jako obsazených" has a genitive-plural adjective agreeing with a genitive-singular noun (should be "obsazeného"). Unreachable in practice — Czech `many` only fires on decimals and this is an integer slot count — so I did not touch it. style-cs.md itself calls `many` optional for integer counts. |
| `library.tab.intake.side.brief` | terminology | minor | cs uses "Zadání" for brief (intake.side.brief, intake.brief.title "Živé zadání", intake.edit.start "Upravit zadání") but also "Zadání pozice" for the intake feature itself (intake.tabLabel, intake.export.fileTitle). Defensible (the intake IS the role brief) but a native reader may read the tab as "the brief" rather than "the intake conversation". Not changed; both readings are coherent and the first pass may have settled this deliberately. |
| `library.tab.interviewCase` | accuracy | minor | cs "Případová studie" drops the "interview" qualifier that de ("Interview-Case") and fr ("Cas d'entretien") keep. In the JD detail panel the context supplies it, so this may be a deliberate shortening; flagging rather than churning since "Případová studie z pohovoru" is long for the heading. |
| `analytics.calibration.recApplying` | fluency | minor | "Používání…" as a button busy label (AnalyticsThresholdSuggestion.tsx:93). It is rule-compliant (CS-PROG: subject unexpressed → verbal noun, like "Ukládání…"), but the noun reads as "usage" rather than "applying the suggestion"; "Používá se…" or "Probíhá použití…" may be better. Left untouched as a defensible first-pass choice. |
| `analytics.rejectedSub` | fluency | minor | The cs plural mixes agreement genders across branches: one "# zamítnut/a" (slash form), few "# zamítnuti" (masc. anim. pl.), other "# zamítnuto" (neuter impersonal). Grammatical in each branch and consistent with the sibling declinedSub, but style-cs.md prefers the impersonal neuter throughout. A native should decide whether to flatten all three to "zamítnuto". |
| `analytics.goals.intro` | accuracy | minor | en names the component ("The funnel flags stages below goal"); cs says "Fáze pod cílem se v přehledu označí", losing the referent. The cs name for the funnel is "Cesta náborem" (analytics.funnel), so "Cesta náborem označí fáze pod cílem" would keep the pointer. Meaning is preserved, so not fixed. |
| `analytics.calibration.axisObserved` | accuracy | minor | "Observed advance rate" → "Skutečná míra postupu" (actual, not observed), while de/fr keep beobachtete/observé. In a predicted-vs-observed calibration chart the plainer "skutečná" may be the better UI word; a native/stats reader should rule. |
| `invite.eyebrow` | fluency | minor | "Připojení k {orgName}" interpolates an org name after the preposition k. Czech vocalizes k→ke before certain consonant clusters (ke Kladnu, ke Group…), which ICU cannot resolve. Low real-world risk (most company names take bare k), but a native may prefer a preposition-free eyebrow such as "{orgName}" or "Pozvánka do organizace {orgName}". Anchor: style-cs.md 'Grammar traps a mechanical translation gets wrong'. |
| `common.scanningCv` | accuracy | minor | en "Scanning CV" (aria-label on ScanAnimation, app/_components/ScanAnimation.tsx:25) is rendered "Probíhá analýza CV". "Analýza" is the name of a distinct product surface (CV analysis / Analyze workspace), so the aria-label may over-claim which stage is running. de says "Lebenslauf wird gescannt", fr "Analyse du CV en cours" — the locales are already split. A native/house call on whether cs should say "Probíhá čtení CV" or keep "analýza"; not changed because it is defensible and the first pass may have set it deliberately. |
| `diagrams.legend.gate / diagrams.status.gate` | style | minor | "lidská brána" is a literal calque of "human gate"; Czech process vocabulary would more likely say "lidské rozhodnutí" or "lidský krok". There is no glossary row for *gate*, and the two keys are internally consistent, so this is a term decision for a native + a glossary row, not a defect to fix in one batch. |
| `about.students.eyebrow` | terminology | minor | "Junioři / studenti" for "Early-career students" uses a slash pair where the sibling keys say only "juniory" (viewsAria) and interviewSim.studentFooter points at "O aplikaci → Junioři". Navigation still resolves, but a native could settle one label for the whole cluster. |
| `analytics.funnelGuide.questionOffer` | accuracy | minor | en "How selective the interview loop actually is" → cs "Jak náročné je ve skutečnosti kolo pohovorů" (how demanding). de/fr both keep selectivity ("Wie selektiv", "sélective"). "Jak přísně kolo pohovorů ve skutečnosti vybírá" would be closer, but the shift is defensible in context and non-mechanical, so left alone. |
| `analytics.log.kinds.offer_drafted` | accuracy | minor | "Nabídka vytvořena" loses the draft/sent distinction that the log's own siblings rely on (offer_drafted vs offer_sent "Nabídka odeslána"); de "entworfen" and fr "rédigée" keep it. "Návrh nabídky vytvořen" would restore it. Native/house call. |
| `analytics.log.kinds.interviewer_brief_sent` | fluency | minor | "Tazatel informován" is a bare masculine for a person of unknown gender (constructions-cs.md CS-GENDER). The slash form "Tazatel/ka informován/a" is heavy for a log chip; needs a house call on whether CS-GENDER reaches role nouns in event labels. |
| `pipeline.scheduler.outcome.fairness_blocked` | style | minor | "férová pojistka" — *férový* is a colloquial loan; CS-SUBSTD bars obecná čeština but does not name this word, so it is not a citable error. Candidates: "pojistka férovosti" or "pojistka spravedlnosti". A native should rule whether *férový* clears kp's B2B register. |
| `pipeline.controlCenter.phasesLabel` | terminology | minor | aria-label "Fáze náboru" vs de "Pipeline-Phasen" / fr "Phases du pipeline". The glossary's inline-pipeline-stays-a-loanword note would argue for "Fáze pipeline", but here the phrase plausibly names the product area (Nábor) and the Czech reads better. Left untouched pending a call. |
| `pipeline.result.noBand` | fluency | minor | "Pro tuto pozici ani trh není nastavené mzdové pásmo" — Czech usually repeats the preposition across a negative-concord pair ("Pro tuto pozici ani pro trh"). Grammatical as-is; a native should decide whether the repetition is worth the extra length. |
| `library.builder.needMoreInput` | fluency | minor | "Doplňte název pozice (a dostatek detailů v potřebě)." — "v potřebě" is a bare rendering of en "in the need" and reads odd even though it points at the field labelled "Popište potřebu". "…v popisu potřeby" would be smoother, but the field-label echo may be deliberate. Left untouched. |
| `library.tab.intake.columns.badge.turns` | accuracy | minor | en "message" is rendered "replika" (a dialogue turn) rather than "zpráva". Correct plural categories (one/few/other) and arguably a better fit for the Conversation column, but it is a deliberate deviation from the source noun. Native call. |
| `library.builder.generateWithAi` | style | minor | en "Generate with AI" -> "Popis vygeneruje AI" ("AI will generate the description"). The call site (JdsBuilder.tsx:75) is an uppercase EYEBROW label, where a declarative reads fine, but it is a deliberate voice change from the source imperative. Native/house call. |
| `jobs.rediscoveryFeed.title / jobs.rediscoveryFeed.sweepFailed` | style | minor | "Silver medalists" rendered literally as "Stříbrní medailisté". German transcreated it ("Zweitplatzierte"); Czech HR does not use the medal metaphor for strong runners-up, so CS-CALQUE arguably bites — but "Druzí v pořadí" loses the product's playful naming. Two keys move together; wants a native call, not a unilateral rewrite. |
| `jobs.shared.reqPrereq` | terminology | minor | "prerekvizita" is an academic loan and mismatches its sibling chip "naučitelné" (adjective) with a noun. JobsShared.tsx renders both uppercase in the same slot, so "NUTNÉ" / "NAUČITELNÉ" would pair. No glossary row for prereq/learnable — worth adding one either way. |
| `analytics.colReachedInterview` | fluency | minor | "Dosáhlo pohovoru" is grammatical (dosáhnout + genitive) but reads stiff as a count-column header beside "V pipeline" / "Přijato". "Došlo k pohovoru" or "K pohovoru" may read better; not changed because the current form is not an error. |
| `analytics.log.kinds.interview_scorecard` | style | minor | "Hodnoticí karta syntetizována" — "syntetizovat" reads chemical/technical in Czech; de chose "Scorecard erstellt". "Hodnoticí karta sestavena" would be more natural, but the term choice (hodnoticí karta vs scorecard) is itself queued in glossary.md, so left untouched. |
| `analytics.compute.computePerHire` | accuracy | minor | "Výpočet LLM" for "LLM compute" reads as "a calculation performed by the LLM" rather than "compute resource cost". It is internally consistent with analytics.compute.title ("Náklady na výpočet"), so a fix would need to move both; a native call on "výpočetní výkon" vs keeping the loanword would settle it. |
| `pipeline.drawer.intakeDegradedBody` | style | minor | "útržek jen se jmenovkou" for "label-only stub" reads translated (*jmenovka* = a name badge). Candidate: "jen zástupný záznam s popiskem". Not swept because *stub* has no glossary row. |
| `decisions.govEligibility` | accuracy | minor | "Eligibility list" → "Pořadník" adds a ranking/queue reading the English does not carry (it is a governance mode: the AI output is used as a list of eligible candidates). "Seznam způsobilých" is the literal alternative but is longer and less idiomatic in a Czech HR context. A native call. |
| `decisions.compliance.ceiling2` | style | minor | "Brána spravedlnosti pro automatické zamítnutí je štít podle archetypu…" mixes two metaphors (gate + shield) and "brána spravedlnosti" reads grandiose. fr found a better device ("garde-fou d’équité"). A Czech equivalent like "pojistka spravedlnosti" may be better; no rule ID covers it, so not changed. |
| `decisions.groupEval.capped` | style | minor | "Zobrazeno top {cap} z {total} kandidátů" uses the colloquial adjectival *top*; "{cap} nejlepších z {total}" is the written-register form. CS-SUBSTD is arguably in play but *top N* is widespread in Czech UI — native call. |
| `analytics.calibration.recApplying` | fluency | minor | "Používání…" reads as "Usage…" rather than "Applying…" (iterative imperfective for a one-shot action). CS-PROG licenses a verbal noun when the subject is unexpressed, so the form is legal; the lexical choice is the question. Candidate: "Používá se…", or "Probíhá použití…". Not mechanical, so left untouched. |
| `analytics.channels.colLeads` | terminology | minor | contract.md §6 lists *lead* among the terms that are translated, but the glossary's only *lead* row is the group-eval sense (favorit), a different concept (CS-HOMONYM). The catalog keeps "lead/leady" in ~15 channels.* strings. Confirm the funnel sense stays a loanword and add a second glossary row. |
| `analytics.calibration.familyAll` | terminology | minor | "Všechny pozice" sits under familyLabel "Rodina rolí"; elsewhere the same filter is "Všechny rodiny rolí" (profile.matrix.filterAllFamilies) or "Všechny obory" (jobs.tab.allFamilies, matrix.allFamilies, history.allFamilies). Three renderings of one filter; needs one ruling. |
| `library.tab.intake.tabLabel` | terminology | minor | After the role→pozice sweep, cs now calls the tab "Zadání pozice" while the brief panel inside it is "Zadání" (side.brief) / "Živé zadání" (brief.title) / "Upravit zadání" (edit.start). Two adjacent concepts (the intake session and the brief document) share one word — a CS-HOMONYM risk. They never render side by side today, so I left it, but a native call on renaming one of them would be cheap now. |
| `library.tab.intake.reopen.note` | fluency | minor | "Rozhovor znovu otevřel zadavatel." forces masculine on a person of unknown gender, which CS-GENDER calls a defect. The slash form ("otevřel/a zadavatel/ka") is ugly for a log line; an impersonal recast such as "Rozhovor byl znovu otevřen na žádost zadavatele." trips CS-PASS instead. Needs a native to pick the least-bad form; the same shape recurs across the event log. |
| `pipeline.tab.statNeedsIntake / pipeline.tab.filterIntake` | terminology | minor | Glossary pins intake → "příjem", and the siblings (candidateRow.degradedSuffix "příjem selhal", events.intake_resolved "příjem zadán ručně") follow it, but these two tight controls say "Vyžaduje zadání". "Vyžaduje příjem" reads wrong in Czech and "Vyžaduje zadání příjmu" blows the stat-tile budget. Needs a native call on a short form before a sweep. |
| `pipeline.events.added / re_applied / pipeline.tab.loadFailed` | terminology | minor | Glossary says inline "pipeline" stays a loanword in cs and only the NAV TAB is "Nábor". These inline uses render it "do náboru" / "už je v náboru" / "Nábor se nepodařilo načíst". Catalog-wide convention question, too large to settle inside one batch. |
| `pipeline.events.scored / scoredDetail` | fluency | minor | "byl/a automaticky obodován/a" — "obodovat" reads like grading a school test; "ohodnocen/a" or "získal/a skóre" may be the better register. Not anchored by any rule, so left untouched. |
| `pipeline.command.placeholder / pipeline.command.examples` | style | minor | The example commands use informal singular imperatives ("zamítni", "posuň", "spusť"). style-cs.md mandates vykání for the app's voice, but here the USER is addressing the app, so this may be deliberate. Confirm the house line on command-bar examples. |
| `profile.evidence.skillProvenanceAria` | terminology | minor | "Původ dovednosti" vs profile.editor.intro's "<b>proveniencí</b>" — provenance is rendered two ways in one cluster. Part of the same deferred evidence/provenance sweep; not touched so the rewrite lands in one pass. |
| `profile.matrix.clearFilters` | terminology | minor | Bare "Zrušit" for "Clear" collides with the app's Cancel button (glossary `revoke` row: "*zrušit* is the Cancel button beside it"; CS-HOMONYM). But changing it to "Vymazat" would break with its own family: cs has 4× "Zrušit filtry" and only 1× "Vymazat filtry". Needs one decision then a 5-site sweep, not a local edit. |
| `profile.result.addNext` | style | minor | Call site (ProfileResultPanel.tsx:71) is an uppercase eyebrow heading over the missing-fields list, not a control. cs "Doplnit dále" is an infinitive and therefore reads as a button (CS-CTRL reserves the infinitive for controls); de/fr split too (de "Als Nächstes ergänzen" = action, fr "À ajouter ensuite" = nominal). A native should pick between "Co doplnit" and keeping the action form. |
| `profile.editor.choice.career_switcher` | fluency | minor | "Mění obor" is a finite clause among noun-phrase choices ("Zkušený", "Student / junior") and duplicates the shape of profile.editor.wantsChange "Chce změnit obor". A nominal ("Změna oboru") would fit the picker better, but no rule anchors the choice. |
| `landing.demoNotice.title` | style | minor | "Živá ukázka teď není dostupná." — "teď" is standard but slightly spoken for a system notice; "právě" or "momentálně" may read better in the formal-vy operator register. Not anchored to a rule, left untouched. |
| `landing.previews.gates.queue.offer.sub` | terminology | minor | "Pásmo" alone for a compensation band. Czech tolerates the bare noun better than German did, and the catalog's report.panel.salaryEvidenceHint also says "aktuální pásmo", so it was left; a native may still prefer "Mzdové pásmo" on this card. Decide once, then sweep both keys. |
| `tasks.integrations.signingSecret` | style | minor | "Podpisový tajný klíč" stacks three nouns/adjectives and reads calqued next to de "Signatur-Secret" / fr "Secret de signature". "Podpisový klíč" or "Tajný klíč pro podpis" may be better in a field label, but the current value is not wrong and dropping "tajný" loses meaning. |
| `profile.archetypes.weightFieldLabel` | style | minor | Renders as "skills váha %" / "career váha %" — an English slug juxtaposed with a Czech noun with no case marking. fr recast it ("Pondération {slot} %"); cs and de kept English's order. A native may prefer "Váha: {slot} (%)". Blocked on the source defect below (the slot is never localized), so left as-is. |
| `profile.empty.listCtaAnalyze` | style | minor | "povýšit ho na profil" — "povýšit" is the promotion-in-rank idiom; the catalog's settled word for pipeline promotion is "postoupit". Deliberately not swept: this is a different concept (CV → saved profile), so CS-HOMONYM argues FOR a distinct verb. A native should confirm "povýšit" over "uložit jako profil". |
| `profile.empty.filed` | fluency | minor | one {# založena} / few {# založeny} / other {# založeno} agrees with an unexpressed feminine "karta" (consistent with `matrixCta` "Založit první kartu"). Grammatically right for that referent, but the string renders under an archetype label with no visible noun, so a native should confirm the feminine reading survives out of context (impersonal "založeno" in all branches is the safe alternative). |
| `report.compare.emptyBody` | fluency | minor | 'ohodnotili proti stejné pozici' calques 'score against'; the same namespace uses the more natural 'vůči' (report.panel.cvRewritesHint: 'vůči tomuto popisu pozice'). Non-mechanical, so left alone under the gate. |
| `report.print` | fluency | minor | 'Tisk / Uložit jako PDF' mixes a noun ('Tisk') with an imperative ('Uložit'); de and fr both use two verbs. 'Vytisknout / uložit jako PDF' would be parallel, but the button is tight and cs runs long (style-cs.md Length). |
| `channels.empty.comms.step1` | terminology | minor | Same drift on the Czech side: "doručovacího endpointu" here vs "Adresa relé" (channels.relay.urlLabel, channels.empty.comms.actionHint) and "HTTP adresa" (channels.email.introUnwired). Decide *adresa* vs *koncový bod* vs the loanword once, then sweep. |
| `devcase.checks.canary.flagged` | terminology | minor | glossary row "flag / alert \| označit / markieren / signaler" would give "označeno", but the sibling devcase.review.flagged already owns "označeno" for a different agent (the system flagged the case, vs the candidate calling out a planted flaw). Applying the glossary here would trade a terminology drift for a CS-HOMONYM collision. de has the same shape ("benannt" instead of "markiert"); fr is glossary-compliant ("signalé"). Needs one house call across cs+de before any sweep. |
| `devcase.cohort.weak` | fluency | minor | "{pct} % slabě" — the adverb dangles with no verb, unlike the sibling devcase.cohort.missed ("{pct} % přehlédnuto", a neuter participle). "{pct} % slabě zvládnuto" would parallel it but may not fit the chip. Needs a native + a look at the rendered width. |
| `landing.previews.rediscover.title` | fluency | minor | "{n} lidé" is a frozen nominative plural (CS-NUM). It renders correctly today because the call site passes HITS.length = 2 (app/landing/spark/previews/RediscoverPreview.tsx), but it breaks at n≥5 ("5 lidé") if the demo array grows. A correct ICU plural must also move the relative clause ("kterého/které už znáte") inside the branches; left alone rather than risk a compile-clean but clumsy rewrite of a string that is currently correct. |
| `landing.features.schedule.body` | leftover-source | minor | "kalendářový ping-pong přes reply-all" keeps the English "reply-all" while de translates it ("per Antwort-an-alle") and fr drops it. "reply all" is common Czech office speech, so this may be a deliberate register choice; a native should decide before a rewrite. |
| `landing.previews.gates.queue.advance.title` | terminology | minor | "Posunout {name} na pohovor" vs the glossary's advance → *postoupit* (and landing.trust.art.audit.blocks.advance = "Postup"). Transitive *postoupit koho* is awkward Czech, so the glossary word may not fit this recruiter-action button; needs a native call on whether the glossary row should carry an exception for the transitive use. |
| `scheduleTab.rounds.human / scheduleTab.rounds.ai` | style | minor | "Lidské kolo" / "AI kolo" as SegmentedControl labels (ScheduleTab.tsx:130-131). The adjective+kolo pattern reads calqued; a Czech recruiter would more likely say "Kolo s člověkem" / "Kolo s AI" or "Osobní kolo". No constructions ID covers a bare adjective calque, so not fixed. |
| `scheduleTab.lifecycle.markNoShow` | fluency | minor | "Nedostavil/a se" sits on a button (ScheduleInviteRecruiterControls.tsx:61), where CS-CTRL wants an infinitive. But en is also a bare noun label ("No-show"), and the alternatives ("Neúčast", "Nedostavení se") introduce a term the closed-state chip does not use. Needs one native call covering markNoShow + closedNoShow together. |
| `scheduleTab.transcript.humanScorecard` | style | minor | "Hodnoticí karta člověka" reads as "the human's scorecard" (possessor) rather than "scorecard filled in by a human". de "Menschliche Scorecard" has the same wobble. fr "Scorecard humaine" is fine. Needs a house call on the AI/human scorecard pairing. |
| `scheduleTab.lifecycle.calendarDropped` | fluency | minor | The `many` branch "# termínu skryto jako obsazených" mixes genitive singular noun with genitive plural adjective; it only renders for decimals, which this counter never produces. Either drop the branch or fix the agreement — needs a native call on which. |
| `scheduleTab.lifecycle.closedDeclined` | terminology | minor | "declined" in the Closed section rendered "odmítnuto". Left untouched because the glossary reserves *odmítnout* for the CANDIDATE declining, and it is genuinely ambiguous here whether the closed reason is the candidate declining or the recruiter refusing. Needs the product answer. |
| `scheduleTab.transcript.telemetryTalkShare` | style | minor | "Podíl mluvení kandidáta" is accurate and grammatical, but `mluvení` as a verbal noun reads a touch plain for a metrics label; "Podíl řeči kandidáta" may be the better Czech. No rule anchors either way, so it is a native call, not an error. |
| `scheduleTab.relay.label` | style | minor | "Kde je štafeta" renders the relay-baton metaphor with `štafeta`, which strictly names the relay/team; the baton is `štafetový kolík`. `předat štafetu` is idiomatic enough that this probably reads fine, and the full term would overflow the META_LABEL. Native call. |
| `scheduleTab.transcript.telemetryDuration` | accuracy | minor | cs says "Délka hovoru" (call length) where de/fr say "Sprechdauer"/"Durée de parole" (speaking time). The underlying field (interview-telemetry.ts:107) is the span from first to last utterance timestamp, so the Czech is arguably the most faithful of the three. Not changed pending a decision on what the English label actually means (see sourceDefects). |
| `nav.recent` | fluency | minor | "Nedávné" is a bare neuter-plural adjective with no head noun; it dangles the way CS-ABLE describes, but that rule is scoped to -able predicates so I could not anchor it. Candidates: "Naposledy otevřené" (long for the rail) or "Nedávno". de "Zuletzt" and fr "Récent" are idiomatic as-is. |

_201 items (24 major, 177 minor)._
