# French (fr) — native-review queue

Strings the `/i18n-translate review fr` pass left for a **native speaker** to confirm — subjective calls, unsettled domain/legal terms, or house-style decisions. High-confidence fixes were already applied to `messages/fr.json`; these are the remaining judgment calls (AI-flagged, need human sign-off).

| key | note |
|-----|------|
| `library.result.openPipeline` | Same English « Open Pipeline » is rendered « Ouvrir le pipeline » here but « Ouvrir Recrutement » in channels.openPipeline. Glossary localizes the Pipeline nav tab as « Recrutement ». A native should pick one: does this CTA target the tab (→ « Ouvrir Recrutement ») or read as the funnel loanword? |
| `report.panel.candidate` | « Candidat(e) » (inclusive) here vs plain « Candidat » in jobs.candidates.auditCandidate, scheduleTab.transcript.roleCandidate and history.colCandidate. Glossary allows candidat(e); native should standardize one form for these generic labels/column headers. |
| `decisions.groupEval.skillStrongTitle` | The whole decisions namespace omits the narrow NBSP (U+202F) before % that pipeline/analytics/results use consistently. Add U+202F before % in the ~14 percent-bearing keys: recAriaConfidence, recConfidenceSuffix, aiReview.pricingBasis, rules.ruleSentence, compliance.aiCheckIntro/anyAdverse/noAdverse, wave.reasons.rejectWould/rejectDid, summary.skillStrengthTitle, groupEval.skillStrongTitle/skillPartialTitle/salaryOver/salaryUnder/weight. |
| `decisions.reconsiderHelp` | 'remettre en présélection' names the Screened stage via the activity noun, not the stage label 'Présélectionné' (enums.stage.Screened). Decide whether to reference the stage label per the stage-name consistency rule or keep the more natural phrasing. |
| `analytics.log.kinds.rematched` | 'Rematché'/'Matché' (also rematched_from, matched) are franglais; the app renders the match verb natively elsewhere ('mis en correspondance'; pipeline.result.task.rematch = 'Nouveau matching'). Pick a canonical form and apply across these log kinds. |
| `analytics.declinedSub` | '{count} déclinés' vs the app's 'refuser/refusée' for a candidate declining an offer. Unify, but keep the reject (recruiter rejects) vs decline (candidate declines) distinction legible in the stat card. |
| `analytics.log.kinds.onboarding_started` | 'Onboarding démarré' and onboarding_failed ('de l'onboarding') use the raw loanword while the rest of the app — and the sibling onboarding_reminder_sent — use 'intégration'. Glossary sanctions both; choose one for the decision log (mind fem. agreement if switching: 'Intégration démarrée'). |
| `comms.interviewInvite.body` | Masculine forms for unknown-gender recipients ('Vous êtes invité'; offer.expiredBody 'intéressé'; interviewerBrief.body 'prévu') diverge from status.json's inclusive '(e)' convention. Apply neutral phrasing or '(e)' per the FR style guide. |
| `pipeline.tab.selectedCount` | Terse counters ('{count} sélectionné', bulkMoved/bulkAccepted/bulkRejected, passAdvanced) use an invariant singular participle, and '=1 {…} other {# …s}' keys (today.*, offerSent.title/titleQueued, queuedBanner, command.doneCount) render 0 with the plural noun. French wants agreement / singular-for-0 — verify count>0 display guards or move to one/other with agreement. |
| `analytics.log.kinds.outreach_sent` | 'Prise de contact' for outreach vs pipeline's 'approche'/'message d'approche' (pipeline.actions.outreach, result.task.outreach). Minor term inconsistency; unify if desired. |
| `landing.trust.human.body` | 'Aucun candidat n'est fait avancer...' — the passive of the glossary verb 'faire avancer' is non-idiomatic. A native would likely prefer 'Aucun candidat n'avance, ne reçoit d'offre ni n'est rejeté par la machine seule.' Rephrase is a judgment call. |
| `match.shared.koReasonsNote` | French 'one' covers 0 and 1, but the verb/pronoun sit outside the plural block: for count=1 it reads '1 poste ... n'ont pas passé le cap — parce qu'ils {reason}' (number disagreement). Needs an ICU restructure; the {reason} interpolation (likely a plural verb) makes a clean singular branch uncertain — native to decide. |
| `matrix.probes` | 'Interview probes' is rendered 'Questions d'entretien' here but 'Points à creuser en entretien' in match.shared.interviewProbes — same concept, two ways. Unify (direction is a judgment call; matrix is a dense popover where the shorter form may be intentional). |
| `jobMarket.salary.medior` | Seniority 'Medior' kept verbatim here, but enums.seniority.medior is translated 'Confirmé'. Same level rendered two ways across namespaces — pick one (jobMarket may intentionally mirror the ISPV band taxonomy). |
| `match.tab.noAnalyses` | 'lancez-en une dans Analyser' points at the Analyze tab, whose label is 'Analyse' (noun, per nav.tabs.analyze and profile.matrix.emptyBody). Parallel key noProfiles correctly says 'dans Profil'. Consider 'dans Analyse' for navigational consistency. |
| `aboutPage.steps.hired.body` | 'check-list' here vs 'checklist' used throughout the onboarding namespace. Trivial spelling unification — both valid French; pick one house form. |

_16 items._

## Queued by the 2026-08-04 `aboutPage` (/about) review

Namespace-scoped pass over `aboutPage` only. Framing verdict: **product**, not
company. Fixed: missing U+202F before ";" in `steps.screen.body`; "relais
d'intégration" → "passage à l'intégration" (*relais* is the comms delivery relay
per the glossary); « recruté » → « Recruté » to match `enums.stage.Hired`;
"check-list" → "checklist" (resolves the item above); and the stacked
appositives in `steps.interview.body`. Left for a native:

| key | note |
|-----|------|
| `aboutPage.steps.screen.body` | "un exercice pratique … — un exercice qui présuppose" repeats *exercice* inside one sentence. The resumptive is grammatical; a copywriter might elide it. |
| `aboutPage.steps.interview.title` | "Puis il parle aux gens" — *il* has no antecedent (en's "it" = the system). Same issue in de. |
| `aboutPage.*` apostrophes | Kept the straight `'` used by ~87 % of the fr catalog (1466 straight vs 216 typographic). A catalog-wide switch to U+2019 is a separate sweep. |

_3 items._

## Queued by the 2026-08-04 landing-page review (namespace `landing` only)

9 keys fixed in `messages/fr.json`; these were deliberately left alone.

| key(s) | severity | note |
|--------|----------|------|
| **AI vs IA, catalog-wide** | major | `landing` was unified on **AI** this pass (nav.trust, features.cases.body, previews.cases.note) — the same page already said AI in ~10 places, and the section rail put "IA responsable" beside "Une AI puissante". But the split is app-wide: 93 AI vs 25 IA across `messages/fr.json`. A native French product would almost certainly prefer **IA** everywhere; the glossary Do-Not-Translate list says AI. One house decision, then one sweep. |
| `landing.nav.about` | minor | "À propos de l'app" is a mild anglicism; "À propos de l'application" is the correct professional register. Left as-is only because `jobMarket.nav.about` carries the identical value and that namespace is owned elsewhere — change both together. |
| `landing.pricing.enterprise.blurb`/`capabilities[1]` | minor | "cloisonnée par **locataire**" for multi-tenant scoping reads as an apartment renter. French SaaS usually keeps "par tenant" or says "par organisation". de solves it with "mandantenbezogen". |
| `landing.pricing.tiers.*.features` ("cas dev conçu(s)") | minor | Renders the metered unit "dev case **design**" as a past participle ("designed"), shifting the meaning. "1 / 5 / 20 conception(s) de cas dev" is accurate but heavier in a bullet. |
| `landing.hero.ctaPrimary` | minor | "Présélection gratuite" is a noun phrase where en is an imperative; the fr style guide asks for vous-form imperatives. French CTAs tolerate noun phrases ("Essai gratuit"), so left. |
| `landing.pile.jana.role` | minor | "Développeuse React" is feminized to the named candidate while de keeps the generic masculine — the same gender-form policy question already queued for `match.tab.candidate`. |
| `landing.cta.body` | minor | "Laissez KandiDate l'en sortir" is grammatical but literary; "aller la chercher" is warmer for a closing CTA. |
## Queued by the 2026-08 `jobMarket` (/market) review

| key(s) | severity | note |
|--------|----------|------|
| `jobMarket.map.median` | major (fixed) | Was "Revenu médian" — *revenu* is income in general (household, capital), while the figure is an ISPV employment wage. Changed to "Salaire médian"; `salary.subtitle` and `orgTypes.subtitle` moved off "Revenus/Revenu" for the same reason. Confirm "salaire" over "rémunération" as the house term for this page (the tile label is very tight, which is why "rémunération médiane" was not used). |
| `jobMarket.demand.share`, `demand.shareTiny`, `orgTypes.subtitle`, `footer.coverage` | minor (fixed) | Missing U+202F before `%` and `:` — the same gap logged for the `decisions` namespace. Now aligned with `hero.updated`/`map.range`. |
| `jobMarket.map.range` | minor | "Les 50 % du milieu" → "Les 50 % centraux". A statistician may prefer "La moitié centrale gagne entre {lo} et {hi}", which reads better but restructures the sentence. |
| `jobMarket.families.frontline_service`, `.sales_marketing` | minor | Shortened for the JD filter chip and aligned to `enums.family` ("Service de terrain", "Vente et marketing"). `education_academic` ("Éducation et milieu universitaire") is still ~50 % longer than its English source in the same chip row — left alone for want of a shorter faithful form. |
| `jobMarket.salary.medior` | minor | Still the open item above: "Medior" verbatim here vs "Confirmé" in `enums.seniority`. |

## Queued by the 2026-08 `simulation` (public guided demo) migration

| key(s) | severity | note |
|--------|----------|------|
| `simulation.criteria.skills.name` | minor | "Correspondance des compétences" is about twice the width of en's "Skills match" in a three-column table. Faithful, but if the column wraps, "Compétences" alone would do — the header already says "Critère". |
| `simulation.status.done` | minor | "Terminé — poste pourvu 🎉" reframes en's "candidate hired" from the role's side (and avoids the gender agreement of *recruté(e)*), matching the cs/de call. `pipeline.controlCenter.hired` still says "Recruté 🎉". |
| `simulation.log.*` past participles | minor | "bloqué", "rejetés", "avancés", "matchés" take the generic masculine for candidates of unknown gender — the same policy question already queued for `match.tab.candidate`. |
| `simulation.diagram.*` | minor | Narrow-NBSP typography is applied to diagram TITLES ("Sourcing : publier…") but not to node labels, which carry no two-part punctuation. Nothing to fix; noted so a later pass does not "correct" it. |

## Queued by the 2026-08-12 full sweep (no-dash recast + full MQM review)

Raised while recasting every dash-as-punctuation string in all four catalogs
and then auditing the whole catalog against the English source. High-confidence
fixes were already applied; these are the calls a native speaker should make.
Each row carries its typed MQM category and severity.

| key | category | severity | note |
|-----|----------|----------|------|
| `apply.script.koAuth` | fluency | major | "travailler dans {location}" (and "à {location}" in apply.script.koModeLocation) picks one French preposition for a free-text job.location (app/_lib/apply.ts:238 passes job.location, a city OR a country). "dans Prague" and "à France" are both wrong. Fixing it needs a design call (apposition device like cs, or a "Lieu de travail : {location}" split), so I left it rather than half-fix it. |
| `legal.privacy.subprocessors.title` | terminology | major | fr uses "sous-traitant" for BOTH processor (legal.privacy.roles.processor, legal.terms.customerData.controller) and subprocessor (this key, subprocessors.body, terms.customerData.processor). RGPD Art. 28 says "un autre sous-traitant" for the subprocessor, so the bare plural is defensible and widely used in FR SaaS DPAs, but on a legal page it is genuinely ambiguous. A native/legal call is needed on whether to move the subprocessor sites to "sous-traitants ultérieurs". Not swept — a half-sweep across the 4 sites would be worse than the split. |
| `landing.proof.footline` | grammar | major | The old fr read "répondue par des preuves" — répondre is intransitive with à, so a passive "répondue" is ungrammatical. Rewritten as "avec des preuves à l’appui". Worth a native confirmation of the new rhythm on this handwritten footline. |
| `match.shared.koReasonsNote` | grammar | major | Moved <b> inside the plural branches to fix a real agreement bug (the old value rendered "1 poste n'ont pas passé le cap"). Also: the shared tail "parce qu'ils {reason}" stays masculine plural even in the singular branch — needs a native call on whether to duplicate the tail into the branches. |
| `devcase.processTrace.noSeed / devcase.processTrace.seedEngagement` | terminology | major | fr renders the planted repo *seed* as "la base" ("Aucune base concrète n'a été générée", "Engagement avec la base"), while cs and de keep the loanword ("seed" / "Seed"). "base" is ambiguous in FR dev speech (reads as database) and collides with "fichiers implantés" in the same string. No glossary row exists for *seed*; needs one decision then a sweep, so left untouched. |
| `jobs.compare.confidence.wide` | fluency | major | Same collocation issue: "confiance large" / "confiance modérée" / "confiance étroite" describe the interval, not the confidence. French statistics idiom is "intervalle de confiance large", or a level scale "confiance faible / moyenne / élevée". Deferred for the same coordinated-sweep reason as de. |
| `jobs.candidates.nearMiss` | fluency | major | "de peu" standing alone as a pill label is elliptical — French wants a verb for it to parse ("manqué de peu", or "juste en dessous"). en's "near miss" is a noun phrase; "de peu" is a bare prepositional phrase, so the label does not name the thing. No constructions-fr rule covers bare-fragment labels, so it is queued rather than rewritten. Suggested: "manqué de peu". |
| `report.jdEditedBadge` | terminology | major | fr renders JD as 'Offre modifiée le {date}', but glossary.md gives fr job description = 'fiche de poste' and the same namespace uses 'fiche de poste' (report.panel.jobFitPlaceholder, report.panel.noKeywords, report.panel.cvRewritesHint). Deliberately NOT touched: glossary.md 'House decisions' lists `fr JD → offre vs fiche` as still open, and warns a half-sweep is worse than none. Needs the house decision, then one sweep. |
| `channels.relay.urlLabel` | terminology | major | FR-ONE-WORD drift on *endpoint*: "endpoint" in channels.relay.urlLabel, channels.empty.comms.actionHint, channels.empty.comms.step1 vs "point d'accès HTTP" in channels.email.introUnwired and channels.email.notWiredBody. Three candidate renderings exist in FR tech (endpoint / point d'accès / point de terminaison) and the glossary has no row. Needs one decision then one clean sweep; a half-sweep is worse than the split, so nothing was changed. |
| `scheduleTab.transcript.readbackUnconfirmedHint` | accuracy | major | NOT in this batch, but it is the third member of the read-back cluster and still says "non repris lors de la relecture". Now that readbackHeading and readbackNote say `récapitulatif`, this key must be swept in the same merge (suggested: "…non repris lors du récapitulatif (peut-être une erreur de transcription)") or the cluster is left split. |
| `feedback.failed` | style | minor | "Impossible d’envoyer le retour." trips constructions-fr.md FR-IMPERSONAL / FR-FORMAL, which ban "impossible de…". Deliberately NOT changed: glossary.md "House decisions → still open" lists fr `Impossible de…` vs `Nous n’avons pas pu…` as an undecided 189-site split and warns that a half-sweep is worse than none. Needs the house call, then one clean sweep. |
| `login.emailPlaceholder vs feedback.emailPlaceholder` | locale-convention | minor | fr splits the sample domain: "vous@entreprise.com" (login) vs "vous@entreprise.fr" (feedback). cs and de are internally consistent (.cz / .de). Harmless but a native/house call on which TLD the FR sample address should use, then normalise both. |
| `skillProfile.methodology` | accuracy | minor | "an AI-allowed work sample" → "un travail où l’AI est autorisée" drops the "work sample" term of art (FR HR: "mise en situation" / "échantillon de travail"). cs and de both keep it ("pracovního vzorku", "Arbeitsprobe"). Worth a native call on the right FR term before sweeping. |
| `models.usage.fallbackCalls` | fluency | minor | "{count} de repli" renders as "3 de repli" in the chip. Understandable as an elided "3 [appels] de repli", but "{count} en repli" or "{count} repli(s)" may read better. Below the bar for a unilateral rewrite of a clean-compiling chip. |
| `onboarding.readyTitle` | terminology | minor | fr splits the onboarding concept: "Prêts pour l’intégration" / "Intégration standard" / "Intégration en santé" vs "Démarrer l’onboarding" / "Aucun onboarding en cours" / "Modèles d’onboarding". The glossary explicitly licenses BOTH ("onboarding / intégration"), so per the house rule that a half-sweep is worse than none, this needs one decision then one clean sweep. Not touched. |
| `onboarding.ledgerSignaturesCount` | fluency | minor | "{count} demandé(s)" is masculine. If the elided head noun is *signatures* it must be "demandée(s)"; if it is *documents* (the parent section is "Documents à signer") it is correct. Needs a look at the rendered chip. FR-PLURAL also discourages the "(s)" form outside genuine space pressure. |
| `analyze.noInputs` | accuracy | minor | "Aucun document joint" vs en "No inputs attached": inputs include pasted text and a GitHub handle, not just files. "Aucun élément joint" proposed; left unchanged under the minor/non-mechanical gate. |
| `history.disposition.advance / .hold / .pass` | terminology | minor | The fr trio is "avancer / en attente / écarter" while cs and de both match their glossary rows verbatim (postoupit/pozdržet/zamítnout, weiterleiten/zurückhalten/ablehnen). glossary.md gives fr "faire avancer" (advance), "mettre en attente" (hold), "rejeter" (reject). Two caveats stopped me rewriting: the en leaf here is "pass", not "reject", so the glossary row does not strictly govern, and "écarter un candidat" is standard French HR. Also the fr trio breaks form (two verbs plus one adjectival state). A native should either bless the fr wording and add a glossary row for the analyst disposition, or sweep all three at once. |
| `jobMarket.families.data_ai` | terminology | minor | fr keeps "Data et AI" while de translates the head noun ("Daten & AI"). "Données et AI" would be the native form, but "Data" is current in FR tech-HR. Per-locale call; not swept. |
| `enums.family.data_ai` | leftover-source | minor | "Data / AI" keeps the English head noun while every sibling family is translated (Logiciel, Produit / Projet, Santé / Clinique). "la data" is genuinely idiomatic in French tech-HR, so this may be correct; "Données / AI" is the alternative. Related to the enums.family vs jobMarket.families divergence already queued in review-cs.md. |
| `match.card.missingTitleEarly` | style | minor | "(souvent acquérable)" — acquérable is attested but rare; a native may prefer "(souvent rattrapable)" or a verb clause. No anchor beyond taste, so not changed. |
| `nav.attentionBadgeGo` | style | minor | "{count} à traiter. Les afficher." — the pronoun "les" resolves to an elided noun that never appears in the string, which is fine spoken but thin as a link name. A native should decide between this and naming the cohort ("{count} à traiter. Afficher la sélection."). |
| `data.eraseExplainer` | style | minor | Colon-plus-enumeration in a sentence that already runs long, on a public candidate surface. Narrow no-break space (U+202F) is set before the colon per FR-SPACE. A native should confirm the colon does not collide with the later parenthetical "(score et étape, sans données personnelles)" for readability. |
| `branding.loadFailed` | terminology | minor | Carried "image de marque" through unchanged from the existing value for "branding". The term is not in glossary.md and the sibling key branding.accentContrast never names it, so the rendering is unverified rather than decided. Worth a glossary row on the next run. |
| `channels.comms.deadLetters` | style | minor | "# messages non distribués demandent votre attention" is faithful to the rewritten English verb but runs long for a chip. "# messages non distribués à traiter" is the shorter native idiom if the control is tight — needs a look at the rendered width. |
| `setup.handoff.readyTitle` | accuracy | minor | Same dodge as cs: "{org} peut démarrer" instead of "{org} est prêt(e)". A native may prefer "{org} : tout est prêt", which keeps the state reading and is still gender-free. |
| `channels.empty.email.effort` | style | minor | Colon-joined efforts ("Environ 3 minutes : une règle par poste") replace the dash across the three sibling effort keys. The narrow no-break space before the colon is present; confirm it survives the merge into fr.json intact. |
| `offer.acceptedBodyGeneric` | style | minor | "Merveilleux, bienvenue à bord !" — candidate-facing warm register. Some French teams find "Merveilleux" over-effusive for a hiring confirmation; "Formidable" or simply "Bienvenue à bord !" are the safer natives. Meaning is unchanged from source, so left. |
| `models.quality.na` | terminology | minor | Chose "n.d." (non disponible). "N/A" is also widely tolerated in French tech UI. Confirm which one this product wants, since the same cell repeats four times per row. |
| `landing.trust.human.body` | style | minor | Recast the dashed tail as "Cela relève de la conception, pas d’un réglage" rather than "C’est par conception…", per FR-IMPERSONAL which bans c’est. Slightly more formal than the English's clipped "by design, not by a setting"; confirm the register. |
| `landing.hero.subtitle` | terminology | minor | "entonnoir" for funnel is retained, per the standing open item in constructions-fr.md (FR-ONE-WORD). Still awaiting the native call; unchanged in this pass. |
| `aboutPage.meta.title` | style | minor | "À propos de l’app : KandiDate" — the colon recast of the site-name dash is defensible but a French team would more likely write "À propos de KandiDate". Same doubt applies to en/cs/de. Confirm with whoever owns SEO titles; ideally the separator moves into code (see sourceDefects). |
| `workspaceAdmin.members.removed` | grammar | minor | "{name} retiré(e)" keeps the bracket form the style guide discourages (FR-PLURAL: avoid "(s)"/"(e)"). A neutral recast such as "{name} a été retiré du compte" still forces masculine. Native call needed on which compromise ships. |
| `results.github.trackedNote` | style | minor | Recasting the dash into a full stop meant the quoted phrase now opens a sentence, so it was capitalised: « Aucune lacune ». Confirm French house preference for capitalising inside guillemets at sentence start. |
| `analytics.compute.perHireMultiWorkspace` | style | minor | FR-IMPERSONAL flags sentence-initial "Impossible de…". Here it mirrors English's "Can't attribute…" (a capability statement, not a failure report), so I left it — but glossary.md lists the "Impossible de…" vs "Nous n’avons pas pu…" split as an open house decision across 189 sites, and this key should follow whatever that decision lands on. |
| `comms.devcaseFeedback.subjectRole` | style | minor | "Votre exercice pour le poste de {role} : quelques retours" is long for a subject line and {role} follows "de", which mis-elides before a vowel-initial title ("de Ingénieur" instead of "d’Ingénieur"). Consider "Votre exercice {role} : quelques retours". |
| `match.card.partialTitle` | style | minor | Left "({pct}%)" without the French no-break space before %, matching the existing value. French typography wants "{pct} %" (as cs already does). Deliberately not swept here — it is a catalog-wide decision, not a one-string fix. |
| `match.weights.weightAria` | style | minor | Value is lowercase "poids {label}" while cs/de/en capitalise. Left unchanged (no dash in it beyond the cs one I fixed), but it violates fr sentence-case. |
| `library.form.titlePlaceholder` | terminology | minor | "Ingénieur senior automatisation AI, RetailCloud" — the comma now separates title from company where the dash did. Fine as an example placeholder, but confirm French job-ad convention (often "chez RetailCloud"). |
| `apply.resumedNote` | style | minor | 'arrete(e)' keeps the parenthesised agreement the FR-PLURAL rule tolerates only under space pressure. A neutral rewrite ('nous avons repris la ou vous en etiez') would avoid it entirely; not applied because the dash did not touch that clause. |
| `jobs.ingest.pasteIntro` | terminology | minor | "un poste structuré et matchable" — matchable is an unassimilated anglicism. "rapprochable" is clumsy, "exploitable par le moteur de matching" is longer but native. The glossary keeps matching as a loanword, so the derived adjective is the open question. |
| `jobs.rediscover.whyNow.closed` | accuracy | minor | "Est allé jusqu'au bout d'un poste fermé en cours de route" preserves the English idiom but leaves the participle masculine for an unknown-gender candidate. The whole whyNow.* family has this issue (allé / Écarté / engagé / Classé); a native should decide between a neutral recast and the candidat(e) form the style guide allows. |
| `decisions.batch.failed` | style | minor | Rewritten as "Échec de la décision pour {count} (toujours sélectionné, réessayez)" to avoid the contested "Impossible de…" (glossary lists that choice as still open, 189 sites). A native should confirm the nominal opener reads right on this inline status, and that "toujours sélectionné" is acceptable un-agreed given the source has no plural. |
| `profile.editor.draftHint` | style | minor | Recast as a parenthetical with a colon: "(collez un extrait de CV, un profil LinkedIn ou des notes : nous structurons + orientons le tout)". The en/cs/de versions keep the source's semicolon; French drops it per FR-SEMICOLON. Confirm the colon inside parentheses reads naturally on this button-adjacent hint. |
| `devcase.review.feedbackPlaceholder` | style | minor | Inside the guillemets the dash became a colon: « trop large pour un junior : restreignez la tâche 2 … ». A colon inside a quoted example plus the mandatory U+202F is typographically busy; a native may prefer a full stop inside the quote. |
| `devcase.emptyLedger.pasteTrace` | style | minor | "À côté d’eux, un collage massif …" — the fronted adverbial is a calque of en's "Alongside them". A native may prefer "En complément" or dropping the connector entirely. |
| `pipeline.tab.moveFailed` | style | minor | FR-IMPERSONAL says match 'Couldn’t…' with 'Nous n’avons pas pu…' rather than 'Impossible de…', but glossary.md flags this as an unsettled 189-site split. Left as 'Impossible de…' here (also bulkFailed, bulkDraftFailed, scheduler.updateFailed) rather than half-sweeping it. Needs the house decision. |
| `interviewSim.intro` | terminology | minor | "son lien à jeton" for "their tokenized link". Confirm against fr usage in the public schedule/status surfaces — "lien personnel" or "lien sécurisé" may be the operator-facing wording already in the catalog. Do not change unilaterally; it needs to match the apply/schedule/status namespaces. |
| `billing.loadFailed` | style | minor | Left as "Impossible de charger la facturation" despite FR-IMPERSONAL preferring "Nous n'avons pas pu…". Deliberate: glossary records this as an open 189-site split where a half-sweep is worse than none. Flagging so the eventual decision picks this string up rather than treating it as already settled. |
| `scheduleTab.transcript.telemetryLongestPause` | fluency | minor | "Plus longue pause après une question" — the idiomatic French superlative would be "Pause la plus longue après une question". The telegraphic label form is defensible in a compact dt slot (PipelineInterviewTelemetryStrip.tsx line 46-48), so left as shipped; a native should rule. |
| `workspaceAdmin.intro` | terminology | minor | "Isolez les données kp dans des locataires distincts." — "locataire" is the standard FR calque for multi-tenant, but in an operator console it can read as "renters". A native FR SaaS reviewer should rule between "locataires", the kept loanword "tenants", or a recast ("dans des environnements cloisonnés"). Not changed: no anchor, and cs/de ("tenantů"/"Mandanten") are both correct. |
| `report.panel.filterAll` | fluency | minor | Rendered as `Tous (12)` over a list of *questions* (feminine), which would argue for "Toutes". Left as is because the sibling chips ("Comportemental", "Technique") are masculine-singular abstract category labels, so the cluster reads as label-frame rather than agreeing with the antecedent. A native should rule on which frame wins. |
| `landing.previews.rediscover.hits.lucie` | style | minor | "Deuxième de peu, Backend II" for "Silver medallist" is accurate but reads slightly unidiomatic; "Deuxième de justesse" or "Finaliste malheureuse" may land better in marketing copy. Left untouched (taste, not an anchored error). |
| `landing.previews.cases.title` | terminology | minor | "contrôles de paternité" can be misread as DNA paternity testing. NOT changed: "paternité" is the established fr rendering of authorship across the devcase namespace (devcase.evalPanel.followupsTitle and 5 more), so a landing-only edit would create the exact half-sweep the glossary warns against. If the house wants "authenticité", it needs one decision plus a full sweep. |
| `landing.trust.art.audit.blocks.pass` | style | minor | "Refusé · {name}" is a participle where the cs/de siblings use nouns (Zamítnutí / Absage) and the fr catalog renders the noun "Rejection" as "Refus" elsewhere. Reads fine in the block; flagged only for a house call on block-label part of speech. |
| `pipeline.result.task.outreach` | terminology | minor | "Approche" for "Outreach". FR recruiting says "approche directe" / "prise de contact"; bare "Approche" is understandable but slightly ambiguous on a task chip. No glossary row exists for outreach, so this is unanchored. |
| `devcase.cohort.evaluated` | fluency | minor | "· {count} évalué(e)s" renders "1 évalué(e)s". A plural expansion would stack with the already-noisy (e); a native should decide whether to drop the inclusive form or the plural s here. |
| `scheduleTab.calendar.weekOf` | fluency | minor | "Semaine du {range}" where {range} is already a two-date range (ScheduleCalendarWeekPager.tsx:37) renders "Semaine du 11–17 août"; idiomatic French is "Semaine du 11 au 17 août", which the placeholder cannot produce. "Semaine {range}" would be safer. Needs a native call on whether it is worth the divergence from cs/de. |
| `feedback.emailPlaceholder` | style | minor | fr uses "vous@entreprise.fr" here but "vous@entreprise.com" at login.emailPlaceholder, from the same en source "you@company.com". cs (vy@firma.cz) and de (name@firma.de) are each internally consistent. Purely a house-style call on the example TLD; no anchored rule covers it, so left untouched. |
| `history.colCandidate` | terminology | minor | glossary.md candidate row gives fr "candidat(e)" with the note "de+fr agree gender; use inclusive form when unknown". de honours it here ("Kandidat/in"), fr does not ("Candidat", and "Rechercher un candidat" in history.searchLabel/searchPlaceholder). Whether fr adopts the inclusive form catalog-wide is a house decision spanning far more than this batch, so I left it rather than half-sweeping it. |
| `pipeline.scheduler.outcome.queued` | fluency | minor | "en file pour approbation" is understandable but reads translated; idiomatic French UI is "en attente d'approbation". Left alone because the sibling pipeline.applied.held_for_review already uses "en attente" and I could not rule out a deliberate queued-vs-held distinction. One decision, then both strings. |
| `pipeline.controlCenter.passLine` | fluency | minor | "{advanced} avancé · {rejected} rejeté · {held} en attente · {alerts} signalé" — the participles are frozen singular and will not agree when a count is >1. Fixing it properly needs four ICU plurals (or invariant nouns: "avancés"/"rejets"), which is a shape change worth a deliberate call rather than a silent rewrite. |
| `library.result.lintHeading` | accuracy | minor | en "Specificity check" -> "Contrôle de précision". cs/de render the concept as concreteness ("konkrétnost" / "Konkretheit"); French "précision" leans toward accuracy rather than specificity. "Contrôle de la précision" or "Contrôle de spécificité" are alternatives; needs a native ear. |
| `library.tab.intake.columns.badge.draftDrafting` | fluency | minor | "pas encore prêt à créer" — the passive-sense "prêt à + infinitif" (as in "prêt à consommer") is defensible, but a reader may parse it actively (the draft is ready to create something). "pas encore prêt à être créé" removes the ambiguity. Left untouched pending a native ruling. |
| `jobs.compare.confidence.tight` | fluency | minor | "confiance étroite" — in French *étroit* does not collocate with *confiance*; statistical French says "intervalle/fourchette de confiance étroit(e)". Czech solved it by dropping the term ("úzké rozpětí"). The call site (JobsCompareInterviewsCohortTable.tsx, text-meta in a table cell) is tight, so the fix is a re-labelling decision: "fourchette étroite" vs keeping *confiance*. |
| `analytics.compute.manualWindowed` | locale-convention | minor | "vue Depuis le début uniquement" capitalizes mid-sentence to quote the control label, which analytics.compute.manualAllTime renders lowercase ("depuis le début"). Either guillemets or a recast ("uniquement dans la vue depuis le début") would fix it; left alone to avoid changing the label reference. |
| `analytics.log.kinds.rematched` | style | minor | "Rematché" (and rematchTo/rematchFrom/csvRematchCounterpart) is franglais. It is consistent with analytics.log.kinds.matched ("Matché à un poste") and glossary.md tolerates "matching" in fr, so this needs one house decision across the whole matching cluster, not a local edit. |
| `pipeline.drawer.humanScorecard` | fluency | minor | "Scorecard humain" — the grammatical gender of the loanword *scorecard* in fr is unpinned across the catalog (cf. "le scorecard" in screenNote/scorecardNote). Needs a CS-LOANGENDER-style ruling for fr before any agreement is corrected. |
| `pipeline.tab.viewNamePlaceholder` | terminology | minor | "Entretiens qui traînent" renders *aging* differently from the sibling slaEditorTitle ("stagnation"). Idiomatic as written, so not swept; if *aging* gets a glossary row, align both. |
| `pipeline.tab.eventsError` | style | minor | "Impossible de charger…" trips FR-IMPERSONAL, but glossary.md lists fr `Impossible de…` vs `Nous n'avons pas pu…` as an OPEN house decision across 189 sites. Deliberately not swept — a half-sweep is worse than the split. Same for the semicolon kept in pipeline.actions.screenNote (95 sites, also open). |
| `jobs.candidates.poolFit` | fluency | minor | "Correspondance vivier ({count})" is an English-style noun juxtaposition; French normally binds with a preposition ("Correspondances du vivier"). The sibling jobs.candidates.poolFitTitle/poolFitNote already say "vivier de candidats", so the term is settled; only the chip's syntax is at issue. Left as-is because the fix costs ~4 chars in a chip that already carries an icon + count (JobsRecruiterCandidates.tsx:91). |
| `jobs.candidates.reachedOut` | fluency | minor | "✓ Contacté" defaults to masculine for a candidate of unknown gender. style-fr.md asks for neutral phrasing or "candidat(e)", but FR-PLURAL discourages the "(s)"/"(e)" form in running text. A neutral recast ("✓ Contact envoyé") would resolve it; wanted a native call on whether the chip reads as an operator action (impersonal, fine as-is) or as a candidate state (needs agreement). |
| `decisions.compliance.covered2 vs decisions.wave.confirmBody` | terminology | minor | "audit record" is rendered "enregistrement d’audit" in one and "registre d’audit" in the other (FR-ONE-WORD). Catalog-wide the split is 5 "piste d’audit" / 2 "registre d’audit" / 1 "enregistrement d’audit", so unifying only these two would be a half-sweep. Needs one decision plus a full pass. |
| `decisions.rules.onlyIfBelow / decisions.wave.onlyIfBelow` | length | minor | "…seulement si la correspondance est inférieure à" is ~2.2× the source and sits in a half-width column of a grid-cols-2 modal (DecisionsRulesModal.tsx:109) and beside a right-aligned value (DecisionsScreenWaveModal.tsx:129). It wraps rather than truncates, so nothing is lost; a shorter idiom would still be better if a native can find one that keeps the meaning. |
| `decisions.groupEval.leadTied` | style | minor | "À égalité de fait" for "Effectively tied" reads stilted; "Pratiquement à égalité" is the natural French. No anchorable rule, left untouched. |
| `library.tab.intake.brief.learnable` | fluency | minor | "apprenable" is attested but marginal in professional French; a native should confirm against "à acquérir" / "peut s’apprendre". Chip-length constraint applies (rendered inline beside a requirement). No style-guide anchor, so not touched. |
| `pipeline.tab.showingCount` | fluency | minor | "{shown} sur {total} affichés" renders "1 sur 12 affichés". An ICU plural on {shown} would fix it, but the control may never show shown=1; left alone rather than churning. |
| `profile.roster.matchTitle` | terminology | minor | "Confronter {name} aux postes ouverts" (and roster.intro "confrontez-le aux postes ouverts") introduces a third rendering of match beside the button "Matcher" and "prêt à être matché"; glossary offers "matching / rapprochement", not *confronter*, and reads adversarial for a candidate-adjacent action. Left untouched because glossary.md's "Still open" list explicitly parks several fr term decisions; needs a house call before a 5-site sweep. |
| `landing.previews.gates.queue.offer.title` | style | minor | "projet d’offre" for "offer draft" — correct contractual French, but "projet" can also read as project on a recruiting surface. A native should confirm against "brouillon d’offre"; not changed. |
| `report.panel.filter` | fluency | minor | "Filtrer" is an infinitive where the call site (InterviewTab.tsx:134) is a non-interactive <span> label preceding filter chips, and the siblings are nouns (cs "Filtr", de "Filter"). "Filtre" or "Filtrer par :" would match the element type, but bare "Filtrer" as a section label is common enough in FR UI that I did not want to churn it. |
| `report.panel.filterAll` | fluency | minor | "Tous" does not agree with the thing being filtered (des questions, fem. pl. → "Toutes"). Generic "Tous" is the near-universal FR filter convention, so this is a house call, not a clear error. |
| `report.panel.coachMustMany` | style | minor | "vos premières puces" for "top bullets": "puces" alone reads as the bullet glyphs; "vos premiers points" or "les premières lignes" may be the better HR-French rendering. |
| `tasks.outcome.advanced` | fluency | minor | "{count} avancé" sits in a stat line beside "{count} en attente" and "{count} à titre indicatif". The participle should agree in number but {count} is unknown at authoring time; "avancés" or a nominal recast ("{count} en progression") may read better. Anchored only in style-fr agreement, which cannot be satisfied cleanly here — a native should rule. |
| `tasks.system.seeds` | style | minor | "Données seed" keeps an untranslated technical modifier where cs went with "Ukázková data" (sample data). "seed" is not in the glossary's loanword list for fr; "données de démonstration" may be the better FR term. |
| `report.verdict.framingStrong` | accuracy | minor | 'Strong overall fit' rendered as 'Bonne adéquation globale' (= good), while report.scoreBands.strong fr is 'Fort'. The strong-tone verdict banner therefore reads one notch weaker than its own band label. 'Forte adéquation globale' would align, but the banner is marketing-adjacent prose and a native should rule on whether 'forte adéquation' is idiomatic here. |
| `report.tabs.compare` | style | minor | Tab label is the verb 'Comparer' while every sibling tab is a noun (Extraction, Adéquation, Salaire, Entretien, GitHub); cs/de both use nouns (Porovnání / Vergleich). 'Comparaison' would match, but it is longer in a tab strip (app/_components/results/ResultPanel.tsx:123) so a native + a look at the rendered strip should decide. |
| `jdPublic.notAccepting` | fluency | minor | "N'accepte pas encore de candidatures" is subject-less verb-first, the same shape flagged and fixed in de. French elliptical UI labels tolerate it better; "Candidatures pas encore ouvertes" is the alternative. Left as written. |
| `channels.empty.ads.step1` | accuracy | minor | "le poste que vous mettez en avant" renders "the role you are advertising" as promoting/highlighting rather than running an ad; cs ("inzerujete") and de ("für die Sie werben") both keep the advertising sense. "que vous annoncez" would match, but the surrounding ad-form cluster already disambiguates. |
| `devcase.processTrace.seedEngagement` | terminology | minor | "seed" is kept as a loanword in cs and de ("seed" / "Seed") but rendered "la base" in fr here and in devcase.processTrace.noSeed. "la base" is readable (the starter codebase) but can be misread as "the database"; "l’amorce" or keeping "le seed" are the alternatives. One decision, then both fr strings together — a half-sweep would be worse. |
| `landing.previews.cases.title` | accuracy | minor | "contrôles de paternité" for "authorship checks". *Paternité (d'une œuvre)* is the correct legal French for authorship, but standing alone on a chip beside "Exercice à domicile" it reads ambiguously. A native should rule between keeping it and "contrôles d'authenticité" / "vérification de l'auteur". |
| `landing.pricing.enterprise.stat2Value` | locale-convention | minor | French number+unit spacing is inconsistent across the batch: stat1Value already uses U+202F before %, but "~23 h", "40–51 h", "0 Kč", "≈ 10 $", "4 min 12 s", "45 min" all use a plain breaking space, where cs and de use U+00A0 throughout (CS-NBSP / DE-NBSP). style-fr.md documents U+202F only before ; : ! ? and inside guillemets, so there is no fr anchor for units — needs a documented rule before a sweep, otherwise it is churn. |
| `scheduleTab.prep.sourceFallback` | terminology | minor | "Gabarit générique" for "Template fallback" — *gabarit* is print/typography French; the software term is *modèle*. No glossary row for template, so not swept. |
| `scheduleTab.transcript.telemetryLongestPause` | style | minor | "Plus longue pause après une question" fronts the superlative the way English does (style-fr.md warns against calquing English order), but article-less superlative labels are established in French dashboard/stat French ("plus longue série"). "Pause la plus longue après une question" is the safer form. Native call; not changed. |
| `simulation.wave.subtitle` | fluency | minor | "{cohort} matchés · {rejected} rejetés automatiquement · {kept} avancés" forces plural participles onto counts that can be 1 ("1 matchés"). The en source has no plural block to expand, so a fix means inventing three ICU plurals in a subtitle. cs sidesteps it with the count-invariant neuter form (style-cs escape hatch); a native should decide whether fr wants the same recast or three plural blocks. |

_94 items (10 major, 84 minor)._
