# Context Scan Report

**Date**: 2026-06-20
**Project**: kp
**Project ID (live registry)**: `a3f8c2d1-7b4e-4f9a-9c6d-2e8b5a1f0d47`
**Project ID (orphaned, from /scan-contexts template)**: `5afc2006-01a0-4f50-86ad-d62b1a485caf`
**Scanner**: scan-contexts — **delta update** over the 2026-06-17 baseline
**Mode**: online — **DB sync COMPLETE** (Vibeman API restarted mid-scan)

---

## ✅ Database sync COMPLETE

The Vibeman API was down at the start of the scan (`localhost:3000` →
`HTTP 000`); the delta was computed offline. The user then restarted the API and
the full delta was pushed to the SQLite store on project `a3f8c2d1-…`:

- **1 context created** (POST) — *Candidate Onboarding Hand-off*
- **11 contexts updated** (PUT) — file lists + apiRoutes
- **0 deleted** — the 2 removed files were dropped from their contexts' file lists

Post-push verification against the live API: **43 contexts, 919 unique files,
0 duplicates.** `context-map.json` was auto-regenerated by Vibeman from the DB and
now matches (43 / 919).

**API contract notes (for the next scan):**
- `POST /api/contexts` creates; body = `{ projectId, groupId, name, description,
  filePaths, category, businessFeature, apiRoutes, dbTables, … }`. Note: `apiRoutes`
  / `dbTables` did **not** persist on create here — they had to be set with a
  follow-up PUT.
- `PUT /api/contexts` updates; body = **`{ contextId, updates: { … } }`** (the id
  goes in `contextId`, not `id`/query; fields to change go under `updates`). This
  is the non-obvious part — `{ id, … }` returns `400 "Context ID is required"`.

**Project-ID note (carried from the 2026-06-17 report):** the `/scan-contexts`
command template still passes the orphaned id `5afc2006-…`, which has zero
contexts. The live registry maps **kp** to `a3f8c2d1-…` (port 3009), which holds
all 43 contexts. `context-map.json` uses the live id. The push targeted
`a3f8c2d1-…`.

---

## Execution Summary

| Metric | Baseline (2026-06-17) | This scan (2026-06-20) |
|--------|-----------------------|------------------------|
| Context Groups | 12 | 12 |
| Contexts | 42 | **43** (+1) |
| Mapped file paths (unique) | 879 | **919** (+40 net) |
| Overlaps (file in >1 context) | 0 | **0** |
| New contexts created | — | **1** |
| Existing contexts updated | — | **11** |
| Files added to map | — | **42** |
| Files removed from map | — | **2** |
| Coverage of tracked source | 100% | ~100% |
| DB writes | 42 created | **1 created + 11 updated** |

Net file change `+40` = `+42 added − 2 removed`.

---

## What changed in the codebase since 2026-06-17

The diff was computed by extracting every `filePaths` entry from
`context-map.json` and comparing it against the live working tree
(`git ls-files` + untracked-not-ignored, restricted to `.ts/.tsx/.py/.mjs/.cjs`).

Five new feature/infra clusters landed (auth gating + workspace lock, the
post-Hired onboarding hand-off, GDPR consent/erasure surfaces, the NL pipeline
command bar, and the dev-inspector tooling), plus marketing (`/about`, Spark
landing pieces) and a batch of public-endpoint hardening utilities.

---

## 1 New Context

| # | Context | Group | Category | Files | DB status |
|---|---------|-------|----------|-------|-----------|
| 1 | **Candidate Onboarding Hand-off** | Offers & Automation | `ui` | 9 | ✓ Created (`ctx_1781942978794_apwkz5k`) |

**Candidate Onboarding Hand-off** — the lifecycle step past *Hired*: a reusable
checklist template per new hire, a pre-boarding entry questionnaire, and an
audit-stamped e-sign provider seam (`markSigned` — a real eIDAS provider wires in
there). Covers the recruiter Onboarding tab and the candidate-facing token-gated
questionnaire reached from an accepted offer.
- `app/features/sub_onboarding/OnboardingTab.tsx`
- `app/onboarding/[token]/page.tsx`
- `app/api/onboarding/route.ts`
- `app/api/onboarding/[id]/route.ts`
- `app/api/onboarding/candidate/[token]/route.ts`
- `app/_lib/onboarding.ts`
- `app/_lib/onboarding.test.ts`
- `app/_lib/onboarding-store.ts` *(owns the `onboarding_*` tables)*
- `app/_lib/onboarding-candidate.ts`
- apiRoutes: `/api/onboarding`

---

## 11 Updated Contexts

| # | Context | Group | Files (was → now) | Added |
|---|---------|-------|-------------------|-------|
| 1 | Auth, Sessions & Workspace Tenancy | Identity, Data & Privacy | 22 → **28** | 6 |
| 2 | Privacy, Consent & Provenance | Identity, Data & Privacy | 8 → **12** | 4 |
| 3 | Pipeline Board & Candidate Drawer | Pipeline, Decisions & Channels | 26 → **30** | 4 |
| 4 | Communications & Inbound Channels | Pipeline, Decisions & Channels | 15 → **17** | 2 |
| 5 | Tasks & System Operations | Platform, Shell & Shared UI | 16 → **22** | 6 |
| 6 | Landing & Marketing | Platform, Shell & Shared UI | 9 → **14** | 6 (−1) |
| 7 | Shared Utility Libraries | Platform, Shell & Shared UI | 20 → **21** | 1 |
| 8 | Model & API Key Management | LLM Provider Layer & Models | 12 → **14** | 2 |
| 9 | Guided Pipeline Simulation | Insights, Analytics & Simulation | 18 → **19** | 1 |
| 10 | Offers & Onboarding | Offers & Automation | 7 → **8** | 1 |
| 11 | Sourcing, Campaigns & Rediscovery | Jobs, JD Library & Sourcing | 22 → **21** | 0 (−1) |

### Per-context additions

**Auth, Sessions & Workspace Tenancy** (+6) — operator/session gating + the
single-workspace safety lock (tenancy is half-built; the lock fails closed until
every PII/business table is workspace-scoped).
- `app/_components/auth/HomeGate.tsx`, `app/_components/auth/SignOutButton.tsx`
- `app/_lib/auth/devAuth.ts`, `app/_lib/auth/require-operator.ts`
- `app/_lib/workspace-lock.ts`, `app/_lib/workspace-lock.test.ts`

**Privacy, Consent & Provenance** (+4) — recruiter consent snapshot/audit for the
candidate drawer, plus the public token-gated GDPR erasure (Art. 17) self-service.
- `app/api/pipeline/[id]/consent/route.ts`, `app/features/sub_pipeline/ConsentPanel.tsx`
- `app/api/data/[token]/route.ts`, `app/data/[token]/page.tsx`
- apiRoutes += `/api/data/[token]`

**Pipeline Board & Candidate Drawer** (+4) — the natural-language pipeline command
bar (pure parse layer + preview-then-confirm route + UI).
- `app/_lib/pipeline-command.ts`, `app/_lib/pipeline-command.test.ts`
- `app/api/pipeline/command/route.ts`, `app/features/sub_pipeline/CommandBar.tsx`

**Communications & Inbound Channels** (+2) — request-level idempotency for the
public inbound webhook (dedupes lead-source retries).
- `app/_lib/webhook-idempotency.ts`, `app/_lib/webhook-idempotency.test.ts`

**Tasks & System Operations** (+6) — the dev-inspector overlay and its build-time
source-location injection tooling.
- `app/_dev-inspector/DevInspector.tsx`, `app/_dev-inspector/devInspectorUi.tsx`, `app/_dev-inspector/devLocate.ts`
- `scripts/dev-inspector/inject-source-loc.cjs`, `scripts/dev-inspector/source-loc-loader.cjs`, `scripts/dev-guard.mjs`

**Landing & Marketing** (+6, −1) — the `/about` page and the Spark dark-theme
landing pieces.
- `app/about/page.tsx`
- `app/landing/spark/AboutCurve.tsx`, `AboutHome.tsx`, `LandingLangSwitch.tsx`, `SparkHome.tsx`, `aboutIllustrations.tsx`
- removed: `app/landing/layout.tsx` *(deleted)*

**Shared Utility Libraries** (+1) — bounded request-body reader (DoS guard for
public token endpoints).
- `app/_lib/request-body.ts`

**Model & API Key Management** (+2) — secret-scrubber that strips provider key
material before error text reaches the Models "Test" panel.
- `app/_lib/redact-secrets.ts`, `app/_lib/redact-secrets.test.ts`

**Guided Pipeline Simulation** (+1) — the public anonymous demo entry that mints a
session scoped to an isolated `demo` workspace and lands on `/?sim=auto`.
- `app/api/demo/route.ts`
- apiRoutes += `/api/demo`

**Offers & Onboarding** (+1) — proactive T-48h offer-expiry reminder
(at-most-once, runs on the instrumentation heartbeat).
- `app/_lib/offer-reminders.ts`

**Sourcing, Campaigns & Rediscovery** (−1) — see removals.

---

## Files Removed From the Map (2)

| # | File | From context | Reason |
|---|------|--------------|--------|
| 1 | `app/_lib/rematch-source.ts` | Sourcing, Campaigns & Rediscovery | Deleted — the `rematchSourceEntry` logic moved into `app/_lib/db.ts`. The companion `rematch-source.test.ts` remains and now imports it from `db.ts` (not orphaned). |
| 2 | `app/landing/layout.tsx` | Landing & Marketing | Deleted from the working tree. |

---

## Findings / Notes

- **`app/api/onboarding/[token]/route.ts` is a working-tree deletion** (the `D`
  entry in git status). It is **not** mapped — correctly excluded. Note that
  keeping both `[id]` and `[token]` dynamic segments as siblings under
  `app/api/onboarding/` would be a Next.js dynamic-route conflict; the surviving
  layout is `route.ts` + `[id]/route.ts` + `candidate/[token]/route.ts`, which is
  conflict-free.
- **`rematch-source.test.ts` is not orphaned** — it imports `rematchSourceEntry`
  from `./db.ts` after the source consolidation. No action needed.
- **Excluded from contexts (intentional):**
  - `next-env.d.ts` — Next.js auto-generated.
  - `uat/driver/drive.mjs`, `uat/driver/drive-ai.mjs` — untracked vendored UAT
    harness overlay (0 git-tracked files under `uat/`); not part of the shipped
    codebase. Add a dedicated context only if `uat/` is committed.
- **Overlap check:** 0 files appear in more than one context (verified during the
  transform).

---

## Verification

The map is internally consistent (computed during the transform):

```
contexts: 43, unique files: 919, total slots: 919, dups: 0
```

Verified against the live API after the push:

```
DB contexts: 43 | unique files: 919 | total slots: 919 | dups: 0
```

```bash
curl -s "http://localhost:3000/api/contexts?projectId=a3f8c2d1-7b4e-4f9a-9c6d-2e8b5a1f0d47" \
  | node -e 'const c=JSON.parse(require("fs").readFileSync(0)).data.contexts; console.log(c.length,"contexts")'
```

---

## Next Steps

- [ ] Decide whether the vendored `uat/` driver should be committed and given its
      own QA/ops context.
- [ ] Confirm the `app/api/onboarding/[token]/route.ts` deletion is intended (and
      not a half-finished rename).
