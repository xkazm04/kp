"use client";

// The React half of the unavailability latch moved to the Studio kit with the
// voice pair; this path stays for the intake modules that import it by name.
export { useUnavailableLatch } from "@/app/_components/studio/useUnavailableLatch";
