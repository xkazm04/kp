"use client";

// Analytics → Economics: "what does this cost, and what earns it back".
//
// The prototype round closed on the BOARD direction, so the variant switcher and
// the three losing designs (baseline four-panel, statement ledger, payback axis)
// are gone and this renders the winner directly.
//
// Why Board won, in one line: the page carried three acquisition tables with
// three different taxonomies and left the reader to normalise the columns
// themselves — the board makes that comparison the page's job instead.
export { EconomicsBoard as EconomicsSection } from "./EconomicsBoard";
