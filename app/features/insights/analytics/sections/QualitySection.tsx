"use client";

// Analytics → Quality & audit: "can I trust the scoring, and can I prove what we
// decided".
//
// The prototype round closed on the INSTRUMENT direction, so the variant
// switcher and the two losing designs (dossier, timeline) are gone and this
// renders the winner directly.
//
// Why Instrument won, in one line: it answers the question that comes BEFORE any
// of the decisions below it — should this score be allowed to decide at all —
// and it makes the honesty gate the headline rather than a caveat buried in a
// panel.
export { QualityInstrument as QualitySection } from "./QualityInstrument";
