/*
 * Chapter 6, consolidated.
 *
 * Three directions were prototyped: What parks, The seal (eight hashed fields
 * linked to the one before), and What they are told (the same record read from
 * the candidate's side). What parks won, and the other two are deleted.
 *
 * It won because it closes the deck on the claim the whole page has been
 * building toward, and it does so by drawing the limit rather than asserting
 * it. Two rows cross the barrier and two stop at it, and the note underneath
 * names what stays autonomous. A chapter that only showed the machine being
 * restrained would be less convincing than one that shows exactly where it
 * is not.
 */
export { GatesQueue as GatesScene } from "./GatesQueue";
