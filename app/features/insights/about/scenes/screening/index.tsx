/*
 * Chapter 3, consolidated.
 *
 * Three directions were prototyped: Cost ladder, The clamp (the model returns
 * an opinion that a deterministic gate collapses), and Approval token (you
 * approved a set of people rather than a rule). Cost ladder won, and the other
 * two are deleted.
 *
 * It won because it answers the question a sceptical reader actually arrives
 * with, which is how much of this is a language model reading CVs. Showing 120
 * candidates reduced to 8 before anything paid runs settles that in one
 * picture.
 */
export { ScreeningLadder as ScreeningScene } from "./ScreeningLadder";
