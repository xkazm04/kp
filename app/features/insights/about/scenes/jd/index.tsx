/*
 * Chapter 1 — consolidated.
 *
 * Three directions were prototyped here (Build: control and concurrency;
 * Grounding: traceability; Intake: the originating conversation). Grounding
 * won and the other two are deleted. It also sets the register for the rest of
 * the deck: a claim on one side, the evidence for it on the other, a drawn
 * thread between them, and — the part that does the persuading — a claim that
 * finds no evidence and is visibly dropped rather than quietly kept.
 *
 * The switcher this file used to hold is gone; there is nothing left to switch
 * between. Later chapters keep theirs until their own direction is picked.
 */
export { JdGrounding as JdScene } from "./JdGrounding";
