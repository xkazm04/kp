{"ts":"2026-08-29T10:51:42.442Z","bundle":"software-engineering","nearest":"agent-chaining","kind":"technique","claim":"When a server writes a typed non-continuation (a stop reason plus a machine code), assert that a CLIENT renders it, because a stop-reason ledger no human surface displays is indistinguishable from having no ledger at all - and the docs will keep claiming it works, since the route half genuinely does.","because":"kp's agent-refresh route built a four-branch typed reply (refreshed:false + reason + AGENT_BRIDGE_KEY_INVALID code), pinned it with route tests, and documented that the operator sees it. The only UI calling that route awaited the fetch and read nothing, so a dead 24h pairing key, an undispatched agent, an unchanged status and a real transition all rendered as the same non-event. Route tests, the doc and the code constant were all green; the ledger reached nobody.","confidence":"medium","from":"scan-sweep@2.7.0"}
{"ts":"2026-08-29T11:06:50Z","bundle":"software-engineering","nearest":"metric-forecasting","kind":"technique","claim":"When a projection SUBSTITUTES a measured rate for one derived leg of a chain, assert the leg structurally exists (a distinct intermediate step, not merely an in-range index) before substituting, because index arithmetic that only avoids underflow will silently address the chain's own start and collapse the substitution to the measured rate alone.","because":"kp's hire forecast rebuilt its offer-to-hire leg as (offerReached/firstReached) x observedAccept, taking the offer stage as funnel[length-2] under a length>=2 guard. On a two-column board (a legal saved axis: entry + terminal) that row IS the entry row, so the ratio is 1 and the projection became velocity x weeks x acceptRate - 72 projected hires where the real 10% conversion gives 12. The signal-floor gate (hasSignal) did not catch it: the cohort was real, only the leg was fictional.","confidence":"medium","from":"explorer@1.2.0"}
{"ts":"2026-08-29T11:06:50Z","bundle":"software-engineering","nearest":"client-fetch-cache","kind":"technique","claim":"A TTL is not a bound: when expiry is only evaluated on read, cap the store and reclaim expired entries before evicting live ones - and never mark an absent optional key field with a printable sentinel when that field arrives from a URL, because a caller can simply send the sentinel and key onto the unfiltered entry.","because":"kp's server-side memo checked expiresAt on get() and never deleted, so entries keyed on raw ?candidate / ?roleFamily params (open vocabularies, on a long-lived self-hosted process where maxDuration does not apply) were retained one payload each forever. The same builders marked 'no filter' with '*', so ?candidate=* computed the filtered result (empty) and stored it under the full-list key, serving an empty decision-records list for the TTL. Both fixes are in the shared core; the joiner the module already used (NUL) made the absent-marker unforgeable.","confidence":"medium","from":"explorer@1.2.0"}
{"ts":"2026-08-29T15:31:11Z","bundle":"software-engineering","nearest":"design-tokens","kind":"technique","claim":"A theme-parity gate must assert that a themed surface RESOLVES its colour at render time, not that a literal equals its light-theme token: a lockstep check makes a hardcoded palette look compliant while it ships one theme's drawing into the other.","because":"kp's diagram renderer imported the JS colour mirror precisely so its literals would fall under the repo's lockstep gate, and the gate passed - but those constants are the light half by construction (the mirror exports a separate DARK object nothing forked on), so every architecture diagram painted a near-white box onto the dark canvas, in the shapes AND in the legend explaining them. The tell was a stroke byte-identical to the coral token. Fixed by painting var(--color-*) from the SVG presentation attributes, which an in-DOM SVG resolves per theme for free.","confidence":"high","from":"explorer@1.2.0"}
{"ts":"2026-08-29T16:29:02Z","bundle":"software-engineering","nearest":"measurement-honesty","kind":"technique","claim":"A metric's SAMPLE must be the population its statistic was computed over, not the population it was selected from - and the publishable/certifiable gate must read that same number, because the two diverge silently wherever the statistic drops rows the selector kept.","because":"kp's metric pack sampled time-to-hire with the hire count (9) while the median was computed only over hires carrying both timestamps and a non-negative duration (5). The floor is 8, so the buyer-facing artifact printed 'over 9 hires', status:measured and certifiable:true for a figure resting on five observations. The producer had already been fixed to return the true count and pinned by a test; nothing consumed it, so the divergence was visible in the payload and still published. The fix had to reach the basis PROSE too - a basis naming a bigger population than the statistic is the same claim one sentence further down.","confidence":"high","from":"explorer@2.0.0"}
{"ts":"2026-08-29T18:10:52.212Z","bundle":"software-engineering","nearest":"agent-memory","kind":"subject","claim":"When building an owner-style agent (not a task executor), give it a responsibilities layer distinct from mechanics - standing obligations, decision rules, refusal rules, escalation rules, what good looks like and how it is measured for the thing it owns - because the llm-agent bundle covers memory, chaining, fleet and transport and nothing says what a responsible owner of a codebase or a hiring pipeline actually does, which is the layer between LLM training data (generic) and a project instruction file (specific).","because":"kp/personas App-master program, 31 sweeps: integrity and mandate obedience solved, product sense absent - the agent never chose its own work because nothing told it what an owner is obliged to attend to. Filed as a lead, not authored: the first subject (codebase owner) should be extracted FROM the P1-P3 evidence in the W8-pre program, per the registry doctrine that knowledge is extracted from operated experience.","confidence":"medium","from":"scan-sweep@2.7.1 (develop round, brainstorm)"}
{"ts":"2026-09-01T19:31:36Z","bundle":"recruiting","nearest":"pipeline-aging-and-attention-triage","kind":"application","claim":"When a board's stage axis is workspace-editable, key the per-stage aging default by stage ROLE and make the threshold resolver take the axis as a parameter, because a name-keyed default table silently degrades every workspace-authored column to the flat fallback cut while the shipped columns keep firing and nothing on screen admits the change.","because":"kp 0f1d0a59: STAGE_SLA_DEFAULTS was keyed by the shipped five names; on a composed axis (the repo's own test fixture carries an 'Interview 2' custom column) every added column aged on the 10-day legacy cut and the SLA editor listed columns the board did not render. Replaced with ROLE_SLA_DEFAULTS + slaForStage(stage, overrides, axis); 4 new tests pin role resolution, override precedence, byte-identity on the shipped axis and the retired-id fallback; 4331/4333 unit, typecheck/lint/design/docs clean.","confidence":"high","from":"friend@1.1.0"}
{"ts":"2026-09-01T20:46:46.712Z","bundle":"software-engineering","nearest":"quality-gates","kind":"technique","claim":"When a config format lets a LATER declaration REPLACE an earlier one for the same rule (eslint flat config, and any last-writer-wins layering), a rule can be fully present in the config text and apply to nothing - so a liveness guard must read the RESOLVED effective config for a representative file per scope, never the config source, because a source-grep guard passes for exactly as long as the shadowing holds.","because":"kp's eslint.config.mjs declared four design-token no-restricted-syntax selectors in its first block; a later block matching a strict superset of those files set the same rule with only its own selectors, so flat config replaced them and `npm run lint` had not seen a hardcoded color under app/ since. The config DOCUMENTS this exact trap beside another selector group ('Spread them into both') and every other group was restated correctly - a known, written-down hazard, still committed. Two documents pointed at each other about which gate held the law and neither did. Proved by probe rather than reasoned: a file carrying both a hex literal and an await-in-transaction reported only the transaction violations. Fixed, and guarded by a test that calls ESLint#calculateConfigForFile for eleven representative files and asserts each selector group is present in scope and absent in its exemptions - verified non-vacuous by re-introducing the shadowing (3 of 12 fail, naming the missing group and the block to spread it into). The distinguishing detail: the population of hex violations was ZERO the whole time the gate was dead, so no symptom, no finding count, no drift - the only observable was the resolved config.","confidence":"high","from":"architect@1.5.0"}
{"ts":"2026-09-01T20:38:26Z","bundle":"software-engineering","nearest":"job-coordination","kind":"technique","claim":"When a route wraps several store calls in one SQLite transaction, every one of them must run on the connection the transaction was opened on; a module-private connection is invisible to it and, under WAL, a write that follows another connection's commit fails with SQLITE_BUSY_SNAPSHOT which busy_timeout never retries.","because":"kp POST /api/jobs/[id]/publish: gate (main handle read) -> setJobStatus (private handle commit) -> debit (main handle write) threw SQLITE_BUSY_SNAPSHOT on every genuine go-live, rolling back the debit while the flip had already committed; reproduced with a real-DB test, fixed by routing job-ingest.ts through ensureDb() (c2749fbc).","confidence":"high","from":"explorer@2.0.0"}
{"ts":"2026-09-01T20:38:26Z","bundle":"recruiting","nearest":"requisition-lifecycle-governance","kind":"application","claim":"A derived matchable record that is re-synced from edited prose must carry provenance-fixed fields (a grounded pay band) across the re-parse explicitly, or the second ingest silently honors the hand-typed override the first one refused.","because":"kp PATCH /api/jds/[slug] re-parsed the markdown and insertJob's upsert wrote salary_min/max from the parse, replacing the analysis band with the recruiter's typed figure or the taxonomy phantom; fixed with one shared helper used by both ingests (65a254fd).","confidence":"medium","from":"explorer@2.0.0"}
{"ts": "2026-09-01T21:07:05Z", "bundle": "software-engineering", "nearest": "concurrency-guards", "kind": "technique", "claim": "When a compare-and-swap lives inside an embedded single-file datastore's transaction, the transaction's ISOLATION MODE is part of the guard: a DEFERRED transaction takes a read lock at the SELECT and upgrades only at the first write, so two writers can both pass the same comparison inside that gap - the CAS must either take the write lock at BEGIN (IMMEDIATE) or re-assert its comparand in the UPDATE's WHERE with a zero-rows-changed skip.", "because": "kp's JD editor CAS (updateJd/revertJd, app/_lib/db/jobs.ts) ran a bare better-sqlite3 tx() while its own doc comment claimed 'the read+compare+write run in ONE synchronous transaction, so a stale base is detected'. It does not follow: single-threaded JS makes the body uninterruptible WITHIN a process, and that is exactly why the gap is invisible - the second writer is another connection (a second app process, a CLI, a WAL writer), which the in-process reasoning never considers. Fixed to .immediate() and pinned behaviourally plus at the source, because a single-process test cannot produce the second connection - which is itself the reason the defect survived a test suite. The repo already had the sibling shape correct one file away, so this is a knowledge gap about isolation modes, not about CAS.", "confidence": "high", "from": "explorer@2.0.0"}
{"ts": "2026-09-01T21:07:05Z", "bundle": "software-engineering", "nearest": "test-harness", "kind": "technique", "claim": "A test that asserts over a file's SOURCE TEXT makes that file's comments part of its input, so an ordering or absence assertion can be broken by prose alone - such a test must pin a marker that cannot occur in commentary (the call including its opening brace or an `await` prefix, never the bare identifier), and the code it guards should say so where a future editor will read it.", "because": "kp pins each rate-limited route by asserting the limiter appears BEFORE a marker for the expensive call. Editing a stale comment in two intake routes to name the pinned markers put those strings above the limiter and failed the ordering assertion for both - the documented trap catching the note about itself. Two earlier entries in the same spec had already hit it from the other direction (the bare identifier matching the route's import line), which is the second sighting that makes it a rule rather than an anecdote.", "confidence": "medium", "from": "explorer@2.0.0"}
{"ts":"2026-09-01T19:59:50.908Z","bundle":"recruiting","nearest":"candidate-communication-integrity","kind":"technique","claim":"When a candidate-facing link rides inside a letter, pin the link locale where the letter locale is resolved (inside the dispatcher), never at the link builder, because the builder cannot see the letter locale and re-deriving it is a second source that drifts.","because":"kp offer links were built bare at two call sites while the letter was composed in entry.locale inside comms-dispatch; the pin only became single-sourced by moving it beside candidateLocale() via a pure pinLinkLocale helper (04ec795b).","confidence":"medium","from":"perfect@2.4.0"}
{"ts":"2026-09-01T19:59:50.908Z","bundle":"software-engineering","nearest":null,"kind":"subject","claim":"When a public capability-link route mutates or discloses (erasure, invite redemption, offer view that lapses on read), throttle it per token AND client BEFORE the token lookup and pin the call site in a contract test, because a leaked link is otherwise a one-shot destructive open endpoint and no software-engineering subject currently governs open-route rate limiting.","because":"kp had 5 sibling token routes throttled and 3 unthrottled (one irreversible POST); the repo law only named routes that spend money or spawn a subprocess, and the registry index has no rate-limiting/abuse-containment subject to cite (596a2d46).","confidence":"medium","from":"perfect@2.4.0"}
{"ts":"2026-09-01T20:49:25.949Z","bundle":"software-engineering","nearest":"design-tokens","kind":"technique","claim":"When a lint gate is expressed as options on a shared rule (eslint no-restricted-syntax) in flat config, every later block that matches the same files REPLACES those options, so a design-law selector must be named once and restated in each overriding block, and its liveness must be probed with a fixture that is supposed to fail - not inferred from a clean run.","because":"kp: the raw-hex/rgba design-law rule had been silently off for the entire app/features + app/_components layer since later import-law blocks were added; a bare bg-[#d6d3d1] linted clean while npm run lint and design:check both reported OK. Re-armed via DESIGN_LAW_SELECTORS restated in both blocks (92d228e7); the \b-vs-Tailwind-underscore anchor gap was the smaller of the two defects.","confidence":"high","from":"perfect@2.4.0"}
{"ts":"2026-09-01T20:04:58.044Z","bundle":"software-engineering","nearest":"webhook-ingestion","kind":"technique","claim":"When an inbound receiver keeps a clock for ACCEPTED work, add a second one for CONTACT - stamped after the token/auth check and before any shape validation - because without it \"the sender never called\" and \"the sender is calling and every payload is rejected\" render as the same silence, and they need opposite fixes.","because":"kp/agent-workforce: the hired-agent report route recorded nothing unless a report parsed, so an operator staring at a silent roster could not tell a mis-pointed bridge from a schema drift. The sibling channels receiver already stamped a receipt in exactly that position; copying the position (not the storage) fixed it in one UPDATE. Two traps the fix had to respect: the stamp must NOT move for an unknown or retired token, or the 404 stops being indistinguishable and becomes a token oracle; and it must not be folded into the OUTBOUND liveness field, which answers the opposite direction and would trade one blind spot for a false statement.","confidence":"high","from":"perfect@2.4.0"}
