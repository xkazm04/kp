{"ts":"2026-09-02","technique":"advisory-guard-fail-mode","subject":"session-continuation","bundle":"software-engineering","context":"doc-sync-stop-hook","seam":"scripts/docs/check-doc-sync.mjs main(): the catch that mapped a missing map to exit 0, and the transcript read that mapped a missing target to an empty edit set","mode":"experiment","verdict":"better","a":"a broken hook payload (no transcript_path; transcript path that does not exist) exits 0 with empty stderr - a broken trigger reads as a clean turn; an unreadable or empty rule map exits 0 the same way: 0 of 2 broken payloads loud","b":"the same payloads exit 3 (CANNOT CHECK) with a diagnostic naming the instrument to fix; 2 of 2 loud; the clean-transcript and stop_hook_active controls still exit 0, so the finding path (exit 2) and the re-entry guard are unchanged","read_from":"exit code and stderr byte count per payload, ab-paired over four inputs","instrument":"node scripts/docs/check-doc-sync.mjs < <payload.json> with payloads {}, {transcript_path:<missing>}, {transcript_path:<empty file>}, {stop_hook_active:true}","branch":"main","commit":"","return_condition":"the rule inverts only for a hook whose instrument failure must block the stop (a release-authority or destructive-mutation class); no Stop hook in this tree is in that class, so exit 3 (non-blocking, surfaced to the human) is the right direction here; re-check if a blocking-class hook is ever added","note":"the B arm was copied from a sibling project whose hook already carried the rule (exit 3 = could not check), not invented; source run intake-omc-0902"}
{"date":"2026-09-03","run":"direction-exec-0903-kube","technique":"probe-design","subject":"health-checks","bundle":"software-engineering","mode":"task","verdict":"better","seam":"deploy/helm/kp/values.yaml:142 (readinessProbe) + scripts/deploy/check-chart.mjs:353 (no-probes)","seam_note":"app/api/health/route.ts already answered the serving question honestly (opens the DB, verifies seeds, judges the scheduler clock BY AGE, 503 with the sub-check named) and the chart pointed both probes at `/`, a server-rendered auth gate that 307s to /login just as happily while the database is locked. The gate could not see the difference: no-probes checked that two probes EXIST, so rule 11 was green on a chart breaking its own stated rationale.","a":"probes reading an endpoint that observes the database: 0 of 2 (both httpGet `/`); probes reading one endpoint: 2; no-probes must-fail fixtures: 1; startupProbe: none, so the liveness delay had to cover the worst cold start","b":"readiness httpGet /api/health, liveness tcpSocket on the port, startupProbe (tcpSocket, failureThreshold 30) suppressing liveness during first boot: 1 of 2 probes observes the database, 0 share an endpoint, no-probes fixtures 2 (both spellings — one httpGet path, one tcpSocket port), and the policy now fails a chart whose two probes read the same thing","instrument":"helm template via alpine/helm in docker on the rendered chart before/after (probe blocks compared); node scripts/deploy/__tests__/check-chart.test.mjs","gate":"npm run deploy:check clean; npm run test:deploy 20 -> 21 checks; npx eslint on the two touched .mjs clean; no TypeScript touched","branch":"direction/kube-rs-round5","commit":"ad6c4449","return_condition":"/api/health runs coreTableCounts() (five SELECT COUNT(*)) plus countActiveTasks() per probe; the cadence was left at the old probe periodSeconds: 10 rather than tightened. Measure the endpoint latency on a large seeded install before lowering it — if it is expensive, the shape is a cheaper readiness variant (DB open + seed check) or probe-caching, which is a new route rather than two values lines.","note":"The container A/B the proposal named did NOT run: no kp image is built locally and a Next production build was out of scope, so `/data` unwritable -> 503 vs 2xx and the stalled-heartbeat no-restart check are read from app/api/health/route.ts:50-73 rather than observed. Two falsifiers were settled statically: the status code is unaffected by the trusted/untrusted payload split (only DETAIL is dropped), and a default install seeds the job catalogue at boot — only KP_EMPTY=1 leaves the catalogue empty, where readiness would hold the pod NotReady; that case is called out in values.yaml and self-hosting.md."}
{"date":"2026-09-03","run":"direction-exec-0903-kube","technique":"deploy-gate-coupling","subject":"deployment-contract","bundle":"software-engineering","mode":"task","verdict":"better","seam":"scripts/deploy/check-chart.mjs:459 (loadChart) + :80 (REVIEWED_TEMPLATES) + deploy/helm/kp/templates/serviceaccount.yaml:1","seam_note":"The gate enumerated five template files by name, so a DELETED template was exit 2 and an ADDED one was invisible — its own header claims every rule is anchored to the tree, and five files is not the tree. No template declared pod identity either, so the pod ran as the namespace default ServiceAccount with a live API token projected into a container that holds candidate PII and spawns Python subprocesses on request.","a":"templates read by the gate: 5 of 8 files in templates/; a scratch templates/worker.yaml with hostNetwork: true and privileged: true exits 0 CLEAN against the parent gate (measured, not argued); policies covering pod identity: 0; ServiceAccount objects rendered: 0; projected API tokens in the pod: 1","b":"templates read: every file in templates/ (9 of 9 after this branch); the same worker.yaml exits 1 with privileged-pod AND unreviewed-template; policies 12 -> 14; helm template renders 1 ServiceAccount with automountServiceAccountToken: false and no Role/RoleBinding, and the pod spec sets serviceAccountName + automountServiceAccountToken: false -> 0 tokens","instrument":"the identical rogue template run against `git show 2780c901:scripts/deploy/check-chart.mjs` and against the new gate (exit 0 vs exit 1); helm template kind census + grep for automountServiceAccountToken; node scripts/deploy/__tests__/check-chart.test.mjs","gate":"npm run deploy:check clean (14 policies at this commit); npm run test:deploy 21 -> 23 checks; npx eslint clean","branch":"direction/kube-rs-round5","commit":"0d1b8955","return_condition":"unreviewed-template is compared in BOTH directions (unlisted file = finding, entry whose file is gone = stale), the discipline .ai/manifest.yaml already applies to CI gates. Its \"has anyone ever added a template\" falsifier fired immediately: this branch adds two (serviceaccount.yaml, pdb.yaml). The empty-Role assertion rests on a grep — no @kubernetes client, no API call in app/, pipeline/ or scripts/; if a code path ever needs service discovery or a runtime ConfigMap read, the artifact becomes a scoped Role, not a token.","note":"loadChart keeps the five named handles REQUIRED (a policy that must read the Deployment cannot be handed \"some template\", and a glob has no missing files by definition) and reads the directory in addition, so the loud exit-2 contract was not traded for the coverage."}
{"date":"2026-09-03","run":"direction-exec-0903-kube","technique":"deployment-config-as-code","subject":"deployment-contract","bundle":"software-engineering","mode":"task","verdict":"better","seam":"deploy/helm/kp/templates/pdb.yaml:1 + deploy/helm/kp/templates/deployment.yaml:33 (checksum/secret)","seam_note":"The chart defends the single-writer invariant three ways INSIDE the pod spec (replicas: 1, Recreate, ReadWriteOnce) and was undefended against the two things a platform does to a pod from outside: a node drain, and a credential that changes in the Secret object while the container keeps the value it read at start.","a":"PodDisruptionBudget objects rendered: 0 — `kubectl drain` evicts the only replica and kp is down for a reschedule plus a PVC reattach, with nothing telling the administrator this workload is a single writer. Rendering the parent chart with auth.secret=y and with auth.secret=ROTATED produces a BYTE-IDENTICAL pod template (measured with diff), so `helm upgrade --set auth.secret=<new>` reports success while the container holds the old value.","b":"1 PodDisruptionBudget (minAvailable: 1, values-gated podDisruptionBudget.enabled, same selector as the Deployment) — voluntary eviction is refused with the budget named. The same rotation now changes checksum/secret (7f3d48f4… -> 72d32b1a…) so the pod rolls; policies 14 -> 16, each with a must-fail fixture.","instrument":"helm template via alpine/helm in docker: kind census before/after, and the pod template diffed across two renders differing only in auth.secret, run against a `git archive` of the base chart and against this one; node scripts/deploy/__tests__/check-chart.test.mjs","gate":"npm run deploy:check clean (16 policies); npm run test:deploy 23 -> 25 checks; node scripts/docs/check-doc-sync-diff.mjs --base 2780c901 --head HEAD clean; npm run docs:check clean; npx eslint clean","branch":"direction/kube-rs-round5","commit":"9411a8cc","return_condition":"minAvailable: 1 blocks ALL voluntary eviction. No doc in the tree names a managed cluster with unattended node rotation, where the strong form can stall a control-plane upgrade — if kp is ever deployed to one, the honest artifact is maxUnavailable: 1 (visible in the PDB status) plus a NOTES line, which is why the object is values-gated. And with existingSecret the chart renders no Secret to hash, so the checksum covers the values path only; self-hosting.md now tells that operator to roll the pod themselves.","note":"The drain itself was not run (no cluster); the measured half is the rendered object and the rotation diff. Rotating KP_SECRET is a supported operation with named consequences in the app (bridge-client.ts refuses with \"KP_SECRET … has changed since pairing\"), so the falsifier that would have inverted this — a pod holding the old value is accidentally protective — does not fire: KP_SECRET also signs sessions (edge-verify.ts), and a pod that never re-reads it keeps honouring credentials the operator believes are retired."}
{"date": "2026-09-05", "run": "intake-apply-0905-session-length-dilution", "technique": "golden-path:session-length-dilution", "subject": "agent-instruction-files", "bundle": "software-engineering", "mode": "experiment", "verdict": "not-better", "seam": ".claude/CLAUDE.md:168-215 (Documentation Sync + The Stop hook) and :228-233 (pathspec-only commits) at blob d7ae8c0c / commit e81b1eac; .claude/settings.json hooks = {Stop} only, no SubagentStop", "seam_note": "The prose-vs-gate split: the pathspec rule and the doc-sync obligation are prose (the doc-sync gate is a Stop hook on the Director's transcript plus a commit-range CI job); tokens, locale parity and await-in-transaction are program gates. The flipped golden-path rule says the file-scale leak is session length, so a rule dropped late should be re-delivered or demoted rather than the file shortened.", "a": "policy 'shorten the file / move the line' read as compliance by .claude/CLAUDE.md line count live at each event: doc-sync owed turns satisfied in-turn 20% at 285 lines (10/49) vs 50% at 310 (19/38); pathspec violations 0 at every length (0/393 git add, 0/751 commit -a, 0 stash)", "b": "policy 're-deliver or demote' read as compliance by position in session: 101 of 107 owed doc-sync turns are the FIRST owed event of their transcript (builders: median 1 turn, 6 of 364 have a second owed turn), so the session-length axis has no spread; the loss (67% unsatisfied) sits at event 1 with the file freshly delivered; the Stop hook fired 321 times on the Director surface and prevented continuation 0 times, and fired 0 times on the builder surface that made 1,920 of 2,202 edits", "instrument": "replay of the hook's own evaluate()/isTurnBoundary() (imported from scripts/docs/check-doc-sync.mjs) over 373 recorded Claude Code transcripts (9 main, 364 subagent; claude-code 2.1.224-2.1.261), each event tagged with the feature-doc-map.json and .claude/CLAUDE.md versions live at its timestamp; plus the same evaluate() over 1,296 commits since 2026-08-05 (747 owed): 68% doc-in-commit before the commit-range CI job (2026-08-27, 141/208, no waiver existed), 83% after (342 doc + 107 Doc-sync trailer of 539), 94% over 2026-09-02..05", "gate": "none run: product code unchanged", "branch": "main", "commit": "", "return_condition": "when a SubagentStop hook (or a per-commit ordinal in the doc-sync record) makes builder-surface compliance observable by position - that is the instrument that would let the session-length axis be read here; until then the axis is unmeasurable in a Director/Builder topology and the loss is a first-event, no-gate loss", "note": "Structural fact nobody designed: the /perfect topology moved 87% of edits into single-event subagent sessions the Stop hook never sees, so the only gate on the surface that edits is the commit-range job. The one main-session owed-unsatisfied turn with the hook firing (2026-08-20) predates 688cd04d, the fix for the hook that had never once fired. Soft sub-rule 'git diff --cached --stat before committing' held at 4% (27/751) flat across ordinals and lengths - a rule lost at admission, not late."}
{"date": "2026-09-05", "run": "intake-codecompanion-20260905", "technique": "history-compaction (amendments: the lossless lane's lower trigger; the provider-anchored count)", "subject": "prompt-assembly", "bundle": "software-engineering", "mode": "simulation", "verdict": "not-better", "proof": "structural-only", "seam": "app/_lib/intake-transcript.ts:14-40 (capTranscript: 48-turn cap + kp:transcript-compacted:N marker, idempotent), app/_lib/intake-run.ts:45-58 (transcriptWindow pinned to the engine's render window), app/api/intake/[id]/voice-turn/route.ts:12 (MAX_UTTERANCE_CHARS 4000), voice-complete route :15,:65 (periodic brief extraction off the fast path)", "seam_class": "short-utterance voice thread with a fixed turn cap, no tool results and no attachments in the transcript", "measurable": "bytes or turns changed by adding an elision lane ahead of the cap: none - nothing in the transcript is re-fetchable", "result": "Three real cases walked: the cap is the whole ladder (drop-count marker is the notice rung); the rolling summary (brief) already runs amortized on the periodic thread, so a lower trigger would sit ahead of a step that does not run on this path; the window is turn-denominated with a 4,000-char utterance bound, so the anchoring amendment has no local estimator to displace", "amendment_improved": "the lossless-lane amendment now carries the precondition 'exists only where the transcript carries re-fetchable bulk; a transcript of spoken utterances has nothing to elide'", "return_condition": "a token-denominated trigger on the intake prompt, or attachments quoted into the transcript", "note": "Structural fact: the stored cap equals the engine's render window by construction so sourceTurn citations stay aligned - a budget derived from a real limit and enforced where the whole artifact is visible."}
